 Welcome back, and in this lesson of the series, I want to cover the disaster recovery architecture and considerations for the various common database products within AWS. By now you know the format, it's going to be entirely visual, so let's jump in and get started. Now I'm going to approach databases from two perspectives, regional and global. First we're going to look at regional, so we're going to start with a similar layout, US East 1, but this time three availability zones. A, B and C, and then an area which represents anything not in a specific availability zone in green at the bottom. Now I'm going to focus on the core AWS database products, so I won't be covering Redshift, Neptune or any of the other situational databases in this lesson, because they're covered elsewhere in the course. Now first, and I hate even mentioning this one, but if you're running a bespoke or custom database on an EC2 instance, then it has this. The same resilience profile as anything on EC2. It's dependent on the host and on the availability zone, so if either fails, then it fails. One remedy to this is to snapshot the EBS volumes to S3, and that does give this architecture a certain level of resilience. But this should always be a non-preferred option, as EC2 is in no way capable of delivering the level of high availability that a production system can offer. So, running databases on EC2 should only ever be done for certain niche use cases. For everything else, you should be looking to use by default an AWS database service. Now another database available in AWS is DynamoDB. This is a public space database, and so it doesn't run inside a VPC or use any specifically selected availability zones. Instead, for a given table, you can run a VPC on the same database. For a given table, the data is replicated between multiple nodes running in multiple availability zones in a completely transparent way. Now this means that it would take the failure of an entire AWS region to remove access to data hosted by DynamoDB. So this is a really resilient public space database product available in AWS, and from a regional perspective, you don't need to consider any specific disaster recovery measures. DynamoDB can survive the failure of an individual availability zone without any problems. And as I'll show you later in this lesson, there are features of DynamoDB that can provide even global levels of resilience. Now another product is RDS. With RDS, you need to create a subnet group which defines the subnets which RDS instances can run in. Now it doesn't mean that RDS has to use them all. A subnet group simply tells RDS which subnets that it can use. So RDS can use within a VPC. For normal RDS, so that means not Aurora or not Aurora serverless, then the architecture is that there are either one or two instances. So either a single instance or a primary and standby instance which have replication between the two. Now each of these instances, the primary and the standby, have their own local storage which is located in the same availability zone. And this is provided by the RDS. And then between these two instances, we have synchronous replication that occurs from the primary to the standby. And this ensures that RDS can deliver a level of high availability. If the primary instance fails, then a failover process starts, meaning the standby becomes the new primary. It means that in order to fully impact service, both the primary and the standby availability zones would need to fail at the same time. If the primary and the standby are not in the same availability zone, then the standby would need to fail at the same time. If the primary only availability zone failed, then the service would fail over to the standby and a new standby instance would take its place. If the standby availability zone failed, then the standby instance would be rebuilt in the background. So RDS is resilient, but there could be extreme situations which can affect both availability zones, which a multi-availability zone deployment is located in, and this could impact service. Now we have another database problem. Now we have another database problem. Now we have another database problem. We have a database product provided by AWS called Aurora, and this provides significant advantages over normal RDS. The first advantage is that Aurora isn't limited to just primary and standby instances. You can actually have one replica in each availability zone. You can even have more than one replica in each availability zone. So with Aurora, your ability to protect against disaster is now relative to the number of availability zones in the region. So you've got trucks running in each availability zone, you can just recommendgenes of different action needs that Mister Maya can implement in funt and that you've already seen in RDS replicas. And then just quickly bushes out other areas where any type of IBS could work. That'sой sh Oy oy, Its okay now! separately as cluster shared storage. What this means is that spread across all of the availability zones in the region are many copies of the data replicated at all times. So data is replicated at the storage level automatically and then the DB instances connect to this shared cluster storage. Now this is a subtle but major change. What this means is that because the storage is replicated across availability zones, it will take multiple availability zone failures before the storage layer is affected. Essentially a whole region would need to fail before the cluster storage is significantly impacted and because at a compute level Aurora can have replicas in all of the availability zones, it would also take a region failure to impact the compute layer. Based on this combined improvement, the Aurora platform as a whole is more and more likely to fail. much more resilient than any of the other relational database products available within AWS. So as long as you as a solutions architect deploy into the maximum number of availability zones that a product supports, you already have resilience and disaster recovery capability that will resist anything but an entire region failure. But we can even go further than this and look at a global perspective. Globally, a few of the databases I just covered offer some features which add resilience to a full region failure and I want to quickly explore those before moving on. First is DynamoDB. The default mode for DynamoDB is to offer a table which is located within a single AWS region. But you can create global tables which are a group of replica tables in different regions which form part of a multi-master replication set, replicating data in two directions between each of the replica tables. Data written to any of the tables is replicated to the other two using a last writer wins replication method. In the region where any particular piece of data is written to, then that particular piece of data can be read in a strongly consistent way. But for any regions which that data is replicated to, they can only read in an eventually consistent way. This means with the correct architecture, any single region can fail and an application can still retrieve data from the other working regions. So this means that DynamoDB is capable of a global level of resilience. Now Aurora also has a global level feature which is called Aurora Global Databases. You can create a global database, add a read-write cluster in one region and then add secondary read-only clusters in other regions. The secondary read-only clusters can be promoted and can scale much larger than legacy RDS replication methods. The replication for Aurora Global Databases happens at the storage layer. So generally all of the secondary clusters are within one second of the primary and it places no additional load on any of the databases. Now this means that Aurora can be replicated to any of the other regions. So this means that DynamoDB is able to read and write in a truly consistent way. Aurora can withstand any serious events which can in theory take out an entire region. But it should be noted that unlike DynamoDB, this is not multi-master replication. Writes are only allowed in the primary region, so while the product and architecture does help with resilience and scaling, it's not in a truly read and write multi-master way. Now lastly for normal RDS, so not Aurora, we've got cross-region read replication. In this architecture, an RDS instance running in a region, potentially a multi-AZ instance, replicates to another AWS region. Now this is asynchronous replication and unlike Aurora, it's not done at the storage layer, so there is additional load placed on the instance in the region, which is the source of the replication. So these are the different global capabilities that are offered by DynamoDB Aurora. So we've got a lot of different types of RDS instances running in the same region. Now that's everything that I wanted to cover in this lesson. We've talked about these different products and features elsewhere in the course, but I wanted to bring everything together with a focus on disaster recovery and resilience considerations. Because it's a good real-world thing to be aware of and it's super useful for the exam. So thanks for watching, go ahead and complete this lesson and then when you're ready, I'll look forward to you joining me in the next.