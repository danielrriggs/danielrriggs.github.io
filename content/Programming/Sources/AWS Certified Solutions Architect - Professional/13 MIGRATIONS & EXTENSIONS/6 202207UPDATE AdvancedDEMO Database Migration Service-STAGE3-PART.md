 Welcome back to stage 3 of this advanced demo series. And at this point in the demo series, this is the infrastructure that you have within your AWS account. The two environments, the simulated on-premises on the left and the AWS environment on the right. And in the previous stage of this demo series, you created a VPC peer between both of those environments. Now in this stage of the demo series, you're going to be provisioning all of the infrastructure at the AWS side. So you're going to start by provisioning the database within AWS. So this includes an RDS subnet group and an RDS managed database implementation. So we're going to configure a free tier eligible single AZ implementation of RDS. And that's going to be the end state database. For this application migrations. So currently the database is stored on the simulated on-premises environment on the cat DB server. And we're going to migrate that data across to the RDS instance. You're also going to be provisioning an EC2 instance which will function as the web application server. So this is going to be a WordPress instance using an EBS boot volume and data volume. And you're going to install WordPress on this instance. And then migrate. All of the non-database assets from the current web server across to this brand new EC2 instance. And you'll be using SSH to sync these non-database assets between CatWeb and this new EC2 instance. And at the end of this stage of the demo series, this EC2 instance will be able to communicate with the cat DB database server. And you'll have migrated at least the web part of this architecture. From the simulated on-premises environment through to AWS. And at this point users can connect to this EC2 instance and see the same application. Now to perform all of these tasks we need to be at the AWS console. So let's switch over and get started. Now this is going to be a fairly command heavy stage of the demo series. So please go ahead and open the text instructions for this stage of the demo series. Which are attached to this lesson. The first thing we need to do. Is to create the RDS subnet group. So let's click on services. And then move to the RDS console. Provisioning the RDS instance is going to be the most time intensive part of this entire demo series. So we're going to get this out of the way first. To get started go ahead and click on subnet groups on the menu on the left. And then create a DB subnet group. Now the subnet group determines where RDS puts the database instances. So although we might have a full range. Of subnets in each of the availability zones in a region. We need to give RDS the directive of where to place its instances. So that's what a subnet group accomplishes. So we're going to call the subnet group A4LDBSN group. And we'll put the same for the description. For VPC click in the drop down. And we're going to create this within the AWS environment. So select AWS VPC. Scroll down further and under add subnets. First we need to pick. The availability zones that we're going to use. So click in that drop down. And just go ahead and check the box next to US-E-1A and US-E-1B. We're going to use both of these availability zones for this subnet group. Now we're going to be telling RDS to put the database into the private subnets. Within the AWS environment. Now if we go to the VPC console and click on subnets. You'll see that we have AWS-Private A. And AWS-Private B. Now A uses 10.16.32.0.20. And B uses 10.16.96.0.20. So we need to select both of those subnets. So click in the subnets drop down. Check the box next to 10.16.32.0. And then the box next to 10.16.96.0. So that's Private A and that's Private B. Once you've selected both of those. Scroll down and click on create to create this subnet group. Next we need to create the database itself. So click on databases and then click on create database. I'll close down these headings just to make it easier to see. Now we could select easy create to make this easier. But we're going to select standard create. Because I want you to have the experience of creating as many RDS implementations as possible. We're going to deploy a MariaDB database instance. So select MariaDB. And then scroll down and under templates. I want you to pick free tier. Because I want to keep this advanced demo as much within the free tier as possible. Now if this was a production deployment. Then you would select production. And that would give you the ability to use larger RDS instance sizes. As well as using a multi AZ deployment. Because this is just a demo we're going to leave it on the free tier. Now scroll down further still. And we need to enter some values under settings. Now these values are the ones I told you to note down in stage one of this demo series. The ones that we used as parameters of the CloudFormation template. If you didn't note them down then just click on services. And type cloud in the find services box. Locate CloudFormation. Open that in a new tab. Click on the DMS stack which you created at the start of this demo series. And select the parameters tab. And all of the options that we'll be using will be contained within these parameters. So back at the RDS console under DB instance identifier. We're going to use A4L WordPress. We're going to use the same for the master username. So A4L WordPress. And then for the master password. If I just go back to CloudFormation. We're going to use the DB password value. So this is just a random password that I created for this demo series. So just copy that into your clipboard. And then paste that in for both the master password and the confirm password. Next scroll down. And we don't need to set anything for the DB instance size. Because that was set using the free tier template that you selected above. Scroll past storage. Scroll past availability and durability. And we need to change values in the connectivity section. We want to deploy this into the AWS VPC. So click in the drop down and select AWS VPC. Make sure that the A4L DB SN group is selected. So this is the subnet group. That you just created. Make sure that it's set to no for public access. And then under existing VPC security groups. Click in the drop down. And I want you to pick the one that says AWS Security Group DB. Now you'll have some text at the start. And some random at the end. Just focus on the one that says AWS Security Group DB. Select that. And then delete the default by clicking on the cross. Scroll down again and expand additional configuration. Under initial database name. Go ahead and enter A4L WordPress. So this tells RDS to create an initial database called WordPress. Which is the one that we'll be using DMS to migrate our data into. That's everything we'll need to do. So scroll all the way down to the bottom. And then click on create database. Now that will take some time to create. Anywhere up to 45 minutes. Although usually it is in the range of 15 to 20 minutes. While that's creating. We're going to go ahead and provision the EC2 instance. Which will function as our AWS web server. So click on services. And then into this box type EC2. And click to move to the EC2 console. Now at this stage you will be running some commands. So make sure that you have this stages instructions open. Because using those you can just copy and paste. As we need to run commands. So to get started go ahead and launch an instance. Okay so first under name type AWS Cat Web. That's what we're going to call this EC2 instance. Then scroll down. Click Amazon Linux. And make sure Amazon Linux 2 AMI HVM SSD volume type is selected. It should say free tier eligible. Just make sure that it does. Directly below that make sure that architecture is set to 64 bit x86. Scroll down. Under instance type again. Make sure this is set to a free tier eligible instance. In my case the default is T2.micro. I have seen some cases where T3 instances are free tier eligible. In either case make sure yours is set to a free tier eligible instance type. Scroll down again. And under key pair login. Click in the key pair name box. And select proceed without a key pair not recommended. Scroll down. Next to networks. Next to network settings click on edit. Change the VPC to AWS VPC. Change the subnet to AWS-publicA. You can leave auto assign public IP to enable which is the default. Under firewall security groups choose select existing security group. And then in the common security groups drop down. Go ahead and select the one that contains. AWS security group web. It will start with DMS and have some random at the end. That's okay. Just make sure you pick AWS security group web. Scroll down further still. We don't need to make any changes to configure storage. Expand advanced details. And look for IAM instance profile. Click that drop down. Select the one that contains AWS instance profile. Again it should start with DMS. And end with some random. That's okay. So select that. That's everything we need to configure. So go ahead and click on launch instance. And once you've done that you can click on view all instances. And we'll need this instance to be in a running state and have two out of two checks before we can continue. So go ahead and pause this video and wait for the AWS cat web instance to be running and have two out of two status checks. Okay. So this is the end of part one of this lesson. It was getting a little bit on the long side. And so I wanted to add a break. It's an opportunity just to take a rest or grab a coffee. Part two will be continuing immediately from the end of part one. So go ahead. Complete the video. And when you're ready, join me in part two.