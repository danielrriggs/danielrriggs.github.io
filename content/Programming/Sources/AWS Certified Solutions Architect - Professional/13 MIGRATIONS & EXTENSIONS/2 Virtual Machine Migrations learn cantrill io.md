 Welcome back, and in this lesson I want to cover the various virtual machine migration services which exist within AWS to allow the migration of virtual machines and or applications from on-premises into AWS, and in some cases, back again. Now we've got a lot to cover in one lesson, so let's jump in and get started. One of the challenging pieces of work which forms part of any form of migration from on-premises to AWS or even on-premises to anywhere is the discovery activity. If you think about it, say that you have a thousand virtual machines running on-premises. Do you know what each of those virtual machines have in terms of resources? What amount of disk space is consumed? Which virtual machines need which other virtual machines? And what applications are installed on each of them? This discovery piece of work is a major challenge for larger migrations. Luckily, AWS have a service to help, and that service is called the Application Discovery Service. And put simply, it allows you to discover your on-premises infrastructure. So this includes what virtual machines you have, what CPU and memory they have allocated, MAC addresses, the utilization of those resources, and it tracks them over time, meaning that you can track them over time. So this is a very important piece of work. You can use the data gathered to help you migrate effectively. Now, Application Discovery Service runs in one of two modes. First, we have an agentless mode, using what's known as the Application Discovery Agentless Connector. And this is an OVA-based virtual appliance which integrates, for example, with VMware and allows the product to do an inventory and review virtual machine usage across an entire industry. environment. In this mode it can measure performance and resource usage of virtual machines but it's limited to the information which can be obtained from outside of those virtual machines. Now this is similar to the CloudWatch agent. Without the CloudWatch agent installed on an EC2 instance you're limited to monitoring EC2 based on what's visible outside of the instance. With the CloudWatch agent installed you can get exposure to things that are occurring within the EC2 instance and likewise you can also use the application discovery service in agent based mode and this gives you additional exposure to rich information from inside virtual machines. So this includes things like network usage, processes and their usage, resource consumption at a much more detailed level. You can see the applications running on virtual machines and even see dependencies between virtual machines by reviewing the network traffic sent and received from every virtual machine. Now for larger migrations this is an amazingly useful information gathering tool for the exam. Remember though that the application discovery service doesn't migrate anything. It simply helps you discover what you have within your infrastructure platform and this feature is much more valuable when you have a virtual machine that has thousands or tens of thousands of virtual machines especially with more complex dependencies between them. Now the application discovery service also integrates with the AWS migration hub and Amazon Athena. So the migration hub tracks migrations of different types within AWS so virtual machine migrations, database migrations and the application discovery service can integrate with that providing the initial set of data to the application discovery service. So this is a very useful information gathering tool for covering the architecture of your on-premises environment. And then Athena can be used to perform ad hoc analysis of the data generated in a serverless way. Next I want to talk about the server migration service or SMS. Now this is a tool which is used to migrate whole virtual machines into AWS. So this includes the operating system, data and applications. All of that migrated as is into AWS. So this is a tool which actually performs the migrations. The application discovery service gathers the information and then SMS actually performs the migrations. Now this product runs as an agentless product. It actually uses a connector which is a virtual machine which runs on-premises within your existing virtualization environment and specifically it can work with VMware, Hyper-V and Azure VM. Now AWS have another tool which is called VMware. So this is a tool which is used to migrate virtual machines into AWS. So this is a tool which is used to migrate virtual machines into AWS. Now AWS has a virtual machine VM import export and this allows for migrations but with this product which is a legacy product it was actually a manual process. You had to export the VM and then import that into AWS. And this is something which meant downtime for any virtual machines while they were being transferred. SMS does incremental replication of live VMs and volumes. So it can migrate in the background while a virtual machine continues being used by the VM. So this is a tool which is used to migrate virtual machines into AWS. Now AWS has a virtual machine called the AWS and it can also orchestrate multi-server migrations. So if a specific application uses multiple servers then all of them can be moved as one unit of compute, one application. Now the product creates Amazon machine images which can be used to spin up EC2 instances manually or can be used with other tooling such as CloudFormation to automate repeated deployment of instances. And then lastly it too integrates with the AWS migration system. So this is a tool which is used to migrate virtual applications into Azure. If I shn't mention that it all works by with application usage 오� idol in my Teachingcut video. Before π Carmen virtual machines. On the right side we have AWS and then SMS because it's agentless architecturally you install a connector within vCenter. This is able to interact with the VMware platform and access the VMs and any associated storage as well as perform management operations. It's this connector which can migrate live virtual machines and storage over into AWS in an incremental way. It's actually transferring the data into S3 within AWS and once the data is within AWS SMS uses that to generate snapshots and those are made into Amazon machine images. These AMIs can then be used to directly create virtual machines or with other products such as CloudFormation to automate the creation of AWS-based EC2 instances using the data transferred from on-premises. Now SMS as I mentioned integrates with the AWS migration hub which can monitor different types of migrations including those from SMS but also database migrations using DMS. Now that's everything I wanted to cover from a solutions architect perspective. Just remember the application discovery service discovers virtual machines and applications as well as their interdependencies. SMS migrates virtual machines so operating systems, data and applications as is into AWS and they both integrate with the migration hub which is your single pane of glass interface for anything migration related happening within AWS. But at this point that's everything that I wanted to cover so go ahead and complete this lesson and when you're ready I look forward to you joining me in the next.