 Welcome back, and in this lesson, I want to cover something which is fairly theoretical. But like all of the theory which is in this course, you'll find it really useful when it comes to using AWS in the real world and for the exam. The six R's of cloud migration are a set of six different strategies for migrating things into the cloud. By understanding each one of these, so the benefits, the negatives, when you would use it and when you wouldn't use it, you can learn to be a more effective solutions architect. So let's jump in and take a look. Let's say that you've been contracted by the Animals for Life organization to perform a migration into AWS. Let's assume that they have around 50 applications which run on a collection of 100 virtual machines. There are many ways that you could tackle this. You could look at the project as a whole and attempt a big bang migration in some way, moving everything into AWS and then flicking a switch. Best practice, though, is to look at applications individually, maybe running a mini project per application. And so the starting point is to discover, assess, and prioritize all of the applications that the business has, so to plan a migration. This way, you know what applications the business has. How important each of them is and any dependencies between those applications. Once you have that list, then for each application, there are six different ways to migrate them. I want to introduce them here quickly, and then I'll cover the key points in detail for each. So you can re-host an application, so pick it up, move it to AWS, and put it back as is. You can re-platform an application, which is just like the above, but with some kind of optimization. You might want to consider re-purchasing, so replacing the service that you have with something new, maybe something that's delivered as a service. The preferred option is to re-factor or re-architect an application to take advantage of the features offered by the specific cloud platform that you're migrating to. You could decide that you no longer need an application, so you might retire it entirely to save on the costs and time involved, as part of a migration, or you might decide to retain the application as is. It might be too much effort or too scary to migrate certain types of applications. Now all of these are valid in certain situations, and to be a good solutions architect means knowing when to use each, and what the benefits and sacrifices are associated with each of the different options. So let's step through those next. First, let's talk in detail about... Re-hosting, which is known as lift and shift, or migrate as is. The concept is pretty simple. Moving something into AWS with the least amount of effort and least number of changes as possible. You might see this method used for legacy applications or applications which are monolithic, or maybe ones which have a pretty strict set of vendor support agreements, where you have little flexibility on the architecture. Now there are benefits. There's potentially less admin overhead. Maybe you're moving a virtual machine to something like EC2, which is an infrastructure as a service model. Maybe you're getting out of a self-managed data center. Both of these present a situation whereby just moving into AWS, you're reducing admin overhead. Doing this type of migration might also mean that the application is easier to optimize when it's inside AWS. Operating with software-defined infrastructure, which is what AWS uses, is easier than working with legacy tooling such as VMware or other physical data center components. There might be other actual and direct cost savings for moving into AWS. If you're moving out of a self-managed data center, then potentially there's cost savings there, but also things like consuming a certain type of EC2 instance, such as the T3 or burstable instance type. So maybe for low usage applications, you can benefit directly by migrating into AWS. Now there are negatives associated with this method. You're not going to be taking advantage of all of the benefits of cloud platforms. Just running virtual machines as EC2 instances is only a tiny portion of what AWS can do. And if you do intend to do anything to the application from a more long-term perspective, then figuratively, you're just kicking the can down the road. You're delaying what you can do today until tomorrow. Now if you do decide to go down this route, you can use things like import-export and the server migration service. Now normally I don't recommend re-hosting on its own. In certain situations, it is the best approach to take, but it is a very niche set of use cases that fit this type of migration. What is almost always better is re-platforming. And re-platforming is another migration method that is similar to re-hosting, only you can apply some optimization as part of the migration process. So an example of this might be that you're migrating a web application into AWS, which has a web server, a database server, a load balancer appliance, and some data center storage for backups and media. Well, as you're migrating this into AWS, you might decide to use the relational database service instead of a self-managed database. You might use the elastic load balancer product within AWS instead of self-managed ones. You could choose to move backup and media storage to something like S3. Re-platforming doesn't involve any huge changes. It's just tweaking the platform which is used. It's using the same underlying tech, the same type of database, the same architectural components, only using better versions. So this approach brings no real negatives, but equally no world-changing benefits. You might achieve some small benefits, so reductions in admin overhead, maybe some cost reductions, some performance benefits, maybe a more effective backup regime and improvements to the high availability and fault tolerance of the application. When you're facing a situation where a migration to AWS needs to happen, and there are some reasons to minimize the amount of change that occurs during the migration, then this is usually my recommended migration method. One often overlooked technique during the migration is not to migrate. A general rule that I live by when looking at architecture is unless you have a reason to use a self-managed application, then don't use a self-managed application. Consume something as a service instead of operating it yourself. With as a service, we can use the same application as a service. You can use a service as a service, but you don't have to worry about its implementation, its management, or maintaining it or scaling it. It's all handled for you by the service provider. So while the cost might appear to be more, the behind-the-scenes cost savings will often more than make up for it. So some examples of this, if you utilize a Microsoft Exchange email system, then you might want to consider migrating to a Microsoft 360. If you run an on-premise, self-managed, customer-relationship management system, then consider moving to Salesforce. If you manage your own payroll, then consider a system such as Xero. Using applications that are delivered as a service reduces the admin overhead, the cost, and the associated risks. And so it's almost always a preferred option to consider before looking at the next three migration methods. The ideal solution is to use Microsoft Exchange as a service, but the ideal way to do a migration is via refactoring, also known as re-architecting. This involves a full review of the application architecture. The aim is to re-architect it to adopt architectures or products which are so-called cloud-native. Now this can mean substantial changes, and so the upfront effort of using this method should not be underestimated. You might look at adopting service-orientated or micro-designated migrations. If you're looking at a service-oriented or micro-designated migration, you might look at a service-oriented or micro-designated migration. Where applications are broken down into smaller, more specialized micro-applications or microservices. You might also adopt a more API-based architecture, or move towards an event-driven architecture, where resources are only being consumed when things are happening with your application, so when users are active within the application. Or even move to a serverless architecture, where you're avoiding the use of any of the services that you're using. The next step is to adopt a self-managed compute. Everything is being consumed as a service, including your compute resources. Now this approach is initially very expensive and time-consuming, and not something you generally want to rush, so don't use this when you have aggressive and firm deadlines. But it does offer the best long-term benefits for an application and its business customers. Running a cloud-native application, it's often cheaper, it's nearly all-in-one, it's nearly always more scalable, it offers better high availability and fault-tolerant characteristics, and the cost of the application often scales in a linear way to the amount of load that the application receives. And so the more usage that your application gets, potentially the more profit generating activities, the cost scales in a linear way with that usage. So it's almost always the right long-term strategy to refactor your application. And that's why I've been using the cloud-native application. Now this being said, there are also some situations when you don't want to migrate applications at all. I've been a part of many situations within my own clients where I've found systems which are running for no apparent reason. Servers are active, applications are running, but with zero load. And in these types of situations, it's often more effort to perform an extensive audit versus just leave them running on a load-free basis. So if you're running a cloud-native application, you're going to have to make sure that you're running it on a low load basis. So if you're running a cloud-native application, you're going to have to make sure that you're running it on a low resource virtual machine. Now a migration is a perfect time to reevaluate this and avoid the costs of a migration. Simply put, if you don't need the application, if you can't demonstrate that it's performing a valid business function, then switch it off. In doing so, you'll reduce your AWS side resource usage and the effort involved in a migration. And in many cases, that itself can save between 10 and 20 percent of the costs of a load-free application. So if you're running a cloud-native application, you're going to have to make sure that you're running a cloud-native application. So if you're running a cloud-native application, you're going to have to make sure that you're running a cloud-native application. And in many cases, that itself can save between 10 and 20 percent of the costs of a load-free application. So if you're running a large-scale migration, always use a large-scale migration as an opportunity to be efficient and identify any components of an infrastructure which don't need to be migrated. Now the last option is to do nothing. In some cases, a migration isn't an all-or-nothing process. You might have some applications where the usage is uncertain, but there are some complicating factors that prevent retirement of that application. So it might be an old application where you know that it has some usage, but it's not worth the effort. Maybe an old financial application which is past its legal requirement, but the finance team like it to be kept around for legacy reporting. So in this case, it might be the optimal approach to leave the application running on a physical machine or virtual server in an on-premise environment and just do nothing about it. You might have a complex application, which is simply too much effort, and one option is to leave it till last or at least a later part of the process. Or you might have a super important application and it presents too much risk to migrate in the short term. In all of these cases, the best approach might be to retain the application as is, complete the bulk of the migrations of the wider process, and then circle back and review the situation with the small number of results. So if you're running a large-scale migration, you might want to do that. So if you're running a large-scale migration, you might want to do that. Now visually, this is the process that I normally use in production-style situations to map my migration projects. On the left, application discovery, audit and prioritization. And then for each application, I need to choose, do I migrate the application or not? If I choose not to migrate the application, then I have two further choices. I can either retire the application and look to plan its decommissioning, or I can do nothing and hold it for future reassessment. If I do decide to migrate an application, then I have four methods to choose from. And these are re-hosting, re-platforming, re-purchasing, or re-architecting. Now re-hosting is a simple move from on-premises to AWS, so maybe a virtual machine to EC2 migration. Re-platforming is the same, but with some customization, so maybe moving from a self-managed database through to the RDS product, or abandoning a self-managed load balancer and moving to the ELB or Elastic Load Balancer product. Re-purchasing is the process of moving away from an application that you self-host through to consuming an application as a service. And an example of this is moving from Microsoft Exchange, which is a self-hosted email service, through to Microsoft Exchange. You might want to do that if you're using Microsoft Exchange for your application. If you have a Microsoft Exchange application, then you might want to do that. For example, if you have a Microsoft Exchange application, then you might want to do that if you have a Microsoft Exchange application, because Microsoft Exchange is not a Microsoft Microsoft 365. And then finally we've got re-architecting and this is the most complex and this involves either moving parts of the application or the whole application architecture through to cloud native architectures or cloud native products. So things like queues, notification products, managed APIs, function as a service and much more. Regardless of the method that you choose you should always design in some validation, transition to production and then finally the move over to a production application running in AWS. And that's it the six R's of cloud migration. Now this might seem like a lot of theory but if you just picture this every time you're thinking about a migration in the real world use this process as part of your project planning in the exam constantly ask yourself which of the following is the most important part of your project planning? Which migration method is being used in a given question scenario because that might help you answer the question itself. If you know the migration method that's being used it will help influence which answer is likely to be correct. With that being said that is everything for the theory. Now I promise it will come in useful so while I apologize for what is a hundred percent a theory bomb it is worth it. Thanks for watching go ahead and complete this video and then when you're ready I look forward to you joining me in the next lesson.