 Welcome to this demo lesson where together we'll implement a very simple instance of AWS Single Sign-On or SSO within the Animals for Life AWS organization. This is the architecture that we're going to aim for, AWS SSO configured within the management account of the organization, allowing a store of identities configured within AWS SSO to access all of the accounts of the Animals for Life organization using various different permissions levels. The idea is that the SSO portal can be used instead of IAM to log into the accounts as required. And as I mentioned at the end of the previous lesson, AWS SSO doesn't have any associated cost, and so there are no downsides, only improvements. So let's jump in and get started. Now you won't actually need any assets or lesson files for this demo lesson. It's something that's purely configured from the console UI. To get started, just make sure that you're logged into the general, so the management account of the organization, as the IAM admin user. And then just make sure that you've got the Northern Virginia region selected. A requirement of AWS SSO is that you have a... valid AWS organization created. And as we've done that already earlier in the course, we can proceed to the AWS SSO console. Now to do that, go ahead and type SSO in the find services box, click on SSO, and that will move you across to the SSO service console. Now, when you enable SSO, it makes some org changes. Specifically, it creates some IAM roles within all of the accounts of the organization. So that's a good start. Now, let's go ahead and do that. It can take up to 30 seconds to enable, but once done, you're welcomed to the product. And that's it. All of the hard work has been done for us by the SSO product. Now, if you do get an error saying that your previous attempt to enable AWS SSO was not successful, then you're welcome to do that again. And that's it. So that's a good start. So that's a good start. So that's a good start. So that's a good start. If your previous SSO failed, then just go ahead and try clicking the button again. And if that doesn't work, click on refresh. Nine times out of 10, that will complete the process and take you to the SSO dashboard. Now, in the previous lesson where I covered the theory and architecture of SSO, I talked about how SSO uses an identity source, the place where all of the identities are stored. Now, by default, SSO uses an internal identity source. And this allows you to manage things. So you can manage things from the SSO console itself and doesn't require any extra identity providers. Now, you can click on change here and select from another option such as active directory or another external identity provider. And both of these are useful if you already have an existing identity infrastructure and you want to utilize that. Now, we're not going to do that. So we can go ahead and click on cancel to return to the previous screen. The SSO product provides. A user portal similar to that provided by on-premise identity providers such as ADFS. And it's here where you can click on customize and change the URL that's used. So go ahead and do that. I'm going to go ahead and enter animals for life. You should enter something similar, but it does have to be unique. So go ahead and enter a suitable name and click on save. And if you do get any errors about it being in use, just change it to something else. Click on save again. And we're going to be able to go back to the SSO. And repeat the process until it accepts the name change. And if like me, you get this rate exceeded message, then just wait a few minutes and click on save again. This will change the portal URL to something that's much easier to remember and work with. Specifically, if this was a production system that we were giving to users. So if you're giving it to real staff of your organization, you want to make sure that the user portal URL is simple to use and make sense for your business. Now, at this point, we're going to continue to work with the SSO. So let's go ahead and configure some permission sets. Permission sets are the way that you can define role-based permissions that can be assigned to a user or a group over a certain AWS account. And this is how we're going to configure our SALI user with billing access over all our AWS accounts. So to do that, we're going to go ahead and click on the permission sets tab and then click on create permission set. Now, you get the option to use a predefined permission. So we're going to create a permission set or a custom permission set. And we're going to use a predefined one. So select that option. And we're going to create a number of permission sets. So the first one we'll create is the administrator access. So select the option at the top and then go ahead and click on next. The permission set name's pre-populated. You might want to change the duration of the session. The default is one hour, but you have the ability to select 1, 2, 4, 8, 12 or custom duration. And this basically determines the amount of time that a user can be logged on before the console logs them out. So generally for production usage, you might pick something a little bit longer than one hour. For this demonstration, we'll be using four hours. So go and pick that option. Then click on next, scroll down and create. We're going to be creating another one of these permission sets. So again, click on create permission set. This time, put view only access. Click next. Change the session name. Set the duration to four hours again. Click next. Scroll down, click on create. We'll do this process again. So create another permission set. This time we'll put power user access. Click next. Change to four hours. Click next and create. And then for the final time, click on create permission set. Pick billing this time. Next. Change the duration to four hours. Next. And then create that permission set. And there are the four permission sets that we're going to use. For this demonstration. So if we assign these permission sets to a particular user or a group within the SSO product. And give that user or group this permission set over one or more accounts. It means that they can gain access to that role based set of permissions whenever required. And they can do that in a very user friendly way using the SSO portal. Now next we need to create a user. And we're going to create the set. And we're going to create a Sallie user. Which is a user that we're going to give billing only access to all of the accounts within the Animals for Life organization. So next click on users on the menu to the left. And click on add user. For the user name go ahead and type Sallie. And then check the box that says generate a one time password that you can share with the user. This will mean when this process is finished we're presented with all of the authentication information that we can provide to the user. And then click on add user. And then click on create. And then click on create. And then click on create. And then click on create. And then click on create. And then click on bring nice to spRay. And then click on save. And then click on create. And then you'll be able to get ela-data. And then click on create. And then click on how to use ayuda-mon coun telescope. And that's the hddm нек to check this all out. And all of the importantities that are in our data. Make sure that you get a signal in there. And make sure you hear me of what this all says. Because I'm going to come down to this wall. And now I want to see what type of data is ese, OK. So now I want to use my data and the data content. To create my data. It doesn't actually have to be a correct email just because we're doing a demonstration. If this was production, you would need to make sure this was accurate. So go ahead and enter that email and then enter Sally as the first name, Jenkins as the last name, and that should set the display name. And then just to show you some of the other data entry options that we've got, we can expand contact methods and there's a phone number, job-related information. There's some other work-related information that's set there. Address, again, that's optional and that contains address information. Preferences, where you can set things like the time zone. And then additional attributes, which are currently used if you're using Office 365, but we're not. So click on Next. Now we want to assign Sally to a group, but we don't have any groups to assign her to. So we need to go ahead and click on Create Group. Once we're at this screen... Enter a group name of Billing. Under Description, go ahead and enter Animals for Life Billing Group. Scroll down. Click on Create Group. Now that's created the group. You can go back to the previous tab, which should still be in the user creation process. Then just click on the refresh icon. You should see the newly created Billing Group. Check the box next to Billing and click on Next. That's assigned Sally to the Billing Group. Then scroll down to the bottom and click on Add User. Now this dialog contains all of Sally's login credentials. So the easiest way to store these is to just click on the Copy button and then paste that down somewhere safe, because you're going to need that shortly. Once you've pasted that somewhere safe, go ahead and click on Close. Now to actually assign Sally some permissions, we're going to do that via the Billing Group. In order to do that, we need to click on AWS Accounts. On the menu at the left. And then select all of the AWS accounts inside the Animals for Life organization. And click on Assign Users or Groups. But then instead of assigning a user, we're going to click on the Groups tab to assign permissions for the Billing Group over these AWS accounts. So check the box next to Billing and then click on Next. And we want to allocate the Billing Permission Set to the Billing Group over all of the accounts in the organization. So make sure the Billing Permission Set has a check in the checkbox next to the name. And then click on Next. And on this screen, click on Submit. Now at this point, AWS SSO will begin configuring those AWS accounts. It performs some processing behind the scenes. And specifically, it has to set up SAML Federation. It has to create role Billing, which matches to the Permission Set also called Billing. And then it has to allocate. All of the permissions. And it does that for all of the individual AWS accounts in the organization. And then if you select Dashboard, you'll see at the bottom the User Portal URL. So go ahead and copy this into your clipboard. And then we're going to need to open a brand new browser tab, either using a separate browser or the incognito feature if you're using Chrome. It'll need to be a completely separate browser session or browser. Because I don't want to have to do that. So I'm going to go ahead and do that. And then we're going to need to open a brand new browser tab, either using a separate browser or the incognito feature if you're using Chrome. And then we're going to need to open a brand new browser tab, either using Chrome. Or we don't want to impact the login that we have to the IAM admin user within the management account of the organization. So make sure that this is a completely separate browser or incognito mode of the same browser. Or if you're using Firefox, a completely separate session. You'll need to enter the username of Sally and click on Next. You'll be prompted for the password that was automatically generated by AWS when you created this SSO user. So enter that and click on Signed. Then you'll have to pick a brand new password, so create a new password and enter the same password in the confirm password box, and then click set new password. And that will log you in to the AWS single sign-on portal for the SALI user. Now you'll see any applications that this user has access to, and because we've only configured account access, you'll see an AWS account icon that you can click to expand all of the accounts that you have permissions over. Now the account names, numbers, and account root user email address will be different for your accounts, but you should see a dev account, a general account, and a production account. Now you can expand any of these. You can move these accounts and see the role which matches the permission set that we defined and allocated to the SALI user via the billing group. So what you're seeing here is the role, the permission set over a particular account, and SALI has visibility of these because she's in the billing group. Now for any of these accounts, let's use the dev account for this demonstration. You can click on management console to be moved into that account. And that's a single sign-on. You don't need to enter any additional credentials. Once you're in that account, if you click on the account drop-down, you'll see that you're currently logged in using a federated login. And at the end of that federated login string, you'll see the SALI user. And that demonstrates that you're currently logged in to this account using SALI. Now to demonstrate the permissions, go ahead and click on billing dashboard. Remember that SALI has billing access over this account, so you should see no permissions there. And you should be able to click on bills and open up the current monthly bill and not get any errors. You should have full access to that area of the console. Now if we close this tab down and return to the SSO portal, this time let's do the same process for the general account. So click on management console. And that will move you into the management console. And if you click on billing to go back to the billing dashboard, click on bills again. You should see a different set. Of bills, because this is a different account. But you should see no permissions errors. Now the flip side of that, if you move to a different service, so click on the services drop-down and select EC2. And then click on running instances. This time you'll get an error. An error occurred fetching instance data. You're not authorized to perform this action. And that's because the SALI user is currently using the billing permission set. Which doesn't have access to any of the services within AWS. Okay, so this is the end of part one of this lesson. It was getting a little bit on the long side. And so I wanted to add a break. It's an opportunity just to take a rest or grab a coffee. Part two will be continuing immediately from the end of part one. So go ahead, complete the video. And when you're ready, join me in part two.