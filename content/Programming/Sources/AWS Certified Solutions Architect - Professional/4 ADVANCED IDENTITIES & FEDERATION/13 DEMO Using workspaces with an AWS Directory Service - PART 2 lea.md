 Welcome back, this is part two of this lesson. We're going to continue immediately from the end of part one, so let's get started. Now that the directory service is in the active state, we can proceed and actually create a workspace. So to do that, click on workspaces on the menu on the left, and when that's loaded, go ahead and click on create workspaces. Now when launching the first workspace, there are a few bits of configuration that you need to provide, and the first bit is you need to define the directory that will be used for the workspaces. You should find that you've only got the one listed, but make sure that you pick the animals for life directory, or whatever you called it in the previous step. Now if we click on next, we'll see an error message. We have to select a registered directory to continue. This isn't a registered directory, and so we need to fix that. So with the directory selected, go ahead and click on register. Now on this screen, we need to pick two subnets that any of our workspaces will go into. Now these two subnets can be any of the three application subnets in availability zone A, B, or C. So we just need to pick two of them, and not all availability zones will be valid to use, and so you might have all three available, or only two. Now if you click in the subnet one drop down, you'll see a problem, you don't see the names of these subnets. All you see is the availability zone, and the CIDR that's used. So to help us with this, we're going to click in the search box at the top and type VPC, open that in a new tab, go to that tab, and then click on subnets. If we order these by name, I want you to locate SN app-A, B, and C. And what I want you to take note of is the CIDRs for each of these subnets. So it's 10.16 in all three cases, and then we have 32, 96, and 160. So remember those, 32, 96, and 160. Then we're going to go back to the workspaces console, and click in the subnet one drop down, and I want you to pick the first available of these. So 32, 96, or 160. In my case, I can pick 32. So I'm going to go ahead and do that. If you can't see 32, then look for 96. You should have two of these available. So I can pick 32, I'm going to do that. Then I'm going to click in the drop down for subnet two, and look for the next available application subnet. Remember the other options I had available were 96 and 160. Now note how I can't see 96. You might see this, but in my case, I can't. So I need to pick the next available. So I'm going to pick 160. Now the other two options under configuration are enable self-service permissions, and enable Amazon WorkDocs. And we're not going to use either of these, so you can leave both of these unchecked, and click on register. Now we've successfully registered this directory, we can click on next. We're going to need to create a user which is going to log into the workspace. So click on create additional user. You'll need to pick a username, a first name, a last name, and a username. So click on that. And then you'll need to pick a username and an email for this user. And it needs to be a valid email. So make sure that it's an email address which works. So enter all of those four values, and then click on next. Now you'll see a list of all of the users within the directory, including the one that you've just created. So go ahead and check the box next to the user that you created. In my case, it's this one at the top, acantrel. And then click on next. At this point, you need to pick the bundle to use, which is the one that's used with the workspace. And a bundle controls the operating system, the software that's installed on that operating system, as well as the performance characteristics of the workspace. Now we want to pick one that's free tier eligible. And there are likely to be multiple of these. One might use Amazon Linux 2, and others might use Windows. So go ahead and pick a free tier eligible standard bundle, which uses the most recent version of Windows. So in my case, I'm going to pick standard with Windows 10 Server 2019 based. Don't worry if this differs slightly when you're doing this demo, as long as you're using a free tier eligible standard bundle, which is based on Windows. So select that and go ahead and click on next. It's at this point when you can pick the running mode. We have always on, which is where workspaces have a monthly cost, and you'll build based on that monthly cost, regardless of how often you use the workspace. And then we have autostop, where you'll build by the hour, and the workspace will stop when it's not used for a certain amount of time. So we're going to pick autostop, and we're going to select number of hours 1. And this means the workspace will stop one hour after it was last used. So go ahead and pick autostop and number of hours 1, and then click on next. At this point, you're offered some final customization. You can change the bundle with this drop-down, as well as being able to encrypt the bundle. You can also change the root and user volume with a specific KMS key, and you can also configure the root and user volume size. Now we're not going to do any encryption, and I want you to go ahead and leave the root and user volume size as the default. So go ahead and click on next, then scroll to the bottom and click on create workspace. Now at this point, the workspace is going to take some time to provision. I'm going to close down the menu on the left by clicking on the cross. That will give me a little bit more room. to see these fields. And one of the fields is status. It currently shows as pending, and we need this to move into an available state. So go ahead, pause the video, and wait for this to change to available. And at the same time, you're going to receive an email, which contains some activation instructions. You'll need both of those before you resume this video. So pause the video, wait for the status to change to available, wait until you get that email, and then you're good to resume. Now after about 20 to 30 minutes, you'll see that your status changes to available, and you'll receive an email complete with setup instructions. Now at this point, we're going to set up the workspace as client. If you're using Mac OS or Windows on your local machine, then there's nothing else that you need to do apart from follow the steps within this video. However, if you're using Linux as an operating system, there is actually an extra step that you need to do. By default, the workspace as product doesn't allow Linux machines to connect. As we can see on this page, and I'll link this attached to this lesson, by default, workspace as web access and Linux clients are disabled. Now in order to allow this, we need to update the directory. Now to do that, back in the workspace as console, click on directories, then click on the directory itself, scroll down, look for other parts, click on platforms, click on edit, and then check the boxes next to Linux and web access, and then click on save. And that will ensure that you can continue with the rest of this demo lesson if you're using Linux as a local operating system. If you're not, if you're using Mac OS or Windows, then you don't need to perform these steps. Now go ahead and locate the email which you should have received, which contains the workspace activation instructions. The first part of the email includes a URL, which you need to open in your web browser. And once you open that URL, you'll need to complete your workspace as profile, and as part of that, you'll need to set a password for the user that you created in a previous step. So go ahead and enter a new password, confirm that password, and click on update user. Next you'll be taken to the Amazon WorkSpaces client download page, and you'll need to download the client for the operating system that you're using. In my case, Mac OS. So go ahead and download the client for the operating system that you're using. Now the installer will be different based on your operating system, but you just need to follow the steps as directed. Once the install's finished, start up the WorkSpaces client, and you'll be asked to enter a unique registration code that's also contained in the setup email. So copy that code into your clipboard, paste it into the box, and click on register. Now that'll take a few moments. It'll connect to the WorkSpace, just verify that everything's okay, and then you'll be placed at a login prompt. And you'll need to enter the username and the password that you created earlier in this video. So in my case, it's my username, and then the strong password that I just set. So enter both of those. You'll need to enter your own details, and click on sign in. You'll be authenticated first. This'll take a few seconds. You'll be asked whether you'd like to remember the credentials. You can choose to pick yes or no, because we'll be deleting this after this demo. And then it's going to connect to those shared WorkSpace resources. So the streaming and authentication gateway. And it's going to launch a session directly into your WorkSpace. Remember, this is a virtual desktop that's dedicated to you. And so it's connecting directly to your Amazon WorkSpace. Now the first time you log into the WorkSpace, it might take a little bit longer, because there are a few setup steps that need to be performed, before you can be connected and logged in. After a few moments, you'll be logged into your WorkSpace. It might show as a black screen for a few seconds, and that's okay. But eventually, you'll arrive at the desktop of your WorkSpace. Now this is a Windows desktop that's fully dedicated to you. This is the WorkSpace that you created earlier in this demo lesson. So if we open Windows Explorer, you'll be able to see if I click on this PC, that we only see the user profile drop. So the system drive is hidden. It's not visible. But for this user profile drive, we can use this to store any data that we want. And if we'd have selected to use the WorkDocs option, then this would be synchronized between the WorkSpace and our devices. But for this demo lesson, you're able to go into this folder. You've got your own home folder. Inside here, you've got your usual Windows home folder folders. So contacts, desktop, documents, downloads. Links, music, pictures. Everything else that you should be used to on a standard Windows desktop. Now because this is running inside a VPC, which does have NAT gateways, we've also got public internet access. So if you load the Firefox browser, and again, this might take a few extra moments while it performs all of the setup steps. And then if we go to say images.google.com, we'll see we've got internet access. And if we type cats, we'll see a collection of awesome animals courtesy of the Google Images search. We don't even need to set any infrastructure up to receive our daily dose of cat pictures. How amazing is the internet? Now you can do anything on this WorkSpace that you could do on a normal desktop PC or laptop. I mentioned in the theory lesson immediately before this one that this provides a consistent desktop experience. So imagine that I'm in the office, I'm browsing cat pictures, I want to close down this WorkSpace, I'm disconnecting for the day. So this is closing down on my work machine. Now imagine now that I've finished my commute, I'm at home, and I want to resume browsing all of these amazing cat pictures. I might choose to use the web client, so click on web access login. You'll need to enter the activation code again from the email. So get that from your email, paste this in and click register. Then you'll need to enter the same username and password that you've set up for the WorkSpace, and then log in. And just imagine that this is a different machine that you're using, but you still have access to the same desktop experience exactly as it was when you logged off from your work machine. So this allows you to have a virtual desktop that you can use from different locations, using different client operating systems, and even using a web-based client. Now that's everything that I wanted you to implement in this demo lesson. I hope that this has been a good practical experience of the WorkSpaces product. Now before we finish up, we're going to go ahead and remove everything that we've created in this lesson. So the first step is just to close down the WorkSpaces client. Go back to the AWS console, click on WorkSpaces, then select the WorkSpace, and then click on delete. Then you'll need to confirm that deletion. That'll begin the process of removing that WorkSpace, so you'll see the status is listed as terminated. Now at this point I recommend pausing this video until the WorkSpace vanishes from this list, because you'll need to wait for it to finish terminating. So go ahead and pause the video, wait for it to vanish, and then we're good to continue. Okay, so now it's vanished. Now we can go ahead and click on directories. Select the directory you created for this demo, click on actions, and then deregister. Then confirm that process. Then select it again, click on actions, and then delete. And you'll need to confirm that deletion. And you'll need to confirm that deletion. Again, you'll need to pause the video and wait for the directory to finish the deletion process, so wait for it to vanish, and then you can resume the video. Once it's deleted, move to the CloudFormation console, select the A4L VPC stack, click on delete, and confirm that deletion. And that will put all the infrastructure back into the same state as it was at the start of this demo lesson. So once again, I hope this has been a useful demo, and you've gained some good knowledge and practical experience of the WorkSpaces product. But at this point, that's everything I wanted you to do in this demo lesson. So make sure you do finish cleaning up this infrastructure, and once you have, go ahead and complete this video. And when you're ready, I'll look forward to you joining me in the next.