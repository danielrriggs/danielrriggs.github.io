 Welcome back and it's time for you to get some experience implementing Amazon WorkSpaces. This is the architecture you're going to implement. You'll start with the Animals for Life VPC, complete with NAT gateways to provide public internet connectivity to any private VPC resources. Then you'll add a directory service which is used to support user management and authentication for WorkSpaces, and then finally you'll deploy and integrate an Amazon WorkSpace. Now the process isn't a complex one, but there are three points which might take up to 30 minutes each, so you'll have some time where you can pause the video and take a break. So let's jump in and get started. Now to get started you'll need to create some infrastructure, specifically the Animals for Life VPC, all of the subnets and other networking configuration, as well as the NAT gateways which will provide privacy. Now to get started you'll need to be logged in as the IAM admin user of the management account of the organisation, and you'll need to have the Northern Virginia region selected. Attached to this lesson is a one-click deployment link, so go ahead and click that link. This will open the quick create stack screen where everything is pre-populated. You'll just need to scroll right the way down to the bottom, check this acknowledgement box, and click on create. Now to continue with the demo we'll need this stack to have moved into a create complete status, and this can take anywhere up to 15 minutes. So go ahead, pause the video, and resume it once your stack changes to create complete. The next step is to begin the provisioning process of workspaces, so to do that click in the services search box at the top, type workspaces, and then open that in a new tab. Once there, assuming that you don't have any workspaces created, there'll be a link to the workspaces that you've created. So go ahead and click that, and you'll be presented with two different ways to create workspaces. So there's a quick setup and an advanced setup. Quick setup does most of the steps on your behalf, but because we want to experience exactly how this process works, and all of the individual steps, go ahead and click on launch next to advanced setup. Now as I mentioned in the workspaces theory lesson that came before this one, workspaces uses a directory. You can use the AD connector, which connects to an existing self-managed Active Directory. You can use the managed Microsoft Active Directory, which comes at a cost, but it provides full native Microsoft AD compatibility. Or you can use Simple AD, which is the one that we'll be using for this demo lesson, and this uses the Samba open source directory implementation, and so it doesn't support a lot of the more advanced Microsoft, Microsoft applications which expect an actual implementation of Active Directory. And this is important to know. If you need to run other applications inside your VPC, whether they're going to be accessed by workspaces or not, if they need a native Active Directory implementation, then you need to use the AWS managed Microsoft AD or the AD connector option. But because we're using this just for this demo lesson, go ahead and select Simple AD. Once you've selected that, go ahead and click on next, and you're given a number of different options. So the first one is to pick the size of the directory. So it comes in two different sizes, small and large, and the difference between these two logically is the amount of entities that it can support. So the small directory can have up to 2,000 objects, including around 500 users, groups, and computers, whereas the large directory can cope with up to 20,000 objects. So the large directory can cope with up to 20,000 objects, so for this demonstration, just go ahead and select small. Now at this point, you'll need to pick your own details for these next three boxes. First, you'll need to select an organization name. So I'm going to go ahead and pick Animals for Life. Next, you'll need a directory DNS name. Now this should really be based on the organizational name that you've selected above this, and I'm going to go ahead and pick directory.animalsforlife.org. Now this doesn't need to be a publicly accessible DNS name. It will only resolve inside your VPC, and best practice is often to take the DNS name of your organization and then prepend it with something like AD or directory. So in my case, directory.animalsforlife.org, but you need to pick something that's relevant to you. Use something different than what I'm selecting on screen. Next, you'll need to pick an optional NetBIOS name for the directory. This will be a smaller directory, but it's not a big deal. So I'm going to use A4L. Try and keep it consistent and set something different than what I've used on screen. Now as part of creating a directory, you're actually creating a directory administrator. So this is the administrator user. The username is administrator, and you need to pick a password for this administrator user. So use a strong password and just make sure that you note it down somewhere safe. So you can see that the user name is administrator. And you can see that the user name is administrator. So you'll need to enter the password in both the password and the confirm password boxes. And once you've done that, go ahead and click on next. Now if you get this error, you could be using an organization name that's used elsewhere. So I've done this intentionally to demonstrate this process. So I'm just going to add AWS onto the end of this. So animals for life AWS. So once you've got all of that set, go ahead and click on next. And keep repeating that process until you get something unique. And when you click on next, you move to this next screen. Where you have to choose the VPC and subnets to deploy your directory service instance into. So first you'll need to select the VPC. So click on the VPC drop down. And make sure that you pick the animals for life VPC. You might well see a default VPC. Don't pick that one. Make sure you pick the animals for life VPC. It should be called A4L-VPC. Once you've done that, you need to pick the subnets that this directory service will be deployed into. So what I want you to do is to pick the application A and application B subnets. So click in the top drop down. And then look through this list for SN-APP-A. And select that one. And then in the second drop down, click on it. And select SN-APP-B. And by setting those, you're selecting which subnets will have a network interface injected into them for the directory service. So once you've done that, go ahead and click on next. Review all of the details and click on create directory. Now this process could take anywhere up to 30 minutes. So go ahead and pause the video and wait for the status column to change from creating to active. Don't worry about the registered column. Simply focus on the status column. And you need to pause the video and we can resume once this status changes to active. Okay, so this is the end of part one of this lesson. It was getting a little bit on the long side. And I wanted to give you the opportunity to take a small break. Maybe stretch your legs or make a coffee. Now part two will continue immediately from this point. So go ahead, complete this video. And when you're ready, I'll look forward to you joining me in part two.