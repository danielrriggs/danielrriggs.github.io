 Welcome back, and in this lesson I want to deep dive into directory service running in AWS Managed Microsoft Active Directory mode. So let's jump in and get started. AWS Managed Microsoft Active Directory is built, as the name suggests, using a native Microsoft Active Directory implementation. It's similar to how RDS uses native Microsoft SQL if you pick that mode. Now it's not only compatible with Microsoft Active Directory, it is Microsoft Active Directory, just delivered as a service by AWS. Now it runs from within a VPC in subnets that you define, and it can be managed using standard Active Directory tools. So from a process and tooling perspective, no changes are needed if you already have an on-premises Microsoft Active Directory. It supports the usual unique... features of Microsoft Active Directory, such as group policy and single sign-on. Because it's actually using Microsoft Active Directory, it has a standard Active Directory schema. And this is a way of describing the way that the database is structured in the product. So it uses a standard Microsoft Active Directory structure. And it can support extensions to that schema for any Active Directory-aware applications which might require it. And examples of these are... products such as SharePoint, Microsoft SQL Server, as well as the distributed file system known as DFS. Now it comes in two sizes. Standard, which is designed for around 5,000 user businesses, and can hold up to 30,000 objects. Or you can select Enterprise, which can cope with up to 500,000 objects. At a high level, the product is used for Active Directory authentication and authorization... for products and services within AWS. All of the Directory Services modes do a similar thing. But this mode is designed for those situations when you need a native Microsoft Active Directory. So that's important to realize for the exam. Now the product is highly available by default. It deploys into at least two availability zones. With one domain controller in each of those AZs. Now you can deploy more... if you want additional levels of resilience. But there will always be at least two. Because it's a managed service provided by AWS, as you would expect, it includes... monitoring, recovery, replication, snapshots, and maintenance windows... which are all configurable and fully managed by AWS. Now towards the end of this lesson, I want to show you the architecture visually. But before I do, here are some aspects... of this specific mode... of Directory Service which are unique. Running Directory Service in Microsoft AD mode means that it supports... trusts between it and on-premises directories. And this is both one-way trusts and two-way trusts. And this means that you can use your identities that are contained within your on-premise... Directory Service to access AWS side resources and vice versa. If you run Directory Service in this mode... it's a fully fledged... Microsoft Active Directory running inside AWS. And this means that if the network link fails between AWS and your on-premises network... well then the AWS side Directory Service still functions as expected. This is not the case with the AD Connector which we'll be talking about in the next lesson. But for the exam, remember that a Directory Service running in this mode... is capable of operating independently of any on-premises Active Directory. Now it also supports radius-based multi-factor authentication. And this is achieved by integrating the product with any on-premises radius server... or radius infrastructure that you already have in place. And this is a keyword that you should look out for in the exam. Overall, if you have a requirement for a directory which supports more than 5,000 users... if you require trust relationships between this service and your on-premises Active Directory... infrastructure, then you need to pick this mode. Now let's look visually at how AWS-managed Microsoft Active Directory... integrates with other parts of AWS, on-premises networks and other cloud platforms. All Directory Service modes logically run inside an AWS account. They actually run from an AWS-managed VPC... but they're injected into a VPC that you manage. Specifically, two subnets within a VPC. And into each of these subnets, an ENI is added which belongs to one of the domain controllers. Now the domain controllers are highly available by design... and they replicate data between each other. And this means that they can tolerate the failure of an availability zone. As a managed service provided by AWS, they also handle patching and maintenance automatically. Now try to commit this one crucial fact to memory. AWS-managed Active Directory mode... means that you have a full directory within AWS... running a native Microsoft Active Directory. It means that it has its own users, its own objects... and it can support any AWS services which require a directory service. So things like Console Single Sign-On, Workspaces, WorkDocs, WorkMail... even products such as Chime, Connect, RDS and QuickSight... can be integrated into your Active Directory. So they use the directory for their authentication and authorization. Directory service running in Microsoft AD mode is the only type... which supports one-way and two-way trusts with an existing on-premise Active Directory. And this means that the directory in AWS and the on-premises directory... are separate things with their own objects. But they can be configured to trust each other. On-premise users can access AWS resources via this trust... and users in the AWS directory can also access on-premise resources. If you're using this type of hybrid architecture... as part of a long-term migration into AWS... or if you expect your AWS environment to be part of your existing IT platform long-term... then you may want to configure them in this type of way. If the intention is that everything continue to exist... as part of a long-term hybrid network and hybrid platform... then Microsoft AD mode is ideal. Now this mode of directory service... because it's an actual implementation of a real Microsoft Active Directory... it's the only one which can support schema extensions... and support applications which require a native Microsoft Active Directory... such as Remote Desktop, SharePoint, SQL Server... and other Windows-focused technologies... such as the distributed file system known as DFS. And as I mentioned on the previous screen... it also supports things like multi-factor authentication... via integration with an existing RADIUS server or RADIUS infrastructure. Larger businesses might also utilize services in other cloud environments... such as Microsoft Azure. And this might be Azure cloud services directly... or office and productivity applications such as Microsoft 365. Now these tools are capable of using their own identity stores... or they can consume things that you already have. And so using this mode of directory service... means that you can use Microsoft-based identity solutions... such as synchronization using a Dersync server... or identity federation using Microsoft Active Directory Federation Services or ADFS. Now these are all advanced features that require a full native Microsoft Active Directory implementation... managed by AWS. As well as this though, AWS infrastructure services such as EC2... can also use the managed Microsoft AD for authentication and authorization. They can use the other modes too, but the Microsoft implementation also supports this. So for the exam, if you see any mention of Microsoft AD... or something that requires Microsoft AD, keep this mode in mind. If you need trusts, then it has to be this mode. If you need MFA using RADIUS... then this mode is one option, but so is AD Connector. If you need something that can support Microsoft Schema extensions... then this is the one to pick. Simple AD mode uses a Samba implementation... and so it's not natively Microsoft Active Directory. Now in the exam, it should be simple to pick between this and Simple AD. If you need to support larger user numbers... or if you need an actual native Microsoft AD... Microsoft Active Directory implementation... or if you need trusts or RADIUS based MFA... then it needs to be this mode and not Simple AD. Picking between this mode and AD Connector... is generally determined by if you actively don't want any directory data in AWS. In that case, if you don't want any directory data in AWS... then it's going to be AD Connector. If you need to be able to continue running... and if the network link fails between AWS and your on-premises environment... then Microsoft AD mode is the correct one to select. But with that being said, go ahead, complete this lesson... and when you're ready, I look forward to you joining me in the next lesson.