 Welcome back. This is part two of this lesson. We're going to continue immediately from the end of part one, so let's get started. Now again, let's close this tab down, and then if we return to the previous browser window, where we've got the AWS Single Sign-On Management Console open, and click on Groups, let's create another group. This time we can call the group Power Users, enter a suitable description, something like Senior IT Support Staff, and click on Create. Then click on that group, and click on Add Users, and we're going to add Sally to that group. So check the box next to Sally, and then click on Add Users. This means Sally is now a member of the Power Users group. So then click on AWS Accounts again, and we're going to allocate permissions over all of these accounts to this Power Users group. So select all of the accounts, click on Assign Users. Again, we'll be assigning the permissions based on groups, so click on the Group tab. Check the box next to Power Users, and click on Next. Now we're editing the permissions that the Power Users group has over all of the accounts inside the AWS organization. And so we want to give this group the Power User Permission Set on those accounts. So select the Power User Access Permission Set, click on Next, and finish this up by clicking Submit. Now this will take a few moments to configure, but once it has, move back to the Power User Access Permission Set, and click on Next. Now we're going to go back to the other browser tab or browser window, where you're logged in as Sally to the single sign-on console. And go ahead and hit Refresh. Now you'll notice even after the refresh, you don't see any additional permissions. To fix that, you need to sign out of the Sally user, so click on Sign Out. And then once you're at the login prompt, log back in as the Sally user. Once you do, you can expand any of those accounts, and you'll see that you now have access to the Power User Access. Permission Set. So for any of those accounts, make sure you're on the Power User Access row, and click on Management Console, and that will move you into the account. But this time, you'll have access to the Power User Permissions. And so now if you go to the EC2 console, and click on Running Instances, now you'll have the permissions to be able to interact with this service. Okay, so let's move back to the window that we're logged in to the SSO Management Console in. And now if you click on Applications, this is where you can configure SSO access for external applications. So go ahead and click on Add a New Application. And then into the search box, go ahead and type Dropbox. This is a common application that you might have used that supports SSO. So select Dropbox, and then click on Add Application. Now this is where you need to provide any SSO-based configuration. So there'll be some AWS applications. There's also some AWS SSO metadata that you'll need to provide to the application that you're trying to integrate with. And then down at the bottom, there's also application metadata, where you need to enter specific information about the application that you're trying to integrate. Now AWS SSO comes out of the box with full support for a number of different common applications. And so for any of those pre-configured applications, you'll tend to find that these boxes are pre-optioned. So you can see that the application is pre-populated. Now we're not going to configure this application, so just go ahead and click on Cancel. Click on Applications again, and then select the Dropbox option, click on Actions, and select Remove. You'll need to confirm that remove, so go ahead and do that. Now at this point, we're going to go ahead and delete all of the configuration that we've done so far, and actually create a replacement admin user for the AWS organization that utilizes Single Sign-On. So first, click on Users. Check the box. Click on the box next to Sally, and click on Delete Users. You'll need to confirm that, so do the same for Groups. So select Groups, select Billing and Power Users, click on Delete Groups, and then confirm that deletion. And then at that point, we're going to go ahead and create a new group. So click on Create Group. We're going to call the group A4L-Admins. And for description, go ahead and type Animals for Life Full Admins, and then click on Create. Next, click on Users. And we're going to use the username of IamAdmin. And again, we'll generate a one-time password that we can share with the user. We'll need to enter a suitable email address. So again, I'll make up a temporary throwaway email. For first name, I'll enter IamAdmin. The same for last name. If this was production, you'd probably pick a little bit more carefully. Display name will pre-populate to IamAdmin space IamAdmin. And that's fine for this demo. Then click on Next. And check the box next to A4L-Admins. Click on Next again. Scroll down to the bottom and click on Add User. At this point, you'll need to click on the Copy button to copy down all of these details into your clipboard. And once done, you can go ahead and click on Close to close this dialog. And then we need to actually allocate some permissions. So select AWS Accounts. Select all of the accounts in the organization. Assign Users. Select the Groups tab. Select A4L-Admins. Then click on Next. We want to assign Administrator Access permissions. So make sure that the checkbox next to Administrator Access is checked. And once it is, go ahead and click on Next. Then Submit. And that will add this permission set across the AWS accounts. Okay, let's switch back to the Browser tab. Where we have the SALI user open and logged in to the Single Sign-On Console. It should still be logged in to the AWS Single Sign-On Console. And let's just close down these excess tabs that we have open to the Management Console for the SALI user and the Instances Console. So both of these. And then go ahead and sign out of the SALI user from the Single Sign-On Console. Now we're going to need to log in as the IAM Admin user. So go ahead and enter IAM Admin. In this username box. And then move on. You'll need to enter the auto-generated password which AWS generated when you created the IAM Admin user. So go ahead and enter that and then move on. Now you'll need to choose and enter a real password for the IAM Admin user. You'll need to enter it once and then confirm. Make sure you've got this noted down because we're going to need it for the remainder of this demo lesson. Once you've set that, go ahead and move on. And you'll be logged in to the Single Sign-On Console. And then you'll be able to see the IAM Admin user. This should take a few moments. But once you're logged in, you should be able to click on AWS accounts. You'll see all of the accounts in the organization. And if you expand any of them, you'll see the administrator access permission set. And if you wanted to, you could click on Management Console to switch into those accounts and have administrator access permissions. Now just like when we configured the account root user and the IAM Admin user, we want to secure this account by allocating multi-factor authentication. And you can do that as the user that you're logged in as by clicking on the My Devices menu on the top right. Now you're going to get an error because this is something that we need to enable as an administrator. So go back to the AWS SSO console. Go to Settings. Then move down and click Network and Security. Click on Network and Security. Click on the Configure button. And we need to enable MFA settings. Now there are a few different options. We can configure MFA as being disabled, which is the option that's selected now. Or we can set it so that MFA is only required when sign-in context changes. So examples of this include different devices, different locations, or any unusual behavior. Or you can set so that MFA is required every time a user signs in. We're going to select this option, so check this checkbox. You can also set which types of MFA are allowed. This includes security keys and built-in authenticators, such as Apple Touch ID, Windows Hello, or any external keys such as FIDO or U2F-capable devices. And you can also allow authenticator applications such as Authy, Google Authenticator, or Microsoft Authenticator. So we're going to select both of these options. And then you can configure whether an MFA device is required or not. Specifically, this is where you can configure what happens if a user attempts to log in without a registered MFA device. There are various different options, but we're going to go ahead and select Allow them to sign in. So this means that a user can sign in without an MFA, and we want them to be able to add their own MFA device. So check this box at the bottom, and then click on Save Changes. Once that's done, move back to the other tab where you've got the IAM admin user logged into the Single Sign-On Console. Then click on Refresh, and now you should be able to continue adding an MFA device. Now to do this, you'll need to go ahead and click on Register Device. You'll have to pick the type of MFA device. We're going to choose Authenticator App. Check this box, click on Next. And then you'll need to use your Authenticator application. Scan the QR code. And enter one code generated by that application, and click Assign MFA. Once that's done, click on Done. Then sign out of the Single Sign-On Console. And sign back in as the IAM admin user. This time you'll need a username, a password, and an MFA code. And at this point, we've implemented this simple example of Single Sign-On within the Animals for Life organization. So this is the architecture that we're going to use. We've created a number of different permission sets. We've created originally a SALI user and a billing group. And we've demonstrated exactly how that looks when logged in via the Single Sign-On portal. And then in the second half of this demonstration, we've stepped through how to create an administrator user equivalent to the IAM admin IAM user. But only using the Single Sign-On product. And then because that user is an administrator, and so presents more risk from a security perspective, we've configured MFA on this admin account. And we've done that using the My Devices menu on the top right. Now the Single Sign-On product is capable of a lot more functionality. I mentioned how it can be integrated with other business applications, and other custom applications that use SAML. They have complex multi-account architectures within AWS. And so if you're a large organization, or one of your clients utilizes a complex multi-account architecture, then AWS SSO should be your default choice for providing Single Sign-On services and permissions management across lots of different accounts inside the same AWS organization. Now at this point, because the Single Sign-On product doesn't carry any associated cost, then I would suggest that you leave this setup operational. Because potentially, at other points in the course, where we're looking at any other identity topics, we can use this as one option to log in to the AWS organization. So there's no downsides to leaving this active. You've picked a strong password. You've selected to use MFA. So it doesn't offer any additional security risk. So at this point, that's everything that I wanted to cover. So go ahead, complete this video, and then when you're ready, I'll look forward to you joining me in the next video. See you in the next lesson.