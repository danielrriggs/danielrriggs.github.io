 Welcome back, and in this lesson I want to talk about Amazon WorkSpaces, a managed desktop-as-a-service product available within AWS. WorkSpaces is a desktop-as-a-service product, so you pay for a dedicated virtual Windows or Linux desktop delivered as a managed service. It's ideal for homeworking or if you need to provide staff with access to a desktop environment within the office. It's similar to software such as Citrix or remote desktop services, only, in this case, hosted within AWS. Now, the benefit of using this type of architecture is that you're provided with a consistent desktop that you can access from anywhere. You might start off by using it in the office, log off, and then log back on from home, and your applications, and crucially their state, are maintained. So it allows you to log off and then log back on from a different location, and resume in exactly the same state. Now, WorkSpaces, a product, provides access to both Windows and Linux operating systems of various different sizes, and these provide differing amounts of CPU, memory, as well as optional extras such as GPU capability. You can also utilize commercial applications on the WorkSpaces, either supplied by AWS or custom images, and you can also use them to run your own software. Now, WorkSpaces are either charged on a monthly basis or an hourly basis, and the hourly basis allows you to suspend the WorkSpace when it's not in use, but there is a minimum monthly base infrastructure cost in addition to the normal hourly rate. So this is something to keep in mind. If you use hourly billing and you don't use the WorkSpace 24-7, then potentially it can offer significant benefits. WorkSpaces use the Directory service as a store of identity and to provide authentication, and so logically, a requirement for using WorkSpaces is also an implementation of the Directory service. Now, you can utilize SimpleAD, which is the cheapest, and that's useful if you're running a proof of concept or doing an isolated WorkSpaces deployment. You can also use the AD connector mode of Directory service if you want to implement WorkSpaces on your own. You can also integrate with your existing on-premises Active Directory without having any Directory infrastructure directly running within AWS. Or you can use the managed Microsoft Active Directory mode of the Directory service if you need to utilize a native Microsoft Active Directory and you want to integrate it with WorkSpaces. Each WorkSpace uses a network interface which is injected into a VPC, and so for almost all the networks, you can use the VPC to run WorkSpaces. And so for almost all the networks, you can use the VPC to run WorkSpaces. And so for almost all the networks, you can use the VPC to run WorkSpaces. Now, if you're running a VPC network and you want to integrate WorkSpaces with your network, that a WorkSpace does, it uses this VPC and any associated networking infrastructure. WorkSpaces are accessed using client software installed on a normal desktop or laptop, and the bandwidth for this is included free of charge. But for everything else, so internal VPC traffic, accessing the public internet, or accessing on-premises networks, your normal VPC infrastructure is used, and this is charged. So the same way that all other data transit inside a VPC and data transit that uses a VPC is billed by AWS. It's all the same. Windows WorkSpaces can access Windows-based VPC resources, such as EC2 instances or the FSx file system, and any existing hybrid networks can also be utilized. So WorkSpaces can access on-premises networks over a VPN, or a Direct Connect private VIF. And Direct Connect public VIFs can also be used for the WorkSpace connection between it and the client to improve performance. So improved transfer speeds and lower consistent latency. WorkSpaces provide a system volume and a user volume, and both of these can be encrypted at rest. It's based on EBS volumes, and so KMS can be used to provide encryption where the customer manages, and manages the keys within the KMS product. So WorkSpaces run within the AWS ecosystem, and interestingly enough, they don't run directly within customer-managed VPCs. So a typical WorkSpaces architecture might look like this. So first, the part of AWS that you manage, the customer-managed VPC. In this example, two availability zones with a public and private subnet within each availability zone. Now both WorkSpaces and the directory service which WorkSpaces requires to run, these both operate within AWS-managed VPCs, and they both inject interfaces into customer-managed VPCs, but more on this in a second. Now something else that also runs within this AWS-managed VPC has some WorkSpace-related shared components, namely authentication and streaming gateway. So the connection between the AWS and the WorkSpaces is a shared gateway, and it's these shared gateways that act as endpoints for the WorkSpaces client application, which runs on customer laptops or customer desktops. And the bandwidth for these connections is included as part of the WorkSpaces product. Now these gateways use the directory service for authentication, and they communicate with the WorkSpaces in the AWS-managed VPC for any management traffic. So the WorkSpaces themselves inject network interfaces into the customer-managed VPC, because they're used for other things such as authorization for Windows file sharing or for application authentication. The WorkSpaces themselves use normal network interfaces, which are also injected into the VPC, and these are used for all normal traffic. So anything that isn't management traffic related to the WorkSpaces use these injected network interfaces. Now it's through the normal VPC networking infrastructure that WorkSpaces can access other network zones. They can use NAT gateways to access the public internet, and any hybrid networking in place, such as site-to-site VPNs or Direct Connect, to access on-premises networks. WorkSpaces can use Windows or Linux and come in various sizes, such as value, standard, performance, bandwidth, and bandwidth. They can also use Power, PowerPro, Graphics, and Graphics Pro. And each size delivers additional resources and capabilities such as graphics processing. Now one important thing which is really relevant for the exam, WorkSpaces are not highly available by design. They're just virtual desktops running on a host within an availability zone injected into your VPC in one specific subnet. And so they're susceptible to AZ-4. Now there is recovery, and you can utilize multiple availability zones and distribute your WorkSpaces across different availability zones to minimize the impact of a failure. But just be aware that WorkSpaces in isolation are impacted by any AZ failures within the region that they operate. Now that's everything that I wanted to cover in this video. Thanks for watching. Go ahead and complete the video, and when you're ready, I look forward to you joining me in the next.