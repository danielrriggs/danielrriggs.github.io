 Welcome back. In this lesson, I want to talk about the AD Connector mode of the AWS Directory Service product. AD Connector is a really powerful mode, but you need to be careful and select the right situations when it should and when it shouldn't be used. And that's what I want to help you with in this lesson. So let's jump in and get started. Like other modes of Directory Service, AD Connector provides you with a pair of Directory endpoints within an AWS VPC. And like other modes, it works by injecting ENIs into two subnets within a VPC. Once created to any AWS products which are capable of using a Directory, AD Connector appears as a native Directory. And so it supports services such as WorkSpaces, WorkDocs. What makes AD Connector different is that instead of handling requests directly, it redirects these requests to an existing on-premises Directory server. This means that no Directory data is stored inside AWS. It's all redirected to existing on-premises Directories. AD Connector mode allows you to use AWS services which require AD Connector. You can create a directory, such as WorkSpaces, and use these with an on-premises Directory infrastructure. Using this mode means you don't need to deploy additional Directory infrastructure into AWS. And products within AWS can see your on-premises Active Directory as though it was an AWS supported Directory. Now AD Connector supports architectures like Proof of Concepts running within AWS, where you want to use your existing base directory. So you can use your existing base directory to set up your own directory. And you can also use your existing base of identities with as little admin overhead as possible. Remember it just relays any requests to your existing on-premises Directory. It doesn't by itself perform any authentication or other Directory related workloads. Now there are two sizes for the AD Connector, small and large. And while they don't impose any explicit user limits, it does influence the amount of compute that AWS allocates to your existing Azure Directory. So it's not just a matter of how much information is being relayed in a given second. You can use multiple connectors to distribute the load. And all of these factors are things that you need to test during any testing or proof of concept phase as part of your project implementation. When you configure an AD Connector, it's placed into two subnets within your Azure Directory. The first one is the AD Connector. The second one is the AD Connector. The third one is the AD Connector. The fourth one is the AD Connector. The fifth one is the AD Connector. The sixth one is the AD Connector. And this is the AD Connector. It's placed within a VPC in different availability zones. And this means that it's resilient against AZ failure. And this means that it's resilient against AZ failure. The connector itself is configured to point to at least one directory server on-premise. Normally two. And as well as that information, you need to provide account information. And as well as that information, you need to provide account information. That the connector will use to authenticate with the on-premises directory platform. Now all of this information is configured when setting up the AD Connector. And once done, the AD Connector sorts and mäts the server with the AD Connector. at that point, AWS services can use it as though it was a full directory service implementation. They can use it for both authentication and authorization as part of those services. But for this to function, it needs a working network connection between the AWS VPC and those directory servers you configured when setting it up. Without a working network connection, it can't function, because there's no caching or data held locally inside AWS. Without connectivity back to the on-premises directory servers, the AD connector itself won't function. Now, the network connectivity is private, so this means either a physical connection, so a direct connect, or a site-to-site VPN between the VPC and your on-premises network. So for the exam, if you need a directory service implementation inside AWS, to use AWS services which need a directory, logically, you're going to use the directory service. But, if you want to do that quickly for proof of concepts, where you want as little admin overhead as possible, then you can use the AD connector. If you want to do that and you're prevented from having any directory data in AWS, maybe for legal reasons, then you can use the AD connector. But, if you do decide to use the AD connector, you need working network connectivity between AWS and your on-premises environment. Otherwise, it simply won't work. Now, let's look at the architecture visually, because it will help commit this knowledge to memory. We start with the Animals for Life VPC on the left and a corporate network on the right. Between the two, a direct connect, or site-to-site VPN. And then, within the two subnets inside the Animals for Life VPC, we've got the AD connector. And then, we've got the AD connector itself. Now, when setting up the AD connector, you enter the IP addresses of directory servers in the corporate office, and the connector instances use these to relay any authentication or authorization requests through to these on-premise directory servers. And this occurs over the VPN or direct connect. Any AWS services which need a directory can utilize the AD connector as this directory. But, it's not always the case. But, just to keep reaffirming this, it is only a redirect. Nothing is stored locally in AWS. Generally, in the exam, you would only answer AD connector when there is a compelling reason to do so, in a given specific scenario that suits this product. Now, a couple of scenarios that meet this requirement are things like a proof of concept. This is normally a really good scenario for the AD connector, or maybe when you have a business which only has a network connection. This is a business which only has a small amount of infrastructure within AWS, but it does need directory capability so that it can use compatible AWS services. Or, the business might have legal or compliance reasons not to have any directory information within AWS. Just be aware, always, that by using the AD connector, you're adding a reliance on the network connectivity. Without working connectivity between AWS and the on-premises environment, the AD connector won't work. Now, the AD connector is a great piece of technology. I've used it myself for many smaller AWS deployments and proof of concepts. But, for anything larger, especially if there's only a single network connection, then I would have significant concerns. It would definitely be something that I would have to investigate before recommending it. So, for larger projects, certainly ones where you only have one network connection, then you do need to give significant thought to if AD connector is the right product. But, with that being said, that's all of the theory that I wanted to cover in this lesson. So, go ahead, complete this video, and when you're ready, I'll look forward to you joining me in the next.