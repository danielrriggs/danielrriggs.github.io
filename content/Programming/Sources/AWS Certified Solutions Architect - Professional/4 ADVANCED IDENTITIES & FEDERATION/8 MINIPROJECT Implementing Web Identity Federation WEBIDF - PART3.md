 Welcome back to stage 3 of this demo series. In this stage, you're going to be creating the Cognito Identity Pool. And it's the function of this Identity Pool to take the Google Authentication Token that you received at the end of the previous stage, and then exchange this using an IAM role for temporary AWS credentials. So once this exchange has taken place, these temporary credentials will be provided back to our web application. And it's using these credentials that we're then going to be able to interact with the AWS resources. So Cognito Identity Pools need to be configured with one or more authentication providers. And once configured, it will exchange successful authentications with these providers for AWS credentials. And as I mentioned a second ago, this is done by assuming an IAM role. And that's what you're going to configure in this stage of the demo series. So to do that, we need to go ahead and move across to our AWS console. Okay, so we're back at the AWS console, and now we need to configure Cognito so it can be used to exchange the Google Auth token into valid AWS credentials. So let's go ahead and move to the Cognito console. So type Cognito into the Find Services box. And then open that in a new tab. Go ahead and move to that new tab. What we need to do is to move to the menu on the left, and we need to click on Identity Pools. So we're going to be creating a brand new pool, and we're going to be calling it Pet IDF ID Pool. So go ahead and enter that in the Identity Pool name. When you configure a pool, you're able to select between authenticated access and guest access. Now you might use guest access, if you're creating a mobile application and you need to give any and all identities, say read-only access to a DynamoDB table. Generally guest access is used when you don't want to handle access control, but need to give a base level of access to all users of an application. Now we're going to be using authenticated access. We want to make sure that we only allow authenticated access to our resources. So that's what we're going to do. So go ahead and check. Authenticated access. Now once you've done that, you need to configure an authenticated identity source, and you'll see that there are lots of different options. Because we're configuring the Google identity provider, we need to go ahead and check the box next to Google. So go ahead and do that, and then move on to the next screen. Now because we're using authenticated access, we need to configure an IAM role, which will be used to provide AWS permissions. Remember, we're exchanging a Google identity token for AWS credentials, and this is done by assuming a role. And this role that we're configuring now is the role that will be assumed to generate those temporary credentials. So we're going to go ahead and create a new role. And for role name, go ahead and enter pet IDF ID pool auth underscore role. Now if you expand view policy document, you can see the base level of the policy document. And you can see the base level of permissions that are provided. We're going to be expanding on this, so don't worry. I just wanted to highlight exactly what's given a standard. At this point, go ahead and move on to the next screen. It's at this point we need to configure the integration with the Google identity provider. You need to enter the client ID, and you should have noted down this in an earlier step. So go ahead and enter the client ID into this box. It's the one that should end apps.googleusercontent.com. And again, make sure that you enter the client ID in the box. This won't function if you use mine. Under role settings, just leave this at use default authenticated role. You don't need to make any other changes to this screen. Go ahead and click on next. You need to name this identity pool. So go ahead and enter pet IDF ID pool. No other changes are needed on this screen. So scroll down and move on to the next screen. You're given the opportunity to review. We don't need to make any changes. We just need to make sure that we've got the right ID. And we're going to click on next. And we're going to click on next. And we're going to click on next. And we're going to click on next.chesindaad asleep resource. We're going to scroll down to сама site ID today. And we want to open up our events global. So as usual, just under plays. Tiktoks ing Hoop profiles you as you click on next and you enter the account ID you're given and your ID has found it. The That's me. Spikes and spricht to email me. The view is a voluntarily Solo Play, no extrademand or no need to a password at the time. And I'm seine That's for sure. So dear friend,�� You are supported by our services. innate Cognito with fear拮 role and using that role's temporary credentials to access S3. So what we need to do next is update the permissions allocated to that role and make sure that it has sufficient access to the private S3 bucket. To do that we're going to move to the IAM console so click in the services box type IAM and then open that in a new tab and once there go ahead and click on roles. Scroll all the way down and we're looking for the role that we just created. It should be called cognito underscore pet idf idpool auth underscore role. So go ahead and click on that role to go inside then click on the trust relationships tab. This is what defines what can assume this role. So the trusted identity is cognito hyphen identity dot amazon aws dot com. So it's cognito which can assume this role. Now it also has some conditions which restrict what can assume this role and the top condition is the one that we're focusing on and this ensures that only this identity pool is able to assume this role. So this means that only requests which are coming into cognito using this identity pool are able to assume this role, get temporary credentials and use those to access AWS. And the only identities which are able to use this idpool are the ones which are configured to be cognito. So this is the only identities which are able to use this idpool. So we're creating this chain of trust between our serverless application, the Google identity provider and then cognito via this idpool. Now on the permissions tab, we currently have one inline policy and this determines what permissions the temporary security credentials have when this role is assumed. Now we want to make sure that those temporary credentials are able to access our private patches effort. So we're going to go ahead and add a private patches effort. So we're going to go ahead and add a private patches effort. Now to do that we're going to add another policy and this was a policy created by the cloud formation template at the start of this demo series. So click on attach policies into the filter policies box, go ahead and type private patches and this should show the private patches permissions managed policy. And if we expand that it's pretty simple, it just grants list bucket and get object permissions on the web IDF hyphen patches private patches. So we're going to go ahead and add a private patches effort. So we're going to go ahead and add a private patches effort. So we're going to attach this policy to this role and this will mean that our serverless application is able to access this bucket. So check the box next to this managed policy and click on add permissions. Now that's everything that you need to do in stage three of this demo series. So now we've created the cognito identity pool, that's created an IAM role and we've adjusted the permissions on that IAM role to allow it to access the private patches bucket. So now we have all of the details required to configure our serverless application to utilize all of these different services. So the Google identity provider, cognito and S3. That's what we're going to be doing in the next stage of this demo series. But for now you can go ahead and complete this video and when you're ready I'll look forward to you joining me in the next.