 Welcome back and in this lesson I want to talk about a pretty niche CloudFront feature called Field Level Encryption. Now for this certification you really don't need to know about this one in detail, but it will help you to know that the feature exists and at a high level how it works. So let's jump in and get started. Now it's important to understand the security architecture of a normal HTTPS connection. The connection is negotiated between your browser and the web server, or in the case of CloudFront, the edge location which is being communicated with. So the path between the client and the origin can be encrypted using HTTPS, which is HTTP running over an encrypted tunnel. The problem with this architecture is conceptually your web browser and the web server are communicating using HTTP, that's then wrapped with a secure tunnel. And this sounds fine, but it means that information is being processed by the web server and all parts of your application as HTTP, which means plain text. Imagine that you're communicating with Amazon.com. So using your browser you add items to your cart, you hand over your credit card and other payment details, and that communication uses HTTP, which is wrapped in a secure tunnel. While it's transferred over the internet. In your browser and in the Amazon.com web server, everything exists in HTTP form, which is plain text. Now the same will be true if you use financial or medical websites. All of this information is handled together and treated the same. Field level encryption is something extra which happens at the edge. You can configure certain fields, which are encrypted by the edge, and then you can configure the edge location using a public key. And this happens separately from the HTTPS tunnel. So from the second that your credit card details or personally identifying information, health information, password, keys, or anything else sensitive hits that edge location, they're encrypted using a public key. And this encrypted data is grouped up with the other HTTP plain text data and wrapped in a HTTP tunnel. übersömtes file for transit across the internet which الإ ‫ have access to that private key, and those switch parts of an application can access the danger. The data could in theory restored directly into the network. Grow up life a database in the encrypted form and only a small group of users or components of your application have the permissions to view the data and this is important in certain industries like finance and medical. Before we look at the improvements that field level encryption provides let's look at a common flow that you might see without it. So we have a traditional cloud front architecture. We start with a viewer on the left then an edge location then a custom origin a database and a user for medical application on the right. For this type of application then conceptually communications would use standard HTTP wrapped in a secure tunnel between the viewer and the edge location and the same architecture would be used between the edge location and the custom origin. Now imagine that if your user was a customer of a cloud front architecture and you were able to view the data and the user would be using a medical application running on the viewer device on the left so the laptop. This medical application would be sending sensitive data in addition to everything else that it's communicating. So this sensitive data would start off on the laptop and it would be packaged up and sent along with everything else inside the HTTP stream within the HTTPS tunnel. So it would go through to the edge location through to the custom origin and the same architecture would be used to send sensitive data to the user. So this architecture would start off on the edge location and then send sensitive data to the user. So this architecture would start off on the edge location through to the custom origin where it would be written to the database probably in the same form and then as required it could be accessed by any authorized users. Now the key thing to understand is that without field level encryption the sensitive data is handled like all other data. While it is encrypted in transit this architecture doesn't really allow for different levels of security to be defined in the stream of data. So this is a problem for any industries which deal with sensitive data. Anything which needs to be handled really carefully don't suit this simplistic style of security architecture. So let's look at how this changes when using field level encryption. So we use the same basic architecture, the viewer on the left, the edge location middle left, the custom origin in the middle and then the database and then our doctor and we have the same sensitive medical data requirement. The same HTTPS tunnels exist between the viewer and the edge location and the edge location and the custom origin. Now the starting point of this architecture is the same. We need to move sensitive data first between the application and the edge location and this is where field level encryption takes over and where the process changes. So field level encryption is architecturally pretty easy to understand. First you generate a public and private key pair. Once configured the edge location uses the public part of this key pair, so the public key, to encrypt the specific field. In this case the sensitive data. Now once encrypted only the matching private key can be used to decrypt the data. Now at this point the data flows in the normal way, so it goes through the CloudFront network, through to the custom origin where it is still encrypted. The custom origin as its processing the data strips away the encrypted HTTPS tunnel leaving the normal HTTP data. So plain text and the encrypted sensitive data. So at this point the data is still encrypted. Now the custom origin might not even have the private key and so the data remains encrypted through to the database where it's potentially stored in its encrypted form. When the data is needed it's transferred from the database through to its intended destination maybe the device of a user with elevated permissions and then via some process the private key is used to decrypt that data so it's accessible in its original form. Now this architecture adds significantly more control. Sensitive data is encrypted at the edge and remains in an encrypted state separate from external. Everything else for its entire life cycle. The private key can be managed and secured in a separate isolated way with separate processes ensuring that only those entities or components of a system which need access have that access. So it's important that you understand exactly why this is beneficial. If we only have standard HTTPS communication then at every point where that HTTPS connection is turned on. The private key is terminated so at the edge location and the custom origin the plain text data exists in a vulnerable form. Using field level encryption the data is encrypted at the edge location using a public key and then only the parts of our application architecture that we want to be able to decrypt that data can decrypt the data using the private key. And if we have effective processes in place to secure that private key. Then we tightly control who has access to this sensitive data. So we can create a security architecture as part of our application which handles different components of data in different ways. And this is really essential for certain types of industries which handle really sensitive data. But at this point that's all of the architecture theory that I wanted to cover. So go ahead and complete this lesson and then when you're ready I'll look forward to you joining me in the next.