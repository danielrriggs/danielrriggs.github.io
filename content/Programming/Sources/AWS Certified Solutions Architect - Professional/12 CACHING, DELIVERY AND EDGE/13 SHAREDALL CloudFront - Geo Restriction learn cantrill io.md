 Welcome back and in this lesson I want to cover a topic which features all of the time on the exam and that's CloudFront geo-restriction. Simply put, this is the ability to restrict which areas of the planet can access content distributed via CloudFront. The concept of geo-restriction is simple enough to understand. It gives you a way of restricting content to a particular location. Now you know by now that CloudFront is a global content delivery network. Normally that content is distributed freely via all of the CloudFront edge locations. If you enable geo-restriction though, that changes. There are two types of restrictions that you'll come across in the real world and which get mentioned in the exam. The first is CloudFront geo-restriction and the second is third-party geo-location. It's absolutely critical for the exam that you understand the capabilities of each of these and most importantly, the differences. CloudFront's built-in geo-restriction is a whitelist or blacklist architecture. So either everything is denied and you whitelist specific countries or everything is allowed and you blacklist specific countries. Now the really essential thing to understand is that built-in geo-restriction functionality only works with countries. Specific international country codes can be allowed or denied. It works using a super accurate geo-IP database which claims 99.8 plus percent accuracy. And the configuration applies to an entire CloudFront distribution. So this is something that's configured on the distribution itself. Now just to reiterate this again, please remember that you can't configure the entire CloudFront distribution. Please remember this for the exam. It will get you at least one mark. The built-in geo-restriction provided by CloudFront is country only. Nothing more. You cannot restrict anything else except country using the built-in geo-restriction functionality. Now on the flip side, third-party geo-location is completely customizable. You can use this to access third-party geo-location. You can use this to access third-party geo-location. You can use this to access third-party geo-location. So it can be much more accurate than the built-in functionality. But the architecture can also be used to restrict based on users or browsers or even the login state of an application. I'm going to show you how this works in a second. But just at this point, remember the differences. The built-in geo-restriction is country only. And the third-party geo-location, as well as being much more accurate, can also be used to filter. Based on lots of other attributes. So let's step through both of these architectures visually so that you can more easily identify them and understand them. So there are six main components involved with CloudFront's built-in geo-restriction. First, we have our customers. Then we have a device, which in this case is running the Categram application. We have the public internet. We've got a geo-IP database. We've got a database. One or more CloudFronts. We've got a cloud front edge locations. And a cloud front distribution, which stores the configuration. Now the flow goes like this. Our customers interact with the Categram application, which at some point requests objects via the public internet, which means they'll be directed at the closest cloud front edge location. Now normally what follows is the normal caching process. But what also happens is that the edge location consults the cloud front edge location. The cloud front edge location is the closest cloud front edge location to the public internet. So we have the cloud front distribution to check if geo-restrictions are enabled. If not, then there are no restrictions and CloudFront just continues and performs the normal caching process. If geo-restrictions are enabled, then the edge location needs to get the location of the customer from the geo-IP database. Now remember, and I'm going to repeat this again, with this method, this is only a country code. Now that the edge location has a country code, for the user making this request, the edge location checks if that location is allowed. It gets a response which is either a yes or a no based on if it's a whitelist or blacklist configuration and if that country code is listed or not. If the country code is allowed, then the normal caching process is followed. Either the object is delivered from the edge or an origin fetch occurs. If the country code is not allowed, then a 403 forbidden error is delivered to our user. Now this method is nice and simple. Everything's handled internally by CloudFront and no extra services are required. But to reiterate this again, it only works based on country code. If you want to restrict based on anything else, then you need to extend this functionality yourself. And let's have a look at how that changes things. With third-party geolocation, we have more moving parts as well as some adjusted configuration. In addition to the previous components, we also need some form of application server or some form of compute. Third-party geolocation requires a piece of compute to be a decider as part of the architecture. And additionally, CloudFront is configured to be private. With this architecture, the flow starts off the same. Customers still interact with the application running on a device. But in this case, the application has some additional compute. Let's assume for now that this is an application server. At this point, keep in mind that because CloudFront is configured as private, no unauthorized access will be allowed. If any access is attempted, by default, a 403 forbidden error will be returned. Because CloudFront is private, any accesses will require a signed URL or signed cookie. And this means that the application server has complete control from here on. The application could keep things simple and just utilize a much more accurate third-party geolocation database. But the application could also check anything else. Examples include the username of the customer. If the customer has purchased something, maybe licensing information, or any other attributes. Now I've titled this screen geolocation or anything. Because once you move to this architecture and add a compute instance, you can go beyond location and beyond the capabilities that CloudFront has by default. And pretty much do anything that you want. The application server in this case controls whether the customers are granted access to the content or not. So in addition to this being used for the application server, it can also be used for any other filtering and authorization that you want to code within your application. If the application determines that you are authorized, then it generates a signed URL or cookie and delivers this back to your browser or back to your application. The application requests the object from an edge location passing in the signed URL or signed cookie. The edge location checks the authorization and assuming that the object is signed, and assuming that everything checks out, delivers the object back to our customer. Now what makes third-party geolocation different from CloudFront geo restriction? And remember this for the exam. It really is essential. Third-party geolocation or the architecture that uses third-party geolocation can be used for things beyond more accurate country checks. It uses a compute instance and it works using signed URLs. Or cookies. And so it can be used to allow or deny access based on anything that the application has exposure to. If you see any exam questions talking about restrictions based on licenses, user data fields, headers or any other data except country, it uses the third-party geolocation architecture. If you see any location restrictions mentioned which go beyond just country, so states or latitude and longitude components, then it uses the third-party geolocation architecture. If it's just country, if it's just country code, then it can use the built-in CloudFront geo restriction feature set. Remember that for the exam because every single time I hear about the exam being taken, there's always a question about restricting content and what architecture is required to implement the requirements. So keep that in mind. You need to be able to pick between geo restriction and third-party geolocation. But with that being said, that's all of the architecture for this lesson. It's really important so I wanted to keep it brief and focused. Thanks for watching. Go ahead and complete the lesson and when you're ready, I look forward to you joining me in the next.