 Welcome back, and in this lesson I want to discuss how caching performance and optimization works within CloudFront. Now, at an associate level, it's enough to understand the basics of CloudFront, so how it works. At a professional level, you'll need to be able to use CloudFront as well as optimize the caching performance. And for that, you need to have a detailed knowledge of how caching actually works within the product. And that's what I want to talk about in this lesson. So let's jump in and take a look. When we talk about caching performance for CloudFront, we use two terms which you're probably aware of by now. If you request an object from CloudFront and it exists in an edge location, then it's called a cache hit. It means that the object will be delivered with lower latency and better performance because it's stored closer to the person requesting it. If an object is not available within the... cache of an edge location, then it's called a cache miss. And this means that the edge location will have to go back to the origin and receive the object for you and then deliver it. This process is called an origin fetch, and it adds additional time, which decreases performance. Now, to have the best result out of CloudFront to achieve the best caching performance, then you need to aim to maximize the percentages of cache hits versus cache misses. And to do that, you have to understand how CloudFront identifies and tags objects which it caches, and how to influence that from a performance perspective. So let's take a look at that over the remaining part of this lesson. When most people think about caching, they think about the process as it relates to static content. And by static content, what I mean is objects which never change until you as a site admin or author change them. They aren't things which are changed based on... parameters that you pass to an application. So in this context, it means that whiskers.jpg is always whiskers.jpg. The image itself, the object, isn't dynamic. It's a static image of a shocked-looking cat. Now, how this might play out in practice is something like this. We have Julie, and she's interacting with a Categram application. She's loading the whiskers.jpg object. Because she's a trendsetter and the first person to interact... with whiskers.jpg, the object isn't cached at her location. And so she experiences a cache miss. The edge location doesn't have a copy of the object cached, and so it initiates an origin fetch, which loads the object into the edge location, and then returns that to Julie. Now, this process isn't optimal because the edge location can't immediately deliver... a cached version of the object. It needs to perform this fetch. However, if another user, let's say Bob, who lives near Julie, follows the same process, when he requests the object, because it's already in the edge location, it will be a cache hit. And this will mean that the object is returned much faster without the origin being involved. So the performance when you get a cache hit is much better than a cache miss, because you don't have to go back to the origin and fetch the object. It's delivered directly to the object. It's delivered directly to Bob, in this case, from a cache that's very close to his location. Now, that's how it works for static content, where the content is always the same for a given object name. But what about dynamic content, where the content being requested isn't always the same? It can be influenced based on parameters passed to an application. Well, let's look at that next. When requesting an object from CloudFront, for static content, you'll be requesting it based on the object name. With whiskers.jpg, the name of the object gives you an expected piece of data, that particular object. If you're dealing with media, then that's usually a safe assumption to make, that for a given object name, the content will always be the same. But it's not always true. In addition to the object name, an application might also provide some query string parameters along with the object. In this example, index.html, and the object name. And then, lang="en", is a key value pair which goes along with that. And this is a pretty common technique used to provide different languages for globalised applications. Now, you might also be providing cookies along with the request. And there'll also often be some request headers. Now, these could be anything, including the type of browser, the type of device, the screen resolution, or even the local operating system being used on your device. Depending on your application, browser, and operating system, a collection of these will be sent along with the request, and without using CloudFront, these would all arrive at your application. When you're using CloudFront, all of these arrive at the edge location. And this is really important to understand, specifically for the exam, and if you're designing real-world solutions using CloudFront. Because you have the ability to configure which of these are forwarded from the edge to the origin, and you have the ability to tell CloudFront whether it should base its caching decisions on all of them or a specific subset of them. And these last two choices can significantly impact how your CloudFront distribution performs. And let's explore exactly why. Let's say that you have an online store which sells socks, and it runs from a custom origin. And let's keep things simple, and say that they sell small, medium, and large sizes of socks in two different colours, blue and red. In the middle of the architecture, we've decided to deploy CloudFront. So CloudFront's in the middle, and at the left are application and application users. Now the way that the application displays previews for customers is to use query strings. Two different key value pairs are provided along with the application, and the first is the item that you want to retrieve. The first is colour equals and then a colour, and the second is size equals and then a size. Based on that, the application returns a preview image to show on the page, based on the size of the sock and the colour of the sock that they've chosen. Now imagine that the online store has millions of different types of socks in a range of colours and sizes. Now to reduce costs for the preview images, the store only uses the medium size. So for all of the socks in their inventory, they take pictures for every colour, but they only use the one size, and that's the medium size. So at this stage, as solutions architects, we need to give some thought to how this is going to work in terms of the query string parameters. Do we forward none or all of them to the application? And do we cache based on some of them or all of them? Let's step through some of these scenarios and look at how the one that we choose can influence the performance of CloudFront. First, what if we decide not to forward the query strings at all to the application and we don't cache based on them? This means that what's requested at the viewer side for say Julie is this. So sock.jpg and then colour equals blue and size equals large. So two query string parameters, colour and size. Now in a configuration where query string parameters aren't used for caching and aren't forwarded, then what the application will receive is this. Simply a request accessing sock.jpg. Now this might well cause the application to fail or error because none of the query parameters have been passed along with the request. The application will only know that sock.jpg is requested but won't know the size or the colour choice. This is actually the default behaviour for CloudFront not to forward query string parameters through to the origin and not to base its caching decisions on any of these query string parameters. Now this can be problematic. Let's assume to keep this example simple that the absence of any colour or size won't cause an application failure. The application has a default. What the application will do is if any of the parameters aren't passed through it will return a default sock. It's going to be a blue medium sock. This means that the response to Julie's request will be a preview image of a blue medium sock. This is going to be stored within the edge location and it's going to be cached as sock.jpg. Remember we're not forwarding any of these parameters through and we're not caching based on them. So the only thing that we use to determine what we're actually caching is the name of the object. So sock.jpg. But if we continue with this example what happens when Bob attempts to interact with the edge location in a similar way? Well Bob is trying to query the cache for a red sock with a small size. Because CloudFront is not configured to forward query string parameters and also not to base its caching strategy on any of those parameters what Bob is querying will actually match the blue sock image in the cache. And so he will get the same image returned. The image for the cached blue sock. Even though the query was for the red one. Now this is obviously problematic because we're not forwarding or basing any caching decisions on these query string parameters. It means that in this particular case Bob has actually received an incorrect image. So this is something that we need to resolve. Let's look at what else we can change with this example. Before we do let's look at the other extreme. This time we've configured CloudFront to forward all query string parameters to the origin and it's also configured to cache using all of them. Remember how earlier I mentioned that to save costs the business only uses one photograph for each colour of each sock. There's only actually one preview the medium size for a given colour. Even though there are multiple sizes. Now keep that in mind as we step through this scenario. Well let's say that Julie is looking for blue socks. But now she's looking at three different sizes. Small, medium and large. Because of the CloudFront configuration the query string parameters will all be forwarded on through to the origin. And what this will look like are three different queries to the origin and this represents three different origin fetches. So each of these will be passed through to the origin as an origin fetch. Now think about this for a minute because we've decided to cache based on all of these parameters. Remember how earlier I said that the same medium sock is used as the preview image for all of the different sizes. It means that because we've sent these three different requests to the CloudFront edge location with three different combinations of parameters so colour equals blue, size equals small, colour equals blue, size equals medium and colour equals blue, size equals large. This will cause the same object to be cached three times. So because we're caching based on all of these parameters as far as CloudFront is concerned this is three different objects. It also means that every time Julie interacts with this edge location she'll get a cache miss. Because as far as CloudFront is concerned none of the times that Julie is caching with this edge location that she makes these requests does it have a matching object cached inside that edge location. If you think about it for this socks example the size parameter, while it's important for the application it's not important for CloudFront because the same image is being returned regardless of the size. We can actually optimise this further and let's look at how. This time we're going to configure CloudFront to forward everything through to the origin so that the application can still see the query string parameters but we're only going to cache based on things that we whitelist. So generally when you're configuring CloudFront you only want to whitelist things which can actually impact the objects. So let's explore how this changes the architecture. So with this particular architecture we're only going to whitelist the query string parameters which can affect the actual image. So we still have Julie and Julie is accessing a small blue sock image and this still represents a cache miss because it's not yet in the cache. Now both of these parameters are forwarded through to the origin which results in the blue sock image being returned to the edge location so it's cached now and it's available. But we're not caching based on all of the query string parameters only the ones that we've decided to whitelist the one in red which is color. This means that the object is now cached based on the name and the color. The size is ignored because it's not whitelisted. Remember there's only one size preview for every color of every sock the medium size and so it's irrelevant what size is passed through to the application. For a given color it's always going to return the same image. This means that we can now see that we can actually reduce the capacity required at the edge and it also means that we can improve the cache hit ratio. Let's have a look at how. Well if Julie queries for a different size say she's still looking for a blue sock but now has selected medium this represents a cache hit because CloudFront is only caching based on the object name and the color. Likewise another query for the large size would be the same result because we've configured CloudFront not to cache based on size only the object name and the color. So no matter what the query the size won't be used for caching decisions and so we will have many more cache hits because any requests for the same color if they've been cached before at the edge location regardless of the size they'll represent cache hits and so this will reduce the storage at the edge and it will also improve the performance. So just in summary when you're configuring CloudFront forward only what the application needs and keep an eye out for this in the exam because it does tend to feature in the professional architecture exam. So forward what the application needs through to the origin but cache only based on what can change the object. So in the previous example we would cache based on the color but not based on the size. The more things that are involved in the caching the less efficient the process is because CloudFront has to maintain a unique version of that object for every combination of things which are used during the caching selection. So you should aim to minimize the number of things that you're whitelisting for consideration when CloudFront is making its caching decisions. With that being said that's everything you need to know for the exam. I know it's been a lot of theory but just try to make sure that you understand all of this end to end. It's a fairly nuanced topic but it is really important for the exam. With that being said go ahead and complete this video and when you're ready I look forward to you joining me in the next.