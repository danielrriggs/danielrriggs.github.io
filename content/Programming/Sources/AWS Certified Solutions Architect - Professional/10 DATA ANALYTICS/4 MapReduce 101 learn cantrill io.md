 Welcome back, and in this lesson I'm going to cover the basics of the MapReduce process. Now MapReduce is commonly known because it's implemented as part of the Apache Hadoop product and ecosystem, and it's the foundation of the Elastic MapReduce, or EMR, product within AWS. Now to fully understand how EMR works, I think it's useful to understand the MapReduce process at a high level. So that's what I want to cover in this lesson, so let's jump in and get started. MapReduce is a framework and system designed to allow processing of huge-scale data in a way which allows parallel processing. Imagine that you have an electronic copy of every single book in existence within one data file, and you wanted to count how many times every word was mentioned in that file to determine how popular certain words are. Well, using one single computer would take far too long, no matter how powerful that computing device is. There are types of workloads where you need to break it down into smaller, manageable jobs and allow for parallel processing, and that's what MapReduce allows. MapReduce has two main phases, Map, and you guessed it, Reduce. And then it has two optional phases which can be used depending on the workload, and they're Combine and Partition. Now you don't need to have a detailed understanding of how MapReduce works at this stage. This is really just to give you a high-level idea. So when I talk about the architectural parts of Elastic MapReduce, you'll understand what each part does and why it's required. At a high level, the process with MapReduce is this. First, input data is separated into splits, and splits are sections of the full input data split up into a certain number of parts. And then you have the process of splitting the data into sections. And then you have the process of splitting the data into sections. And then you have the process of splitting the data into sections. Now, when you want to split up a sis, we often say get out of this number by adding in sections. Every split has its location in MapReduce's группar. In Entity裡 of the difference between Entity Treble and Entity平 about m1, set handfuls of different tokenалоes in a single position. And if you'reolove, you automatically have several endpoints such as a sketchroom, a display, an array of data, a data cell differential, etc. I escreve here is how to outline the layers in such a way that in Elastic MapReduce yourself, there's realism, there's some thing-可以 listen because you'reprobably not in the general opp veel the results you get out of the amount of data that you're looking for, or of the elements that you quoted. The idea is the larger workload is broken down into splits, where processing can be completed in shorter timeframes. If the processing of a particular split fails, then it can be retried. Now, after all of the splits have been processed, the data can be recombined, and this is the reduce part. And the end result is the final output data, the result of the end-to-end job. So at a high level, it's about taking a big job, breaking it into smaller jobs, processing those smaller jobs using lots of mappers, which are the workers, and then recombining all of that data together to give you an end result. Now, once again, you don't need to know the inner workings of the map reduce process, but I thought it might be good just to do a quick high-level example. So let's look at that next. So with this example, to make it easier to work with on screen, it's going to be heavily. But I want you to imagine every book or document ever written by humankind in one single data file. I want you to imagine that you need to analyze that document to determine how often each word occurs. You need to work out the popularity of all words used over the entire history of humankind. For this example, we're going to be using a tiny subset. This is what we'll be using. Adrian has four cats, and they are the best cats ever. The first cat is Ruffle. The second cat is Truffles. The third is Penny. The fourth is Winky. Adrian has the best cats. Just imagine that this document is trillions and trillions of times bigger. All of the written information in the world ever. Now, this data within MapReduce is known as the input data. And logically, it has to be stored somewhere. Now, I mention this now because when we talk about the AWS implementation of MapReduce, it will start to make more sense. So this input data starts off stored somewhere that's accessible to the MapReduce process. So the first step is to split this data into splits, also known as input splits. And this is based on the split size. Now, in this example, to keep things simple, I've set the split size to be nine words, meaning that the number of splits is equal to the total number of words in the input data, divided by nine. In this case, we have a total of four splits. So the first split says Adrian has four cats and they are the best. The second split, cats ever. The first cat is Ruffle, the second. The third split, cat is Truffles. The third is Penny, the fourth. And then finally, the fourth split is Winky. Adrian has the best cats ever. Now, this is how MapReduce scales, because we have independent jobs. These splits are then assigned to mappers, which are the unit of compute within MapReduce. The jobs performed by each mapper can be customized. But in this example, the mapping function generates a list of key value pairs. The key is each word that it receives in its split, and the value is the number of times that it occurs in that split. Each split is a small isolated piece of work, which can be completed, and then, when it's completed, it's a split. Each split is a small isolated piece of work, which can be completed, and then, when it's completed, it's a split. It can be repeated at different rates, and it can be retried if it fails. Now, this alone doesn't help us directly with our larger workload, because these are just the word counts for each individual split. So next, we move on to the shuffle phase. The shuffle phase takes the outputs of the map phase and shuffles them. The idea is to move all of the same words so they're grouped together. Remember right now, each split might have a count Remember right now, each split might have a count for the same words as another split. The, for example, is contained in all of the splits. So next, we shuffle them together based on the key, which is the word itself. Now, this improves the efficiency from now on. We still have lots of individual counts for the same words, but at least now they're organized, they're collected together. Now, we do this because the whole point of MapReduce is efficiency at huge parallel scale. In this case, we can now actually use the shuffled data, and we use it by passing it into the reduce phase. In the reduce phase, all of the individual counts for words generated originally by the mapping phase and then shuffled into groups are aggregated. We get total word counts for each individual word. Now, once we have that, once the reduce phase has generated the data, we can now actually use the shuffle data. Now, we can now actually use the totals for each individual word. Then these totals can be passed to the final output. So the output of the reduce phase is passed into the final output. Everything's brought together. The final output is generated and then delivered to the user of the system. And generally, this is also stored on some form of persistent storage, so it can be accessed for any reporting or future analytics requirements. So this is the process end-to-end, how a large, complex piece of data is generated. So the input data is broken into splits, processed through the MapReduce system, and then is used to generate this final data output. Now, this is a simple example, but as I said at the start, imagine if this input data was trillions upon trillions upon trillions of times the size of the simple example that we've used. At that point, I hope you can start to see the benefit of breaking it up and using large-scale parallel processing. And that is what MapReduce offers. It's a system designed entirely to perform this large-scale parallel processing. Now, there's a couple more things I want to talk about before I finish up this lesson, and the first is HDFS, or the Hadoop file system. Now, the Hadoop file system is something that's available within any MapReduce systems, so it's a fairly generic file storage system available within MapReduce products. So if you use Hadoop, if you import the Hadoop file system, you can get a very generic Hadoop file system. Now, if you implement your own Hadoop cluster, then you're going to have access to HDFS. Now, this file system is stored across multiple nodes within that cluster. So it's a file system that replicates any data stored on that file system across lots of different nodes. So inside a MapReduce or a Hadoop cluster, generally you'll have independent nodes, each with their own independent isolated storage. And HDFS provides a file system that can be stored within a single file system that's layered over the top of that, where data is replicated between lots of different nodes in the same cluster. So HDFS is replicated across any compute instances running inside a Hadoop or MapReduce cluster that's running the data node roles. So anything running as a data node manages HDFS, or the Hadoop file system. We've also got the concept of a name node, and this provides the namespace for file systems. So while the data is replicated between the two, the data is the name node. So when we're evaluating the elastic map reduce product, we're evaluating the elastic map reduce product. So when we're evaluating the elastic map reduce product, we're evaluating the elastic map reduce product. And this is a segment of data on HDFS. And generally this is 64 megabytes of size. So this is all high level terminology, which I think will be useful to understand when we're evaluating the elastic map reduce product. But with that being said, that's all of the high level theory that I want to cover in this MapReduce 101 lesson. So go ahead and complete the lesson. And when you're ready, I look forward to you joining me in the next.