 Welcome back and in this lesson I want to talk through the theory of AWS Batch, which is a managed compute service commonly used for large-scale data analytics and processing. Now this is a service which could have easily fit in both the computing section of the course and this section. It just makes more sense to have it along with all of the other analytics products because that's a very common use case. Now we've got a lot to cover so let's jump in and get started. AWS Batch is a managed batch processing product. So what is batch processing? Well it's another one of those words like cloud and serverless which gets overused by people who don't really understand the core meaning. One of the best definitions which I found is that batch processing means jobs that can run without end-user interaction or can be scheduled to run as resources permit. Essentially batch processing... is about running large quantities of potentially difficult and complex jobs automatically as resources are available. Generally these jobs are going to be run on large-scale compute which you have to share with other users. Which means there will be queues and priorities of those jobs in order to effectively access this large-scale compute platform. And generally this type of scheduling and orchestration system is something that you need to build and manage. It's complex and expensive to implement, even more complex and expensive to maintain and it's something that isn't very cost effective for anything but the largest scale organisations. But AWS Batch does this for you. It lets you worry about defining jobs and it handles the underlying compute and orchestration. Now to help you understand one of the common batch processing style situations consider what should be at this stage... a familiar architecture within AWS. This is an event driven workflow using S3. Let's assume that we have a scientist, Winnie. And Winnie is a genius and she has an idea while playing with her cat for a cure for a fatal disease. So she experiments and creates a candidate treatment. She takes this treatment and then stores all of the simulation details to run some initial tests on S3. Now the company... which Winnie works for has an event driven workflow. Where the data being uploaded to S3 generates an event. So this event is passed through to a Lambda function and the Lambda function performs some tests. It first gets the data from S3. It runs a test suite identifying any major side effects of this potential new treatment. And then it stores the results data on S3 for further analysis. Now this might sound like a lot of work. But it wouldn't sound like an efficient architecture. No resources are consumed by default. Only when Winnie uploads data to S3 does any form of processing begin. And for any small workloads, this is actually an example of best practice because Lambda scales really well. And is the best product for most event driven compute workloads. But we have a problem. What if the workload is larger? So it would take more than the 15 minute execution limit of Lambda. That would be just a waste of time. So it would take more than the 15 minute execution limit of Lambda. in order to fully process this job. This is where Lambda as a product begins to show its limitations. AWS Batch is commonly used in the same type of architecture, but when more substantial resources are required. So more compute or storage resources to allocate, or when longer time periods are required for processing. Now let's talk through some of the core components of AWS Batch, and then we can start looking at the solutions architecture aspects of the product, which are important for the exam. The most basic thing within AWS Batch is the job. Think about a job as the thing that you want to run. The actual script, executable, or Docker container, which will perform the work that you want done. Now jobs are actually executed using containers within AWS. So think about a job as some task running within a container, within AWS. Now the critical thing to really understand is that the job defines the work. These can be simple jobs, or jobs which have dependencies on other jobs, or even jobs which require specific hardware, such as GPUs. Next we have the job definition, and think about this as defining what a job needs to run. So it's the resource configuration of a job. This might include IAM permissions, allowing the job to interact with other AWS resources. The definition stores the compute and storage requirements in order to complete the job, so the resources that the job will get access to, and any other elements such as mount points. So if you want to map storage to the running jobs to allow them to access certain capabilities or certain types of storage, then this is all controlled within the job definition. Next we've got job queues, and these are easy to understand. Jobs are submitted to queues where they wait until they have access to the necessary compute resources. Queues are associated with one or more compute environments, which I'll cover next, and queues can have priorities. So you can have high and low priority queues for jobs of different levels of importance, or you can define priorities of compute environments, so certain environments are preferred versus others. Then lastly we have the compute environment, and these are the compute environment that are preferred. So if you're using a virtual environment, then you can define the compute environment for the actual work of the actual work. You can either allow Batch to manage this, so you get to define the types and sizes of instances, the virtual CPUs or cores that you require, and optionally if you're using spot pricing, then you get to define a spot price that you're willing to pay, and you allow Batch to handle the provisioning and scaling of these compute environments on your behalf. Alternatively, you can provide a spot price for the actual work, or you can point the product at an environment that you manage within ECS. So you have the ability to use existing environments that you manage, or you can allow the product to create and manage an environment based on certain criteria that you specify. Now visually, this is how this flow looks. So we start with an AWS Batch deployment, inside of which we define a job and job definitions which store all of the metadata for a job. Now we might have many jobs and many job definitions, each of them performing different tasks. Jobs are submitted to queues, which control the order of processing. You can have high priority and low priority queues, queues for different business units or functions. At a high level, it's using queues that you can influence the order of things within AWS Batch. Now jobs from these queues flow onto the computing environments, and again, these can be created based on priority and cost requirements. Environments can be set with the purchasing model for compute, so spot or on demand, as well as the sizes and types of instances, as well as various other considerations. So that's the relationship between the different components. Now let's zoom out and see how Batch integrates with other AWS products and services. So in the middle, we have AWS Batch again, and remember, it's designed to run jobs. Now jobs can come from many sources and in many different ways. For example, jobs could be initiated via Lambda or Step Functions, making API calls, or in the case of Step Functions, via direct integration. AWS Batch can also be an event target for EventBridge and CloudWatch events, so jobs can be run based on almost anything. And the event data used to initiate the job can be transformed and passed into JobsBatch. So you can use Jobs as parameters so that the job can act on that data. You can also have S3 uploads being used as an event and have the job pull in any objects referenced. So Batch can be called upon to run jobs from a wide variety of sources. And as a solutions architect doing real-world projects and for dealing with exam questions, you need to understand these sources especially well. Now internally to Batch, jobs will be added to queues. Queues are the ones that are used to run jobs. Queues will have linked to them compute environments. These can either be managed or unmanaged, which I'll cover in a second, but they contain certain compute resources, so on-demand instances or spot instances, either managed by Batch or by an administrator. So jobs via queues are allocated to a compute environment. To run a job, a container image is loaded from a container registry such as ECR, and these are used to run containers, which are used to run the job on that compute resource. Now these jobs can logically enough generate outputs, and these can interact with other AWS services either with that output or when the job completes. So Batch might store output data on S3 when it's completed the processing or metadata about that job into a product such as DynamoDB. Once a job finishes, if it was started via step functions, the completion might allow the state machine to continue its execution by calling other AWS products and services. And you can also have other services started as a result of Batch jobs completing by using the AWS Batch event stream, which is available within EventBridge. AWS Batch can be used as a drop-in replacement for any event-based architectures which use Lambda, such as the one I showed previously, for any jobs which require a Batch. This is a really good way to get access to longer-running compute or more resources than Lambda can provide. Okay, let's step through a few more key points before we finish the lesson. At this point, I want to compare Batch and Lambda focusing on a number of key points which will really matter for the exam. So remember that Lambda has an execution limit. At the time of creating this lesson, this is 15 minutes. So if you've got any workloads which need longer than this, then you probably shouldn't use Lambda. Lambda also has limited disk space available within the environment. So environments or runtime environments only have very low amounts of disk space. And if you need extra, you can use products such as EFS or the Elastic File System. But this means running the Lambda with VPC-based networking. So not running it as a public service, but running it inside a VPC. Now, Lambda... Lambda is fully serverless, but it does have a limited selection of runtimes that can be used for Lambda functions. Now, Batch is not serverless. It uses Docker. And so anything which can be containerized can be run using Batch. Batch has no time limit and no effective resource limit. It uses EBS for storage and EC2 for compute. And so it can scale with both of those products. So for the exam, if you've got any event-driven style workloads where you need to run flexible compute, but do it in an event-driven way or do it at scale using a product which can manage infrastructure on your behalf, well, if it needs more than 15 minutes or needs access to large amounts of resource, then you should pick Batch. If you can do it within 15 minutes with limited resources, then you should choose Lambda. Now, next I want to talk about the different types of compute environments, and I'll talk about the different types of compute environments available within Batch. So if you decide to take the low admin overhead option, then you can use managed environments. And with managed compute environments, AWS Batch manages capacity based on your workload needs. So you get to decide whether to use on-demand or spot instances, and you can pick the size and type of those instances that you would like to use. You get to determine your own maximum spot price, and so you can effectively control the cost of these environments. And keep in mind that these compute environments do run within a VPC, so they can run within private or public subnets. But if you choose private subnets, you do need to make sure that you also have full configuration of the VPC, and that includes any VPC gateways required to provide these jobs with access to the resources that they need. So if they need to interact with public space AWS services, then your VPC will need the appropriate gateways. So using the managed compute environment approach means that you're allowing AWS Batch to manage all of this on your behalf end to end, and you're just tweaking some high level values, such as the type of instances, the size of instances, whether you want to use on-demand or spot, and then what your cost strategy is, so what the spot price is that you're willing to pay up to. The alternative to this is that you can use an unmanaged environment, where you manage everything. You create the environment and you just direct AWS Batch to use that environment. And this is generally used if you already have a full compute environment ready to go, and you want to tightly control the costs of that environment. Generally though, for most people when using AWS Batch, they will choose to use managed compute environments because it has next to no admin overhead. Now that's everything that I wanted to cover about AWS Batch. It's essentially a very simple way to manage your environment, and it's a very simple way to manage your environment. Batch processing service for running flexible compute at scale within AWS. Now this will feature in the exam, so you need to make sure that you're comfortable with the scenarios when you would and wouldn't use it, and specifically where you'd use AWS Batch versus Lambda. At this point though, go ahead and complete the lesson, and then when you're ready, I'll look forward to you joining me in the next.