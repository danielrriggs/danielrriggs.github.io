 Welcome back, and in this lesson I want to introduce the Amazon Elastic Map Reduce service known as EMR. In the previous lesson I introduced you to what Map Reduce as an architecture does. Well, EMR is a product by AWS which performs Map Reduce tasks. So let's jump in and take a look at exactly how it works. EMR is a managed implementation of Apache Hadoop, which is a framework for handling big data workloads using the Map Reduce framework. EMR includes other elements of the Hadoop ecosystem such as Spark, HBase, Presto, Flink, Hive, and Pig. Now don't worry about understanding what these are, just try and remember the names because if you do see them mentioned in the exam, it's a surefire sign. that EMR is being used or should be used. Now most larger organizations have, or at some point in the past, have had on-premise Hadoop clusters. The thing about Hadoop is it's complex and time-consuming to create and maintain. So the benefit of using EMR is that you can still operate clusters long-term, but you can also provision ad-hoc or transient clusters for short-term specific workloads. Historically clusters were large, long-running, and shared. So different teams needed access to the same cluster for workloads, which often meant delays and contention of different workloads within the same business. With EMR you can use either a single long-running cluster architecture, or you can do a cluster per workload . Run and complete the workflow and then terminate it afterwards. Now EMR runs for about six hours a day. runs in one availability zone only within a VPC. And this is logical because it benefits from the high-speed networking between resources within that availability zone, and it uses EC2 for all of its nodes. Now EMR can use spot instances, instance fleets, reserved instances, and on-demand, and I'll talk about cost optimizations and options for that at the end of this lesson. Now EMR at a high level, and this is an exam key point to understand, is used for big data processing, data manipulation, analytics, indexing, data transformation, and much more. It's actually used by the Data Pipeline product within AWS, which is a product that I'll be covering elsewhere in the course. Data Pipeline uses EMR to do a lot of work, and it's used for a lot of things. So, for many of the high-volume tasks that it performs, such as imports and exports from DynamoDB, or data transitions from one data source to another. Now for the exam, you don't need to have an operational understanding of EMR. Instead, you just need to know about its architecture, its key components, and exactly what other AWS products and services that it can integrate with. So let's look at that next. Now from an architectural perspective, start with an EMR cluster. And remember, this is running in a single availability zone within a VPC. So to achieve the high level of performance required for EMR, it has to be running in a single availability zone. This is important to understand because this does have high availability implications. So any of the storage that it uses natively, any of the compute nodes that it uses will all be based in the same availability zone. So unless you've taken a appropriate data structure, you're not going to be able to do that. So you're going to have to be skeptical of the high availability provided by EMR. Now at a high level from a data perspective, an EMR cluster can load input data from S3 and store output data back into S3 when the cluster job has finished. EMR actually has some other really cool storage features, but for now we're just focusing on the native Hadoop functionality that it provides. So at this point, it's able to load data in from S3 and store output or resultant data back into S3 when the job has completed. Now within a cluster, you're going to have one or more nodes. And nodes are units of compute, so EC2 instances within the EMR cluster. EMR has a number of different types of nodes, and each of these run different roles and perform different tasks within the cluster. Now every cluster has to have at least a master node, and it's a master node's job to manage the cluster and its health. It accepts workloads, and it distributes them. It functions as the name node within MapReduce, and if you ever need to connect to the cluster using SSH, then it's the master node which you're connecting to. Now the master node can, for tiny or test clusters, be the only node, in which case it can also run map and reduce tasks. But generally, this is not something that you do for any kind of production. So it's a master node that you can use to run map and reduce tasks. It's critical to understand that if you only have this one single master node, if it fails, then the entire cluster stops working. Historically, you could only have one single master node in a cluster, but with recent versions of EMR, you can actually launch with three master nodes, which adds high availability to the services provided by the master node. Now remember the critical nature of the master node when I talk at the end of this lesson about EC2 instances. So if you have a cluster that's not working, then you can only have two purchase options, because the master node is key to the operation of the cluster. Now if you decide to have multiple nodes within a cluster, then one of those nodes will be the master node, and one will be a core node. And clusters can have many core nodes. Core nodes handle a number of important things. First, they track tasks. So as well as performing the actual workloads, something needs to track the completion of those workloads. And so ideally, you don't want to run core nodes on any form of compute which can be disrupted, because it affects task efficiency in the cluster. So core nodes help to track tasks within the cluster. They can also run tasks within the cluster, so run jobs as part of the workload that's being performed by the cluster. Now if a particular map or reduce task is interrupted, then it's not a big deal, because individual tasks can be rerun. But the tasks, tracking function, if that's interrupted, that can be more disruptive. So ideally, you don't want core nodes to be disrupted, because just from a task perspective, it can affect the efficiency of the cluster. Now what's perhaps more important is it's the core nodes which run HDFS, so the Hadoop file system. This is the shared file system of Hadoop, and this runs using locally attached storage on each of the nodes, and this is replicated by the cluster. So if you run a task on a cluster, you can see that the cluster is replicated between these core nodes. So the core nodes run the storage. They manage the provision of this storage to the cluster. Now other nodes can make use of this storage, but it's the core nodes that provide the access. So HDFS is a file system that runs along with the cluster. It starts when the cluster starts, and it's deleted when the cluster is terminated. So the lifetime of HDFS is linked to the lifetime of the cluster, and it's the lifetime of the cluster. So if you run a task on a cluster, it scales really well, both in terms of resilience and performance, but it is ephemeral, and it's stored in one single availability zone, because it's managed by core nodes, which themselves are in one particular availability zone. So any issues which impact the stability or the operation of the core nodes can also impact HDFS, and this can have wide-ranging consequences for the healthy operation of the cluster. Now lastly, we also have task nodes, and these nodes, as the name suggests, run tasks. Task nodes are fully optional. You don't have to have any. You can just have master nodes and core nodes. Where task nodes come in handy is that they only run tasks, and so if they fail or are terminated, it doesn't impact HDFS storage. Task nodes are ideal to scale using spot instances, because if they're terminated, the tasks are not going to be able to run. So if they're terminated, simply rerun on other available core or task nodes. So if you want to scale an EMR cluster in a really cost-effective way, then you can utilize spot instances for your task nodes without much in the way of downsides. Now there's one final really cool feature of EMR that I want to talk about, and that's EMRFS, and this is a file system which is actually backed by S3. This means that it can persist past the lifetime of the cluster, and it can be used to run tasks, and it can be used to do the same thing. So if you run a task and you run a task, and you run a task and you run a task, and because it's got the resilience profile of S3, it means it provides resilience against the failure of the specific availability zone that the EMR cluster is in. From a storage perspective, generally S3 will be used as the input to EMR. Intermediate data can be stored on HDFS or EMRFS, most commonly though EMRFS, and then the output is stored back into S3. If your intermediate storage remains in the EMR cluster, then you can use S3 to run tasks, and if you run a task and you run a task, and you run a task and you run a task, and you run a task, and you run a task, and you run a task, requires extreme levels of IO, then you can use HDFS for intermediate storage, and then persist the results back to S3. Because HDFS utilizes local storage volumes, it's always going to offer better performance, specifically better IOPS, than EMRFS, which is based on S3. So keep this in mind for the exam. If you need extreme storage performance, then you'll be using HDFS, which is based on local storage. If you need extreme storage performance, then you'll be using HDFS, but you do need to be aware that the lifetime of HDFS is linked to the lifetime of the cluster, which means that you need to handle the persistent storage of that data somewhere else when it's finished with. So this is the architecture of EMR. Master nodes are critical, they run the cluster. Core nodes are fairly critical, they can run jobs, but they also manage the HDFS storage. Task nodes are less critical, because they just run tasks. Now when deciding what purchase options to use for EMR, it's good to start by thinking about how long the cluster will run for. You can choose on-demand or spot for each node type, and if you choose on-demand, then you can use savings plans or reserved purchases to further reduce the cost. Now there are a few important points that you need to be aware of when designing purchase options for an EMR cluster. If you use spot for the master nodes, they're going to be more difficult to run, so you may as well use spot for everything else. Because if your master nodes fail due to termination because of the spot price, then everything else will stop functioning. So if you use spot for the master in an attempt to reduce costs, then you may as well use spot for everything else. You should only use spot for any non-important workloads, or where cost is an absolute priority, and then only for clusters which are short-term. Remember, if your master nodes fail, or core nodes fail, HDFS also fails, which removes any of the intermediate storage that's part of your cluster. So if you decide to use spot, then you need to think of your EMR clusters as short-term or temporary, only used to perform one single task, and then terminated once that task has been completed. If you want to run your clusters long-term, then you should select on-demand, and then reduce those costs using either reserved instances, or short-term. If you want to run your clusters long-term, then you should select on-demand, or savings plans. With core nodes, if it's a temporary cluster, you could choose to use spot, but if you do that, the storage can be impacted because of core node failures. Generally though, task nodes are the things that are suited to using spot pricing, because if task nodes are terminated, then tasks can be retried on compute as it becomes available. So if you want to get the best cost efficacy out of an EMR cluster, then what you tend to do is reserve the capacity for all of the important nodes, so the master node, and maybe the core nodes, and then as jobs come along, you can add task nodes using spot pricing as and when required. So keep that in mind for the exam. For any cost-sensitive workloads, consider using all spot instances, as long as you design the processing of that workload to be temporary. For any important short-term workloads, consider using on-demand for the master and core nodes, and spot for the task node, for anything long-term, you can reserve your steady state components, the master node, and maybe the core nodes, and then use spot pricing for everything else. Because EMR uses EC2 for its compute, the decisions you're going to make with EMR are much the same decisions that you'll make with EC2. So savings plans, instance reservations, spot, or on-demand. With that being said though, that's everything I wanted to cover from this architectural theory lesson focusing on EMR. Thanks for watching. Go ahead and complete the lesson, and then when you're ready, I'll look forward to you joining me in the next lesson.