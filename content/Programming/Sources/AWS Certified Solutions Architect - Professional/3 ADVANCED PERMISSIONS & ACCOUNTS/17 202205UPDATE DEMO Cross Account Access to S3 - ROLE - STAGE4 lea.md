 Welcome back, and in this stage of the demo series, you're going to be interacting with an S3 bucket using a cross-account architecture, only now you'll be using a cross-account role. So just to demonstrate this, the first thing I want you to do is to get the URL for bucket 3, so petpix3, and attempt to open that while you're logged in to the IAM admin user of the general AWS account. And what you should see is you get a permissions error, because you have no permissions on this bucket. We haven't added any ACLs, and we haven't added a bucket policy to petpix3. This time, we're not even going to interact with the bucket from the production AWS account directly. What I want you to do, though, is to go ahead and click on this account dropdown, and just go ahead and switch into the production AWS account, because I'll need you to note down the production AWS. AWS account ID. So copy the account ID for your production account into your clipboard, and just store this somewhere safe, and once you have, click on switch back to move back to the general AWS account. Once you're there, move back to the main AWS console, click on the account dropdown, and click on switch role, because we're going to switch into a role inside the production AWS account. Now, this is a role which was created... by the one-click deployment you applied to the production AWS account. So go to switch role, in the account box, enter the account ID of the production AWS account. So you should have just noted this down. Now, you should have also noted down during the setup phase of this demo series, the role name that you created as part of the one-click deployment inside the production AWS account. So go ahead and paste that into the role, and then for the display name, just put X for cross, ACC for account, and then demo. And I want you to pick the blue color. So do that, and click on switch role. So now what you've done is you've switched role into a role inside the production AWS account, only it's not the organization account access role. This time, it's buckets-cross account S3 role, and then some random. So this is a role, as I mentioned a second ago, that you created as part of the one-click deployment that you applied to the production AWS account. So using this role now, we're going to go to S3. And remember, you're now interacting with S3 using this role, which has a very limited set of permissions. It doesn't have full admin permissions. So don't worry if you see any insufficient permissions errors on the main area of the console. What I want you to do is go inside the PetPix3 bucket, click on upload, go to add files, and then inside the bucket images folder that you extracted from the download earlier in this demo series, go to PetPix3, and select maxi.jpg. Click on open, scroll down, and upload this object. Now this will take a few seconds to complete. Once it has, you can go ahead and click on close. Now once that's uploaded, keep in mind that we're still still interacting using this role that's inside the production AWS account. So the owner of this object will be the account which this role is within. Now this role is within the production account, so the production account owns this object. It also owns the bucket. So if we open this object, so click on open, and we can see Maxi. What a good doggo. Now what's important about this is because the production account owns both the bucket and the object, if we click on this account drop-down and switch back to the general account, and then we open the URL for bucket 3, even though we were logged in as the IAM admin user, we were assuming a role in the production account. And so our interaction with S3 was with that role in the production account. So we still have no permissions on this bucket from an identity in the general account. If we switch through to our normal admin role in the production account, because the production AWS account owns both the bucket and the object inside the bucket, from our normal role, organization account access role, we can also interact with this object. So we can see the good doggo Maxi. Now why this matters is using a role in this way. So allowing other identities to assume a role inside an AWS account, it means that that AWS account owns both the bucket and any objects uploaded into that bucket. And so it's a lot tidier and there's a lower admin overhead. So generally, rather than using a cross-account access method using ACLs or bucket policies, which can result in objects being owned by the uploader, if you use this architecture, so you create a role in your AWS account and you grant other identities the ability to assume that role, then anything inside the bucket is owned by your AWS account. So you always have access. It just makes it easier and cleaner if you're managing this at scale. So it's important both the real world and the exam that you understand these three different architectures. How ACLs is the legacy access method. And this relies on knowing the canonical account ID of any account you want to grant access. And any objects which are uploaded are owned by that external account, by the account uploading those objects. So whilst you might own the bucket, you won't own objects within that bucket. That's not guaranteed. The second method that you've looked at is using bucket policies. So this has the same restriction that objects that are uploaded to the account can be owned by the entity uploading them. But using a bucket policy means that you can use the supported method of account IDs and IAM users within a resource policy. So it's a lot easier to manage at scale. The final method is using a cross account role, where you have to create a role inside the same account as the bucket, grant access using the trust policy so that other identities can assume that role. But in doing so, they operate as an identity. So you can use the trust policy to grant access to the entity within your account. And that means your account owns the objects and the bucket. And it's much tidier. For the exam, you have to understand all these three different types of security architecture. So make sure you're really comfortable with who owns objects and buckets in all three cases. Now at this point, that's everything I wanted you to do within this demo series. So all that remains is to tidy up this account and put it back into the same state as it was at the start of this series. To do that, just go back to the main page and click on the PetPix 3 bucket and delete the maxi.jpg object. Click on close. And that should mean that all three buckets are now empty. So just to be sure, probably verify that. So go inside PetPix 1, that is empty. Go inside PetPix 2, that's also empty. And then just confirm PetPix 3. And once you've done that, go back to the main AWS console. As a reminder, you're still within the production AWS account. Go to cloud formation, select the bucket stack, which is the stack creation. And then you're going to be able to see the stack creation. So you can see the stack created by the one-click deployment. Click on delete and confirm that deletion. And that should take a few minutes to delete. But once it has done, the production account is back into the same state as it was at the start of this demo series. And once it's vanished, you can go ahead and click on the account dropdown. Click on switch back to move back to the general account. And that's everything that you need to do. So at this point, I hope you've enjoyed this demo series. Go ahead and complete this video. And when you're ready, I hope you'll join me in another exciting demo. lesson.