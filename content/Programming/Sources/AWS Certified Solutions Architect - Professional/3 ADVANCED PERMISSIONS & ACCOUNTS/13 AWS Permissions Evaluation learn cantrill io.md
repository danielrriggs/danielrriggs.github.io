 Welcome back, and in this lesson I want to talk about policy evaluation logic within AWS. You'll be designing, fault-finding, or implementing pretty complex architectures within the AWS platform, and these will involve a security architecture which could have many levels of identity and resource policies, identity and account boundaries, and even multiple AWS accounts. You'll need to understand the process that AWS uses to determine the final effective permissions when an identity accesses a resource, and that's both in the same account and different accounts, and that's what I want to cover in this lesson, so let's jump in and get started. When evaluating effective permissions, there are a few different components involved. The first is that you have to consider any AWS organization service control policy. And these impact what identities inside organization member accounts can do. There might also be policies on the resource being accessed. The identities themselves may have boundaries, so even though their identity policies might allow a certain thing, their boundaries don't, and that also needs to be evaluated. If you're accessing a resource using an IAM role, so if you've assumed a role, then you might have to consider the following. You might have the full permissions of that role, or you might be using a session with limited permissions, a subset of what that role can allow. So that also has to be evaluated. There'll also be identity policies involved. You'll need to fully understand how multi-account access influences this process. Now, this needn't be a scary process. There's actually a pretty straightforward flow, which you can use to determine what permissions something will have. And once you understand and you're used to this flow, then permission structure becomes easy. So let's take a look, and we'll start by evaluating exactly how it works with a single AWS account. Now, the core components that AWS uses to evaluate permissions are explicit denies, service control policies, resource policies, boundaries, session policies, and identity policies. Now, I'm going to step through each of these one by one, because this is how AWS handles the process, and it makes things really easy to understand. So the first thing that AWS does is to gather up all of the policies which apply to an identity accessing a particular resource. So any policies that apply to that access attempt. Think of a big pile of policies. AWS checks this entire list of policies. To determine if there is an explicit deny which applies to what the identity is trying to do. So if Bob is trying to access an S3 bucket full of cat pictures, is there an explicit deny? If there is, then processing stops at this point. Remember, deny, allow, deny. An explicit deny always wins. Now, if there aren't any explicit denies, then we continue processing. And check if we have any service control policies. Remember that service control policies are part of the AWS organization's product, and they can influence what permissions are able to be allocated inside member accounts. So the first thing that we need to determine is, are there any service control policies? Now, importantly, we're only focusing on service control policies within the same account as the identity. So if the identity is not an explicit deny, then we're only focusing on the identity. If the identity and the resource are in the same account, then this is simple. But if the identity and the resource are in different AWS accounts, then the only SCP which is important is the one inside the account that contains the identity. It's only an SCP that's attached to the account that an identity is contained within, which applies to this process. If we do have them, then we need to determine, do they allow the action? If not, then the action is implicitly denied. So if there are any service control policies, and those service control policies don't allow the identity to access the resource, then that action is implicitly denied. If the service control policy allows the action, or if no service control policy exists, then processing continues to resource policies. Now this resource policy will be for the resource policy, Now this resource policy will be for the resource policy, that we're attempting to access. And if that resource policy contains an allow, then the action is allowed, and processing stops at this point as the final decision. Now if the resource policy does not contain an allow, then processing continues to permissions boundaries. And we follow the same process. So is there an applicable permission boundary? And if there is, does it allow the action? If it doesn't, then it's not. If it doesn't allow the action, then the result is an implicit deny and processing stops. If there is no boundary, or the boundary allows the action, then processing continues to session policies. Now if AWS are evaluating this step, it means that an IAM role is being used as the identity accessing the resource. And so AWS check if a session policy is being used. Remember, this is a reduced set of permissions. Remember, this is a reduced set of permissions. By default, when you assume a role, you gain access to the full identity or permissions policy of that role. If you choose to receive a reduced set of permissions, i.e. if your session contains a reduced set of permissions, that's what's being evaluated at this step. So if we are using an IAM role, and there is a session policy, AWS need to determine if it allows the action. If it doesn't allow the action, then the final response is an implicit deny and processing stops. If we don't have a session policy, or if we do have one and it allows the action, then processing moves on to identity policies. And this is the last step. And at this point, AWS reviews any applicable identity policies. If any of those policies contains an explicit allow, then access is allowed. If there are no explicit allows, which match the identity accessing the resource, then the end result is a deny and no access is provided. Now this multiple step process is used to evaluate every single access to a resource from an identity, as long as it's in the same account. It follows the same logic that I've been talking about all the way from an associate level. So deny, allow, deny. The highest priority is always an explicit deny, processing stops. If there is no explicit deny, then all of these individual steps are evaluated. So service control policies, resource policies, any boundary or session policies. And then when it gets to the identity policy stage, if there is an applicable explicit allow, then access is granted. If not, the default or implicit deny takes effect. So this process happens every single time an access is occurring within a single policy. So if you're using a single AWS account, but what happens when multiple accounts are involved? Well, that's actually much simpler. Once you understand this process, understanding how this works from a multi-account perspective is actually pretty trivial. With multi-account permissions, conceptually, we have account A on the left, which contains the identity, and account B on the right, which contains the resource. So account A will contain lots of identity policies, and account B will contain the resource policy. Now, in order for an identity in account A to access a resource in account B, two things have to occur. A successful access requires an applicable allow from account A and an allow in account B. So account B needs to allow the access from account A, and account A needs to allow the access to account B. And if both of those things are allows, then access is granted. And this is known as multi-account or cross-account access. In the situation where account A allows the access and account B denies it, then the access is denied. Conceptually, you can imagine this as the request being allowed out of account A, but account B doesn't allow that request in. In the situation where account A denies access, then the access is denied. If both of the accounts deny access, but account B allows it, then access is also denied. Think of this conceptually as account B being welcoming, but account A not allowing the access to leave the account perimeter. And of course, logically, if both of the accounts deny access, then the request itself is also denied. Now, that's everything that I wanted to cover in this lesson. I just wanted to give you a summary of how AWS evaluate permissions for both a single account and multi-account access. So as long as you understand how this process works from a single account perspective, it's not really any more difficult evaluating this from a multi-account point of view. Essentially, you've just got to make sure that the access is allowed out of the source account and into the destination account. And if both of those are allowed, the access is allowed, for anything else, it's denied. But at this point, go ahead, complete the video, and when you're ready, I'll look forward to you joining me in the next.