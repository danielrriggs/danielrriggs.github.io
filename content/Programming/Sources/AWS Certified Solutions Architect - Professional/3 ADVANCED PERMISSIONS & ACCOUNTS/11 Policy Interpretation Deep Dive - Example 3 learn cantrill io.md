 Welcome back, and in this video we'll be examining another policy document together. This example is a little bit more complex, but again, we'll be following the same process. Now, step one, as with both of the previous examples, is to identify how many statements the policy contains. Now, this policy document is more complicated than the previous two in terms of the number of statements. In this example, this policy document contains three different statements. We've got the one here at the top, the next one is this one in the middle, and then we've got the one at the bottom. Now, policy documents can contain one or more statements, and the more that they have, the more careful we have to be, especially in high-pressure exam situations. Now that we've identified the number of statements within this policy document, we need to evaluate exactly what effect each of them has. And in this particular example, it's made slightly easier. Due to the fact that each of them is an allow. So the top statement's effect is to allow, the middle statement's effect is also allow, and the effect of the bottom statement is also allow. And that makes it much simpler. Now, the purpose of a policy document is to control access to something or to a set of things. And because all of the effects in this statement are to allow, we don't have to worry about overlaps. Because even if overlaps did exist, an overlap of an allow on top of an allow doesn't actually influence the final set of permissions. If you have two allows, the final effect is simply an allow. So in this case, we don't really need to worry about the overlap in terms of the eventual permissions. What we do need to do, though, is to understand exactly what access rights are granted by all of these statements in combination. And we'll start with the top one. So this statement has two access rights. It has S3 colon list all my buckets and S3 colon get bucket location. And for this statement, the resource that's matched is a wildcard. So it's a star, and it means that it applies to all resources. And this is a pattern that you need to be aware of. There are a few S3 actions where you need to use the star as the resource. These are two of them. So list all my buckets and get bucket location. And the other one is create bucket. So with any of those three actions, you always need to specify star as a resource. And often, the reason why you split things up into different statements is because you need to refer to different resources. So in this particular case, because these two actions refer to star as a wildcard, it makes sense to isolate them off into their own statement. Now, this statement means that whatever identity has this particular statement, it's going to be used to isolate the other two actions. So in this particular case, because these two actions refer to star as a wildcard, it makes sense to isolate them off into their own statement. Now, this statement means that whatever identity has this particular statement, it's going to be used to isolate the other two actions. So in the account these three statements really define who这个 ?? выз? And you can actually immediately send a list and a list of all those actions inside. And in the Bioniнес Office Creator, I want to identify backwards in order to clear this one out. So I can scroll slowly down theTime Chase VisionF cevich. So what should the owns this particular statement have done in order to change the 어�nsfile address to luck? You can change the alias. You can also change the alt E, or type in any other formal name if not in the world. Such interesting stuff. And of course, an item IDs aージ cmetuffs attitude will enable you to try to change it sometimes to a different way. But it only really eats the monster ofoths. And that's important to understand. Now, before we move on, I'm going to tell you what this policy actually does, because then what I'm talking about will make a lot more sense. Imagine that you have a bucket, and inside that bucket there's a folder called Home, and inside the Home folder you'll have folders for every user in your environment. And this is a traditional Home folder structure. Ideally, what you want is for anybody to be able to list anything inside the bucket, but not see inside anybody's Home folder except their own. And what you don't want to do is to have to create a unique policy for every single user. Imagine if you had a thousand IAM users, and you needed to create a thousand different unique policies, each of them containing their Home folder location, hard-coded in a particular statement. That would be a lot of admin overhead. And luckily, when dealing with AWS identity policies, we don't have to do that. And I'm going to use this as an example to illustrate some really powerful features of IAM policies. Now, the next statement is also an allow statement, and it allows the list bucket action. And what list bucket allows you to do is to list the contents of a particular bucket. And it specifies one bucket in particular. CL-ACTION. And it specifies one bucket in particular. So this is one of the buckets belonging to the Animals for Life organization, our scenario for this course. So in absence of anything else, this would allow you to list all of the objects inside the entire bucket. But I just finished telling you how this bucket is used for Home folders for all of the users of the organization. And so you don't want people to be able to list any folders apart from their own. And this is why I'm going to use this as an example. And this is where this much more complex condition block comes in handy. So this condition block is also doing a comparison. This time it's string-like. And the string that it's comparing is S3 colon prefix. If you remember from the associate level, S3 doesn't actually have folders. It's a flat structure. Folders are created by adding prefixes to object names. And this matches prefixes. Now, the first prefix. The first prefix that's matched is an empty prefix. And this means that this condition will evaluate to true if you're attempting to perform a list bucket operation at the top level of the bucket, i.e. no prefix. This second line will match anything inside the Home prefix. So if you're attempting to do a list bucket inside the Home prefix, so getting a list of all of the individual Home folders, then this will be allowed. Now, this last line matches a prefix which starts. And then forward slash. And then dollar, open curly brace, AWS colon, username, close curly brace, forward slash, star. And this is an IAM policy document variable. This will resolve to whatever user is being evaluated as part of this process. So if you have an IAM user called Bob, and Bob has this policy attached, and Bob is trying to do a list bucket against this policy, then this will be allowed. And then forward slash. And then dollar, open curly brace, AWS colon, username, closed curly brace, forward slash, star. And then forward slash. And then forward slash. And then forward slash. And then forward slash. And then forward slash. And then forward slash. And then forward slash. And then forward slash. else. So an IAM user called Bob could not perform a list bucket operation inside a prefix called Julie. He could only do it inside his own Bob home folder. And so this condition only matches when these prefixes are in use and it allows you to perform a list bucket operation. So this is how you can do an object listing either at the top level inside the home prefix or inside your own allocated home folder. Then this last statement, it's also an allow and it allows any S3 actions. So this allows get object, put object, delete object, pretty much any S3 operation. And it allows those actions to occur both on your particular home folder and any objects inside your home folder. So this also relies on this IAM variable. So it allows you to perform any S3 operations that you want to perform inside your home folder. So this also relies on this any S3 operations that you want to perform inside your home folder. So this also relies on this inside the bucket, inside the home prefix, and then inside your particular folder, which is referenced using this IAM variable name. So again, this means you can have one single policy. You can create it once, apply it to hundreds or thousands of IAM users, and it will give them the ability to interact with a slightly different area of an S3 bucket based on their username. Now there are a few things you can do. First, you can use this variable to generate a specific policy. So you can use this variable to generate a specific policy. So you can use this variable to generate a specific policy. I'll make sure that I include a link which details them all attached to this lesson. And I do recommend that you do check out this link and familiarise yourself with all of the different variables that can be used because they do feature on the exam. Now at this point though, that's everything that I wanted to cover in this lesson, which is the final lesson of this three lesson series. So you need to be comfortable passing a policy of this level of execution. So you need to be comfortable of complexity. Now as a couple of final exam hints, I want you to remember that an explicit deny always overrules everything else, and if you don't have an explicit allow, then the default is to deny. I also want you to remember that if you see a statement which doesn't have any allow components, then it needs to be used in conjunction with another policy, because in isolation, a policy that only contains deny will have no measurable effect, because the default is this implicit deny. And you don't make any changes if you deny something twice, because the default is to deny. Just adding a deny policy will have no effect. Now the last thing that I want you to be aware of going into the exam is to be on the lookout for any not items. So anything that says not in the question, anything that says not in policy conditions, anything that says not in policy conditions, anything that says not in policy conditions, anything that says not in policy conditions, any other terms which suggest the inverse of what you expect by default. There are unfortunately a lot of trick questions, where if you're scanning over something quickly, you might think it's asking for potential solutions to something, or a potential cause of something, where actually it's asking you for not a solution or not a potential cause. So be aware of all of those things for the exam. But at this point, that's everything that I want to talk about today. All of these policies are available inside this lessons folder in the course GitHub repository. But at this point, that's all of the content of this lesson. So go ahead, complete the video, and when you're ready, I'll look forward to you joining me in the next.