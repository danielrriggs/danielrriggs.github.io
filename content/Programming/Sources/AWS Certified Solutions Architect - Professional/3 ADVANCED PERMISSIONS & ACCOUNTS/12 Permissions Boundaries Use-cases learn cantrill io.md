 Welcome back, and in this lesson I want to talk about the theory and some use cases for permissions boundaries. So let's jump in and get started. Now it's quicker to explain how permissions boundaries work if we do it visually. Let's take the example of an IAM user called Bob who has a selection of permissions. Six allows and one deny. Without using boundaries or any other form of account level restrictions, these are the permissions that Bob would have, unless there are any deny permissions assigned to the resource which Bob was attempting to access. Now IAM permissions boundaries only impact identity permissions. They don't influence permissions allowed or denied within resource policies. Now at first glance, permissions boundaries... look very much like IAM identity policies. They're JSON documents and they list actions and resources. Conceptually, they fit over the top of all identity policies a given identity has, and they can be assigned to IAM users and IAM roles. Now a really important thing to understand about permissions boundaries is they don't themselves grant any level of access. Conceptually, they don't grant any level of access. Conceptually, they function like walls around identity-based permissions. They define a maximum level of permissions that an identity can receive from identity policies which that identity has allocated. Let's take the set of permissions on screen as an example. Bob has a total of seven permissions, six allows and one deny. Notice that four of the permissions are inside the boundary. The four leftmost permissions. So that's three allows and one deny. They're all inside the permissions boundary. But notice that we have three outside. So the three allows on the right-hand side. Well, because these are outside of the permissions boundary, they're actually ineffective permissions. Even though they're granted to the identity, because they're not allowed by the permissions boundary, because they're outside of the permissions boundary, they have no effect. They're not in the field. They're not in the final set of permissions that Bob, in this case, receives. So even though Bob has a total of seven permissions, because the permissions boundary limits this to four, then the three on the right are ineffective. Now, one of the common scenarios where permissions boundaries are used is when you need to delegate permissions. Let's look at an example of why delegation is problematic without using permissions boundaries. So take this example. We have Julie, who's an IAM user of the Animals for Life AWS accounts, and she's been granted the administrator access managed policy. So Julie is a full account administrator over this one particular AWS account. Now, Julie has a trusted member of staff called Bob, and she wants Bob to be an identity manager for the organization. So she wants Bob to be able to manage all IAM users, groups, roles, and permissions within the AWS account. And so she creates a policy containing the IAM colon star action, and she assigns this to Bob's IAM user. Now, the intention of this is clear. She wants Bob to be able to manage identities, but there are two potential problems. The first problem is that there is nothing to stop Bob changing his own permissions. If Julie gives Bob IAM colon star, then he has all of the permissions. He requires to attach the administrator access managed policy and become a full account administrator. Now, it's possible to restrict that because you can make it so that Bob can't edit his own permissions. But let's assume for a second that Bob wants to bypass that, either because he wants access to some secret cat pictures, or maybe Bob is secretly evil. Well, what he can do is just go ahead and create another IAM user and give him the permission to edit his own permissions. And give that user full administrator access. So even if we prevent Bob controlling his own user, there's nothing to stop Bob creating another user with administrator access. And these are the type of problems that we need to use permissions boundaries to help us fix. Now, I'm going to quickly demonstrate how to create this delegation architecture. And some of this is going to involve working in the console. Now, you don't have to perform these steps inside your own AWS console. This is entirely optional. You can either watch what I do in my console, or if you want to get some practical exposure, you can do it as well. But it is purely optional. So let's say that we want to create this delegation architecture. Well, we start with Julie, who's the account administrator. Julie has full administrator access over the account. And the first thing that Julie needs to do is to create a boundary policy. And boundaries use JSON documents, just like identity policies, but remember, they don't grant any actual permissions. They just limit what permissions an identity can receive. So Julie creates the boundary as a managed policy, so it can be attached to any users which need it in future. Now this boundary limits the permissions of any identity to which it's attached to a predefined set of services and actions which are approved by Julie. So this boundary policy is a very simple one. It's a very simple one. It's a very simple one. But it's a very simple boundary policy. And it's also a very simple boundary policy. It's a thing that controls the permissions that any normal user inside the AWS account is able to receive. So at this point, I'm just logged in as the IAM admin user of the management account of the organization. And as always, I've got the Northern Virginia region selected. Now, if you are following along with this in your own environment, I've linked three JSON files attached to this lesson. You'll need to download those files because we'll be using them throughout this walkthrough if you're doing it in your own environment. environment. If you're not doing it, it may be good to download them anyway so you can open them and review them as I'm stepping through the process. So the first component is to create the user boundary policy. So to do that, I'm going to move to the IAM console. I'm going to select policies, create a policy, click on the JSON tab to define this manually and just delete the placeholder. Now this is the user boundary policy that I'll be using. It limits any users created by Bob to only having access to these three services, so EC2, S3 and CloudWatch. And then it allows those users to perform some basic interaction with IAM, mainly for changing their own details, so changing their own passwords and updating their own access keys. So this is the boundary which will need to be applied to any users that Bob creates and we're going to enforce that. So I'm going to go ahead and copy all of that into my clickbank. Move back to the AWS console, paste that in, then click on next and next review. And then I'll call the policy A4L user boundary. I'll scroll down and click on create policy. So this is the boundary policy that will be used for Animal for Life users. And then we'll go back to discuss the next theory component of this architecture. So now the boundary has been created, the next step is that Julie needs to be created. And remember, the intention is that Bob can manage all identities except for his own. And he also needs to be limited so that he can't create any new IAM users with permissions that exceed the standard set that Julie has defined. And so we need to make it so that Bob can only create IAM users with the user boundary policy applied. Now this is where the power of boundaries come from. So to meet this boundary, we need to create a boundary policy that is not only a boundary but also a boundary for Bob. So to meet this boundary policy, we need to create a boundary policy that is not only a boundary, but also a boundary for Bob. And this boundary is the part that restricts exactly what Bob can do. So now I'm going to move across to the console and implement this part of the architecture. Okay, so now I've explained the theory behind the next components of this architecture. Next, I'll be creating the admin boundary. So I'll go back to policies, and I'll click on create policy. Again, I'll click on JSON and delete the placeholder. And this represents the admin boundary that we'll be applying to Bob. This essentially limits what Bob can do. So Bob can perform all of these actions as long as he's performing those actions on a user that has the permissions boundary associated with it. And he can also perform this list of actions as long as it's not on himself. So he can perform this list of actions on any user that's not Bob. And then finally, this section prevents Bob from making any changes to the boundaries. So he can't update the boundaries that have been defined by the accountability. So he can't update the boundaries that have been defined by the accountability. So this is the boundary that will restrict the permissions that Bob has. Now within here, there are some placeholders that I'll have to update. So what I'm going to do first is copy all of this into my clipboard and move back to the AWS console and just paste this in and then scroll to the top. And I'm going to replace all of these placeholders. We'll need our account ID. So click on this dropdown and then click on the copy symbol next to account ID. So now that I've got the account ID, I'll look for all the placeholders. So management account number is one of the placeholders. So I'll replace this with my AWS account ID that I've just copied. If you're doing this in your environment, you'll need to use your account ID. I'll scroll down further. This is the next placeholder I'll need to change. I'll scroll down further still. I'll replace both of these. So that's a total of four replacements of those placeholders. So the two for the boundaries. I'll click on the copy symbol next to the placeholder. And I'll click on the copy symbol next to the placeholder. So those are the four I need to replace. So I'll click on next and then next review. I'll call it A4L admin boundary and create the policy. Next, I'll need to create the permissions policy for Bob. So I'll click on create policy. Again, select the JSON tab and delete this placeholder. And then in the last JSON document, this is where Bob receives his actual permissions. This gives him full IAM admin access, which obviously the boundary will restrict. And he also has access to a number of different CloudWatch related actions. So I'm going to copy all of that into my clipboard, move back to the console, paste that in. So I'll click on next and then next review. I'm going to call this one A4L admin permissions policy and create the policy. And then I'll go ahead and create the Bob user. So I'll go to users, add user, username is Bob. I'll select AWS management console access. I'll select custom password and pick a strong password to use. And then untick require password reset, go to next permissions. And then I'll attach the A4L admin permissions policy that I just created to the Bob user. So this gives Bob full admin rights over the IAM service. But at the same time, I'm going to set the permissions boundary. So I'm going to select use a permissions boundary to control the maximum user permissions. And then I'm going to select the A4L admin boundary. So this is the boundary policy that restricts what Bob can actually do with those full IAM admin rights that I've gave him in the step above. So then I'll click on next tags, next review, and I'll create the Bob user. Now, before I demonstrate logging into the Bob user and demonstrating exactly how this looks from Bob's perspective, I just want to return and discuss the last few things that I've done. So I'm going to go ahead and create the next user. So I'm going to go ahead and create the last few bits of architecture before I continue. Okay, so Bob is now a delegated IAM administrator. And so he can create other IAM users as long as those IAM users have the user boundary attached to them when created or updated. And that user boundary ensures that Bob can never create any additional IAM users that have more permissions than those that Julie has approved. So Bob has an identity policy that he can use to create additional IAM users. So Bob has an identity policy that he can use to create additional IAM users. So Bob has an identity associated with him that grants him full administrator access over IAM, but also an admin boundary that prevents him removing his own boundary policy. It prevents him editing his own account, and it prevents him interacting with any IAM users unless the user boundary is also applied to that user. And this is the full delegation architecture. So now I'm going to switch back to my console and just demonstrate exactly how that works. Okay, so now we've covered those bits of architecture, I'm going to log into the Bob user in a separate session. So I'll go ahead and do that. Now to do that, first I'll need the IAM user signing link for this account. So I'll copy that into my clipboard. Then I'll open up a completely separate session in my browser, and I'll open up the IAM user signing link. So this won't affect my existing login to AWS. I'll be able to log in as the separate user. So I'll type in the IAM username of Bob. And the secure password. And then I'll move across to the IAM console. So notice you can already see some level of restriction that Bob is not authorized to perform some of these operations, and that's fine because he's restricted. What he can do though, if I go to users, and I go to add user, and then I'll add the user I was talking about moments ago. I'll also select AWS management console access and give this user a strong password. Disable require password reset and go to next. Next permissions. Now if I go ahead and say attach existing policies directly, and let's say that I selected the administrator access managed policy. So I want to try and elevate my permissions. I want to give this user full admin access over the entire account. So let's try and do that. Click on next, and then next review, and then create user. Now I won't be able to do that because the boundary that's associated with the Bob user will only allow me to create a user. I am users as long as I attach the animals for life user boundary. So because I can't do that, I'm going to exit out of this process and go back. I'm going to click on add user again. Use the same username. Select management console access. Use a strong password. Uncheck change password next login. Go to next permissions. This time I'm still going to allocate the administrator access permissions, but now I'm going to add the user. I'm going to use the animals for life boundary. If I scroll down and set a permissions boundary, use permissions boundary to control maximum user permissions, and then select the A4L user boundary, now it will create without any problems. So now I have this brand new user. But even though this user does have administrator access, so I've attempted to perform permissions escalation, because I've set this permissions boundary, if I log in as this user, I won't have those admin credentials. This boundary policy will restrict what I can do. It will limit my service interaction to S3, EC2, CloudWatch, and some limited IAM functionality. So this has been a pretty simple demonstration of how you can use permissions boundaries to delegate certain admin rights to other IAM users. You can delegate those rights. You can use a boundary around those delegated administrators to control what they can do, and also insist on any users they create also having their own boundary, which means they can't perform privilege escalation. Now if you did follow along with this in your own environment, do make sure that you delete both of the IAM users that you created, as well as the policies that you created. So the boundary policies and the permissions policy. That is everything I wanted to show you today. I wanted to cover though in this brief demonstration, so I'm going to go ahead and clear up all of the resources that I created during this demo. At this point though, you can go ahead and complete this video, and when you're ready, I look forward to you joining me in the next.