 Welcome back, and over the next three lessons of this three-lesson series, I want to step through some examples of more complex identity policies which you might find within AWS. For the exam, you might be presented with a policy and need to identify exactly what it does, or you might need to diagnose a policy which isn't working correctly, or you might need to pick a suitable policy for a specific requirement. Now, over the next few lessons, we're going to step through a few examples. It'll help you understand how to break them up into their components and really understand exactly how they function. So let's jump in and get started. Now, the three different policies which we're going to be looking through in the three lessons of this series are all contained in the course GitHub repository, and there's three different files. We've got policy one, which is holiday gifts, policy two, which is region, and policy three, which is home folders. So we're going to step through each of these, one of them in each lesson of this series. And in this lesson, we're going to start with policy one, which is holiday gifts, and the process that we go through in order to break apart a policy and understand exactly what each component does is the same no matter how long or how complex the policy document is. Now, the first step in understanding a policy document is to identify how many statements make up this policy document. So every single policy document has this statements block. So it starts with statement, and it's either a single statement or a list of statements. And you can identify if it's a list of statements based on it having square brackets. So square brackets in JSON indicate that it's a list of things. So in this case, we have one statements block, and inside that statements block are a number of individual statements. The way that we can identify statements is inside the list. So inside these square brackets, we'll have one or more pairs of curly braces. So this is one pair of curly braces. So we've got an opening curly brace and a closing, and then we've got another pair of curly braces. So this particular policy has two statements inside the statement block. We've got this top statement, and then we've got the second statement block. So two statements inside the statement block. So step one is identifying the number of statements, and step two is to identify at a high level what each statement actually does. What is the effect of each statement? So inside policy documents, every single statement has an effect. It's either allow or deny. And that tells us whether the rest of the statement allows a certain set of activities on a certain set of resources, or denies it. Now it's important at this stage to refresh our memory on two very important concepts. The first is deny, allow, deny. That's the priority order of permissions inside AWS. And the second is that AWS starts off with an implicit or a default deny. If you're not granted the ability to do something in AWS, then by default, you start off with no permissions. This is the implicit or default deny. And it matters, because when you're reviewing a policy document, you need to start off from the basis of zero permissions, and that will help inform what exactly this policy document does. Now in terms of the order or priority of permissions, if something is denied explicitly, that always wins. If something is explicitly allowed, then it's allowed unless there's also an explicit deny. Remember, explicit denies always win. If neither of these are true, if there is no explicit deny and no explicit allow, then the implicit default deny takes effect. So in this particular case, we have our two statements inside the statement block. The effect of the top statement is allow, so this allows something, and the effect of the bottom statement is deny, so it denies something. Now if these two statements apply to different actions or different resources, if there's no explicit deny, if there's no overlap, then it's nice and simple. But if there is an overlap, it's critical to remember that an explicit deny, which is what this bottom statement is, will always override an explicit allow. So the next step is to evaluate exactly what each statement does, and then identify any overlap. Now when I'm evaluating policy documents, I like to start with any allow statements. Generally, most policies tend to have a large block, of things which are allowed, and then some smaller bits which are denied. So let's start by looking at this top statement. So this statement has an effect of allow, and it allows five different actions to occur. So it allows put object and put object ACL, get object and get object ACL, and then finally delete object. And these operations are allowed on a particular named resource. In this case, we can identify a specific S3 resource. And because it has the forward slash star at the end, we know that it refers to objects. This is a wildcard, meaning all objects and paths inside the holiday gifts bucket. Now as a tip, whenever you're evaluating a policy, you should look to identify whether it refers directly to a resource such as an S3 bucket, or if it's focusing on sub resources. So in this case, objects, the bucket. In this case it's a wildcard which is referring to sub-resources, so objects inside this S3 bucket. So this top statement allows whatever identity has the policy associated with it to access objects inside the holiday gifts bucket. They can put objects, get objects and delete objects. But there's also this second statement and this is a deny statement. So the first thing we need to identify when focusing specifically on this particular statement is to identify if the deny overlaps with the allow. If not it's much simpler. If there is an overlap we need to identify what the overlap actually is so that we can interpret what is allowed and what's denied. Now notice how the resource identified inside this deny statement is not the same as the allow statement. So the deny statement matches the allow. That's what we're looking for. That indicates that there must be at least some overlaps. So both of these statements refer to the same resource, any objects inside the holiday gifts bucket. Now the actions also have some overlap. For the deny statement we're actually denying get object and get object ACL and both of those are also contained inside this allow statement. So the deny statement is not the same as the get object statement. So there is an overlap for a particular set of actions, in this case get object and get object ACL inside the same resource. If this was all there was then whoever had this identity policy associated with them would only be able to run put object and put object ACL as well as delete object operations on this bucket. Because deny always takes priority and even though get object and get object ACL are allowed, there's no need to have a set of actions. So the condition is that the action is only applied if the condition is met. And the condition in this case does a date and time check. This, which is AWS colon current time, always resolves to the current date and time at the point that this policy is being evaluated. So if it was being evaluated right now, then it would resolve to the time right now. And so the condition does a check and it says that the current time, so the time at which this policy is being evaluated, needs to be greater than this explicitly defined date and time. So the first of December at midnight. And it also specifies that the AWS current time needs to be less than this explicitly defined date and time. So the 25th of December at 6 AM. So if the current date and time is anywhere between the very start of December and 6 AM on the 25th of December, then this condition will evaluate to true and the whole statement will be effective. So if the current date and time is anywhere in this period, then this particular deny statement will take effect. So between the start of December and Christmas morning, the book is valid. The date and time bucket is write only. Outside of that period, it's both read and write. The policy essentially stops anyone peeking at their holiday presents stored in the S3 bucket during the month of December. Anywhere from the start of December to Christmas morning, this bucket is write only. At any other point in the year, it's both read and write. This is a typical multi-part policy. It has a deny and it has an allow. In terms of the resources and actions though, it is pretty simple, referencing only one ARN and a small list of actions. So you need to be aware of exactly what a deny statement matches, even if it has a conditional block, and be able to overlay that with any allow statement, and be aware that deny always takes priority over allow. Now that's the end of this lesson. In part two, we're going to look at something a little bit different, this time involuntary. So go ahead, complete this lesson, and then when you're ready, I'll look forward to you joining me in part two.