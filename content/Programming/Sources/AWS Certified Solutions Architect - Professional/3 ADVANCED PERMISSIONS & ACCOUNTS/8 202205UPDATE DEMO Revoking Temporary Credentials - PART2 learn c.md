 Welcome back, this is part 2 of this lesson. We're going to continue immediately from the end of part 1, so let's get started. So how exactly do we fix this problem? Well there are a few ways. Let's switch back to the AWS console, and let's click on services, and we're going to move to the IAM console. So type IAM in the services box, open it in a new tab, and then move to that tab. If we click on roles, we'll see A4L-instance role, and this is the role that these EC2 instances are using to interact with AWS products and services. So one option is that we could select this role and delete it. But if we do, this would impact the thousands of EC2 instances that the real Animals for Life hosting business uses, and you'd then need to create a new role and change the configuration. So we're going to go ahead and create a new role and change the configuration. So we're going to go ahead and create a new role and change the configuration. Which is something that's a significant amount of admin overhead, but it's important to understand that it would prevent any further damage to the account. Another option that we have is to click on this role to edit it. Go down to permissions and make changes to the permissions that this role has, because the temporary credentials that our attacker is in possession of are directly linked to the permissions policies on this role. So if we change that, we're going to have a new role. If we change the permissions on this role, if we remove access to certain services, then the permissions granted by the temporary security credentials in the possession of the attacker are also adjusted in the same way. But again, the crucial thing to understand is that this would also limit all of the other EC2 instances that are using this instance role. The ideal solution that we're looking for at a high level is that we want to invalidate the credentials, which have been leaked and are in the possession of our attacker, without causing any problems for the legitimate users or instances in this case of this role. So if we can do something which invalidates the temporary credentials without affecting anything else, then that's an ideal solution. And we can do that by revoking sessions for this role. So let's have a look at exactly what that does. So we've currently got four managed policies. EC2 read-only access. CloudWatch agent server policy. Amazon SSM managed instance core. And Amazon S3 read-only access. So to invalidate these temporary credentials, we can go to revoke sessions. What revoke sessions actually does is to apply an additional policy onto the IAM role that denies any access when the role was assumed at a previous point in time. So let's do that. Go ahead and click on revoke actions. And then click on revoke active sessions. You'll need to acknowledge it and then click on revoke active sessions to make those changes. Now if we go to permissions and scroll down, we'll note that we have another policy that's now attached to this role. And let's expand that just to review the JSON that this policy uses. And this policy has the effect of denying any actions on any resources. And it has a condition. The condition is date less than. And it's essentially saying, that this policy applies if the AWS token issue time is less than the current date and time. So the point at which I revoked the sessions was the 14th of May 2022 at 05.34.15 UTC. So for any sessions for this role that have been assumed before the point when I clicked revoke, this will apply. It's a deny all policy. And as we know, deny allow deny. So an explicit deny, which this is, will always overrule everything else. So now that this applies, if we switch back to our command prompt or terminal, and try to interact with AWS again using these temporary security credentials, now instead of that access returning a list of S3 buckets, now we'll receive an access denied message. Because this role was assumed before the point, we can see that the attacker has no more access to the AWS account. And because the attacker didn't legitimately assume the role, the attacker has no way of performing another STS assume role operation. So the attacker now in effect has no more access to the AWS account. Assuming that we've patched the security vulnerability that allowed the attacker to get into the EC2 instance in the first place, that attacker has no more access to our AWS account. So the attacker now in effect has no more access to the AWS account. Now the same is true with the EC2 instances. So that session's expired, so what we'll need to do is go back to the EC2 console, right click on instance A and put connect, click on session manager and then click on connect, and this time it won't work. The reason why it won't work, is in order for session manager to work, it requires permissions to interact with the systems manager service. And because this role is no longer active, that the instance should be active, that the instance should be active, we don't have those permissions. So these instances now have the same problem because they assumed the role before we revoked the sessions, they no longer have any valid access to AWS. Now the quickest way to resolve that is to stop and start the instances. So right click, stop instance, and then stop, and then do the same on the other one. So right click, stop instance, and then stop. And then we'll need to keep refreshing until both of those instances have changed. And then we'll reverse the process. So right click on instance A, and go to start instance, and then do the same for instance B. So right click, start instance, and we'll need to wait for both of those to change into a running state. And the stop and then start of those instances will cause the EC2 service to assume the role again, and gain access to brand new temporary credentials, which won't solve the issue of the EC2 service, which won't solve the issue of the EC2 service, which won't solve the issue of the EC2 service, or will suffer from that deny all policy on the role, because these new temporary credentials will have been generated by assuming the role after you clicked on revoked sessions. And so both of these should now be valid. We'll have to give it a few moments for both of those instances to fully start up. Once they started up fully, you can right click on instance A, put connect, click on session manager, click on the connect button, and that will connect you into that instance again, because it now has access to AWS. We'll give them a second 한 ALL space down, And you can verify that by running an AWS space S3 space LS to display a list of all the S3 buckets inside this account. If we go back to our command line and try to perform that same operation, that will still be denied because this is using temporary credentials which were generated before the revoke session operation. So this is suffering from the explicit deny all policy that's on the roll. Because these credentials were generated before the date and time when we revoked the sessions. And that's the end of this demonstration. It's been a fairly simple demonstration, but it's highlighted some of the nuances of dealing with IAM roles. So when you assume an IAM role, you get these temporary credentials which are generated. And these have an expiration date and time which cannot be changed. You cannot expire temporary credentials. But what you can do, if you ever do have a situation... where they're exploited, is you can add a deny all policy by clicking on revoke sessions. Which means that any credentials generated at a previous point in time suffer from this explicit deny all policy. And you've stepped through the architecture that's on screen now and effectively invalidated the access that our intruder had... by placing this explicit deny all policy on a roll that invalidates any credentials... generated in the past. And you've seen how you can just regenerate the credentials for legitimate EC2 instances... by stopping and starting those instances. And this is a fairly common response to a security incident of this type. At this point though, that's everything that I wanted you to do in the demo lesson. Now to clear up the account and put it back into the state as it was at the start of this lesson... just go back to the AWS console, then move to CloudFormation. And you'll need to select... the A4L stack, which is the stack created by the one-click deployment. So make sure you've got that selected. Click on delete, and then confirm that deletion. And that should remove all of the infrastructure used within this demo lesson. Now it's possible that you might get an error while you're deleting this stack. And the error might be on the IAM role. And the reason is because we've manually edited that role when we revoked existing sessions. So to fix this, you need to move back to the IAM console... click on roles, find the instance role that you used within this lesson... select it, delete the role, and confirm that deletion. Once you've deleted it, then you can move back to CloudFormation... select the A4L stack again, click on delete. You don't need to worry about this checkbox, just go ahead and then confirm that stack deletion. And this time, it should work without any problems. Again, this is only because we made a manual edit to a resource that's managed by a CloudFormation stack. And so fairly often, you need to go ahead and manually delete that resource... and then attempt the stack deletion again. So that's the end of this demo. I hope it's been useful, and I hope it's helped commit the theory to memory... and given you some practical experience of how you can interact with IAM roles in a security-style situation. At this point, go ahead, complete the lesson... and once you're ready, I'll look forward to you joining me in the next.