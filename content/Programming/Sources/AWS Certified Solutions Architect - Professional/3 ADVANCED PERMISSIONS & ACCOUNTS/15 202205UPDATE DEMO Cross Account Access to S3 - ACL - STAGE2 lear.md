 Welcome back to part two of this demo series. And at this point, you should have all of the infrastructure created within the production AWS account, and you should still be at the CloudFormation console within the production account. So just so you can verify that all three of these S3 buckets are working as expected. So from the production account, just keep in mind that you're currently assuming a role within the production account. So remember when you created the AWS organization, you either created an IAM role or AWS created one on your behalf, and it's called Organization Account Access Role. So that's who you're currently interacting with this account as, and this role has full admin permissions. So if you go ahead from the production account, from where you're currently assuming this role, if you open each one of these links in a brand new browser tab, then what you'll see is you'll be taken to the S3 console, and you'll have access to that particular bucket. So PetPix 1, PetPix 2, and PetPix 3. So that's as you'd expect, because you're logged in as an identity of the production account, and these are buckets also within the production account. So if you close down these extra tabs, and then from the account dropdown, go ahead and click on Switch Back, and this will move you back into the general... AWS account. So now that you're back in the general account, you should have noted down the URLs for all of those three S3 buckets. What I want you to do now is to try and open one of those S3 buckets from the general account. So just paste in the URL to access one of those three S3 buckets. You should have noted them down in the previous step. What you'll see is an error message saying, insufficient permissions to list objects. And the reason for that is, because we're currently using an identity within the general AWS account, and we're attempting to access a bucket within the production AWS account. And we haven't configured any permissions to allow this. And because these are separate accounts, there's no trust between the accounts. So the bucket that's inside the production account doesn't trust any identities within the general account. Now we're going to change that, and we're going to use first, access-cruise. So we're going to use first, access-cruise. So we're going to use first, access-cruise. So we're going to use first, access-cruise. So we're going to use first, access-cruise. So we're going to use first, access-cruise. So we're going to use first, access-cruise. So we're going to use first, access-cruise. Now access control lists are the legacy method of permissions within S3. In fact, AWS don't recommend that you use these any longer. Instead, you should be using bucket policies. But I want to demonstrate exactly how they work, because existing deployments do tend to use access control lists. Now access control lists, because they're legacy, don't use account IDs or IAM users. They use a different, type of security identifier. To show you what I mean, click on the account drop-down, and click on security credentials. Remember, we're logged in as the IAM admin user of the general AWS account. And I want to draw your attention to this. It's the account canonical user ID. So this is a legacy way of referencing an AWS account. So this references the general AWS account. So I want you to go ahead and copy this canonical user ID into your clipboard. And once you've copied that, I want you to click on the account drop-down and switch back across to the production AWS account. Then click on AWS at the top left to move to the main console, and then move to S3. Now because we're using an identity within the production account, so remember we're assuming organization account access role, we'll have full access to all of these S3 buckets, because this role has administrator access. So go into PetPix1, and we're going to grant access to the general AWS account to be able to interact with this bucket. So click on permissions, and scroll down below bucket policy, below object ownership, you're looking for access control list. Now when you find this, go ahead and click on edit, and you'll see entries for the bucket owner, which is your AWS account, and this is the production account, because we're running this inside. So we're going to go ahead and click on edit, and you'll see entries for the bucket owner, which is the production AWS account. You can grant public access, you can grant authenticated users access, and you can grant the S3 log delivery group access, but we're going to add a grantee. So click on add grantee, and you'll need to enter the canonical ID that you copied into your clipboard moments ago. So enter that into the box, and then go ahead and check all of these permissions. And once you've done that, scroll down and click on save changes. So what you've just done, using legacy access control lists, is you've granted access to an external AWS account. Specifically, this external AWS account, which is the general AWS account. Now just to reiterate again, ACLs are a legacy access control mechanism. So this is something you're only going to be using in a very niche set of circumstances, but it is important that you understand it. So now go ahead and move back to the menu, and you'll see that you've granted access to a legacy access control list. So go ahead and move back to the main AWS console, and once you're there, click on the account drop down, and switch back to the general AWS account. Now again, consult that list of items that you copied down from the setup step of this video, and we need the URL for bucket 1, petpix 1. So go ahead and open that URL. Remember, this gave us a permissions error when we tried it earlier. But now, we're able to access the S3 bucket, because we've just granted permissions by entering the canonical user ID and giving it full permissions to this bucket. So now we can upload objects to this bucket. So go ahead and click on upload, then click on add files, locate the bucket images folder, remember this was created when you extracted the zip that you downloaded attached to this lesson in the setup step of this demo series. Go ahead and open petpix 1, click on gini.jpg, and click on open. And once you've done that, go ahead and click on upload. So you're now uploading the gini.jpg object to an S3 bucket inside the production AWS account, and you're doing so from the general AWS account. Now why I'm getting you to do this is because we're going to close this down, go back to the main AWS console, we're going to switch across to the production account again, and remember, the production account owns all of these S3 buckets. So go to the S3 console, we're going to go into petpix, and we're going to click on open. So go to petpix 1, remember we do have permissions on this bucket, click on gini.jpg, and click on open. Notice how you get an access denied, and this is a critical security concept to understand about S3. Even though the production AWS account owns this S3 bucket, it doesn't automatically own objects which are uploaded to this S3 bucket. By default, the identity which owns this S3 bucket is the same as the identity which owns the S3 bucket. By default, the identity which owns this S3 bucket is the same as the identity which owns the S3 bucket. By default, the identity which owns this S3 bucket is the same as the identity which owns any uploaded objects, is the AWS account where the identity which does that upload is located. So in our case, we uploaded this object from the IAM admin identity within the general AWS account. And so the owner of this object is the general AWS account. If we go to permissions, we can see that we don't even have permissions to interact with this object. If we click on the account dropdown, and click on sort, switch back, and then go back to the S3 console, and then open the URL to petpix1, so bucket1 again, you should have noted this down earlier, and then click on gini.jpg and go to permissions, notice how it's our AWS account, so the general AWS account, which owns this object. The production AWS account has no access to this object, and that's critical to understand. So we can see that the account ID which contains the identity which uploads the object, which owns that object. In our case, the account ID of the general AWS account. So that's everything I wanted to demonstrate in this part of this demo series. It's just a simple example of how you can grant cross-account access using ACLs, and also how the permissions model works for ACLs. Now just to tidy things up, just go back to the main area of the bucket, select the account ID, and then click on the account ID. And then click on the account ID, and then click on the account ID. And then click on the account ID, and then click on the account ID. So now that we know the main area of the bucket, select the ginit.jpg object, click on delete, type delete to confirm, and then delete that object. And we're doing this because you won't be able to delete the s3 bucket without it being empty, and remember that the production AWS account doesn't have permissions on this object, and remember that the production AWS account doesn't have permissions on this object, so it's best practice to tidy things up from the account where we created the object. so it's always best practice to tidy things up from the account where we created the object. So now this bucket's empty, and that will mean we can tidy up at the end of the demo series. So now this bucket's empty, point, that's everything that you need to do in this stage of the demo series. Remember, this stage was all about controlling access using ACLs. In the next part of this demo series, you're going to be interacting with an S3 bucket using bucket policies. So go ahead and complete this video, and when you're ready, I'll look forward to you joining me in the next.