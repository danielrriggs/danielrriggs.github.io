 Welcome back, and in this demo lesson, you're going to get the practical experience of how to handle a realistic security event involving AWS. I want to make sure that you're really comfortable with the architecture of IAM roles and how a role grants the ability to interact with AWS products and services. So this is the architecture that you're going to be implementing in this demo lesson. It's a simulated hosting environment which is operated by the Animals for Life organization. In reality, let's assume that this business has thousands of EC2 instances running most of the world's popular cat, dog, and chicken image blogs. In this demo, we only have the two because it's far simpler and cheaper for a demo lesson. The instances are public. They're accessible from the internet using IPv4 and IPv6. And all of the thousands of EC2 instances that are used by the organization use instance roles to gain access to the AWS resources that they require. Legitimate accesses to all of the EC2 instances occur over TCP port 80, meaning HTTP. In this scenario, you're going to step through what happens when an instance is exploited and AWS credentials are leaked. Now, to get started, we need to apply... ...a CloudFormation template to create the infrastructure. So let's do that first. Just make sure that you're logged in as the IAM admin user inside the general or management account of the organization. And as always, you'll need the Northern Virginia region selected. Now, attached to this lesson is a one-click deployment link. So go ahead and click that link. Everything should be pre-populated. The stack name should be A4L. Just scroll all the way down to the bottom. Check the acknowledgement box and click on create stack. Now, also attached to this lesson is a lessons commands document. I suggest you go ahead and open that in a new tab because that will list all of the commands you'll be using throughout this demo lesson. But at this point, this template is going to take a few minutes to create successfully. So go ahead and pause the video and resume it once the stack moves into a create complete status. Now that that's complete, let's just verify that these EC2 instances... ...are functioning as expected. So click on services and move to the EC2 console. Now remember, the Animals for Life Hosting Incorporated business unit runs animal picture blog sites. And so currently we've got a subset of these running on instance A and instance B. So let's select A4L hosting A and just copy the public IP version for DNS into your clipboard and open that in a new tab. Now, this first website... ...shows a detailed ranking of the world's best chickens. And Sophie insists that the list only needs to include her. No other chickens are required. The second website details a historic 2020 grudge match of epic proportions over the best dog treats. So as you can see, two really important websites. Now that we've verified that these two instances are functioning correctly... ...based on this architecture... ...we're going to assume that instance A has a vulnerability. And that a bad actor has managed to take advantage of that vulnerability to gain SSH access to the instance. So let's follow that through. Let's connect to instance A. So right-click on instance A and put connect. Click on session manager and then click on connect. Now because the Animals for Life instances are utilizing EC2 instance roles... ...this means that EC2... ...and STS are working together. And constantly in the background, temporary security credentials are being generated... ...every time these roles are assumed by the EC2 service. And these temporary credentials are available within the instance's metadata. Now our attacker is aware of exactly how the architecture of AWS works. And the first step to a successful exploit is to obtain the name of the role that's attached to this EC2 instance. And this can be achieved by using this command. And again, this is contained in the lesson commands document. So paste or type this command in and press enter. And it will provide you with the name of the role that's associated with this EC2 instance. And yours will be different. So make sure in the next step when you copy this role name down, you use your role name. Now once you have the role name, the next step of the exploit is to retrieve the temporary security credentials... ...that are contained within the EC2 instance. And this is the same thing that you do with the other EC2 instances. And the way that you do that is using this command. But we have to delete this placeholder at the end. And we need to replace the placeholder with the name of the role that's attached to this instance. And once again, yours will be different. So go ahead and make sure that you copy the entire role name down. So it should start with A4L and it finishes with the letter or number directly before SH. So copy that into your clipboard. And then paste it in at the end of this command and press enter. And that will return the temporary security credentials that this instance has received... ...when the EC2 service assumed this role. So copy these down into your clipboard. You're currently operating as the attacker. And we're going to need these to perform the next step. So just go ahead and copy down this entire block. The important parts are the access key ID... ...the secret access key... ...and the token. So copy those into your clipboard and paste them somewhere safe. Now the expiration date of these credentials is 11-52-18 UTC. And that's currently several hours from the current date and time. And so these credentials can't be invalidated. They're going to be active and provide this same level of access until the expiration date and time. So it's really important that these temporary credentials... ...under normal circumstances are guarded. Now this is where it gets dangerous because this instance has access to the Animals for Life organisation. And so we could interact with the AWS products and services directly from this instance. We could, for example, run AWS S3 LS to display a list of all the S3 buckets within this account. Or alternatively, we could enter this command to describe all of the EC2 instances... ...that are running on this account. ...that are running on this account. Or alternatively, we could enter this command to describe all of the EC2 instances... ...that are running inside this account in this region. Now perhaps the more dangerous thing is that the temporary credentials that we just copied down... ...have this same level of access. And they don't need to be used on the EC2 instance. And I'm going to show you exactly how dangerous it is to have a leak of AWS credentials. So go ahead and open either your terminal if you're running macOS or Linux... ...or your command prompt if you're running Windows. So now we're going to run the EC2 instance. So now that we're in our local terminal, I want to demonstrate that I don't have any configuration for the local command line tools. So if I run an AWS S3 LS, I'm told that I don't currently have any credentials configured. So this means that my local CLI tools currently have no access to AWS. Now what we can do as a malicious attacker is to make use of the security credentials that we just got from the EC2 instance... ...and configure them... ...on our local machine. Now in order to configure the command line to use these temporary security credentials... ...what we can do is to set what are known as environment variables. And the exact way that you do this depends on the operating system that you're using. If you're using macOS or Linux, then you'll be using the export command to create these environment variables. For Windows, it's a similar structure. You're just using the set command. So only if you're using Linux... ...or macOS should you follow what's on my screen. If you're using Windows, the appropriate commands to run are contained inside the lesson commands document. Now the first environment variable that we need to set is a variable called AWS underscore access underscore key underscore ID. So this is the access key ID. Now if you go to where you noted down the temporary security credentials earlier in the lesson... ...you'll see a structure that looks very similar to this. And the first line will be access key ID in between speech marks and then a colon. And then in between the next speech marks will be the access key ID itself. So copy this into your clipboard. Move back to your terminal or command prompt. Paste this in and press enter. And that sets the environment variable to be equal to the access key ID of those temporary credentials. The next thing we need to set is the secret access key component. So again in the document where you noted these down earlier... ...just copy the secret access key component into your clipboard. Go back to your terminal or command prompt and paste that in and press enter. And then lastly we need the AWS session token... ...which is an extra piece of information that's needed whenever you're using temporary security credentials... ...versus just the access key ID and secret access key... ...which are the parts that you need with long-term credentials... ...known as access keys. So the session token is an extra thing that's needed for temporary credentials. Now this is much longer so you need to make sure that you copy it in its entirety... ...and it should stop on the line above expiration. So make sure you get it all. Everything between the speech marks. Copy that into your clipboard. And then paste that in and press enter. And now you've given your local command line tools everything that they need... ...to utilize these temporary credentials. And then you've given your local command line tools everything that they need... ...to utilize these temporary security credentials. And so at this point you should be able to run AWS space S3 space LS... ...and interact with the animals for life AWS account. And you should get an output of any S3 buckets in that account. And if you run this command which is an AWS space EC2 space describe hyphen instances space... ...U.S. hyphen east hyphen one and press enter. Now you'll get an output of all of the running instances in this region... ...of the animals for life AWS account. And so as the bad actor as the person who's exploiting these leaked credentials... ...we can interact with the AWS account locally from our local machine. We don't need to do this interaction from the EC2 instance. And so whenever you do have a security event such as this... ...it's not enough to simply stop the bad actor interacting with EC2. We also need to find a way of preventing the credentials being used... ...to cause any further damage to the business. Now the thing about these credentials is that we can't expire them. They have an expiration date and they're valid until this expiration passes. There's no way that we can explicitly invalidate... ...this set of temporary credentials. Okay, so this is the end of part one of this lesson. It was getting a little bit on the long side... ...and I wanted to give you the opportunity to take a small break... ...maybe stretch your legs or make a coffee. Now part two will continue immediately from this point. So go ahead, complete this video and when you're ready... ...I'll look forward to you joining me in part two.