 Welcome back and this is going to be a pretty quick lesson where I'll be covering Elasticsearch which is known as ES. Now I want to keep this high level because you only need to have an architectural understanding of the product for the exam. Elasticsearch is actually a fairly complex and feature rich product and it's not possible to communicate all of the features and exactly how things work within this one lesson. And so all I want to do is to give you an awareness of when and where you would use Elasticsearch within existing AWS architectures. So let's jump in and take a look. So AWS Elasticsearch or ES is a managed implementation of Elasticsearch and this is an open source search solution. You might have heard of the name before and you might have come across it as part of the ELK stack which includes Elasticsearch or ES, Cabana, and Logstash and I'll talk about what they are in a second. The key to understanding if you need to use Elasticsearch is it's one of the products where you almost always know if you need to use it. And if you're not sure you probably don't need to use it. If you have a current Elasticsearch system and you want to migrate it into AWS as a managed solution then you might pick AWS Elasticsearch. Or if you have an Elasticsearch requirement. And you, need to provision it quickly and effectively in AWS in a way which can be integrated with other AWS products and services, then also Elasticsearch is a good idea. Now architecturally it is not serverless. It uses servers which are injected into your VPC. So that's an important consideration when you're answering exam questions. Elasticsearch is an alternative to other AWS services such as CloudWatch logs or CloudWatch, but only for situations where you have a specific need or you already use Elasticsearch or the Elk stack. It's used in such cases such as log analytics, monitoring, security analytics, full text search and indexing, and even click stream analytics. Now focusing specifically on the Elk stack, this is a collection of products which tend to be used together. And these all come as part of the ES service. So first we have Elasticsearch which provides search and indexing services. And this is not a serverless solution, just to reiterate. It's injected into a VPC and users compute. So this is not a pay-as-you-go service. There are going to be some charges to use this product whether you're using it for searches or for processing data or not. Next we've got Kibana. Kibana. Kibana. Kibana. Kibana. Kibana. Kibana. Kibana. Kibana. Kibana. Kibana. Kibana. Kibana. Kibana. Kibana. Kibana. Kibana. Kibana. Kibana. Kibana. Kibana. Kibana. Kibana. Kibana. Kibana. Kibana. Kibana. Kibana. Kibana. Kibana. Kibana. Kibana. So this comes part of the Elasticsearch service and this is often used as an alternative to the dashboard functionality available within CloudWatch. Lastly we have Logstash and this is similar to CloudWatch logs. So this provides a service which allows you to ingest logging data and then perform searches on that data and it links in with Kibana and the Elasticsearch service itself. Kibana. Kibana. Kibana. Kibana. you need to use a Logstash agent which is installed on anything that you need to ingest the data from. So that's a keyword that you need to understand for the exam. Now don't be concerned about this product. Genuinely, you only need the most surface level of understanding about how this product functions. So let's look visually at how you might use Elasticsearch in a familiar architecture. So visually, if we were to use the Elasticsearch product for the Categram application, then it might look something like this. Users of the application interact using the mobile application on their phone. And let's say with this example that Bob is uploading a new image of whiskers to the Categram application. Well in this style of situation, he might actually be generating three streams or three different types of data. The first application. This type of data is the actual upload of the image to S3 using temporary credentials generated by the application. Next we have metadata. So all of the data surrounding this image upload and this might be added to DynamoDB. And then finally, throughout the usage of the application, we're going to have click stream data which is being streamed into Kinesis data streams. Now let's assume that we have a team of data scientists who want to capture the data, search it, understand it, and then we can use it to create a new application. So let's assume that we have a team of data scientists who want to capture the data, search it, understand it, and then we can use it to create a new application. So let's assume that we have a team of data scientists who want to capture the data, search it, understand it, and then we can use it to create a new application. And present it using dashboards. Step one will be to enable DynamoDB streams so that we have a rolling record of changes to the DynamoDB table or tables used by the application. Now to each of those sources, we then create Lambda functions which are invoked when data is added. In the case of DynamoDB and Kinesis, it's an event source mapping based invocation. And for S3, it's an event source mapping based invocation. And for S3, it's an event source mapping based invocation. The result is the same. The Lambda function is invoked with data or records which are changed on the associated data source. The clickstream Lambda is coded to prepare that data for ingestion into Elasticsearch. The same with the product search Lambda. And then finally, the image exif Lambda is going to take the exif data from images uploaded to S3. And this data is metadata which is added to the image when it's loaded. And this data is metadata which is added to the image when it's taken. In all cases, these invocations are happening in real time. And the Lambda functions add the data into Elasticsearch where it can be consumed by our team of data scientists in real time. Now for the exam, it's important to know the style of architecture that you might use Elasticsearch in. In general, you would choose to use it as an alternative to other AWS services when and only when you have a good reason to do it. So even though you can use Elasticsearch to replace certain pieces of functionality of CloudWatch logs and CloudWatch, you need to understand the downsides. And those are that it doesn't have as tight integration with other AWS products and services. Deciding to use Elasticsearch is a similar choice to using something like OpsWorks. Now with OpsWorks, you choose to use it if you had a need for Chef. With AWS Elasticsearch, you choose to use it... if you have a need for either Elasticsearch, Kibana or Logstash. You generally default to using AWS products and services rather than something specific like Elasticsearch. So keep an eye out in the exam for any questions which have keywords which would require you to use Elasticsearch. Because if they do, then that's going to be the answer that you select. Now at this point, that's everything I wanted to cover in this lesson. It's just been surface level. It's just a simple architectural theory. At this point, go ahead and complete the lesson and then when you're ready, I'll look forward to you joining me in the next.