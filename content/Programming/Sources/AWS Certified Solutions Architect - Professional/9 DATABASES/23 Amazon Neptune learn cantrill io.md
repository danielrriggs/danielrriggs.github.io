 Welcome back, and in this lesson I want to very quickly touch upon Amazon Neptune, which is a managed graph database within AWS. Now it's something which I only expect to feature on the exam at a very high level, because it's a relatively new product, and it doesn't yet feature extensively on the exam. So you only need to know that it exists, and know the types of situations where you would and wouldn't use this product. So let's jump in and get started. Now Neptune is a managed graph database service. A graph database is one where the relationships in the database are as important as the data in that database. It's designed for situations where relationships are fluid and changeable, and where queries against that database are generally going to be against the relationships between data as much as they're about the data itself. Now Neptune is much like RDF, in that it runs within a VPC, so it's private by default. It's resilient, it can be deployed in multi-AZ mode, and it can scale for reads by adding read replicas. Also, like RDS, it does continuous backups, and it allows point-in-time recovery. In summary, Neptune is very much like RDS, only for graph-style data. When you use Neptune, when you go to deploy a database, even the user interface looks very... very familiar if you've used RDS before. So for the exam, just remember, it's essentially like RDS, but only for graph-based databases. Now it's well beyond the scope of the exam for you to understand all of the product's details and features. Instead, you just need to know the product's architectural structure, and what a graph data model is, as well as some of the use cases where the product might be useful. So let's look visually first at what a graph data model looks like. So graph databases have nodes. And think about nodes as things in the database. So we've got Bob, Bob's house, Bob Goblin, his house, Whiskers, Julie, Bob's credit card, and then we've got Woofy. So these are all examples of nodes within a graph database. Nodes are things, things that you want to store in that database. And they can have attributes, which are the key value pairs associated with those nodes. In this case, we have date of birth for the people, addresses for the houses, and finally, card information for the credit card. Now in a relational database, you can have relationships, but they're really second-class citizens. You generally define them in advance. They're one way, and you can't easily adjust them. It's also very computationally heavy to query data with complex relationships. And it's almost impossible at scale and at speed to follow relationships from entity to entity. Now graph databases have been custom designed to allow this type of data access. An example would be that Whiskers has a minion, and this is Bob. And he also has an enemy, and this is Woofy. Now these are examples of relationships which are directional. So Whiskers has a minion called Bob and an enemy called Woofy. But even though they're directional, you can trace them in either direction. So you could look for all of the enemies of Whiskers, or you could look at all the entities who have Woofy as an enemy. And in addition to that, anything else in the database can have these relationships. Again, these relationships are fluid and can be queried in the same way as nodes can be. Now relationships can be useful because we can look for shared relationships, where, for example, say the credit card belongs to two people, but they have separate nodes. They have separate addresses. Graph databases make this type of relationship-based data exploration really easy. Now this isn't a database course, and so I want to keep this focused on the exam and solutions architecture in a professional setting. But I do think it will help you to have an understanding about what graph-based data models look like and what they're used for. For the exam, I think it's going to be essential that you understand some of the common use cases for graph-based data models. So let's look at that next. So firstly, keyword analysis of exam questions. If you see any questions which state graph-style data, then it's likely to be Amazon Neptune. AWS generally want you to pick their own managed services for scenarios which fit. So if you see a situation that needs a database to hold graph-style data, then it's going to be Amazon Neptune. Specifically, though, that's because they're used for graph-based data models. though, any types of data which is involved in social media, so anything involving fluid or dynamic or ever-changing relationships. So social media like Twitter or Facebook, where you can follow or unfollow, friend or unfriend, these are ideally suited to a graph database, and so Neptune would be ideal. Graph databases are also great for fraud protection, because you can look at different relationships, who lives at which address, who's using which social security or credit card information, and build up patterns of any data which looks like an anomaly. It's also great for recommendation engines, so you can look at the relationships between entities, so nodes, and anything else that they interact with. So if you know that a particular node is friends with a certain group of people, and that node shops at a certain place, then you can make recommendations based on what other similar entities and relationships have done in the past. So it's really good for recommendation engines. Graph-style databases are also great for network and IT operations, where you can model the dependencies between different entities. And likewise, it's also great for biology and any other life sciences, because you can define the relationships and current links between two different entities. So which drugs interact, with which other drugs, and which drugs have been successful in treating certain conditions. By mapping this data in a graph-style way, you can get much more value, because you can trace through these relationships. Now that's everything that I wanted to cover about Amazon Neptune. It has been just a high-level introduction, but this is everything that you'll need for the exam. At this point, go ahead and complete this lesson, and when you're ready, I look forward to you joining me in the next.