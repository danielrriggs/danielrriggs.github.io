 Welcome back, and in this lesson I want to briefly touch upon another niche database product within AWS, and that's the Amazon Quantum Ledger Database, or QLDB. Now this is a really interesting one because it's part of the AWS blockchain range of products, and so it's pretty cutting-edge. Now let's jump in and explore the architecture of how this product works. So QLDB is a really special database. It's actually an immutable, append-only, ledger-based database, and I'll explain what this means visually very soon. But in short, it means that you can have a cryptographically verifiable transaction log, so anything which is added, updated, or removed from the database, you know what's changed, who changed it, and nothing can be faked or hidden. It's transparent, which means that you can always see a full state of the data that you're using, and it has a very consistent history of all previous states and the current state within the database. And this makes it perfect for any audit-heavy workload, so banking, finance, or the medical industry. Anything where the legitimacy and history of data really matters. Now architecturally, it's a serverless product. It provides ledgers and tables, so you don't have to manage any servers. It's also resilient across three availability zones, and it replicates data within, each of those availability zones. So it really is a highly resilient solution. And it can also stream data into Kinesis. So it can generate a constant output of any changes to the database, and stream those into Kinesis. Now it's actually a document database model, and you can think of this as storing JSON documents, so key value pairs with a nested structure. And it's also an ACID-modeled database. And this means that if you perform a transaction involving multiple parts, for example, debiting $10 from one account, and adding $10 to another, then you can define transactions. So either all parts of the transaction will work, or none of the transaction will work. And this is really important, again, for financial or banking-based industries. Now let's look visually at how the product works, because I think this will help you to understand it. We start the architecture with QLDB in the middle, with application data coming in from the left. Now there are two main ways that QLDB can help you view data. The first is a current state of the data within the data store, but also a history of all previous states. Now QLDB can batch export data into S3, and as I've just mentioned, it can stream any changes to data in near real time into Kinesis. But these are just periphery features. These aren't what makes QLDB really special. The awesome part of QLDB is how it ensures data accuracy, and how it can guarantee the append-only nature of the database. Let's say that some data is written into the database. So we have block one here on the left in red. As part of that block, a hash is created from the data, and this is a one-way calculation. You can take a hash, and you can take the data, and easily confirm that they match each other. Now when the next block is created, so the next block, the next time that data is added, modified, or deleted, it also has a hash, but that hash consists of both the hash of the previous block, and a hash of the new piece of data. This means that block two is linked to block one. There's a chain of integrity, and the same process happens again and again with every new piece of data added into the product. The hash of the current piece of data being added, deleted, or modified, and then the hash of the new piece of data being added, is modified in any way within the database, is created by using the previous hash, and the hash of the current data. And by doing this over and over again, it means that it's impossible to change data in an unverifiable way without recomputing the whole chain. This ordered, recorded history, which is cryptographically verifiable. It means that if you're dealing with any sensitive industries where the integrity of data really matters, for example, finance or banking or medical, then this type of database offers significant value. Now, some particularly good use cases for this type of database include anything relating to finance, where account balances and transactions are really important, because you can go back through the history of QLDB and identify whenever certain elements of that database were changed. And you always know that one change is linked back to the other in a cryptographically verifiable way. You can also use QLDB for medical applications if a full history of any data changes matters to the specific application that you're using. So if you're detailing any drugs added for a patient or a group of patients, then if you're using QLDB, you know that that entire history of that patient is cryptographically verifiable, and you can guarantee that nothing has been adjusted. You could also use this type of database for logistics to track movements of objects, again, in a cryptographically verifiable way, or it can be used for certain legal situations where you need to track the usage and change of data. And this is especially useful for any data custody-style scenarios. So QLDB is one of those products which is relatively niche in its usage, but for a particular set of requirements where a cryptographically verifiable history of transactions and an append-only model really matters, QLDB is massively valuable. Now, I don't expect you to get many questions on this in the exam, but I wanted to give you an architectural overview, so if you do, at least you know the types of scenarios where you would choose Quantum Ledger Database. With that being said, go ahead and complete this lesson, and when you're ready, I look forward to you joining me in the next.