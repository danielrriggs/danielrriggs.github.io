 Welcome back and in this lesson I want to talk about a feature of Direct Connect called MACSEC. Now MACSEC is a security feature which improves or at least partially improves a long-standing problem with Direct Connect which is the lack of built-in encryption. Now let's just go ahead and get started straight away covering how this works and when you might use it. MACSEC is a standard which allows frames on a network to be encrypted. Frames are the unit of data which occur at layer 2 of the OSI 7-layer model. MACSEC is a standard which extends standard Ethernet and it's called IEEE 802.1 AE. Now MACSEC provides a hop-by-hop encryption architecture and this means that two devices need to be next to each other at layer 2 in order to use MACSEC and this is referred to as layer 2 adjacency. Now MACSEC... MACSEC provides the following high-level features. First, confidentiality. It provides strong encryption at layer 2 by encrypting the frame's Ether type and payload so it becomes completely opaque to anyone observing it from the outside. Second, it provides data integrity. MACSEC adds additional fields to ensure that data cannot be modified in transit without both parties being able to disagree. This means that the data is not protected by the MACSEC server. Next, it provides data origin authenticity. This ensures that both parties can see that frames have been sent by the other trusted peer in a MACSEC relationship. And then lastly, and this one isn't really relevant to this particular topic, it provides replay protection. Now two really important things to understand about MACSEC as it relates to AWS is that it doesn't replace IPSec over Direct Connection. So, if you're using a MacSEC server, you're not going to be able to replace IPsec over Direct Connection. So, if you're using a MacSEC server, you're not going to be able to replace IPsec over Direct Connection. Because it's not end-to-end. It's a hop-to-hop, layer-2, adjacent encryption standard. It's used to encrypt a small part of the flow of data through the Direct Connect architecture. And I'll show you exactly how that works very soon. It is, though, designed to allow for super high-speed terabit networks which use encryption. So MACSEC is capable of some really high levels of performance. Now there are a few key components. that you should be aware of when using MACSEC. The first is a secure channel. And this is the base component of MACSEC. Each MACSEC participant creates a secure channel that's used to send traffic to the other participant. So these channels are unidirectional. If you imagine a two-party relationship where you're one of those parties, then one channel is used to send data from you to the remote participant, and another channel is used to send data to the remote participant. And you send data from that remote participant back to you. Next, the secure channels are assigned an identifier known as the secure channel identifier, or SCI. And you'll see that mentioned elsewhere in this lesson. This is just something that uniquely identifies a secure channel. Next, we have secure associations. And communication that occurs on each secure channel takes place as a series of transient sessions called secure channels. So this means that over the lifetime of a MACSEC relationship, then you're going to have multiple secure associations take place on every secure channel. Each secure channel generally only has one secure association which is running at one time. So generally only one of these exists. Now the exception to this is when these secure associations are being replaced. Because a new secure association will be created before the old one is torn down. Now MACSEC modifies Ethernet frames by inserting a 16 byte MACSEC tag. And we'll refer to this as a SEC tag or secure tag. And this occurs just before the Ether type. And it also adds a 16 byte integrity check value at the end of the frame. And I'll show you how this looks visually coming up next. Now in terms of how MACSEC handles keys, this is done using the MACSEC Key Agreement Protocol or MKA. So the Key Agreement Protocol, it manages peer discovery, authentication and the generation of encryption keys. And then finally we have the Cipher Suite. And the Cipher Suite controls how the data is encrypted. So it controls the type of algorithm that's used, the number of packets per key. And it also controls the key rotation. So much like other Cipher Suites in AWS, this controls the how of the encryption and decryption process. Now let's take a look visually at the problems of Direct Connect when we aren't using MACSEC. So without MACSEC, this is a pretty typical architecture. We have an AWS region on the left, a DX location in the middle, and a business premises on the right. In this example, there's a private VIF running to allow access to the VPC in the AP Southeast 2 region. Now the issues that we have is that private VIFs are not encrypted. This might not seem like a concern, but consider the physical architecture that's being used on screen now. We have plain text 802.1Q frames being moved across the cross connect between the DX port and our port on a customer or provider router. Now the issue is that the data center that this is running in, while it might be a major and well regarded data center, it's not something that's owned by AWS. And so we've introduced another entity that we have to trust or manage risk for into the chain of communications. If we've extended the cross connect or any of the VIFs through to our business premises, then that's another party that we have to trust and another portion of the communication which requires risk management. The only option that we've had available historically as a network architect or specialist is to use a public VIF with an IPSec VPN running on top to provide secure and private access. The key thing to keep in mind as I continue talking about MACsec is that an IPSec VPN provides end-to-end encryption. But this comes at a cost, and this cost is speed because of the computation requirements. MACsec is a different model and I want to start by introducing why. So from a layer 2 perspective, this is how MACsec works. This is a standard 802.1q frame. It has the VLAN tag field which allows it to be used to create multiple virtual LANs in the same layer 2 space. It's not though encrypted. Anything within a layer 2 ethernet or 802.1q frame is plain text. At least unless the payload it's using is a plain text. At least unless the payload it's using is a plain text. The VLAN itself has been encrypted higher up the OSI stack. With MACsec we take the same basic frame but we add a few additional elements. First is the MACsec header known as the security tag. And second is an integrity check value field. Now the security tag contains a few fields but the notable ones are the details of the channel, the details of the secure association and the packet number. Now these frame differences are what we call the security tag. The frame differences are what allow MACsec to function. And what it means is two things. First that this part of the frame is encapsulated and encrypted. To any network devices not directly involved in the hop to hop MACsec link, this portion of the frame is unreadable. It's encrypted. Additionally the entire frame is authenticated and integrity protected. MACsec devices authenticate frames received from the other. And can detect any alteration to those frames. This combined with the encryption means that at least for a single hop, frame payloads are protected from eavesdropping and from alteration. So let's move on and look at the architecture of MACsec. Configuring MACsec on Direct Connect is pretty easy. First MACsec is something which is defined on either a DX connection or a link aggregation group known as a LAG, which I cover elsewhere in this section of the course. In both cases you need to create and assign a CAC CKN pair onto the interface or the LAG. Think of this like an encryption key. You don't need to understand the whole architecture for the exam, but this is something which is configured on both the DX connection or the LAG and on the customer side. Now once this is done, secure channels are established one in each direction. Over which secure associations can be created. And remember normally this is one at a time, but two will be used when changing associations. They have a lifetime and they need to be refreshed. Once secure associations are active, frames will be encrypted and their integrity will be verified. So this architecture shows the MACsec link between the AWS Direct Connect router in the AWS cage and a customer router in the customer cage. And both of these are connected. And both of these are at the DX location. Now it's possible to extend this. This requires a dedicated physical extension of the cross connect. And this type of extension requires layer 2 adjacency. Because the customer device and the AWS DX router need to be able to communicate with each other using layer 2 frames. So you need to be careful when using a comms provider for this. The cable will have to be dedicated and capable of extending this layer 2. Adjacency outside of the DX location. So this is something very specific that you need to check with the communications provider. And confirm if any extensions that they provide are capable of this layer 2 adjacency. Now MACsec is a great feature which allows you to protect the data path between your customer equipment and the AWS DX router. Since that link goes through a non-AWS owned data center and is potentially in the network. It's very easy to get lost. And if you have a non-AWS owned data center and is potentially extended by a comms provider. It allows you to secure that link at layer 2. It is not however a substitute for IPSec VPN running over a public VIF. As that provides end to end encryption. And also allows you to configure connections to VPCs outside of the local DX region. So remember these distinctions for the exam. At this point though that is everything which I wanted to cover in this lesson. So go ahead and complete this video. And when you're ready I look forward to you joining me in the next.