 Welcome back, and in this lesson I want to talk in a little bit more detail about IPv6 in general, but with a focus on how IPv6 is supported within AWS VPCs and resources. In general, as a solutions architect, you need to understand how IPv6 is structured and why it's needed, so how it differs from IPv4 and how it's implemented within a VPC. To fully understand the need for IPv6, as well as the differences in capabilities that it provides, it's useful to first look at IPv4. So this is a representation of the IPv4 address space. Historically, it's been broken up into three commonly used classes of IPs. So we've got Class A, which grants access to 128 huge networks, each containing 16.6% of the IPs. Class B grants access to 16,384 networks, each of which has 65,536 IP addresses. Class C, there are over 2 million networks within this class, each of which providing 256 IP addresses. Now we also have Classes D and E, and these are other ranges, but these are much less commonly used. And so we're going to look at how IPv6 is structured within this class. So I'm not going to go into detail about all of these different classes in this lesson. All of these IP addresses, except a few exceptions, are publicly routable, meaning if you have one of them configured on a device, you can communicate with another device which also uses a public IPv4 address over the public internet, assuming that there's no security in the way. Now parts of this address space are designated as private addresses, and I spoke about this in the previous video, but I'm not going to go into detail about that. I'm going to talk about these at an associate level, so networks like 10.0.0.0.0.8. If you have one of these, the public internet won't route your data. They're only used internally, but you're free to allocate them however you like. And these internal ranges are commonly used within AWS VPCs. Now all of these addresses are known as the IPv4 address space. And in total, there are four. 1,294,967,296 IP addresses. Now this might sound like a lot of addresses, but not all of these addresses are available for use, and even if they were, that's less than one address per person alive on the planet today. And how many of us have a mobile phone and a computer? What about providers like AWS who have huge public IP requirements? IPv6 was developed to solve this problem. The problem that we have far too few IPv4 addresses, and at this point have essentially exhausted the supply. With IPv6, we have more IP addresses available to use, and to fully appreciate this, I want to change the perspective. This reduction in size isn't even to scale, but any smaller and you won't be able to see the blue square, which represents the IPv6 address space. Imagine that the blue square is several thousand times smaller than it is on screen now. And with that in mind, this represents the available IPv6 block of addresses. The entire IPv4 address space available on the public IPv4 internet is just over 4 billion IPv4 addresses. With IPv6, the entire address space is 340 sextillion addresses. Now humans are bad with large numbers, but to put this into perspective, it means 670 quadrillion IPv6 IP addresses for every square millimetre of the Earth's surface. Or it also represents 50 octillion IP addresses per human alive. Another way of looking at this is that this represents 79 octillion IPv4 internet's worth of addressing. So take all of the addresses on the IPv4 internet, multiply this by 79 octillion, and this is the number of IPv6 addresses available on the IPv6 internet. What I want to make you comfortable with, is that with IPv6, the concept of IP addresses as valuable things just goes away. And that's important to keep in mind when thinking about how you design IPv6 networks within AWS. You essentially don't have to do any of the usual micro-level IP management. It's just not required. You don't need to think of IPv6 addresses as a valuable entity. Now let's just re-familiarise ourselves with how IPv6 addresses are used in the internet. So we start with a VPC in an AWS region, and this gets allocated when you create this VPC with a range of private IP addresses. Crucially, these IP addresses are not publicly routable. Now inside the VPC, you can create subnets, which can be classified as public or private. Public subnets are created with a default IP address, and this is a very common feature in the internet. Public IP addresses are routed with a default IPv4 route, which points at an internet gateway. Meaning any instances or services in the public subnet can be allocated with a public IPv4 address, and use this to communicate with the public IPv4 internet using IPv4. Remember, only public IPv4 addresses are routable on the public IPv4 internet. Now because IPv4 address space is limited, AWS resources are not automatically given public addressing. AWS also has the concept of private addressing for VPCs, and so we have the concept of private subnets, which also operate within VPCs. And these subnets are private because they only use private addressing. Instances or services that are provisioned into a private subnet, have no allocated public addressing. They can't directly communicate with the internet, and so instead they use a process called NAT, or Network Address Translation, and in a best practice way this is delivered by using a NAT gateway within AWS. Now a NAT gateway is a device which accepts communications from lots of private only resources, and translates their packets to have a public address. The same public address is used for many private services. This means that a private resource can access the internet indirectly, using a default IPv4 route pointing at the NAT gateway, which is running in a public subnet, and use that to access the internet. So the traffic goes to the NAT gateway as the default gateway for those services. The NAT gateway translates the packets to have a public address, and sends them on through to the public IPv4 internet using the NAT gateway. Now within AWS there are two really important things that you need to remember as a solutions architect about IPv4. The first is that public and private IPv4 addresses are completely different. Private addresses can't communicate freely over the public IPv4 internet. They can only be used in private networks such as a VPC. Whereas public addressing can be used to communicate externally to AWS. Private addressing, so RFC 1918, is translated into public addressing using a NAT gateway. So that's outbound only communication. Private instances can use a NAT gateway to initiate communications to a public IPv4 IP address, and receive responses, but public IPv4 IP addresses on the public internet cannot communicate with private instances inside AWS. So there is a firm distinction between private and public IP addressing when you're working with IPv4 within AWS. Now the second really important thing, and you'll hear me repeat this a lot within any AWS courses that I teach, is that you will never see a public IPv4 IP address in your network. You will never see a public IPv4 IP address configured within the operating system of an EC2 instance. You will only ever see the version 4 private address. Any public addressing is dealt with by gateway appliances outside of EC2. So it's never the right answer to any technical problem you encounter, or any exam question to configure a public address within the operating system of an EC2 instance. That never happens. In IPv4 only private addressing is configured within the operating system of EC2 instances. Even for public instances, when using IPv4, you never have the public address configured on the instance operating system. Okay, so this is the end of part one of this lesson. It was getting a little bit on the long side, and I wanted to give you the opportunity to take a small break, maybe stretch your legs, or make a coffee. Now part two will continue immediately from this point, so go ahead, complete this video, and when you're ready, I'll look forward to you joining me in part two.