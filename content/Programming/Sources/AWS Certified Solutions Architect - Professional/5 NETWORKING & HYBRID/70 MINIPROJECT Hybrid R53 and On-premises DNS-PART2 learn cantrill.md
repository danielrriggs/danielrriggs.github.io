 Welcome back, this is part two of this lesson. We're going to continue immediately from the end of part one, so let's get started. So now we're at this stage and we've completed step one of the demo. The next stage is to establish the hybrid network connectivity between the AWS environment on the left and the Animals for Life simulated on-premises environment on the right. Now on this diagram, it shows us using a direct connect, but for this demonstration, we're going to simulate this using a VPC peer. So let's go ahead and do that. So to create the VPC peer, let's go ahead and close down this EC2 terminal, move back to the AWS console, click on the services dropdown, and then in the find services box, just type VPC and click to move to the VPC console. Once we're there, click on peering connections on the menu at the left, click on create peering connections, for the peering connection name tag, just type AWS-on-prem and this will indicate that it's a peering connection between the AWS environment and the simulated on-premises environment. Now the requester VPC will be the AWS side, so make sure you select the VPC that's called A4L-AWS. This should show a CIDR of 10.16.0.0 slash 16. Scroll down and then for the acceptor VPC, we're going to select the simulated on-premises environment. So click in the dropdown and select A4L-on-prem and this one should show a CIDR of 192.168.10.0 slash 24. All being well, you should see no error messages and you can go ahead and click on create peering connection. Now we're going to finish setting up this peering connection, so click on the actions dropdown and then click accept request. You'll see a confirmation dialog and you can go ahead and click on accept request. Now that's the peering connection created between the AWS environment and the simulated on-premises environment. Creating the peering connection itself is only one part of the process. We also need to configure the routing. And to do that, click on route tables under virtual private cloud on the left. Now just expand the name column. And you'll see two named route tables. We have A4L-AWS, which is the route table for the AWS VPC. And then we have A4L-on-prem, which is the route table for the simulated on-premises environment. And we need to edit both of these, adding a route for the other VPC. So first we'll select the AWS route table. Select that one, click on routes. There'll only be the local route for the CIDR range of that VPC. So we'll need to add another one. So click on edit routes and then add route. We're adding a route for the CIDR range of the simulated on-premises environment. And that's 192.168.10.0 slash 24. So this is a route which allows the AWS VPC to communicate with the simulated on-premises environment. So the target for this route, if you click on the target dropdown, is going to be a peering connection. And select the AWS-on-prem peering connection. And once you've done that, click on save changes. Hopefully everything's okay. And you can go ahead and click again on route tables. And then you need to follow the process for the simulated on-premises environment. So go ahead and select the on-premises route table. Make sure that the route table is on-premises. The routes tab is selected. And then click on edit routes. This should have a local route for the CIDR of the simulated on-premises environment. That's fine. You'll also see another route for the S3 endpoint that's also in this VPC. Both of those are fine to leave as is. What you'll need to do is to click on add route. And we're adding a route which allows the simulated on-premises environment to communicate with the AWS VPC. So we need to add the CIDR range for the AWS VPC. And we're going to add a route which allows the simulated on-premises environment to communicate with the AWS VPC. Which is 10.16.0.0.16. And again, the target is going to be the peering connection. So click on the drop down, select peering connection. And then click on AWS hyphen on-prem and save that route. And again, it should tell you that the routes have been successfully edited. And that's good. Go ahead and click on route tables. So now the network connectivity is in place between the AWS VPC and the VPC for the simulated on-premises environment. And what we need to do next is to test that. So to do so, click on services and select EC2. Then click on running instances. Now you're going to need the IP address of one side of this newly connected network. So select the on-prem app server. And then just copy the private IP address of this into your clipboard. Now note, this is on the simulated on-prem. So you can see that the IP address of this newly integrated network is on the on-premise side of this newly integrated network. So what we need to do next is select an instance that's on the AWS side. So go ahead and select AWS hyphen EC2 hyphen A. Right click, connect. Again, we're going to use session manager. So select that and click on connect. So now if we type ping and then a space. And then paste in the IP address of the app server. And press enter. We will now get a ping response. Now this indicates that we do have network connectivity. So layer 3 connectivity between the AWS environment and the simulated on-premise environment. What we don't have though is DNS integration. If we type ping and then a space. And then app.corp.animalsforlife.org and press enter. We still get name or service not known. So while we do have layer 3 network connectivity. We don't have any DNS integration. And that's what we're going to do next. This is the end of part 2 of this lesson. It's a pretty complex lesson. And so I wanted to give you the opportunity to take a small break. Maybe stretch your legs or make another coffee. Part 3 will continue immediately from this point. So go ahead complete this video. And when you're ready I look forward to you joining me in part 3.