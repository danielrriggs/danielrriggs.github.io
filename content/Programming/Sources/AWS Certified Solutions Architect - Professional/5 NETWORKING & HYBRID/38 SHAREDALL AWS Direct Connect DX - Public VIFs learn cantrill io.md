 Welcome back, and in this lesson I want to talk about public VIFs. Now I've covered private VIFs in the previous lesson, so this time we need to look at how public VIFs work and how they differ. So let's just go ahead and jump in and get started. Public VIFs are used to access AWS public zone services. So this is anything which is located within the public zone. This means both public services such as SNS, SQS, and S3, and public IPv4 or elastic IP addresses assigned to VPC-based resources. Now what you can't access directly over a public VIF are private VPC services. So private IP addresses which are assigned to things running inside a VPC. Now what makes a public VIF really useful is that you can access all AWS public services. You can access all AWS public regions over the single public VIF. They don't have the regional limitation that private VIFs have with a single public VIF in US East 1. You can access the US regions and others such as AP Southeast 2. Now this is because AWS advertise all IP ranges of all of those regions to you over the public VIF. And any data transmitted is going over the AWS global network, not the public internet. Meaning it's going to be higher performing and less variable performance. Because the network is optimized for transmitting data between AWS regions. Now you're free to advertise any public IPs which you own over BGP. They have to be public IPs and you have to own them. If you don't have any public IPs then AWS can work with you to allocate some. But this is something that you'll have to work out with AWS support. Public VIFs also have a lot of different ways to work with AWS. You can also support bidirectional BGP communities. Which are a way that you can filter what routes you receive based on their geography. But I've got a dedicated lesson coming up elsewhere in this section where I'll be explaining this in more detail. Importantly, advertised prefixes are not transitive. Your prefixes don't leave AWS and this includes to other customers. So you can only access AWS public services over a public VIF. You can't for instance route over a public VIF to other customer IP addresses. If those customers are using AWS elastic IPs for say EC2 services, this does work. So in summary you can only contact other AWS public IP ranges using a public VIF. Now creating a public VIF is easy. Again assuming that you already have the DX connection created and operational. First, like with private VIFs, you can only use a public VIF if you have a public VIF. So you can only use a public VIF if you have a public VIF. Now creating a public VIF is easy. Again assuming that you already have the DX connection created and operational. First, like with private VIFs, you can only use a public VIF if you have a public VIF. So you can only use a public VIF if you have a public VIF. If you have a public VIF, you can only use a public VIF. In private VIFs you pick the connection that the VIF will run over. You choose the owner of the interface so this account or another. The VLAN which again needs to match the customer side configuration. You need to choose a customer side BGP ASN and ideally this is a public ASN. So that you can get the full functionality of a public VIF. Now more on this limitation later. But you can also pick a private ASN in the 64512 to 65518. to 65535 range. You'll need to configure authentication, and you'll need to select peering IP addresses, and then finally, you'll need to set which prefixes you want to advertise. And when adding new prefixes, there's generally a delay, as there's some verification required at the AWS side to avoid issues with BGP advertisements. Visually, this is how public VIFs are architected. We have AWS on the left, the DX location in US East 1 in the middle, and a business network on the right. Inside AWS on the left, we have a few VPC networks, but none of these can be accessed via public VIFs. So for now, we're going to ignore those. We also have the public zone in US East 1. In this example, I've included SQS S3 and any other public resources such as elastic IP addresses. In the DX location, the cross-connect between the AWS DX router can either terminate in the customer DX router or be extended to the customer premises by that customer or a comms provider. Over the cross-connect, we configure a public VIF, and our business network advertises our business network ranges to AWS. AWS, using BGP, advertises the US East 1 public zone, network ranges, but also it advertises public zones within other AWS regions. And all of these are provided using the BGP peering relationship to the Animals for Life network on the right. This means from this single public VIF, you have access to all AWS public zones using the AWS global network. Now, when I talk about all AWS regions, assume, unless I say otherwise, that China is excluded. In many ways, within AWS, China is architected as a separate partition of the AWS global network and infrastructure platform. So whenever I say all regions, you need to assume that I'm excluding China unless I say otherwise. And that is everything I wanted to cover about AWS public VIFs. Now, there are some awesome things that you can do using public VIFs, such as using a VPN connection to provide encrypted access to private VPC networks. But I'm going to be covering all of those exciting features in upcoming lessons elsewhere in this section of the course. For now, that's everything that I wanted to cover. So go ahead and complete this video. And when you're ready, I look forward to you joining me in the next.