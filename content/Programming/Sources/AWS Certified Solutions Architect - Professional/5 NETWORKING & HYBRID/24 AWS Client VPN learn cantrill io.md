 Welcome back, and in this lesson, we're going to be covering the client VPN functionality available within AWS VPCs. So far, I've talked about site-to-site VPNs, which is how you connect one or more business premises to AWS. Client VPN is similar, but instead of sites connecting to AWS, it's clients which connect. Now let's just jump in and get started straight away. A client VPN is a managed implementation of OpenVPN, which is a product you might have heard of. Nothing prevents you from building your own OpenVPN server running on an EC2 instance, in the same way that nothing prevents you from installing your own database on EC2. Client VPN is the same for OpenVPN as RDS is for databases. It's an AWS-managed implementation. Because it's based on OpenVPN, it means that any client device which can use the OpenVPN software is supported. And because OpenVPN is an open standard, pretty much every operating system and consumer-grade router can support it. Now architecturally, you connect to a client VPN endpoint, and this endpoint, when created, can be associated with one VPC. You also associate it with one or more target networks, and it's when you do this, that the product begins to charge. You're charged based on two elements. First, a flat rate based on the number of subnets that you configure as target networks, and you can do one or more for high availability. And then the second element of charging is an hourly fee per client VPN connection. Now the reason for the subnet billing architecture is that for every subnet you configure as a target network, an interface is placed into that subnet, which is used for traffic flowing between the target networks and the client devices. And those interfaces have a cost, just like any other interface endpoints do within a VPC. So architecturally, this is how client VPN looks. On the left, we have our clients, and we've allocated these a CIDR range of 10.18.0.0.16. In the middle, we have two VPCs, A and B. A has a CIDR of 10.16.0.0.16, A has a CIDR of 10.16.0.0.16, A has a CIDR of 10.16.0.0.16, and B has a CIDR of 10.17.0.0.16. Now VPC A has a private subnet and a public subnet, and VPC B has a private subnet. Then on the right, we have the public internet. Now to get started, we create a client VPN endpoint, and we associate this with a VPC and one or more subnets within that VPC. And this creates an interface within that VPC. Now this is a VPC and one and one subnet. Now you can only pick one subnet per availability zone, and you should view this as a method to ensure high availability of the solution. Now client VPNs can use various methods of authentication. You can use certificates, federated identities or the AWS directory service. It also integrates with CloudWatch, so any connection logs are stored in CloudWatch logs, and metrics are stored within CloudWatch. You define a route table with the client VPN endpoint, and by default that route table is pushed to any clients which connect to the client VPN endpoint. Now by default this replaces any existing route table on the client machines, but more on that in a second. The route table contains any of the target networks by default. So in this case, the subnet on the left of VPC-A, but you can add other routes to this route table manually. For example, if you want to allow connections from the clients to the public internet, then you could add a default route for the public internet, and logically you'd also need a NAT gateway and an internet gateway, because the connections are coming from the client VPN interface within a private subnet. Now you could also allow connections to instances in VPC-B. To do that, you'd need to create a VPC peer and configure routing as usual for that, and then add a route to the client VPN endpoint route table, allowing connectivity to the peered VPC. What's important to understand is that you treat any client devices as normal things running within the VPC. They need the same networking gateways, in this case a peering connection, a NAT gateway, and an internet gateway, to access resources just as a normal VPC interface would need. Additionally, the default behavior is that the client VPN route table replaces any local routes on the client. This is because the client VPN route is a bit more complex than the client VPN route table. If you're running a client device on a local network, you can't access any local network. Any communications between clients, in this example client 1 and client 2, occur over the client VPN, which is actually a suboptimal path for client local communications. Now you also have the option of using what's called split tunnel VPN. So as a reminder, by default, all traffic is routed via the client VPN endpoint. So as a reminder, by default, all traffic is routed via the client VPN endpoint. If you enable split tunnel, it means that any routes from the client VPN endpoint are added to client machine route tables. They don't replace anything that the clients already have. What this means is that connections to the public internet will go directly through the client machine's normal default route. And connections between clients, or from clients to other devices on the local network, will not use the client VPN. And this represents an option. For the exam, remember that split tunnel is not the default. By default, all traffic will go via the client VPN, and you have to specifically enable split tunnel to change this behaviour. Now at this point, that's everything I wanted to talk about regarding the theory of client VPN. What's going to make this much easier to understand is if we have a demo, and so that's what's coming up next. You'll get the chance to implement client VPN in a real-time environment, and you'll be able to use it to run your own virtual network. I'm not going to go into the details of what client VPN is in a realistic way, but at this point, that's everything that I wanted to discuss. So go ahead and complete this video, and when you're ready, I look forward to you joining me in the next.