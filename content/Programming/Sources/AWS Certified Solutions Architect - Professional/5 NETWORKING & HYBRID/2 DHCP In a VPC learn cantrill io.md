 Welcome back, and in this lesson I want to dive a little deeper into how DHCP works within AWS VPCs. Now DHCP stands for Dynamic Host Configuration Protocol. In the generic sense, it's a protocol which allows for the automatic configuration of network resources. If you've ever wondered how a laptop or phone gets an IP address on your local network at home, it's probably using DHCP. A device starts off with a hard-coded MAC address. Remember, this is the Layer 2 address which is unique to a specific device, consisting of a vendor part and a device part. Because this is all that a device has to start with, DHCP begins with a Layer 2 broadcast which allows it to discover a DHCP server on the local network. Once discovered, the DHCP server and the device, known as a DHCP class, communicate using Layer 2 initially, which means frames containing MAC addresses. The end result is the device obtains Layer 3 networking information and additional pieces of configuration. This means things like an IP address, a subnet mask, and a default gateway, and these are the minimum requirements for communication at Layer 3 on the local network and beyond using the default gateway. Now DHCP within a VPC, also configures which DNS service to use. Typically, these are the Amazon provided DNS within a VPC, and it also allows a custom domain name to be configured on resources within the VPC. It also configures NTP servers for time synchronization, as well as NetBIOS name servers and NetBIOS node types, and these are only used if you're making use of the NetBIOS protocol. Now, I'm going to be talking about the AWS specific implementation of DHCP next, but before I do, when you're setting which DNS service to use within a VPC, you can either explicitly provide values or you can use Amazon provided DNS, and this always maps onto the Route 53 resolver running within the VPC. You're also allocated one or two domain names for any resources provisioned into the VPC. For example, with EC2 instances, you're allocated a private DNS name, and the format of this name depends on whether you're using US East 1 or other AWS regions. You're also given a public DNS name if the instance has a public IP address allocated. Now, you might want to give your EC2 instances custom domains, and you can do that as long as you use your own DNS servers. You can add the custom domain name and the custom DNS servers to the DHCP option set, which I'll be talking about next. So now let's look specifically at the AWS implementation of DHCP, and this is configured, as I just mentioned, using DHCP option sets. And it's probably going to be best if we look at this from an architectural perspective, and we do this visually. So let's start with two DHCP option sets. These are independent. They're created individually with their own settings. The settings could be the same or completely different. Once created, option sets can't be changed. They're immutable. And that's an important point to remember for exam questions. DHCP option sets can be associated with zero or more VPCs. You can create one and not assign it to anything, or you can assign it to one or more VPCs. In this case, VPC A on the left and VPC B on the right. The other DHCP option set might itself be associated with another VPC. For example, VPC C on the right of your screen. Each VPC can have a maximum of one option set associated with it. It can have zero, but only ever a maximum of one. When you change the DHCP option set associated with a VPC, the change itself is immediate. But because it's controlling DHCP within the VPC, any new settings contained in the new DHCP option set will only affect anything in the VPC once a DHCP renew occurs. And the timing of this can vary. So keep that one in mind for exam questions. Now, I mentioned previously that DHCP in general is how a device gets the IP address, subnet mask and default gateway. And this is also true for DHCP within a VPC. But you can't configure these values within an option set. The IP address is allocated automatically. The subnet mask is based on the VPC subnet and the default gateway is always the subnet plus one address, which is the address that the VPC router interface uses within that VPC subnet. What you can configure within an option set is the DNS service to use. And by default, this is the Route 53 resolver, which can be referenced using Amazon provided DNS. Now, this uses the VPC plus two address. The subnet plus two address is reserved in every subnet. But since I know that you like precision, it's actually the VPC plus two address, which the Route 53 resolver uses for DNS within the VPC. You can also use option sets to configure which NTP service to use if time synchronization within a VPC is important. Anything provisioned in the VPC also gets DNS names allocated. You always have a private one, which takes this form. And if the instance has an allocated public IP, it also gets a public DNS name. Now, AWS provide these automatically. And if you want to customize the DNS names, you need to supply your own DNS service to use within the DHCP option set. So that's DHCP in general and DHCP option sets within a VPC. It's not really all that complicated as a concept, but you need to be sure that you understand all of these key points, you're going to get the chance to use these in a demo elsewhere in the course. For now, I just wanted to introduce and reconfirm your architectural theory knowledge. So at this point, go ahead and complete this video and when you're ready, I look forward to you joining me in the next lesson.