 Welcome back, this is part 2 of this lesson. We're going to continue immediately from the end of part 1, so let's get started. So what about IPv6? How does that differ? Well, let's take a look. IPv4 addresses within AWS, they're all publicly routable. Because there's no realistic chance of running out of version 6 IP addresses in the short or medium term, every IPv6 address in use within AWS is classed as publicly routable. And so this means, conceptually, there isn't really the concept of public or private subnets within AWS for IPv6. Because of this, NAT isn't used for IPv6. NAT gateways don't support IPv6 because IPv6 has no need for NAT as a process. NAT allows services which use private IP addresses to access public internet services by translating packets that are sent by those instances, changing the source address into a publicly routable address. And because all IPv6 addresses are already publicly routable, NAT as a process and NAT gateway as a product isn't needed for IPv6, and so it's not supported. To use IPv6, you need to have a public IP address. To use IPv6, you need to have a public IP address. To use IPv6 within AWS, the first step is that it needs to be enabled on a VPC. And there are a few ways that this can be done. You can use your own range of IPv6 addresses using the bring your own IP feature, or you can utilize an AWS provided range, which is the most common approach. When using this approach, AWS will allocate you a unique slash 56 IPv6 range to use for that VPC. When using this approach, AWS will allocate you a unique slash 56 IPv6 range to use for that VPC. So this range will be entirely unique for this VPC, and all addresses in that range will be publicly routable. Now see how the VPC IPv6 range that I'm using in this example ends in 2200. Well, every subnet that you have inside a VPC, if you choose to allocate an IPv6 range to that subnet, it gets allocated inside the VPC range. And that's because it uses a hex pair to make it unique in that range. So for this example, the subnet on the left gets 00 at the end, and then the subnet on the right gets 01 at the end. Now because it uses a hex pair for these last two digits, and this hex pair can range from 00 to FF, that means that there is space for 256 unique values. This means 256. So that's many, many orders of magnitude, more than the whole number of IP addresses available in the public IPv4 internet, available within each of your VPC subnets. Now again, the reason I'm stressing this is just to illustrate, how when dealing with IPv6, you never have to worry about exhausting the number of IP addresses that you have available. At least in the short and medium term, the fact that each subnet is given 18 quintillion publicly routable addresses, means that you don't need to engage in any low-level IP planning within a subnet or a VPC. If you're dealing with IPv6, IP addresses are no longer valuable in the same way, if you're dealing with IPv6, IP addresses are no longer valuable in the same way, if you're dealing with IPv6, IP addresses are no longer valuable in the same way, that they were for IPv4. Now routing for IPv4 and IPv6 is handled separately within AWS. So we have the same route tables and the same internet gateway, but we have different routes. We have IPv4 routes and IPv6 routes. Now let's say for this example we added this route table. It has a total of four routes, two local routes for the VPC side, one for IPv4 and one for IPv6, and then two other routes. And these are both default routes, one in orange and one in pink. 0.0.0.0.0, so the orange route, is the default IPv4 route. It matches any IPv4 traffic that's not been matched by any other more specific route. And then the route in pink is the default IPv4 route. It matches any IPv4 traffic that's not been matched by any other more specific route. And then the route in pink, which is double colon slash zero, this is the same for IPv6. So this matches any IPv6 traffic, which has not been matched by a more specific IPv6 route. So remember this concept, inside AWS, IPv4 and IPv6 have separate routes, and so they can be handled differently. So with this route table applied, it means that instances in both subnets, it means that instances in both subnets, can communicate using the internet gateway, both IPv4 and IPv6 in a bidirectional way. So this set of instances can communicate out to the public internet, using either the IPv4 protocol in orange, or the IPv6 protocol in pink. Now there are situations when you want to allow IPv6 resources to have outbound communication, or you want to allow IPv6 to have inbound communication, but not allow inbound. Remember, IPv4 provided the concept of the NAT gateway, which allows outbound only access, while protecting instances against inbound communications. Now NAT doesn't work with IPv6, because again, all addresses are publicly routable. The equivalent using IPv6 is to deploy an egress-only internet gateway. The equivalent using IPv6 is to deploy an egress-only internet gateway. The equivalent using IPv6 is to deploy an egress-only internet gateway. Then we can change the IPv6 default route, so that it no longer points at the internet gateway, instead pointing at the egress-only gateway. And this means that the IPv4 traffic will continue to be routed via the internet gateway, but IPv6 won't. Instead, it will be routed from both of these subnets via the egress-only internet gateway, and this allows outbound only access. And this means that the IPv6 is now able to be routed via the egress-only gateway, via the egress-only internet gateway, and this allows outbound only access, via the egress-only internet gateway, into the Internet. Now I need you to be comfortable with this architecture, because I've seen many exam questions which test you on your ability to allow bidirectional IPv4 traffic, but only allow outbound IPv6. You can only have one Internet gateway associated with a VPC, have both an internet gateway and an egress only internet gateway attached to the same vpc and then use both of them one for version 4 traffic and one for version 6 traffic by having two different routes as i'm showing on screen now so remember this architecture because version 4 routes and version 6 routes are dealt with differently it's possible to have different gateways and deal with version 4 and version 6 traffic in different ways now before we finish this lesson there are a number of final ip version 4 considerations that you need to be aware of ip version 6 can be set up while creating a vpc or subnet so you need to allocate an ip version 6 range on a vpc and then you can allocate it on subnets but it can also be added after the fact so you can migrate an ip version 6 route to a vpc or subnet and then you can allocate it on subnets but it can also be an existing vpc so that it supports ip version 6 you can enable ip version 6 on a vpc and then enable ip version 6 ranges on the subnets you can add routes and appropriate gateways so either adding a route for ip version 6 pointing at an internet gateway or add an egress only internet gateway specifically to deal with ip version 6 traffic so you've added the vpc range you've added the subnet ranges you've added routes you've configured the appropriate gateways the final step is to configure ip version 6 addressing on any services which you want to have ip version 6 access so services such as ec2 can be configured to receive ip version 6 address allocations and assuming all of the other infrastructure is in place they can operate over the ip version 6 public internet a computer that can be used to access the internet and the communicate with other version 6 services now be aware that not everything within aws supports ip version 6 elsewhere in this section i talk about private link i've talked about interface endpoints elsewhere in this section as well and these are both services which don't support ip version 6 in the same way as ip version 4 what i want you to do from this point onward in the course think of it as a homework assignment is for every service or architecture that i discuss i want you to go away and work out to what extent it supports ip version 6 or not so there should be three possibilities either it supports ip version 6 in its entirety it doesn't support ip version 6 at all and an example of this would be the nat gateway or a third option is that it does support ip version 6 but not in the same way as ip version 4 so maybe you can do that in the same way as ip version 4 so i'm going to go ahead and show you the next one so if you're interested in this topic i'm going to go ahead and show you the next one it doesn't support the full range of services or there are some product limitations when using ip version 6 with that being said though that's everything you need to be aware of ip version 6 still is relatively new in terms of its adoption you might see one or two questions on it in the exam probably focusing on migrating existing solutions to ip version 6 you might be required as an associate or professional level solutions architect to actually design and implement the an ip version 6 capable design but on the whole you only really need to be aware of what's been discussed in this lesson but at this point go ahead and complete this lesson and when you're ready i'll look forward to you joining me in the next