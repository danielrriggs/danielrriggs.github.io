 Welcome back, and in this lesson I want to talk about AWS PrivateLink. So PrivateLink is a technology which you've already come across because it's the tech stack which supports VPC interface endpoints. But it goes beyond that. AWS PrivateLink enables you to connect to services in a secure way which are hosted by other AWS accounts. And these are called endpoint services. Now you can connect to these directly or you can utilize AWS Marketplace partner services. And in both cases, these services are presented into your VPC via private IP addresses and elastic network interfaces. So they work in the same way as normal VPC interface endpoints. Now to allow you to understand exactly how this architecture functions, let's jump in and take a look at the architecture visually. Now a typical situation where PrivateLink might be used is where you have an application that you want to securely provide to another AWS account. Or when you're on the other end of that arrangement and you want to consume a service provided by a service provider. Now visually, let's assume that we're working in US East 1. And to keep the diagram simple, we're going to assume that we have two availability zones. Now because we're working in US East 1, we're going to assume that we have two availability zones. Now because we're working in US East 1, we're going to assume that we have two availability zones. Now because we're going to be working across accounts, we need to work with availability zone IDs. Because as you know by now, what's US East 1A in one account might be US East 1C in another. Availability zone IDs allow us to keep consistency across accounts. And so if you're going to be working with availability zones across accounts, you need to pay attention to the availability zone ID. In this case, USE1-A. USE1-A. USE1-A. USE1-A. USE1-A. USE1-A. USE1-A. And USE1-AZ2. Now when you're dealing with PrivateLink as a service, you have two sides to the relationship. First, you have a service provider, which is the VPC from where the service, as the name suggests, is provided from. And then as well as the service provider, we have any number of consumers. And also, as the name suggests, these are VPCs which consume the service. Now let's say. service provider we have a cat picture application that runs from two subnets within the provider VPC. Specifically it runs from two EC2 instances, one in each subnet and thus in each availability zone within that VPC. Let's also assume that there's a demand to access this application from the consumer VPC, specifically from workspaces running inside two subnets in this VPC accessed via the workspaces client that's running on user devices. Now the cat images application is actually private. We don't want all of those cats exposed to the world. They want to remain anonymous. Now to allow that we need to create an endpoint service within the provider VPC and then to ensure that the application performs well and is resilient we use a network load balancer. And this creates network load balancer nodes in each availability zone within the provider VPC. Now the provider logically needs to make sure that the network load balancer is configured for cross zone load balancing so that each of the nodes can balance connections to instances in other availability zones. This makes sure that the load is distributed and the solution is resilient. And this is important if the application is going to be used by lots of consumer VPCs and lots of customers. Once the endpoint service has been created then access can be granted to other entities and this can be individual IAM users, it can be roles, it can be AWS accounts or even open access. Once access is granted consumer VPCs can deploy endpoints into subnets within a VPC and these can be protected by using VPC security constructs such as the VPC security constructs. So if you have a VPC that's not a VPC you can use such as security groups and network access control lists. Interface endpoints provide access to resources outside of the security boundary of the VPC in a secure way which doesn't leave the private AWS security zone. Interface endpoints are delivered as elastic network interfaces within a VPC and so they're accessible via IP version 4 in that VPC as though the service itself is located within that VPC. So if you have a VPC that's not a VPC you can use this as a security interface. So if you have a VPC that's not a VPC you can use this as a security interface. So if you have a VPC that's not a VPC you can use this as a security interface. So if you have a VPC that's located within the consumer VPC. Now the data flows through the interface endpoints through to the service provider VPC it hits the network load balancer via the load balancer nodes and then the connections are distributed to the compute resources providing the application services and the end result the application is accessible via the consumer VPC in a secure way. So AWS private link outside of its common user interface is able to be used to provide access to the user interface is able to be used to provide access to the user interface. So AWS private link is able to be used to provide access to the user interface. So AWS private link is used to provide as the technical basis for interface endpoints is generally used to provide secure access to services delivered by service providers. So a service provider either directly or via the marketplace provides a service that's consumed by service consumers using AWS private link. And this is a feature that's very often used within larger AWS deployments that you may have to design as well as business that has to operate within the AWS private link. So if you have a within strict security guidelines. So if you're a medical data company, if you work in financial services, you may need to consume a service that's provided by a service provider, but do so in a way where the data doesn't transit over the public internet. An AWS PrivateLink is one way that you can do this. In the case of this example, it's a CAT application, but imagine that it's SNS, SQS, or any other AWS service. It's the same technology behind anything which uses interface endpoints. Those services are delivered from provider networks which AWS manage and injected into your VPC using normal interface endpoints. The technology is the same. Now let's look at a few key facts which will help you in the exam, and it's worth doing your best to remember all of these next points. So with PrivateLink as a whole, if you want to ensure that you're delivering a highly available, then you need to make sure that you deploy multiple endpoints. So one within every availability zone inside VPC that you need to consume that service from. Additionally, PrivateLink supports IP version 4 and TCP only. IP version 6 is not currently supported. Now you've got the ability with PrivateLink to use private DNS. So an example of this is I could have my CAT application that's available on the public internet using the DNS name of app.awscats.com. And if I provide this service as well using PrivateLink, then any of my consumers can use private DNS and override the default public DNS with one which points at the interface endpoint for the PrivateLink service. So in the same way that you use private DNS to override the default service endpoint for interface endpoints, you can do the same with PrivateLink. So this means that applications can use services provided by PrivateLink without requiring any code changes. They can maintain the usage of the existing DNS endpoint addresses and use private DNS to override those to point at the PrivateLink endpoints. So this is exactly the same architecture that interface endpoints use within a VPC. And then also, just like with interface endpoints, you can access PrivateLink services over a Direct Connect, site-to-site VPN, and over a VPC peer. So keep all of those in mind for the exam. Now that's everything I wanted to cover about PrivateLink in this lesson. It has been a theory-only lesson because to be honest for the exam, you just need to know the service exists and how it's architected. With that being said, go ahead, complete this lesson, and when you're ready, I'll look forward to you joining me in the next.