 Welcome back, this is part two of this lesson. We're going to continue immediately from the end of part one, so let's get started. So let's move back to the AWS console, and now we're going to configure an endpoint policy so that the private VPC can only access the private cats and public cats S3 bucket. We're going to configure it so that it can't access the dogpix bucket. Now to do that, we need to move to the VPC console, so click on the services drop down, and type VPC if it's not already in your recently used services or your history on the left. So move to the VPC console, and then scroll down and select endpoints. Now I'll just make this a little bit easier to see. You should have a small number of endpoints listed, and one of them should be using the com.amazonaws. So that's the one that we're going to edit. So make sure that that one's selected, and then we can just drag this bottom pane up to make things easier to see, and then click on the policy tab. Now note here how it has the default policy, which is a full access policy. Now remember, this doesn't grant any permissions over any service. It simply... means that the gateway endpoint won't impose any restrictions on what can be accessed via that endpoint. Now we do want some restrictions. We only want to be able to access the public cats and private cats bucket from within our private VPC using this endpoint. So we need to edit the policy. So go ahead and click on edit policy. Now the options you have are a full access policy or a custom. Now the options you have are a full access policy or a custom. And we're going to go ahead and apply a custom policy. Now also attached to this lesson is a file called endpointpolicy.json. Now what this does is to define a number of specific actions that can be performed through whichever endpoint this policy is applied to. Now specifically, it allows anyone using this endpoint to list all of the buckets and to get bucket location. It allows the S3... list bucket action, which gets the contents of any buckets, but only on any resources that we explicitly set. And then it allows any S3 operations on any objects inside those explicitly defined buckets. So we're going to configure this endpoint policy so that we can only interact with objects within the public or private cats buckets. The public or private cats buckets themselves. And perform list all my buckets on the account itself. So go ahead and copy all of this into your clipboard. Then move back to the AWS console and just paste that in. Now we will need to replace some placeholders. We're going to need two things. We're going to need the name for the public cats bucket and the name for the private cats bucket. So what I suggest, click on services, type S3. Right click and open that in a new tab. Then once you're there, locate the public cats bucket. We'll do that one first. And just select the full name of that bucket. Make sure you've got it highlighted and copy that into your clipboard. Then move back to the tab where you're editing the policy. And we've got two placeholders that we need to replace. So first, I want you to highlight starting with the P in public cats bucket name. And stop before the forward slash. It should look like this. And then paste in what you just copied into your clipboard. Next, scroll down and again select starting with the P for public cats bucket name. And stop before the speech mark. So it should look like this. And again, paste in what you just copied into your clipboard. Now we're going to do the same process for the private cats bucket. So go back to the S3 console. Select the full bucket name for the private cats bucket. And copy that into your clipboard. Then go back to the edit policy tab. And then follow the same process that we just used. Only this time, it's for the private cats placeholder. So scroll to the top. And for private cats bucket name, select starting with the P. And move all the way to before the forward slash. It should look like this. And then just paste in the full bucket name for the private cat pics bucket. Scroll down. Select the P next to the second occurrence of private cats bucket name. And then move all the way across to before the speech mark. It should look like this. And then paste in the full name for the private cats bucket. So now it should look like this. What you're doing is you're allowing any S3 action against any object inside the private cats or public cats S3 bucket. You're allowing a list of all the public cats in the S3 bucket. You're allowing a list bucket operation against either the private or public cats buckets. And then you're allowing list all my buckets and get bucket location against any resources. So once you've done that, scroll right down to the bottom and click on save. And this is modifying the endpoint policy for the S3 gateway endpoint inside this private VPC. Now that that's done, just go back to the session manager tab and just make sure you're still connected. This may have disconnected. If it has, you'll just need to right click on the instance again, put connect, and then select session manager and put connect. But if you are still connected, you don't need to do any of that. We can just continue straight away. Now at this point, the endpoint is configured so that we can still access the public and private cats bucket. So if we just use the up arrow and go through the history, we should still be able to do a listing of all the S3 buckets in this account. Because the gateway endpoint allows us to do that. We should also still be able to do a listing on both the private cats and the public cats bucket. So let's try it with public cats. And we can still do the listing. Now let's try private cats. And we can still do that listing as well. But if we attempt to perform any operations on the dog picks bucket, we're going to get an access denied. And that's because the endpoint policy no longer allows us to interact with the dog picks bucket. It only explicitly allows us to interact with the public and private cats bucket. It does not contain anything for dog picks. And in absence of an explicit allow inside an endpoint policy, we are implicitly denied from interacting with this particular resource. Now this doesn't impact our ability to interact with the dog picks bucket from our local machine. Because this endpoint policy only applies to the endpoint. So if we move back to our terminal and use the up arrow to where we listed the contents of the private cats bucket. If we just delete that now. So leave the two forward slashes. And then select the forward slashes. And then select the full name for the dog picks bucket. Copy that into our clipboard. Paste that in. And then at the end, double hyphen profile. And then a space. And then I am admin hyphen general. And press enter. We can still do a listing of the dog picks bucket. This just illustrates that an endpoint policy only impacts accesses when using that endpoint. We've done nothing to restrict access to any of the S3 buckets. We've only restricted accesses which go through the gateway endpoint inside the private VPC. So next, based on this architecture, we're going to apply some restrictions which limit who can interact with the private cats bucket. Currently, there's nothing to stop me interacting from my local command line with the private cats bucket. I can perform a full listing. And that makes sense. Because my identity. The profile I have. The profile I am admin hyphen general. Has the ability, the permissions to interact with the private cats bucket. But in this scenario, we want to limit access to this bucket so that only the private VPC can access this sensitive data. So let's do that. And to do that, we need to return to the AWS console. Go to the S3 console, which you should still have open in another tab. And then click on the private cats bucket. Now what we're going to do. Is to apply a bucket policy. To do that, we need to go to permissions. Scroll down to bucket policy. And click on edit. Currently, the bucket has no bucket policy. Which means that there are no restrictions on who can interact with this bucket. Now also attached to this lesson is a file called bucket policy dot JSON. And if you open that in a text editor, it should look something like this. This is a bucket policy which will limit who can access that bucket. Restricting it to only one particular VPC endpoint identifier. So what I want you to do is to copy all of this JSON into your clipboard. And then move back to the AWS console. Once you're there, paste that in to the bucket policy editor. Now there are two things that we need to edit. First, we need to update these two placeholders under resource. Because we need to change it so that this policy applies to the private cats bucket. Even though it's a resource policy on this specific bucket, we need the lines under resource to match. So copy in the full ARN for this bucket into your clipboard. And this will be different for you. Next, under resource, select everything starting with the A, going all the way, and stopping before the speech mark. It should look like this. Once you've done that, just go ahead and paste in the ARN. That you've just copied into your clipboard. Next, on the line directly below that, select starting from the A, and moving all the way to the forward slash. Don't select the forward slash. And then just paste in the ARN of the bucket. Now this is the point where you need to be careful. Because now we're going to lock this bucket down to only the private VPC. So we have to be careful because potentially you can lock yourself out of your own office. So what we need to do is to move back to the VPC console. You should still have this open in a separate tab. Just make sure that you still have the S3 gateway endpoint selected. And I want you to go ahead and copy down the endpoint ID. So you can click on copy to clipboard next to endpoint ID to make sure that you get the full endpoint ID. It's really important that you don't miss any of the characters. So copy that into your clipboard. Then go back to the S3 console. Next to AWS colon source VPC E. Look for the second set of speech marks. And select all of the text between those speech marks. So it should look like this. Once you've done that, just paste in the VPC endpoint ID that you just copied into your clipboard. What this is doing is applying an explicit deny policy. So that it will deny any S3 actions against this bucket when this condition matches. And this condition will match whenever the bucket is being accessed not from this VPC endpoint. So if it's accessed from anywhere other than this VPC endpoint, it won't work. So go ahead and click on save. And then click on objects. And hit refresh. And what you'll see is an access denied error. Now the reason for that is just interacting with this bucket from the AWS console is accessing from somewhere that's not the private VPC. It's not using the VPC endpoint ID that you just set on that bucket. So we've now locked ourselves out of this bucket in terms of accessing it from the console UI. Additionally, if you move back to your local console, or if you move to your local command prompt or terminal, and attempt to perform a listing of the private cats bucket, you should also get an access denied error. Because you are not coming via the gateway endpoint ID that you just set. So the explicit deny in the bucket policy is matching because we are not coming from the VPC endpoint ID that you set inside that bucket policy. So accessing from anywhere else in the VPC endpoint ID is not a good idea. So accessing from anywhere but that VPC endpoint will now generate an access denied because it's explicitly denied. We should though still have access from our private VPC. So move back to the AWS console. Open the systems manager tab. Make sure you're still connected. And then let's just clear the screen to make it easier to see. And then use the up arrow to go through your history. And just locate the command where we did a listing of the private cat bucket. And if you press enter, you should still be able to access the private cats bucket. And this is because this condition, this explicit deny that's on the bucket policy is not being matched now. Because we now are coming from the VPC endpoint ID that you configured in that policy. Now that's everything that I wanted you to do in this demo lesson. But we've actually locked ourselves out of the private cats bucket. And so we need to correct that. To do that, just move back to the AWS console. Select the VPC tab. Make sure that you've got the gateway endpoint for S3 selected. Just click on the policy tab. Edit the policy. Just return it back to full access and click on save. Because we still have access from this private VPC, we can use the command line to remove the bucket policy entirely. And to do that, we need to use this command. So AWS. Yes, space. And then S3 API. Because we're interacting with the APIs directly for S3. So this is a low level command. And then delete hyphen bucket hyphen policy. And then a space. And then double hyphen bucket. And then a space. And then we need to specify the name of the bucket. So the name of the private cats bucket. So select it from above. Make sure you get it all and copy that into your clipboard. And then paste it in and press enter. And that will remove the bucket policy on the private cats bucket, restricting it only to the private VPC. Which means you can now complete the cleanup steps for this demo lesson. Then go to services. Then cloud formation. Select the stack that you created at the start of this lesson. Click on delete. And then click on delete stack. And when that stack is deleted, the account will be returned into the state as it was at the start of this lesson. So that's the end of this demo lesson. Great job. You've implemented a pretty complex architecture inside AWS. So you've implemented a cloud formation template that creates a private only VPC. You've configured a gateway endpoint inside that VPC to limit access to only a number of explicitly defined buckets. And then you've configured a bucket policy on one of those buckets so that it can only be accessed when using that gateway endpoint. And this is a fairly typical architecture that you'll come across when creating private only highly secure VPCs inside AWS. With that being said though, that's everything I needed you to do in this demo lesson. So go ahead, complete the lesson. And when you're ready, I'll look forward to you joining me in the next.