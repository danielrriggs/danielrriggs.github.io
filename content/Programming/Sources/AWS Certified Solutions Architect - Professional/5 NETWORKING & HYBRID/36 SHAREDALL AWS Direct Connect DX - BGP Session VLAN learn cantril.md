 Welcome back, and in this lesson I want to talk about the logical way in which Direct Connect integrates with your existing network, and that's using a combination of BGP sessions and virtual LANs, known as VLANs. Now I want to cover this theory first, before moving on to talk about virtual interfaces or VIFs in the upcoming lessons of this section. So let's just go ahead and jump in and get started. DX connections are physical connections, so that means Layer 1, physical cables running Layer 2 data link protocols. Remember from the VLANs lesson that this means a single shared medium. Direct Connect allows connections between a corporate network and many different networks in AWS, such as VPCs and the public zone AWS services. Now we need a way to connect to these multiple types of Layer 3s. So IP Layer networks over a single Direct Connect connection, and that's where virtual interfaces or VIFs come in handy. VIFs allow you to run multiple Layer 3 networks over the Layer 2 Direct Connect. A VIF is simply a BGP peering session, so something which exchanges prefixes which allow traffic to be routed from one point to the other. And all of this is isolated. So the VLAN isolates the different Layer 3 networks using 802.1q, and that's VLAN tagging. BGP exchanges prefixes so that each end knows which networks are at each side. And BGP MD5 authentication means that only authenticated data will be accepted by either side. Now VIFs come in one of three types. We've got public, private, and transit. Public VIFs are used to connect to public zone services, so services which don't run within a VPC, or services which do run in a VPC but have public IP addressing. Private VIFs are used to connect to AWS private networks, so VPCs, anything communicating between your corporate network and AWS VPCs using private addressing. Transit VIFs I'm going to be talking about later. They allow integration. They allow connection between direct connect and transit gateways. And I'll be covering each of these in detail within dedicated lessons in this section of the course. A single dedicated DX connection can have a total of 50 public and private VIFs, as well as one transit VIFs. For hosted connections, this is just one VIF. The BGP session, which is part of what makes the VIF, is between the customer DX router and the AWS DX router. is between the customer DX router and the AWS DX router. is between the customer DX router and the AWS DX router. is between the customer DX router and the AWS DX router. is between the customer DX router and the AWS DX router. is between the customer DX router and the AWS DX router. And as I've mentioned before, this can be extended through to the customer premises. Now I just wanted to set the scene and establish a baseline amount of knowledge before I talk in much more detail about virtual interfaces. Let's finish this fundamentals lesson by looking at this visually. So on the left, we have our business premises. In the middle, we have a DX location in the same physical region, which contains a customer DX router, and an AWS DX router. On the right, we have the actual AWS region. In this example, AP Southeast 2, which contains a number of VPCs and the AWS public zone, which I've illustrated here in green at the bottom, containing the SQS and S3 services. Now first things first, let's talk about a few key architectural points. Between the direct connect location and the AWS region, there are always multiple high-end services. There are always multiple high-end services. There are always multiple high-end services. There's going to be a lot of high-speed links, and this ensures that the DX location and region always have reliable high-speed connectivity. Between the customer router in the DX location and the AWS DX router, is the physical cross connect. This is a Layer 2 connection, and it's known as having Layer 2 adjacency. This means that the customer DX router and the AWS DX router can communicate directly two data units. Now we need to run layer three connections over the top of this, multiple layer three networks, because we're providing access to multiple AWS VPCs and the AWS public zone. And so we're going to use VLANs, multiple VLANs running over the top of this layer two connection. This allows for isolation, multiple isolated layer three connections to be run over the top of this physical connection. In this case, we have VLAN 100 at the top, 200 in the middle, and VLAN 300 at the bottom. Using these VLANs, we create VIFs, and VIFs use a specific VLAN and a BGP session together. In this case, we have VIF 1, a private VIF, VIF 3, also a private VIF, and VIF 2, which is public. Each VIF has IP version 4 or IP version 6 BGP. And so we have VLAN 100, which is a private VIF into VPC 1, VLAN 300, which is a private VIF into VPC 3, and VLAN 200 in the middle, which is a public VIF into the AWS public zone. Now these BGP sessions use authentication so that only valid data is available. The VLANs and thus the VIFs can be extended to the business premises using Q-in-Q encapsulation, which I'll be covering in depth elsewhere in this section of the course. So that, architecturally, is how virtual interfaces, or VIFs, work. It's a VLAN to isolate traffic. It's BGP to exchange prefixes so that each side knows the other side's networks. And it's MD5 authorization. It's BGP to use authentication to ensure only valid information is allowed. In the upcoming lessons, I'm going to be covering how the different types of VIFs work, as well as introducing additional features of Direct Connect. At this point, though, that's everything I wanted to cover. So go ahead and complete the video. And when you're ready, I look forward to you joining me in the next.