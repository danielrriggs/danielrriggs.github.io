 Welcome to part 4 of this lesson. We're going to continue immediately from the end of part 3, so let's get started. Okay, so to configure the outbound component of this integration, let's move back to the Route 53 console and select Outbound Endpoints towards the bottom left. And we're going to create an outbound endpoint. We'll call the endpoint name A4L Outbound. This will also be running from within the AWS component of our architecture. So click on the VPC dropdown and select A4L-AWS. It's also going to need a security group, which is how we can secure these endpoints. So click in the dropdown and select the AWS security group. And then select IPv4 under Endpoint Type. The outbound endpoints will use two IP addresses, so scroll down, select Availability Zone 1A, pick the Private A subnet, and we'll let AWS assign. The IP address automatically. For IP address 2, select 1B, choose Private B subnet, and again, we'll allow AWS to assign the IP address automatically. And then click on Create Outbound Endpoint. And this is creating the outbound Route 53 endpoint. Again, this will take a few moments to create, so we'll need to wait for that process to finish. So go ahead and pause and wait for it to move out of the creating status. Once that finishes creating, we'll go back to the Outbound endpoint. Once that's done creating, it should show as operational. And next, we need to configure the forwarding rules that will use this outbound endpoint. So go ahead and click on Rules towards the bottom left, and we're going to create a rule. We're going to call the rule A4L-On-Prem-Corp Zone, so that we know the role that it provides. And then scroll down, and under Domain Name, we need to configure the domain name that this rule will forward queries for. So we need to use... The zone that's located within the simulated on-premises environment. So this is corp.animalsforlife.org. So type that in. And then we need to pick which VPCs will use this rule. So it's going to be A4L-AWS. We want this rule to apply to all services within the AWS VPC. And then we need to pick the outbound endpoint that this rule will apply to. So click in the dropdown. And then we need to select the A4L outbound endpoint that you just created. Now this rule is going to forward any queries for this domain name. And we need to pick where those queries are forwarded to. So we need to give two IP addresses. So click on Add Target. And we need to provide the private IP addresses of the DNS servers that are running within the simulated on-premises environment. So go back to the EC2 console. And we need the private IP addresses of the DNS servers that are running within the simulated on-premises environment. So go back to the EC2 console. And we need to provide the private IP addresses of the DNS servers that are running within the simulated on-premises environment. So first let's do DNS A. So copy that into your clipboard. Move back to the Route 53 console and just paste that into the top IP address box. Go back to the EC2 console. Pick DNS B. Copy that into your clipboard. And paste that into the second IP address box. So what this is doing is it's going to forward any queries that are forwarded to the IP address box. So what this is doing is it's going to forward any queries that are forwarded to the IP address box. And that means that you can forward any queries for coop.animalsforlife.org through to these two IP addresses. And these just to reiterate are the DNS servers running within the simulated on-premises environment. And these just to reiterate are the DNS servers running within the simulated on-premises environment. So the DNS servers are being launched within the simulated on-premises environment. So go ahead and click Submit. That rule is applied and it's now active so let's go back to the EC2 console. And we're going to test it by connecting to one of the AWS resources. Let's use EC2-A. Click and select connect. Choose session manager. Click on connect. So now we're inside the AWS environment. And let's ping a resource inside the simulated on-premises environment. This will test the network connectivity and the DNS resolution. So type ping, space, app, so app.corp, C-O-R-P, dot, and then animalsforlife.org and press enter. And now that we've configured the integration, it will resolve this name to this IP address. And because of the connectivity that we have between the two environments, that ping will return a response. So the architecture of this is that this query originates inside the AWS environment. It goes to the Route 53 resolver. Because we have those rules in place, the Route 53 resolver knows for any corp.animalsforlife.org queries to use this. So we can use these particular outbound endpoints. And then these outbound endpoints are used to forward that query through to the DNS servers running inside the simulated on-premises environment. And those are the servers that are providing an answer to the DNS query that we've just run as part of that ping. So we get the IP address back in response to that query. And then we can ping that IP address. And at this point, you've successfully configured integration. In both. And you've configured the reverse. So integration from AWS to the simulated on-premises environment using outbound endpoints. And that's everything that I wanted you to do in this demo lesson. I hope it's been useful and you've gained some practical experience of how to perform a fairly easy, complex integration of AWS and on-premises DNS environments. At this point, all that remains is for you to tidy up to put the accounts back into the state as they were at the start of this demo. So to do that, move back to the AWS console. Select rules. And then click on the rule that we've just created. It should be called A4L on-prem-corp zone. Select the VPC that it's associated with. And select disassociate. Now that will take a few minutes, so just pause the video and wait for that to finish disassociating. Once that has disassociated, then we can go ahead and delete the rule by clicking on delete. This time you'll need to type delete. Once the rule is deleted, then you can go to outbound endpoints. Select the outbound endpoint and click delete. Again, you'll need to type or paste delete into that box. Do the same for inbound endpoints. So select the A4L inbound. Click on delete. Type delete. Next we'll need to delete the VPC peer. So click on the services drop down and select VPC. Click on peering connections. Make sure the VPC peer that you've got selected is the one between the AWS environment and the simulated on-premises environment. Click on the actions drop down. Delete the VPC peering connection. Now a new option with the recently updated console is you can actually select to delete the route table endpoints. So that's the one that we've got selected. And then you can select the route table entries that you've created for this VPC peer as part of deleting the peering connection. So you can select delete related route table entries. And that will cause it to delete those entries when you delete the peering connection. Now I'm going to show you the process manually. So go ahead and click do not delete the route table entries. You'll need to confirm the deletion by typing delete into this box and then clicking on delete. And once you've done that, go ahead and click on route tables. And we need to edit both of these. So select the on-prem one. Click on routes. We're going to remove these black hole routes. So click on edit routes. Click on remove next to black hole. Click on save. Then select the other one. Again, routes, edit routes. Click on remove next to black hole. And click on save changes. Now we did both of those because we edited those route tables to point at the VPC peer which no longer exists. And so we can now edit them. So we just want to make sure that we return those route tables into the same state that they were when they were created by the one-click deployment at the start of this demo series. And then the last step, click on services. Select cloud formation. And we're going to delete the stack hybrid DNS. So select it. And this is the stack that was created using the one-click deployment. So select that stack. Click on delete. And then delete stack. And once all of that's finished, your account will be back into the same state as it was at the start of this lesson. With that being said though, that's everything you need to do for this demo lesson. I hope it's been enjoyable. And I hope it's given you some good practical experience of how to operate with Route 53 endpoints. So go ahead, complete this lesson. And when you're ready, I'll look forward to you joining me in the next.