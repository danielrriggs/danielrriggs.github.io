 Welcome back to part 2 of my Advanced VPC Structure Series, which looks at how to choose the structure of a VPC. In part 1, I talked about how many availability zones to use within a design, and what effect that choice had on the efficiency of the design. Now in part 2, which is this lesson, I want to talk about subnets and tiers within a VPC from a design and architecture perspective. So let's jump in and get started. Now if we exclude any applications which use the serverless or microservices architectures, then applications are designed based on tiers, each of which provides a different area of application functionality. First is the presentation tier, which is responsible for the interaction with users of the application. It translates application logic into a form which the user base can interact with. Then we have the logic tier, which contains the business logic of the application. And this tier provides what you would traditionally think of as the application functionality. So anything which the application does is handled by the logic tier. And then finally we have the data tier, which handles the persistent storage of data to disk. Now strictly speaking, each of these tiers are logical or architectural concepts, but because each of them is semi-isolated, and they communicate with each other, they have a different structure. They communicate with each other to deliver application functionality. In legacy infrastructure platforms, physical infrastructure tended to be built around these tiers. In this example, a subnet, potentially a physical subnet, would be built around each of the application tiers. Now historically, each tier has had its own networking and security configuration. Upper tiers, such as the presentation tier, were installed on web servers, and those were considered dirty, open for potential attack. Lower tiers were considered clean, such as the database. Now in legacy physical environments, each of these tiers was generally isolated, and data crossing between the tiers was tightly controlled using firewalls. One of the biggest mistakes that you can make as a real-world solutions architect, and a very common mistake in the exam, is to mimic this design within AWS. Put simply, don't. AWS provide security functionality and networking features, which mean this type of tiered approach, specifically using multiple subnets for each tier, is no longer required within AWS. Now there are some situations where you do need multiple subnets to deliver a specific set of functionality, and I want you to know when that's required versus when it's not. But as a base level solution, I think it's important to know when that's required, and when it's not. But as a base level solution, I think it's important to know when that's required, and when it's not. So for every single starting point, you need to understand that there's no longer a requirement to separate different application tiers into different subnets. It is possible to deliver all of these from a single subnet, you just need to be aware of all the different decision points that affect how highly available, cost-effective, performant and secure your application is when using single subnets versus multi-subnets. So let's look at that next. Let's use a familiar example that we've been using for a while now. Let's use a familiar example that we've been using for a while now. Let's use a familiar example that we've been using for a while now. And that's the Categram Enterprise application. And this is currently running within one subnet within AWS. So this is a one subnet and one network tier design. So this is a one subnet and one network tier design. Now let's brush straight past this at this point. Now let's brush straight past this at this point. Because as you've learned in previous lessons, in this section of the course, and elsewhere in the course, this doesn't provide any form of high availability. If we're only using one subnet, If we're only using one subnet, then we're only using one availability zone, because one subnet is always in one availability zone. And this means that if US East 1A fails, then all three of these instances fail, and we can't provide application functionality to our users. Now it also has another interesting limitation. Because it's one subnet, then we can only have one network ACL associated with that subnet. Either the VPC default network ACL, or a custom one. And this means that in this particular case, the network ACL can't be used to filter traffic between any of the instances, because it only filters traffic when it enters or exits a subnet. Now you might not require a network ACL to filter traffic in this case, but it is one common reason to split things into different subnets, so that they can benefit from the very, granular filtering provided by network ACLs. And this includes explicit denies. If you have any requirements to explicitly deny traffic flow, maybe you've allowed open access to a set of web servers, but you need to block one network range or one specific IP, then network ACLs are required. They're the security construct in AWS which can explicitly deny particular IP addresses or ranges of IP. And if network ACLs are required, then these are placed at the subnet boundary. And this might influence how many subnets you have for the application. So keep that in mind as we move on in this lesson. Now from now on, I want to focus on how you can decide whether to use application tiers or not. So how many subnets you need for a particular application design. So let's move on. Now if we need to add high availability to the category, we can do that by using the program. This is usually a reason to move to a multi-subnet design. For example, if the application required a minimum of two EC2 instances to provide that base load, but if we also needed the ability to cope with the failure of one availability zone, then this means we need to use three availability zones. And the simplest way of doing this is to put one instance inside each of those availability zones. Now with this design, we still have a single application tier, because we haven't yet discussed the reasons why we might want additional tiers. But because we need this tier across multiple availability zones, we need three subnets, because a subnet cannot span multiple availability zones. So this means one subnet per tier per availability zone. In this case, three subnets. Now this is a very simple design. Now this is a very simple design. Now this is a very simple design. Now this is a very simple design. This is why the default VPC in every region has the same number of subnets as the number of availability zones in that region, because one subnet can only ever occupy one availability zone. So as a starting point in the exam, you need to keep in mind the number of availability zones that you might need to utilise for your application, because this will form part of the calculation to determine how many subnets you require. Okay, so this is the end. This is the end of part one of this lesson. It was getting a little bit on the long side, and I wanted to give you the opportunity to take a small break, maybe stretch your legs or make a coffee. Now part two will continue immediately from this point, so go ahead, complete this video, and when you're ready, I'll look forward to you joining me in part two.