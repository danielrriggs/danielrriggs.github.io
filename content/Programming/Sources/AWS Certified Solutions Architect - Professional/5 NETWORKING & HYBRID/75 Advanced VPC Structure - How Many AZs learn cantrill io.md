 Welcome back, and in this lesson series I want to cover a topic which I find features in more and more exam questions, and that's how to design the structure of a VPC. In this lesson specifically, I want to focus on how many availability zones to use in a design which focuses on high availability. For a given scenario, you should be able to work out the minimum number of availability zones required to deliver on a specific set of requirements, as well as know how to optimise the cost while still delivering on those requirements. So let's jump in and get started. Let's look at an example, and we'll be using the example of Categram Enterprise, which is an on-premises version of the well-known application Categram, only the Enterprise... is used for sensitive cat pictures, or when you need enhanced SLAs. Now the application is currently running within one availability zone, and that's US East 1A. To support the load that the business requires, the application uses six EC2 instances, and for now, no autoscaling group. So all of these instances are within this single availability zone. Now I hope by now that you understand that in this course, in the current form, the solution isn't highly available, nor is it fault-tolerant. The application requires six instances to operate reliably based on normal load, and if US East 1A fails, then all six instances in that availability zone also fail, and access to the application is lost. Now obviously nobody's ever going to use a single availability zone for anything which is production or business critical, But I wanted to illustrate how, in the exam, you need to follow a process to determine the minimum number of availability zones and instances to meet a certain requirement. So given that this is a critical application and we do require high availability, what can we do to improve this application? Well, we could use another availability zone. Now we have the solution running in both US East 1A and US East 1B. We've got three instances running in each, so the same six instances as we had before, only now we've added availability zone 1B. Now keep in mind that the original requirement is that the application requires six instances for normal operations. Now we still have those six instances, but now they're split between two applications. So the question is, does this help us? Does it add any extra resilience? Do we have any benefit from running the application in two availability zones versus one? Well, not really, and this is a problem that most students fail to understand for the exam. This application requires a minimum of six instances, and we still have six instances. If one availability zone fails, we drop from six. So we have six instances to three instances, and this is below the minimum required. Now in the previous example, if we lost an availability zone, we'd lose all six instances. And it's easy to assume in this case, when we have an availability zone failure, because we still have three instances running, that's better than zero. But if the application requires an absolute minimum of six to cope with the business load, then running on three may cause some load-based failure. Which means the application will fail anyway. So it's not always the case that increasing the number of availability zones improves the availability of the application. In this case, because we've gone below the minimum, it will cause disruption to our customers. And so this doesn't improve the situation in terms of high availability. Now to fix this, we could over-provision our instances. Instead of using three instances per availability zone, we could provision six instances in each of the two availability zones. Now this would certainly work. It means that the availability zone on the left could fail, which would remove six instances. We'd go down from 12 to 6. We'd still have the fully functional six instances available in US East 1B. The price of resilience, or high availability in this case, is six extra servers. And since our infrastructure already uses servers, and since our infrastructure already uses servers, and since our infrastructure already uses servers, that represents 100% extra cost. We're over-provisioning our infrastructure by 100%. Now this is the least efficient way of handling a high availability requirement. 100% over-provisioning while it works is not cost effective. In the exam, you'll face questions like this. And this answer is the one that you will often pick if you don't know how to design efficient, highly available solutions. If a question with this answer is not efficient, if a question with this scenario asks you for a highly available solution, then technically this is a correct answer. It is highly available. But it probably won't be the most cost effective highly available solution. And that tends to be what the exam is looking for. For a given set of answers, you'll often need to pick the most cost effective answer, which meets the high availability requirements. Now a much more efficient way to provide high availability for this particular solution, is to use even more availability zones. Now let's explore this visually. And remember, what we started with was an application running in one availability zone, with six EC2 instances. Now as before, we can start by adding another availability zone, US East 1B, and then we can distribute the instances that we already have across these two availability zones. But this still means that the application is not going to be able to run in one availability zone. So we can only add one more EC2 instance. So this is the only way to do this. So we can add another EC2 instance, but we can also add another EC2 instance. So the application can't tolerate an availability zone failure without going below the minimum number of EC2 instances required to support the base load. So we could add yet another availability zone, US East 1C. And into this availability zone, we add three more EC2 instances. So now we have a total of nine EC2 instances. So now let's look at what happens if an availability zone fails. Well when this occurs, we still have two EC2 instances. We have two fully functional availability zones, each with three instances. This represents a total of six running instances, which is capable of delivering the base load of the application. So with this architecture, our application can still tolerate the failure of one whole availability zone, and still meet the minimum requirements of the application. But using this architecture, we've only used an extra three EC2 instances, which represent the minimum requirements of the application. So that means we can now use the additional availability zones. So with this architecture, we've only used one EC2 instance, which represents 50% over-provisioning. Remember the previous design which used two availability zones required six instances in each, so this had a hundred percent over-provisioning. So by utilizing additional availability zones, it becomes cheaper to tolerate the failure of any one availability zone. Now there is a trick to designing applications which need to tolerate the failure of one or more availability zones. Let's say we have another EC2 instance, which requires two EC2 instances. other application, in this case PetTube. What we want to design is the best way to achieve high availability for this application. So the first thing we need to do is to think about how much resilience our application requires, how many availability zones can fail and we would still require our application to continue to operate. Now in the exam this tends to be one, so most exam questions will require you to design a solution which can tolerate the failure of one availability zone, but in the real world it can be one or more availability zones and I refer to these as the buffer AZ or buffer AZs. Now the next step of this is you need to determine what region you're deploying your application into and you need to calculate the total number of availability zones in that region and subtract the buffer A. availability zones and this gives you the nominal or normal availability zones, the number of availability zones which you can use for normal operations while leaving the required number of buffer availability zones available. Now sometimes this initial calculation can influence which region you pick because some regions have more availability zones than others and so it's cheaper to use the regions which have more availability zones to implement high availability. Now next you need to calculate how many instances your application requires for the minimal operational load and this is your nominal instances. In this example let's say that our PetTube application requires five running EC2 instances at any point in time and let's say we're deploying it into a region with six availability zones and we need it to survive the failure of one. So we're going to use the region with six availability zones and we need it to survive the failure of one. of them. Well the calculation that we need to do to achieve the most efficient high availability is to take the number of nominal instances, divide this by the number of nominal availability zones and that gives us the optimal number of instances per availability zone. So in this example we're in a region with six availability zones and we need to withstand the failure of one. So we have five nominal availability zones to use for our application. Now the minimum number of instances, the nominal instances that our application needs is five. So five divided by five is one and this means that each availability zone will have one instance and this represents the cheapest way of delivering high availability for our application because we only need to over provision one instance with this architecture and this represents only 20% over provisioning. We'd use six instances rather than five. So as a general rule whenever you're considering the number of availability zones for a question always default to the highest number possible because this represents the cheapest way of delivering high availability. But in exam questions you might often be challenged to provide the minimum number of availability zones to reach a given requirement. So pay attention in the exam whether it's asking you for the minimum number of availability zones or whether it's asking you for the most cost. effective solution to deliver on those high availability requirements. In both cases you should be able to use the information in this lesson to answer the minimum number required and the most cost effective required. Now this is the end of everything that I wanted to cover in this lesson. In the following lesson I'm going to be talking about how you can calculate the minimum number of subnets required to deliver on a certain scenario. So I'll be talking about how you can design a solution. And how you can use the minimum number of subnets required to deliver on a certain scenario. And how you know when you need multiple tiers for an application versus when you can deliver it from a single subnet or a single subnet per availability zone. But at this point go ahead complete this video and when you're ready I'll look forward to you joining me in the next.