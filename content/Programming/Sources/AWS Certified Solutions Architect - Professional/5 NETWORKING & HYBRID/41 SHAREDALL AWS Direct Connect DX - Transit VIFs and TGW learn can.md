 Welcome back, and in this lesson I want to start bringing together some different topics. We've talked so far about private VIFs, which allow connectivity to a VPC over a direct connect, but only in the same region. We've extended this talking about how DX Gateway and private VIFs can provide access to VPCs globally. And we've covered Transit Gateway, which is a hub-and-spoke network device which can connect things such as VPCs and VPNs in a hub-and-spoke architecture. In this lesson I want to continue this and cover how Transit Gateways can be connected to DX Gateways to provide connectivity through to direct connects. Now we've got a lot to cover, so let's jump in and get started. As a reminder, this is a typical direct connect gateway architecture. Remember, DX Gateways, logically enough, are used with direct connect. So you connect to them using a private VIF running over a direct connect. So rather than the private VIF connecting directly to a virtual private gateway, it uses the DX Gateway. The DX Gateway is a global gateway object. Once you're connected to it using a private VIF, then it can be associated with VPCs in different AWS regions. So you can connect to it using a private VIF, then it can be associated with VPCs in different AWS regions. Using the single private VIF. The important limitation to be aware of for DX Gateway is that while the on-premises networks can access all of the VPCs in this example, and the VPCs can all access the on-premises network, what isn't possible is connectivity between the VPCs which are associated with the DX Gateway. And that's really important to understand. A DX Gateway doesn't route between associations of that gateway. It only routes from on-premises through to the AWS side, or from the AWS side through to the on-premises side. Now as another refresher, this is a typical Transit Gateway architecture. We have a central Transit Gateway in AP Southeast 2, and connected to this are VPC attachments, in this case two of them, and a VPN attachment. Now Transit Gateways are regional. Meaning that the AP Southeast 2 Transit Gateway can only be associated with VPCs and VPNs in that region. But it's possible to peer Transit Gateways, allowing connections to VPNs and VPCs in remote regions. An important benefit of Transit Gateways is their hub-and-spoke architecture. Meaning that anything associated with the Transit Gateway is able to communicate with other things which are also associated with that region. So it's a hub-and-spoke architecture. And this hub-and-spoke architecture also works across Transit Gateway peering attachments. Okay, so now we've done a refresher on Transit Gateways and Direct Connect Gateways. Let's look at how these can be integrated together, and that's using Transit VIFs. So this is a typical architecture where you might use Direct Connect and Transit Gateways together. Let's say that you currently use two APs. You have two AWS regions, US East 1 and AP Southeast 2. In those regions you have a number of VPCs and remote sites. So let's say that recently, to help you simplify your network management, you decided to deploy AWS Transit Gateways to provide hub-and-spoke networking for the VPCs and VPN-connected remote sites. This means any of the VPCs in US East 1 can communicate with you, and it means that the VPC in AP Southeast 2 and the site-to-site VPN, which also terminates into that Transit Gateway, can also communicate. You can even create a Transit Gateway peer, which means that the two regions can act as one from a networking perspective. Now you also have a business HQ, and you want to benefit from the consistent high performance which Direct Connect provides. So you provision a DX connection via a network. So you can connect to your network via the same network. This is a great way to use Direct Connect, but it also supports a local DX location. In previous lessons I've discussed how you can use a private VIF and a DX gateway to connect up to 10 VPCs per DX gateway. And these can be globally located. Now you can use multiple private VIFs, allowing this architecture to scale to many VPCs. But it also supports another type of VIF, the transit VIF, and you can only have one transit VIF per DX connection. And the transit VIF can be used via a DX gateway to connect up to three transit gateways to that direct connect. Now a really important thing to understand though is that an individual DX gateway can be used with VPCs and private VIFs or with transit gateways and transit VIFs, not both. Really, really remember that one for the exam. I promise it will come in handy. So just to reiterate, a single DX gateway can only be used for either transit VIFs or for private VIFs. So you can do one or the other. You can't... You can't do both. Now when connected using this type of architecture, it means that from the transit VIF side, so the on-premises side, it can communicate to any of the attachments of the transit gateways. The transit VIF creates a transit connection to the transit gateways and then anything attached to them can be accessed over the transit VIF. Now there are some important considerations that you need to be aware of. First, the DX gateways. The transit gateway doesn't route between its attachments. So in this example, we have two transit gateways attached to the middle DX gateway. Those transit gateways can't use the DX gateway to communicate between them. And that's why the peering attachment between the transit gateways is still required. If this isn't clear right now, I'll cover this in more detail in a second. Another thing to keep in mind is that each transit gateway can be attached to up to... 20 DX gateways. So you can create some pretty complex architectures. And it's also worth keeping in mind that each transit gateway supports up to 5,000 attachments. As well as 50 peering attachments. And this means that each transit gateway can be peered to 50 other transit gateways, which themselves can have thousands of attachments. And since with the DX gateway we can connect to three transit gateways... Well, that's a lot of VPCs over a single Direct Connect. Now let's focus for a couple of minutes on the routing limitations of a DX gateway. Consider this architecture. Two business premises. Both of which using Direct Connect via two DX locations. And then one AWS region on the left. US East 1. Suppose that we have a DX gateway in the middle. Well, what this allows is that each of the business premises... Well, what this allows is that each of the business premises... Can reach the VPC. And that VPC can reach both of the business premises. However, what's not allowed using a DX gateway is this flow. DX gateway does not support routing between interfaces which are attached to the DX gateway. Only routing between a private VIF using the DX gateway and any associated virtual private gateways or vice versa is allowed. Any VPCs which are associated... With the DX gateway can't directly communicate. And the sites can also not communicate with each other. The DX gateway, just to reiterate, only allows communication from a private VIF to any of the associated virtual private gateways or vice versa. But we can work around this using transit gateways. With this example architecture, we have the same two business premises and the same AWS region. US East 1. US East 1. Only now, we add a transit gateway as well as two direct connect gateways. And then we use transit VIFs to connect through these gateways to our transit gateway. While the DX gateway isn't capable of routing between interfaces on that gateway, the transit gateway is. So we can use this to connect the two on-premises locations. It's the transit gateway which is doing the routing. Not the DX gateways. The DX gateways just create the bridge between the direct connect and the transit gateway using transit VIFs. So the flow from business premises 1 moves across the transit VIF through the DX gateway, through the transit gateway which performs the routing, and then it's sent back through the bottom DX gateway and through that transit VIF to business premises 2. Now this architecture works if you only have services in one region. Because, remember, transit gateways are regional services. But what if you have multiple regions and all of these need to be involved in a global network? Well, let's take a look. This time we have an even more complex architecture. We have two AWS regions, US East 1 and AP South East 2. Inside those regions we have one transit gateway in each, which have VPC attachments. We also have two DX gateways and we're using transit VIFs to connect through these two gateways. And to allow communications between US East 1 and AP South East 2, we would need to add a transit gateway peering attachment. But what this architecture also allows, because remember, transit gateways can route for us on behalf of our direct connect. So what this means is that the top business premises can access AP South East 2 services. And likewise, while I haven't put this in the description, I'll leave it in the description. And likewise, while I haven't put this in the description, I'll leave it in the description. And likewise, while I haven't put this in the description, I'll leave it in the description. To make sure you're paying attention to this on the diagram, it also means that the bottom premises could access the US East 1 region. And it also means that the top business premises can connect to the bottom business premises and vice versa. Transit gateways will route to and from DX gateway attachments, even across transit gateway peers. So anything connected using a DX gateway, which is attached to a transit gateway can access by default, anything that's connected to that transit gateway, or access anything which has peered to that transit gateway, which can include VPCs and VPNs or other DX gateways. And this means that you can access other sites connected with direct connect. And that's the example that's shown on screen now. Okay, so this has been a lot of theory. So I'm going to leave it here for this lesson. I'll be talking more about this in upcoming lessons. But for now, thanks for watching. Go ahead and complete this video. And when you're ready, I look forward to you joining me in the next.