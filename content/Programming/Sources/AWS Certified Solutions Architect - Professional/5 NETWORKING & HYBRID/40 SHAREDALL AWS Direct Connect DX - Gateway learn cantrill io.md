 Welcome back, and in this lesson I want to talk about the theory and architecture of Direct Connect gateways. Now these add significant capability to the private VIF functionality within Direct Connect. As a networking architect or engineer, you need to understand this and be able to use it within AWS. So let's jump in and get started. Now by this point, you'll understand that Direct Connect is a regional service. It's physical, and so that should make complete sense. It's a port on a DX router connected to a customer router at a DX location, which is often extended to customer business premises or data centers. Now you can add multiple DX connections if you want to add resilience, and I cover this in a dedicated lesson elsewhere in this section of the course. Now once connected, we can use the DX connection and create VIFs. A public VIF can be used to access all public services in all AWS regions. Not just those in the local region. Remember, AWS advertise all their public prefixes over a public interface by default. Private VIFs, on the other hand, by default can only connect to VPCs in the same region if they're terminated into a virtual private gateway. And historically, this was the only way to terminate private VIFs. Now obviously, I'm going to be talking about ways around that in this lesson. But it's important to understand the original VIFs. The original and foundational limitation of private VIFs. Historically, you could only use them to connect to VPCs in the same region as the DX location that you use. The so-called local region. Now let's step through how a public VIF works as a super quick refresher. We've got the Animals for Life premises on the left, a direct connect location in the middle, and a DX connection running between them. On the right, three AWS regions, US East 1 at the top. In the middle, we've got US West 1. And then right at the very bottom, we have AP South East 2. Now the Animals for Life organization uses the same router for the DX connection and its normal access to the public internet. And that's handled by a default IP version 4 and IP version 6 route, which points at the organization's internet service provider. Routing outside AWS works in a similar way to inside AWS. So generally, the more specific a route is, so the longer the prefix, the more priority that it has. So the default route has the lowest priority, but if it's the only one, it will be used for everything, including, without any other configuration, connections between the Animals for Life business premises and the public space AWS endpoints. Now that we have the direct connect in place, we can create a public VIF over the DX connection. The effect that this has is that each of the regions starts advertising the IP ranges for its public services over BGP. The public VIF, remember, is a VLAN and BGP session. And so the router on the Animals for Life business premises will hear and integrate these BGP advertisements onto its routing table. Now these routes, because they're more specific than the default route for the internet, will be used preferentially versus the default route for AWS networks. When accessing the public AWS service, the internet won't be used. Instead, the connection will go over the public VIF, giving all of the performance benefits that we've previously talked about. So that's public VIFs, but that's not how private VIFs work by default. Private VIFs can only, by default, when using virtual private gateways, connect into private resources in the same region. To move outside of the region, we need to use direct connect gateways. So let's take a look at the architecture with and without direct connect gateway. As a reminder, this is how private VIFs are architected when not using DX gateway. So each DX location is isolated. Connections into a DX location can be used with private VIFs terminated into virtual private gateways, attached to VPCs in that same region only. In this example, before we involve DX gateways, we can't connect a private VIF in the US East 1 region to a virtual private gateway attached to a VPC in the AP Southeast 2 region. In addition, we need a single private VIF for every VPC that we want to establish connectivity to. Now, this might not seem like a limitation with two VPCs, but what about 20 or 100? What if those 20 or 100 are in two, five or 10 different AWS regions? Well, without DX gateway, the architecture of private VIFs running in AWS presents significant challenges. So let's look at how direct connect gateway helps us implement more efficient and capable network architectures. Well, first, the direct connect gateway is a global device. If you create it in any public AWS region, then it will be accessible from any AWS public region. The way that you integrate with a DX gateway on the on-premises side is to create a private VIF and associate this with the direct connect gateway instead of a virtual private gateway. This integrates your on-premises router with the DX gateway. Then, on the AWS side, you create VGW associations from any VPC in any AWS region. This connects those VPCs to the gateway and onward using the associated private VIF into your on-premises network. Now, DX gateways allow all the VPCs connected to it to route through to the on-premises environment and vice versa. But what they don't allow is VPCs connected to the gateway to communicate with each other. The gateway routes only between the VPCs and the on-premises environment or vice versa. Now, there are ways around this which I'll cover later in the lesson, but when directly using a DX gateway, no inter-VPC routing is allowed. Now, you can have 10 virtual private gateway attachments per DX gateway. So, 10 VPCs can be connected using a single DX gateway. And because one DX connection can use up to 50 private VIFs, each of which supports one DX gateway, and because one DX gateway supports 10 VPCs, you can connect up to 500 VPCs using a single DX connection as well as DX gateways. Now, let's look at this visually so that you can easily see how all of the different components fit together. We start with a typical architecture, two AWS regions on the left, US East 1 and AP South East 2. We have a DX location in the middle and let's assume this also is in US East 1, and then find a connection to the other two. So, let's say we have a connection to the other two. And finally, a business premises on the right. And again, let's assume that this is in US East 1. First, with this architecture, we create a direct connect gateway. And this is a global gateway object. And there's nothing to stop us creating multiple. Next, we create a private VIF. Only this time, instead of terminating directly on a virtual private gateway, which is attached to a VPC in the same region, it terminates into the direct connect gateway. And just like we created a direct connection to the other two, we create a direct connection to the other two. And like we created a direct connection to the other two, we create a direct connection to the other two. But now that we've created multiple gateways, we can also create multiple private VIFs and have one terminating into each direct connect gateway. Now, direct connect gateways don't have a cost. You do have cost for data transit, but this is the case for all AWS products. There's no specific extra cost for the gateway itself. With DX gateways, you create associations. And with this architecture, I'm going to associate it with several virtual private gateways attached to several VPCs in different AWS regions. At this point, for those VPCs which I've highlighted, we're using one private VIF. One private VIF into one DX gateway. And that means the DX gateway and our customer DX router will begin exchanging routes. And these routes will be for the VPC siders and the on-premises networks. It means that all associated VPCs can communicate with the on-premises networks, and the on-premises networks can communicate with the VPCs. What it doesn't allow, though, is direct communications between the VPCs which are connected to the direct connect gateway. The DX gateway cannot route traffic from VPC to VPC, only from VPC to customer router via the private VIF, or from the customer gateway through to an associated VPC. Now, there are ways around this. You can use the customer DX router to actually route traffic back towards the DX gateway, allowing VPCs to go via the customer side and back again. But the DX gateway itself can only route from the VPC through to the customer side, or from the customer side to the VPCs. Now, each DX connection can have up to 50 private VIFs, and each VIF can connect to one DX gateway, meaning we can have a total of 50 DX gateways. And each DX gateway can be associated with 10 VPCs, meaning a single DX connection using DX gateways can connect up to 500 VPCs globally. Now, as you'll learn when I talk about transit gateway integration in a different lesson, this can be extended significantly by using another feature of direct connect gateways and transit gateways, but I'll be talking about that in a separate lesson. Let's take a look at one more thing before we finish this lesson, and that's multi-account DX gateways. DX gateways. DX gateways. If we have this architecture, so a business premises on the right, connected via a DX location in the middle to an AWS account containing a DX gateway on the left, which is a shared services account, then other accounts which want to connect, for example, this prod account, could create what's known as an association proposal. And this association proposal would need to be approved or otherwise by the admin of the shared services account. by the admin of the shared services account. If approved, then the appropriate VPC in the prod account is accessible using the private VIF connected to the business premises. The shared services account owns and controls the routing as it owns the DX gateway. Now, DX gateways are really flexible products, and I'll show you additional functionality in an upcoming lesson where I talk about how it can allow integration with transit gateway, another feature-rich product available within AWS. For now, though, that's everything that I wanted to cover, so go ahead and complete the video, and when you're ready, I'll look forward to you joining me in the next.