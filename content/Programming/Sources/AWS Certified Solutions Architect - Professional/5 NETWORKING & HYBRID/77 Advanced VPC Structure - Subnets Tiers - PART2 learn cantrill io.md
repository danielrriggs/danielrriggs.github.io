 Welcome back, this is part two of this lesson. We're going to continue immediately from the end of part one, so let's get started. But at this point I want to focus on some reasons why you might want multiple application tiers. You're going to face some exam questions which talk about a scenario and ask you to select the minimum number of subnets required to implement the scenario. And to do that you need to know the minimum number of availability zones required and the minimum number of tiers. So let's investigate that next. Right now all we know is that this is the Categram Enterprise application and that it's running on EC2 instances. So let's assume for this example that these instances have been allocated with public IP addresses in addition to the private IPs that all resources which run inside a VP2 are allocated. If the Categram application is going to be publicly accessible, then these public IP addresses will be used by anyone to access that application, including bad actors who could be looking to exploit vulnerabilities in that application. Now one of the common reasons that solutions architects begin thinking about using additional application tiers is when databases are involved in applications. Now assume... Assuming that the Categram application does use database instances, then putting them in the public subnets along with the application instances puts them at risk, right? Any resources placed into any AWS subnets are given private addressing. And it's logical to assume that given that these are public subnets, they'd also be given public addressing. Now if this were true, it would put these database instances at risk. And that would be a reason... It would be a reason to consider adding another tier to our infrastructure. But there are two things which AWS provide which mitigate this risk. First, you can just not give public IP addresses to the database resources. Public subnets can be configured to allocate public IPv4 addresses by default, or you can have it set to not allocate by default and then explicitly allocate them to appropriate... ...to appropriate... ...to appropriate resources on a per-resource basis. In either case, you can always explicitly decide to not allocate public addressing to resources, even if they're located in a public subnet. So this requirement alone is not a reason to have multiple tiers for your application. You could decide to put database instances into a public subnet as displayed on screen now. Just choose not to allocate them with... ...to allocate them with public addressing, and this would mean that no public entity coming in from the public internet could access these database resources. Now AWS also provide security groups which can be allocated onto network interfaces or resources. And these provide the ability to control security on a much more granular level. Security groups can't explicitly deny anything, but in this example, the security groups which are attached to the database... ...instances can be configured to only allow connectivity from the web servers and nowhere else. And this would mean that any bad actors can't directly access these database instances from the public internet, even if they had public addressing. So in this example, we've got two levels of protection. First, we don't allocate the database instances any public addressing, which prevents network connectivity. And we can also not explicitly allow that access using security groups. So that's two really effective ways of securing things inside public subnets. So this alone is not enough reason to justify a separate application tier. So what is? Well, you might just be given it as a requirement. Let's say in this example that you are simply told that the database instances cannot share a subnet with any other... ...resource types. This means that you need to separate the application and database tiers. And that is one valid reason to create another tier for your infrastructure. Now, you might get asked this in a real world situation for security reasons, or it might be a requirement on an exam question. If you know that you can't have database instances sharing subnets with any other infrastructure, then logically the databases need their own subnets. And this is a... ...separate tier. So now we have two tiers for this application. We've got the application tier at the top, running the application or the web servers, and the database tier at the bottom. And we need this because of the directive that we were given. We now need six subnets. One for each tier, so application and database, so two. And then those duplicated across three availability zones, which we need for high availability reasons. So this represents... ...a total of six subnets that are required. It means that the database instances only receive private addressing because the database subnets themselves are private. And this means that any communications between the application and database tiers use private IP addressing. So it's much more secure. And it also means that we can use individual network access control lists, or NACLs, around each subnet. And this allows us to be very granular with the application security architecture. And this is another reason why you might want to create additional tiers of your infrastructure. If you want traffic to flow between application instances and database instances, and have the opportunity to filter that using a network ACL, you need those things to be in separate subnets. So keep that in mind. Separate subnets means separate tiers. Now there's another reason to create additional subnets or tiers, and that's route tables. Let's simplify the on-screen architecture to make it easier to understand. Route tables, as you know by now, are associated with subnets. So in this example, on the left we have a single subnet containing both the application server and the database server. On the right we have the same application, but this time it's split between two subnets. Now both these subnets could use the same route table, but they could also use a different one, as with this example. Route tables matter when you want to control the destination for data flows within an application within AWS or wider hybrid networks. Let's say in this example that we had the Animals for Life business premises and the public internet. The single subnet is the one that controls the data flow. The single subnet on the top left has a single route table. And route tables are associated with a subnet and thus everything within that subnet. Now this means that from a routing perspective, logically, since the subnet has the application inside it, if that application had users on the public internet, it would have a route to and from the public internet. But it also means that same route applies to both the application and the network. If you need a route to the database server from the on-premises location, then that same route also applies to the application server. Now you can be granular with routes to a certain extent, but as a general rule, you have to assume that whatever you configure for a subnet applies to everything in that subnet. And if you want to have different routes for different services, then you have to have a route to the application server. And if you want to have different routes for different services, then you have to have a route to the application server. Then you need to split your subnets and use multiple route tables. In this example, one route to and from the internet for the app server and separate routing to and from the corporate network for the database server. The same logic would apply for different types of AWS gateways. You can't have NAT gateways in the same subnets as the private instances that they support because the NAT gateway in order to run the application, in order to have internet access needs a default route pointing at the internet gateway. But the private IPv4 instances, they need to have a default route pointing at the NAT gateway. And you can't have two different default IPv4 routes on the same route table. You can't have two different route tables on the same subnet. And this means that in order to support NAT gateways or NAT instances, you need to separate the gateway into one route table. You need to separate the gateway into one subnet and the instances which use it into another. And different subnets, assuming you want high availability running in different availability zones, means different tiers. This is another very common requirement for additional subnets within a VPC. One of the most common reasons for having different tiers or multiple subnets is to support different routing decisions. So keep that in mind for the exam. Okay, so this has been a lot of visual architecture. And I want to summarize it with a few bullet points. How do we calculate the number of subnets that a particular application requirement has? And this is important for the exam. First, and this is the only time I'm ever going to write this down, is ignore high availability to start with. We need to know how many subnets the application requires without considering high availability. So the starting point, how many subnets does your application require? And what does your application need? Now keep these key decision points in mind. You don't need multiple subnets to control public and private addressing and security. All of these can be mixed and matched within one subnet. The only exception to this is if you want to filter traffic using a network ACL, then you need the two different things communicating to be in different subnets. But in general, you can mix and match public and private addressing, and you can also use the same network ACL to control public and private addressing. So if you want to perform different routing decisions, based on the type of service running in a subnet, then you'll need multiple subnets. One really common trick answer in exams is introducing load balancers. Now you can get internet facing or internal load balancers. And it's often confusing whether a public load balancer needs public instances, in order to work, and it doesn't. An internet facing or public load balancer simply means that the nodes for that load balancer are running in a subnet and they've been allocated public IP addressing. An internet facing or public load balancer can still communicate with private instances. It doesn't matter whether those instances only have private addressing, or whether they have private and public, that doesn't influence whether an internet facing load balancer can connect to them. The only restriction is that an internet facing load balancer needs to run from public subnets. So keep that in mind. Now at this point, once you've determined the number of subnets that your application requires, do you need multiple subnets for routing, or do you need to utilise network ACLs, or do you have some other justifiable reason to require multiple subnets? Well at that point, you can take that number, and multiply it by the number of availability zones that you need to deliver the application's high availability requirements, and that will give you the total number of subnets needed for that application. And you should consider this a minimum. You can always increase the number of subnets by adding availability zones, or additional subnets maybe for address space issues, but the exam tends to test you on your ability to understand the minimum number of subnets required for a given solution. So make sure you're comfortable with all of the key points that I've talked about in this lesson. Focus on the two key facts that you can have both private and public instances within public subnets. So a public subnet is capable of having both public and private instances, whereas a private subnet can only do private instances. So keep that in mind. You don't need to have separate subnets for a load balancer. A load balancer can inhabit public subnets in addition to the instances, so that can be one tier. Some of those instances can also not be allocated public addressing, so essentially private instances, and that solution can still be delivered with one subnet per availability zone, so one tier. Generally, the only reason to have multiple tiers is if you need different routing, if you need certain security, or if the real world requirements or the exam question dictate that a specific set of services can't be located in the same subnets as anything else. And the example I gave is database services that need to be in their own subnets. Now at this point, that's everything that I wanted to cover in this lesson. It's been a lot of architectural theory, but it is really important for the exam. Now I've covered everything that you need to know in this lesson, so go ahead, complete this lesson, and then when you're ready, I'll look forward to you joining me in the next.