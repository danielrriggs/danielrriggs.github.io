 Welcome back, and in this lesson I want to cover the theory of AWS Direct Connect Private VIFs. I've covered these before at other levels of study, but here I need to go into much more depth. Now we have a lot to cover, so let's jump in and get started. Private VIFs are created on a Direct Connect connection. They're used to access one AWS VPC, specifically the resources within one AWS VPC using private IP addresses. So if you have EC2 instances in a VPC, and you're trying to access the instance via its private IPv4 address, a private VIF will work. If you're trying to access the same EC2 instance using a public IPv4 or Elastic IP, then private VIFs cannot work. It's only private IPs within a VPC. Now I have an asterisk next to the one VPC, because you can work around this limitation by using Transit Gateways and Direct Connect Gateways. But for now, associate private VIFs with accessing one VPC. Private VIFs are generally associated with a virtual private gateway, which is attached to one VPC only. And this has to be in the same... region as the DX location that your connection terminates in. This is critical to remember. A private VIF, by default, attaches to a VPC via a virtual private gateway in the same region only. Now elsewhere in this section, I'll show you how to work around this, but for now, associate a private VIF with a connection to a VPC in the same region. So one private VIF, one virtual private gateway, and one VPC. That's the architecture of how these things are associated together. And there are ways around this, as I've mentioned, but more on this later in this section of the course. Another important thing about private VIFs is that there is no encryption. If you have a private IP on your corporate network, and a private IP in a VPC, and these are communicating using a non-encrypted protocol, then the data will transit in plain text form between the two VPCs. Direct Connect isn't adding encryption, and neither is the private VIF. Now again, there are ways around this, but start with the default assumption that there is no encryption added by Direct Connect. Now what's more, while applications can add encryption, such as HTTPS, there are often badly behaved applications which assume that private addressing means an internal secure network. And so often, these assume that the network is secure. But they don't need to add any additional encryption. Thankfully, this is changing. And these days, most applications offer default or easy to add encrypted application layer protocol options. And an example of this is HTTPS. With private VIFs, you can use normal or jumbo frames, meaning an MTU of 9001, but you need to make sure that everything in the communications chain supports it, including any EC2 instances that you're communicating with. A private VIF which terminates into a virtual private gateway can also use route propagation. This allows routes to automatically be added onto route tables, which are learned from the remote side of the VIF. Now it's a useful feature, but it means you need to terminate your private VIF into a virtual private gateway. And as you'll learn elsewhere in the course, there are other ways that you can terminate a private gateway. But you can terminate a private VIF, and these don't offer the same type of route propagation. Creating a private VIF is easy enough, assuming that you already have the direct connect connection configured and ready to go. First, you pick the connection that the VIF will run on. You choose if the VIF will terminate at a virtual private gateway or a direct connect gateway, and I'll be covering the latter elsewhere in this section of the course. You'll need to choose who owns the internet. You'll need to choose the VIF's interface, either this account or another AWS account. So if you manage a multi-account architecture, VIFs can be created in other accounts and attached to VPCs in those accounts. And if you do this, the other account will logically need to accept it. You'll have to choose a VLAN ID, this is using 802.1Q, and this VLAN ID needs to match the configuration at the customer side. You'll need to enter the BGP Autonomous System, known as ASN, for the BGP session between AWS and your customer equipment. And for a private VIF, this can be a public ASN that you own, or a private ASN, in which case you'll need to use the 64,512 to 65,535 range. You can also either choose IP addresses for the AWS side and customer side peers, or have AWS generate them. AWS will advertise to you the VPC side range and the BGP peer IP addresses, which will be using slash 30 prefixes. You can either advertise a default prefix, so 0.0.0.0 slash 0, or specific corporate prefixes for your on-premises networks. Now an important thing to remember for the exam is that there is a match and there is a maximum of 100 prefixes which can be advertised using a private VIF. If you attempt to do more, the interface will move into an idle state and won't work. Now this is a hard limit. You cannot change it via a support request. It is different for private VIFs and public VIFs, and if you try to advertise more, the interface will no longer be functional. So remember this one for the exam, because it tends to form part of fault finding style questions. Now that's everything for the high level creation process, and once you've created the VIF, you can download a configuration file to speed up the process of configuring your customer equipment. Let's look at how this looks visually. Let's say that we have AWS on the left. I've shown this with two regions, US East 1 at the top and AP South East 2 at the bottom. Each of them has VPCs, so private networks we want to connect to from our business locations at the right. One in the US at the top and one in Australia at the bottom. So let's start by focusing on US East 1. In the middle, we have an AWS DX location with a DX router and a customer router, together with a cross connect between the two. And let's assume that this has been extended through to our customer premises. Over this cross connect, accessing VPCs and VPCs, in the same region, is easy. We create private VIFs. Each of them terminates into a virtual private gateway, which is attached to a VPC. When we do that, BGP advertises prefixes for the VPC CIDR and any peer IPs across to the customer side infrastructure and vice versa. What we can't do though, is connect using a private VIF from US East 1 into the AP South East 2 region. Private VIFs in this form are same region only, and this doesn't scale well with many regions. Now I'm going to cover how direct connect gateway changes this in an upcoming lesson. For now though, this is the limitation of private VIFs in their normal form. Connecting to the AP South East 2 region would have to be done using a DX location also in that same AP South East 2 region. Once again, this does not scale, but you have to understand this fundamental limitation before I cover direct connect gateways and transit gateways in depth elsewhere in this section of the course. So this is the architecture visually of how you might use private VIFs. Before we finish up with this lesson, let's just move on and cover a few final key learning objectives. First, you use private VIFs to access private AWS services. So VPC private IP addresses. You cannot use private VIFs to access public services or public IP version 4 IP addresses of EC2 instances within a VPC. So this is really important to understand. These type of VIFs are used to access private IP addresses within a VPC. So the usual architecture is one private VIF to one virtual private gateway to one VPC. Now, the VPC needs to be in the same region as the DX location that your connection terminates in. So a direct connect that terminates into US East 1 cannot natively access VPCs running in the AP South East 2 region. Now, the virtual private gateways I've covered elsewhere in the course is configured with an AWS ASN. Either AWS assign one or you can configure one. You also set an ASN when creating the private gateway. So you can create a private VIF and this represents the ASN that your customer premises equipment users. Now over a private VIF runs the border gateway protocol or BGP. And this can be IP version 4 or IP version 6. You're essentially creating a peering relationship running across that VIF. And you can create an IP version 4 one or an IP version 4 and IP version 6 peers. These are separate things. And need to be configured separately. So you configure your ASN on the VIF. AWS's ASN is configured on the virtual private gateway. That's an important distinction to understand. Now on a private VIF you can either configure your own publicly owned ASN. Or you set a private ASN in the 64512 to 65535 range. That's really important to understand. For a private VIF. It's either a public ASN or a private ASN in that range. Now unfortunately Direct Connect is one of those technologies which is really hard to demo. Unless you're actually implementing one. So I'm going to do my best to give you a really good in-depth overview of the architecture of how these things work. And hopefully you'll be in a situation within your employer where you can practically get the experience of implementing a Direct Connect. But what I've talked about in this lesson are the important points that you need to understand for the exam. At this point though that's everything I wanted to cover. So go ahead and complete this video. And when you're ready I'll look forward to you joining me in the next lesson.