 Welcome back. In this demo lesson, you're going to get the chance to implement private S3 buckets and gain some experience of how to make operating inside a private-only VPC much easier. So let's jump in and get started. So this is the architecture that you're going to be implementing in this demo lesson. It's a private-only VPC. There are two subnets, one in each availability zone, an EC2 instance, which occupies one of the subnets, an S3 gateway endpoint, which is used to allow this private VPC to access a limited number of S3 buckets, and then there are also three S3 buckets, private cats, public cats, and dogpicks. Now, to get started, you'll need to apply some infrastructure to your AWS account. Move across to your AWS console. Make sure you're logged in to the IAM administrator. You'll need to be a main user of the general AWS account, so this is the management account of the organization. And as always, you'll need to make sure you've got the Northern Virginia region selected. Now, once that's done, attached to this lesson is a one-click deployment. So go ahead and click that link. That's going to take you to the quick create stack screen. Everything should be pre-populated. You'll just need to scroll all the way down to the bottom, check this box, and then click on create stack. Now, that's going to take you to the create stack screen. That will begin the creation process of the infrastructure required for this lesson. We'll need this stack to be in a create complete state before we continue, so go ahead and pause the video, wait for this process to complete, and then you can continue. Okay, so now that's in the create complete status. We've got this private VPC architecture implemented inside our AWS account. So as a reminder, this is the architecture that's being implemented. We've got those three S3 buckets. The private VPC, the private instance, and the gateway endpoint. What we don't have at this initial point is the endpoint policy, which restricts what the gateway endpoint can do, nor have we implemented any bucket policies. Those are the tasks that we'll be performing together throughout this demo lesson. So the first thing is just to verify that everything's worked correctly. Just go ahead and move to the S3 console. The CloudFormation template should have created three S3 buckets. There will be some randomness because it's been created using CloudFormation, but in this middle section, you should have dog pics, private cats, and public cats. So first go into the dog pics folder and just click on the dog.jpg object and click on open. You might have a pop-up blocker, so just acknowledge that to open up this object. And there we go. We've got a good doggo resting under a tree. Next just open up public cats, and you should see three objects. Smokey one, two, and three. So just click on one of those and click on open. And there we go. What a good catto. Interesting fact, Smokey only has three legs, so of course he has superpowers. He is actually a superhero. And then lastly go inside the private cats bucket. Just select one of those objects and click on open. And there we go. We've got a cat picture. Now there are three cat objects inside this bucket, so just verify the second one. Again, another cat. And then lastly cat three. Just click on that and click on open. And again we should have another really handsome cat. There we go. So everything is working as it should be. Now at this point, we're going to move to the EC2 console. So click on the services drop down and just select EC2 at the top. We're going to connect into the private instance using session manager. So click on running instances. Right click on the private instance. Select connect. Select the middle option, which is session manager. And then click on connect. Now if you aren't able to connect initially, then you might just need to leave it a few minutes. So go back to the main running instances screen. Just wait a few minutes and then try to connect again. If it still doesn't work, what you can try is to right click on the instance and reboot the instance. Then leave it a few more minutes and try again. Eventually you should be able to connect to this instance using session manager. And even though this instance has no public internet connectivity, inside this VPC is a systems manager VPC endpoint that allows us to use the session manager product to connect into this private instance. So that's the reason that we can connect into this instance which doesn't have any public addressing. See there, there's no public connectivity whatsoever to this instance. It's completely private. Now because the CloudFormation template created a gateway endpoint for any of these instances, we can list all of the S3 buckets. So we're interacting with S3 using the gateway endpoint. So we can run AWS S3 LS and press enter. And we'll get a list of all of the S3 buckets inside this account. So you should also see at least three out of these four. Don't be concerned if the last part is random and different. We'll just have to do a little bit of testing. And then we'll see if we can get the S3 bucket to connect. So because we've been supporting Windows 365 the phrase model for this step tell us if the stack is allacre or not. And if it does, that means that it's custom. Now let us take a look inside our own webを So. copy in any of these S3 buckets and just paste that in and you'll get a listing of all of the objects inside that particular S3 bucket. So this is dogpix. Let's follow the same process for the other two just to verify that we have full connectivity and full access. So next we'll do private cats and there we go we've got a full listing and we'll do public cats. So copy that, paste it in, press enter and you'll get a listing of all of the smoky JPEG objects. Now at the start of the course you configured the AWS command line utilities on your local machine and if you did that it means that from your local command line or terminal you should be able to run AWS space S3 space LS and then a space double hyphen profile and then a space and then the profile name. So you created for the general AWS account in my case it's I am admin hyphen general and if you do that you should also get a list of all of those S3 buckets. What you should also be able to do is to get a listing for an individual bucket so let's do that for private cats so AWS space S3 space LS space S3 colon forward slash forward slash and then select the full name for the private cats bucket. So you've got a list of all of those S3 buckets yours will be different than mine that's okay. Paste that in space double hyphen and then profile because we want to use this specific configuration profile and then a space and then I am admin hyphen general. Press enter and you should then get an output of all of the objects in the private cats bucket and that's because at this point based on this architecture we have no restrictions in place as long as you're using an identity. So if you have access to these buckets then you can access them both from the private VPC and from the public internet. Okay so this is the end of part one of this lesson. It was getting a little bit on the long side and I wanted to give you the opportunity to take a small break maybe stretch your legs or make a coffee. Now part two will continue immediately from this point so go ahead complete this video and when you're ready I'll look forward to you joining me in part two.