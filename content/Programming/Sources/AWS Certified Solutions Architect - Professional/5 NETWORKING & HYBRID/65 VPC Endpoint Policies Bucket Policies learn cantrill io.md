 Welcome back, and in this lesson, I want to continue from the associate level, extending your knowledge on VPC endpoints. If you recall, at the associate level, I introduced VPC endpoints, talked about the features they provide, and detailed how exactly they work from an architecture perspective. In summary, they provide access to AWS public space services inside a VPC which doesn't have public connectivity. So any VPC which doesn't have an internet gateway can be provided with access to AWS public space services using a VPC endpoint. In this lesson, I'm going to be talking about endpoint policies and bucket policies, and how these can be used together to support more complex private VPC architectures. Now, at the associate level, you only needed to know the basics of VPC endpoints. So in my VPC endpoint lessons, within my associate course, I detailed how you create a VPC endpoint inside a specific VPC, inside a specific region, and these endpoints provide access to an entire service in that region. For example, S3. Now, the way that this actually works is that on supported endpoints, you have a policy associated with that endpoint. It's an endpoint policy. And by default, it looks like this. Principles to perform all actions on all resources. Now, an important concept that you need to be aware of for the exam about endpoint policies is that they don't grant access to any AWS services in isolation. An endpoint policy such as this just means that the endpoint itself doesn't impose any restrictions. Identities which are accessing resources still need their own permissions to be able to interact with those resources. So an endpoint policy just controls restrictions. Now, in order for endpoint policies to be available, it needs to be a supported service, and any accesses need to be through an endpoint. So an endpoint policy only limits access to a particular service when it's being accessed through that endpoint only. That's really critical to understand. And an example of this is if you create an S3 gateway endpoint in a VPC, and if you adjust the endpoint policy on that endpoint to only allow access to one specific S3 bucket, then any identity using that endpoint to access that bucket, A, would need permissions on that bucket, and then only if that identity was accessing S3 through that endpoint would that restriction apply. So two really critical things. The endpoint policy only affects that one endpoint, and then only applies for accesses via that endpoint, and it doesn't grant any permissions on its own to any identities. Now, anatomically, the endpoint policy contains a principle. Generally, this is set to an asterisk wildcard to apply to all accesses via that endpoint, and it can also contain conditions, which is generally how you can be granular and can configure who has access to what. Now, endpoint policies are commonly used architecturally to limit what a private VPC can access. I'll be covering private-only VPCs in more detail later on in the course, but the idea is to make a VPC which has no internet connectivity at all. This means no attached internet gateway. Maybe it's connected to your on-premises environment via a direct connect or VPN, but it doesn't have any internet access. But there might be situations where, instead of having a VPN, you might need to perform updates or get other data from public services such as S3. So in that type of situation, you could use an S3 gateway endpoint and use a policy to limit access to predefined buckets. So examples of this might be updates or other sensitive corporate data. Now, let's take a look at some common architectural scenarios. Architecturally, we have AWS on the left with a private-only VPC, and the public internet on the right. The VPC is private, holding a sensitive application. So there's a subnet inside AZA with an instance inside it. The VPC has a VPC router. We've created a gateway endpoint, and the instance can then access S3 via the VPC router and via the gateway endpoint. Now, in the AWS public zone, we've got three S3 buckets. We have animal records, software upgrades, and a public network. So, if you're using a public network, you can use the VPC router to access the animal records, and software updates, and cat pics. And in this configuration, without any changes, the endpoint would allow access to all of the buckets from the private VPC. And in addition, the buckets would be accessible from the public internet, assuming that the identity accessing them had the appropriate permissions. What we can do to secure this further is to add an endpoint policy so that the gateway endpoint can only be used to access the animal records and software update buckets, and not the cat pictures. The application running on the instance can access the sensitive data and the software updates, but not the cat pictures. Now, the software updates bucket is likely to be something that we don't directly manage, and so it's likely not to have any further restrictions. And this means that the bucket can be accessed both from the private VPC via the gateway endpoint and from the public internet. Let's say that the cat pics bucket also has no further restrictions, and that the cat pictures bucket is not directly managed, and so it also can be accessed from the public internet. But because the endpoint policy doesn't explicitly allow it, this bucket is not accessible from inside the VPC. Now, the animal records bucket is a special case. This contains sensitive data, and we need to make sure that it's only accessible from within the private VPC. And we can do that by adding a bucket policy, which denies anything that the animal records bucket which isn't from the gateway endpoint. And this has the effect of blocking any public access whilst allowing access from the private VPC. Now, that's the architecture at a high level, but I want to spend a few minutes reviewing the detail of exactly how this is implemented. We have a demo coming up next where you will implement a similar architecture, but I want you to fully understand how this works before we do the demo lesson. So let's get more specific. We've got another private VPC, one subnet in one availability zone. We've got the VPC router, a gateway endpoint, and a software updates S3 bucket. And since the VPC has no internet gateway, the gateway endpoint is the only way that S3 can be accessed. Now, we can limit exactly what's accessible from this VPC by applying an endpoint policy. And this is an example of one which limits an endpoint to access the network. So let's say we're accessing Amazon Linux 2 software updates only. And this locks the endpoints to only the allowed bucket or buckets. And notice here how we're allowing the get object action, and then there are wildcards in the resource ARN. And this is perfectly acceptable. We can use wildcards in this way inside ARNs. Now, that's one side of the restriction process. This is how we can control exactly what a private VPC can access. But what about the other side of the equation? Well, let's look at that next. Let's look at how we can restrict buckets so they're only accessible via a specific VPC endpoint. So this time we have a similar architecture, an AWS environment, but this time with two different VPCs. In each of these VPCs is a gateway endpoint and a VPC router. I just didn't have space to put a VPC router in the bottom one, but there is one. Now, in this case, we're going to use a VPC router. So in both of these VPCs, the gateway endpoint is the only way that we have to access S3. So all accesses to the S3 service go via these gateway endpoints. And each of these gateway endpoints has a unique VPC endpoint identifier. So the top one is 1A, 2B, 3C, 4D. And the bottom one, 2B, 3C, 4D, 5E. The important thing is to understand each gateway endpoint has its own unique ID. Now, in this case, we also have an S3 bucket, which is storing sensitive data. And this is called A4L Animal Records. Let's assume for now that neither of the endpoints has an endpoint policy and there are no restrictions on the bucket. And this means that the bucket would be accessible from both of the VPCs and the public internet as long as the identity accessing the bucket had the right ID. So we can use this as a bucket policy to configure a more restrictive bucket policy and apply it to that bucket, something which adds an explicit deny. In this case, the explicit deny matches any accesses to the Animal Records bucket when the condition is true. And the condition matches if the VPC endpoint address is correct. So we can use this as a bucket policy. And we can also use this as a bucket policy to configure a more restrictive bucket policy. So we can use this as a bucket policy. The endpoint ID does not match a certain value. And that certain value is the VPC endpoint ID of the top VPC. It means that the top VPC can access the Animal Records bucket. But what it also means for the bottom VPC is that because the endpoint has a unique identifier and because that identifier does not match the one that's in the bucket policy, the bottom VPC can't access the animal records bucket policy. VPC will be denied from accessing the S3 bucket. Now there are two really common elements that you're going to find within resource policies for more complex architectures inside AWS and one of them is on screen. It's AWS colon source VPC E and this will always match the VPC endpoint identifier if an access is coming via a VPC endpoint. So you'll only use this if you want to do conditional matches when VPC endpoints are involved. Another really common element is AWS colon source IP and source IP matches external IP addresses. So if you want to control access to an S3 bucket to a specific set of trusted external IPs then you can use AWS colon source IP. So if you want to control access to an S3 bucket to a specific set of trusted external IPs then you can use AWS colon source IPs and source IPs to control access to an S3 bucket. AWS colon source IP but this only works foAr external IP addresses. This cannot be used to match internal IP addresses like the ones that are used for AWS VPCs. If you want to restrict access to private AWS networks then generally you'll use the AWS colon source VPC and that can be used to match against one or more endpoints inside one or more VPCs. With that being said though if you're looking for a demo it's time for a demo. Now that you know the theory behind endpoints, endpoint policies and bucket policies now it's time for you to implement this yourself inside AWS and so in the next lesson which is a demo you're going to get chance to do just that. So go ahead complete the video and when you're ready I'll look forward to you joining me in the demo lesson.