 Welcome to this lesson where I'm going to cover the VPC router in some additional depth. The VPC router is at the core of any networks which involve AWS and so it's essential that you understand it to a really deep technical level. So let's just jump in and get started. The VPC router, as the name suggests, is a virtual router within a VPC. It's something that you'll be aware of, but as you'll see throughout this lesson you'll have very little direct control of or visibility of. Now it's highly available across all availability zones within the region. When you create a VPC, the VPC router is created along with it and it operates in all of the availability zones in the region which the VPC runs from. There's no management overhead for this availability. You don't have to worry about provisioning in every availability zone explicitly. This process is automatic. It's also scalable and there's no performance management overhead required. It delivers whatever performance the resources within the VPC provide. At its core, the VPC router routes traffic between subnets within the VPC. Remember, subnets can be in different availability zones, so potentially in different underlying physical facilities. And the VPC router ensures that data gets from one subnet to another. It also routes from external networks into the VPC and from the VPC to external networks. External networks in this context can mean other VPCs, on-premises networks, other cloud platforms and even the public internet. Other networking gateways are required for these connections to be active, such as the Internet Gateway for Internet Connectivity, but it's the VPC router which controls that. The VPC router is the router that controls the routing within the VPC to or from these other networks. The VPC router has an interface in every subnet within the VPC. This is known as the subnet plus one address. It's the first IP address in each subnet after the network address itself. This is provided to resources in the VPC as the default gateway, via the DHCP option set associated with that VPC. Now you control how the VPC router routes traffic using route tables, which are associated with subnets in the VPC, and I'll be detailing those in a second. Before I do that, let's have a look visually at how this architecture looks at this point. We're going to start with a cut-down version of the Animals for Life VPC. This one has two subnets in each of three availability zones, and as you can see, it uses the same IP address as the other subnets. And as you can see it uses the same IP range which we're going to be using through most of the course. The VPC itself is using 10.16.0.0.16 and each subnet uses a slash 20 range within the VPC. The VPC router conceptually sits in the middle of the VPC. From a networking perspective though, each subnet has an interface for the VPC router inside that subnet. Those interfaces use the subnets as a basic interface. Each subnet has a web interface for the VPC router, which is the one we're going to be talking about in this video. So in the case of the subnet at the top right, the subnet address is 10.16.48.0, so the VPC router interface would use 10.16.48.1. Any services running inside the subnets have this plus one address as their default gateway, and so any packets which are destined for outside of that local subnet use the plus one address as the default gateway, and the VPC router controls where the data goes from that point forward. Now the decisions that the VPC router makes are controlled via route tables, and these are associated with subnets within the VPC. And I'll be talking in more detail about route tables. It's the VPC router configured with route tables together with other gateway objects which can direct traffic entering and leaving the VPC. For example, for on-premises networks, the VPC router will ensure that data from the VPC reaches the on-premises networks, generally using a network gateway object such as the transit gateway or virtual private gateway. Alternatively, using gateways. Using public network gateways, it can ensure that VPC traffic reaches services in the AWS public zone or the public internet. All of this is controlled by the creation and modification of route tables, and that's what I want to cover next. Every VPC is created with a main route table. Now this route table is going to be the default for every subnet within the VPC. Custom route tables can be created. Custom route tables are associated with subnets in the VPC, but that removes the association with the main route table. So subnets, and this is important to understand for the exam, they're associated with one route table only. And this is either the main route table, which is the default, or a custom route table that you create. So if you create a custom route table, you can associate that custom route table with any subnets within that VPC. Once you do associate it, the main route table is disassociated. If you disassociate the custom route table, the main route table is associated again with those subnets. Now in many of the deployments which I've been involved in, I've not made any changes to the main route table. The main route table in general should be left blank. Any routes that you want to create should be added to a custom route table, and these custom route tables can be explicitly associated. With VPC subnets. Now the reason that you do this, is that if you do disassociate a custom route table, then as I mentioned, by default, that subnet is automatically associated with the main route table. And from a network security perspective, you don't want this automatic association to add any unintended routes. So the main route table should generally be left completely blank in the default state as it's created when you create the VPC. Now routes... Route tables contain routes. A route table can contain many routes, and these are used to match traffic by the VPC router. Now there is a priority order, and that's that the most specific route wins. So a slash 32 route is the most specific IP version 4 route that you can use, and this matches one specific IP address. The other extreme of that is 0.0.0.0 slash 0, which is a default IP version 4 route, and this matches any IP version 4 address. And so when processing route tables, the VPC router attempts to pick the most specific route. Now if you're studying for the advanced network specialty, then there's going to be additional detail that you need to be aware of, such as what happens if there are two routes which are equally as specific, but are learned from different things. And so you need to be aware in detail of how different routes approach the VPC router. And so you need to be aware in detail of how different routes approach the VPC router. And so you need to be aware in detail of how different routes approach the VPC router. And I'm going to be going into much more detail if you're watching this as part of the advanced networking specialty course. If you're doing an associate course or a professional course, then you don't need to be aware of any additional detail. Now in addition to associating route tables with subnets, you can perform what's known as an edge association. And this is where you associate a route table with a network gateway. And again, if you're watching this as part of the advanced networking specialty course, then I'm going to be going into significantly more detail on this later in the course. If you're doing an associate or professional course, then you don't need to be aware of this level of detail. Now let's look at how route tables are presented within the console UI. So a route table looks like this. In this case, this is the main route table for this specific VPC. Now a route table is just a list of routes, and each of these is a route. So when traffic is in a route, and traffic is leaving the subnet that this route table is associated with, the VPC router reviews the IP packet. A packet has a source address and a destination address together with some data. The VPC router looks at the destination address of all of the packets leaving this subnet. And when it has those addresses, it looks at the route table and identifies all of the routes which match that destination address. And it does this by checking the destination field of every route within the route table. This determines what the route matches. Now a route can match one specific destination IP address. So a route could be a single IP with a slash 32 prefix. A route could also be a network match. So matching one or more networks. Or it could be a default route. For IP version 4, this might be 0.0.0.0.0.0 which matches all IP version 4 IPs. This is known as a default route, which is a catch-all. Now in the case where traffic leaving a VPC subnet only matches one route, well then that one route is selected. If multiple routes match, maybe there's a specific slash 32 route which matches. Maybe there's also a slash 16 network match, and maybe there's also a 0.0.0.0.0.0 default route. In this case, the prefix is used as a priority. The higher the prefix value, the more specific the route, and the higher priority the route has. So a slash 32 route is preferenced over a slash 16 or a slash 0. Once one route within a route table has been selected as the best match for traffic leaving that VPC subnet, once one route within a route table has been selected as the best match for traffic leaving that VPC subnet, the traffic is forwarded to its destination, which is a gateway inside AWS which gets the traffic through to its final destination. An alternative is that we also have the target of local, and this means the destination is in the VPC. All route tables have at least one route, the local route, and this matches the entire VPC CIDR range, and it lets the VPC router know that traffic destined for any IP address within the VPC CIDR is local, and it can be delivered directly. Now if the VPC is IPv6 enabled, it also can have IPv6 routes, and there is also going to be a local IPv6 route which matches the IPv6 CIDR for the VPC. And this is the case with this example. So the bottom route in this route table is the local route, and the local route is the IPv6 route for the IPv6 CIDR associated with this VPC. Now these local routes can never be updated. They're always present, and the local routes always take priority. They're the exception to the previous rule. Remember for the exam, route tables are associated to zero or more subnets. It is possible to create a route table and not associate it with any subnets, but a subnet has to have an associated route table. Either the main route table or a custom one that you create. A route table controls what happens to data as it leaves the subnet or subnets that that route table is associated with. Local routes are always there, uneditable, and match the VPC, IPv4 and IPv6 CIDR ranges. For anything else, higher prefix values are more specific and take priority. A default route, which I'll talk about more, in an upcoming lesson, is what happens if nothing else matches. And this is generally used to direct any unmatched traffic through to a suitable gateway. For example, the internet gateway, when you're wanting to direct any traffic that's not destined for a VPC or any other networks that you control through to the public internet or the AWS public zone. Now at this point, that's everything which I wanted to cover about the VPC router. Don't worry. There's going to be plenty more depth to come in upcoming lessons. And in these lessons, I'll be going into much more depth on specific network components within AWS. For now, go ahead and complete the video. And when you're ready, I look forward to you joining me in the next lesson.