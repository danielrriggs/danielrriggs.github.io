 Welcome back, and in this lesson I want to talk about API Gateway stages and deployments. Now I touched upon these in the methods and resources lesson, but now I want to expand a little and cover some important concepts for the exam and real-world usage. So let's just jump in and get started. When you're editing an API within API Gateway, you're actually editing settings which are not live. It's similar to how you're editing unpublished changes with Lambda. Keep this in mind because it's one of the most common mistakes with API Gateway, where people change settings and wonder why they don't apply to the live API. To make changes live, you have to deploy the current state out to a stage. Now the official definition of a stage is that it's a logical reference to a lifecycle state of your API. What this means in practice is that you take the current state of the API defined within API Gateway, and publish a pointer. You can then print in time copy of that out to a stage. Now you can create stages with whatever naming makes sense to your organization. You might create them representing environment types such as prod or dev or test. Or application versions such as version 1, version 2, and version 3. And this one's great if you're working through breaking changes because specific versions of your application can talk to specific versions of your API. And these are deployed into different stages. And you can then create a new configuration for each of those stages. Now the key to stages is that each one has its own configuration. It's entirely separate from all of the other stages. But unlike Lambda versions, they're not immutable. You can overwrite configurations stored within stages and even roll them back to previous configurations. Now this is going to be much easier to understand if we look at it visually. A typical architecture with API Gateway starts with the API configuration. Now this is going to be much easier to understand if we look at it visually. A typical architecture with API Gateway starts with the API configuration. And initially in this example, we deploy this out to our version 1 stage. And for now, treat this as our production stage. Our users access this stage via an invoke URL. Because each stage has its own configuration, the version 1 stage points at the version 1 of a Lambda function. And our users access both of these using the version 1 invoke URL. So everything lines up. The invoke URL points at a stage. And the stage points at a specific Lambda function version. But we can also deploy the API configuration to another stage, the v2 stage. And let's say that this represents the beta version of our application. Now because this is a separate stage, it has its own unique invoke URL and can point at an entirely different Lambda function. Or it can use an alternative version of the same Lambda function. You have complete flexibility whether to point it at a specific Lambda function version, an alias of a Lambda function, or you can point it at a completely different Lambda function. This part is entirely up to your development team and how you want your architecture to operate. Now we can enhance this even further by making use of a feature of API Gateway known as stage variables. For this example, let's say that we have three stages. Prod, beta, and dev. And logically each of those has its own unique invoke URL. One for prod, one for beta, and one for dev. Now the way that we can enhance this and utilize stage variables is that we define a stage variable in each one of these stages. In this case it's called env, and we set it to prod, beta, and dev, the same as each of the stage names. So the prod stage has env equals prod, the beta stage has env equals beta, and the dev stage has env equals dev. And then within the integration settings for the resources in each of these stages, we use the stage variables to define the stage variables. So we can use the stage variable to reference Lambda aliases rather than pointing at them explicitly. This means that the configuration of the integrations is fixed and static. It's the same across all three stages. But it's using a stage variable where the variable is being used to point it at a different alias depending on the stage. So the prod stage points at the prod alias, the beta stage at the beta alias, and the dev stage at the dev alias. And that's all configured using the stage variable. So we can use the stage variable to define the stage variable. That's all configured using a single string, but that string includes a stage variable. Now we can configure the aliases however we want. A typical architecture might be that the dev alias points at dollar latest, meaning the mutable, last saved, deployed version of the code within the Lambda function. Generally dollar latest would only be used in a dev stage because of its mutability. For anything else, you'd want fixed and immutable code. It's far safer for any of the stages, of the stages which interact with customers for them to use fixed versions of Lambda functions, because we know that that code will never change and we can use formal testing processes as part of a beta phase or a production rollout. So typically what we might have with this example is the production stage will point at one version of the Lambda function, say version one, and then the beta version might point at a newer version two of the Lambda function. It means that you can keep a consistent set of configuration which automatically adjusts based on the stage and leaves all of the configuration to be done at the Lambda side. So any CI-CD systems that your businesses use can make all of the configuration changes using Lambda aliases. Now you also have the option of using weighted aliases. For example, if you wanted to perform A-B testing you could direct a portion of the production stage requests towards version two of the function instead of version one. So that's the way the Lambda function works. Now there is a number of ways to do this, and I'll explain that in a bit. If you want to perform A-B testing you could direct a portion of the production stage requests towards version two of the function instead of version one. version 1, but you need to be careful if any of these changes are breaking changes that will generally prevent you using A-B testing. So that's all of the theory which is relevant for the exam and most associate level real world usage. You're going to get some practical experience with working with these over the coming lessons in this section, but from a theory and architecture perspective that's everything that you need to know. So at this point you can go ahead and complete this video and then when you're ready I'll look forward to you joining me in the next. Thank you.