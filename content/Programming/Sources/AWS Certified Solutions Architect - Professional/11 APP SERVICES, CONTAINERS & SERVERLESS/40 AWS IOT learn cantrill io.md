 Welcome back and this is going to be another quick lesson and in this lesson I want to cover AWS IoT or Internet of Things. It's something which features in the exam and so I want you to know its functionality and architecture at a high level. But it's a really feature-rich product and so this lesson will just focus on the architecture. Now let's jump in and get started. So IoT or Internet of Things is another one of those vague terms which means different things to different people. The idea is that you might have tens, hundreds, millions or even billions of small low-power devices. These might be sensors or even robotic vacuum cleaners. And all of these need to be managed, so provisioned, controlled, updated and there needs to be a way of handling the data to and from these things. AWS IoT or AWS IoT Core is a product set within AWS. It's not a specific product. It's used for managing huge numbers of IoT devices. And these can really be anything. Temperature sensors, wind sensors, water sensors, lights, even things like valve control systems. All of these need to be registered into a system to allow secure communications so that they can be managed. So provisioning, updates and control. And communication to or from these devices is likely to be over low power. Or unreliable communication links. And so AWS IoT also provides device shadow functionality. Now a device shadow is like a virtual copy of a device which you can communicate with. And I'll explain exactly how this works visually on the next screen. AWS IoT also provides rules which allow for event driven interaction with other AWS services based on what happens to IoT devices. Now it's quite common. It's going to be far easier to understand how the AWS IoT range of products work if we explain them visually using an example. So let's do that. So consider the scenario of a huge scale application called Pet Monitor 9000. It's the job of this application to monitor millions of IoT sensors in pet owners' homes globally. So we start with sensors. Now these can be water sensors, temperature sensors and lights. Now to keep this diagram simple I'm going to just display these three. But the architecture works equally as well if we have 3 million, 30 million or 300 million. It's a scalable and resilient architecture. Now next we have the IoT Gateway. And this is a service provided via AWS IoT. As I just mentioned it's scalable and it's highly resilient. And it's the endpoint which all of your IoT devices communicate with. Now remember these sensors are low power. This means communications channels aren't kept open constantly. And they're likely to be less reliable and fairly sporadic. You can't assume that the sensor is always going to be available to request data from or to make updates to. And to help with this we have the concept of device shadows. These are virtual representations of actual devices. The idea is these are a copy of the copy of the device. And a copy of the config of all of the IoT devices. You can read from them and get the data that they submitted the last time the device communicated with IoT. If a sensor communicates with IoT then its shadow is immediately updated. And you can write to the shadow and the next time the sensor communicates with the AWS IoT Gateway. That data is sent through to the sensor. Device shadows are essentially a way to avoid the unreliability associated with low power. It's essentially a proxy for a real device. Device messages are sent using JSON which is encrypted with X509 certificates. And messaging uses MQTT style topic names. So sensor forward slash temp forward slash cat bed which is the temperature of a cat bed. And sensor forward slash water forward slash cat bowl one. Which represents the water level in one specific cat bowl. Are just two examples. And these are region and account specific. So that's the device side. And IoT handles all of this communication as a service. But this can be extended. You can define IoT rules. And these match events on the messaging bus which IoT provides. And if certain criteria match then actions can be performed. Let's use the example of the Pet Monitor 9000 application. Let's say that we have sensors which monitor the water level. In millions of cat bowls globally. Each owner is able to set a minimum desired level. So if the sensor reads a water level in a bowl. Then the IoT rule can invoke a Lambda function. The Lambda function can then consult DynamoDB. Check the minimum value set by the owner. And if it's lower it can use SNS to push a notification to the pet's human. All of this just using managed services by the pet. And managed services provided by AWS. Now you could also integrate Kinesis to persist this data to S3. Or send it into other AWS services for further analysis. Now there's much more that the product can do. But I wanted to give you just a basic architectural overview. Just so that you have in your mind the types of scenarios that you would use it for. Versus those that you wouldn't. Now you could configure IoT sensors to send data directly into Kinesis. After all Kinesis is a service designed for the ingestion of large scale data. But IoT is custom designed to handle IoT devices. And it provides all of these additional features. Such as device shadows, encryption, provisioning management and IoT rules. So when you're faced with an exam question which mentions IoT devices. You should default to AWS IoT. And only move away from it if absolutely necessary. With that being said that's everything I wanted to cover in this lesson. So go ahead complete the lesson. And when you're ready I look forward to you joining me in the next.