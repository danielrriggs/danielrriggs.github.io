 Welcome back, and in this lesson I want to talk about the Simple Workflow Service, or SWF. Now this has always been an interesting name because the product is anything but simple. That aside, it's actually a legacy product at this point, with step functions being the recommended replacement for it in most scenarios. Now in this lesson I want to quickly cover it as a product and step through some key decision points between using it versus step functions. So let's jump in and get started. SWF allows you to build workflows, which are ways to coordinate activities over lots of distributed components. Think of an order flow when you use Amazon.com. There are online parts, automated parts such as checking stock, financial parts such as fraud prevention, and human parts such as picking and packing. Now SWF allows you to build these complex, automated, and human-involved processes. Now you might already be getting confused between this and step functions, and that's okay, because this is actually the predecessor to step functions. AWS created and introduced SWF years before step functions was a thing. It has the same patterns and anti-patterns as step functions, so long-running workflows. The difference is that SWF is older, it uses servers, and it's capable right now, of some slightly more complicated workflows. Your starting point in the exam and the real world at this point should be to default to step functions, and use SWF only if there is a very specific need to do so, which I'll be covering later in the lesson. Now there are a number of key parts of SWF, so key building blocks to the service, and we start with workflows, which are the basic end-to-end flow, and these are comparable to state machines, and they're all built in the same way. So, let's start with the basic step functions. A workflow is a set of activities that carry out some objective, together with the logic that coordinates the activities. For example, a workflow could receive a customer order, and take whatever actions are necessary to fulfill it. Within workflows, you have activity tasks and activity workers. Now a worker is a program which you create to perform tasks. For example, in an order flow, where there is a dispatch step, involving humans doing picking and packing, the activity worker would be a program that they interact with as part of that stage. The activity task is the thing that they do. And workflows also have deciders. So the coordination logic within a workflow is controlled by a decider. This is an application running on a unit of compute in AWS or on-premises. This is one of the key differences between SWF and step functions. The requirement for server-based decider logic within SWF means it has a footprint which you need to meet. The decider schedules activity tasks, provides input data to the activity workers, processes events that arrive while the workflow is in progress, and then eventually ends the workflow when the objective has been completed. Workflows within SWF have a one-year maximum runtime, much like step functions. So again, you might be thinking that SWF and step functions sound very much like the same product. And for many use cases, they are. You can use either product for 90% of workflow-based flows. Step functions would be lower admin overhead for those 90%, because it's serverless. But a certain percentage of workflow requirements is going to be beyond the capabilities of step functions. And for those, you will still need to use SWF. Now I want to step through how to decide between the two products next, and then look visually at the architecture of a common SWF-based workflow. Now these are the types of points that you'll need to think about in the exam if you do see any questions where you need to pick between SWF and step functions. To start with, step functions is serverless and represents a lower admin overhead, and so it should always be your default. If you see any questions which talk about the AWS flow framework, then choose SWF. So this is beyond the scope of this lesson, but this is a way of defining workflows that's only supported by SWF. If you need external signals to intervene in processes, so if other systems need to be able to input data or make signals to influence how a workflow is processed, then you need SWF, because that's supported natively. If you need to launch child flows as part of activities inside a workflow, and then have the processing return to the parent, then you also need to use SWF. If you've got bespoke or complex decision-making logic, then use SWF, because you can literally code a custom decider application to implement whatever logic you want. Step functions is somewhat limited in that regard, because it's defined using the Amazon States language, and this currently is less flexible than the custom decider logic that you can implement with SWF. If you need to integrate with Mechanical Turk, which I'll be talking about elsewhere in the course, then you should also use SWF. This is a suggested AWS architecture. For everything else, and this goes for 99% of workflow processes in AWS, you should use step functions. Over time, step functions will develop in terms of its functionality and entirely replace SWF. So SWF at this point really is a legacy product, but you do need to be aware of exactly what it does. Now let's look at a high-level example flow through SWF before we finish this lesson. So a typical workflow might look like this, and it's an online order flow. So the workflow starts when the customer submits the order and proceeds to the verify order stage, and once complete, picking and packing takes place in the shipping stage before moving to order finalization and then the end of the workflow. Each of these are activities, unique parts of the workflow, which need to be completed in order, and there's a decider controlling the entire process, orchestrating the move from one activity to the next. Now each of these activities can be automated using units of compute or have human interaction. For example, the verify order activity might be statically automated, i.e. a set of checks, or it might use machine learning. The billing activity might involve interaction with a credit card processor. The finalize activity might perform updates of stock levels and other areas of the database. And then finally, the shipping activity might involve human beings and physical movements of things within a warehouse. And that's all supported using SWF. And as I mentioned previously, activities can also run child workflows. So maybe the verify order activity includes address verification and maybe some fraud detection processes based on machine learning technology. And again, all of that is supported by SWF. Now this has been a brief lesson, but I just wanted to give you a high-level overview of exactly what the SWF product does. Focus on the details. There are a number of differences between SWF and step functions, but in 9 out of 10 questions and 90% of scenarios, you will probably be selecting step functions, because that is the more recent product and the one that's being suggested by AWS. With that being said, thanks for listening. Go ahead, complete this lesson. And when you're ready, I'll look forward to you joining me in the next.