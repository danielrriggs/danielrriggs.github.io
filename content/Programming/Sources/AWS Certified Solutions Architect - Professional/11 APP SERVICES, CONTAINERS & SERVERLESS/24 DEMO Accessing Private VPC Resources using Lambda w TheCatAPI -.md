 Welcome to this demo where you're going to get some practical experience of working with a Lambda function which runs within a VPC and you'll get the experience of extending the storage available within the Lambda runtime environment using the Elastic File System or EFS. Now this is going to be a simple demo lesson but all of the skills that you'll gain by taking this demo lesson can be used for much larger and more complex projects. Now attached to this lesson is a one-click deployment link which you'll be using in a moment and also full text instructions for every step of the demo. Now we will be referring to those full text instructions so I would recommend that you go ahead and open that link now. Now before we get started let's go ahead and start off the deployment of the VPC and Elastic File System. This is going to be done using a one-click CloudFormation deployment. And while that's deployed... Now if you just go ahead and make sure that you're logged into an AWS account with admin privileges and that you have the Northern Virginia region selected. And once you do, go ahead and use the one-click deployment link that's attached to this lesson. When you use that link it will take you to the Quick Create Stack screen. Everything should be pre-populated. The stack name should be Lambda VPC. All you'll need to do is to check the capabilities acknowledgement. And then click on Create Stack. Now this process will take anywhere up to 15 minutes and it will need to be in a create complete state before we continue. So while this is creating let's go ahead and step through the architecture that you'll be deploying. So what you'll be deploying throughout this demo lesson is a pretty interesting architecture. First, deployed by the CloudFormation template that you just clicked on is a VPC together with two public and two private. Subnets a pair in each of two availability zones. It also deploys a pair of two EC2 web servers which will communicate with the public internet using an internet gateway. The template also deploys an EFS based shared file system and a pair of NAT gateways running within the public subnets. Once the CloudFormation template has finished deploying which deploys this architecture. Then you're going to be creating a library. You'll be using a Lambda function called cat downloader which when invoked uses the NAT gateways to connect to a cat API and download some cat pictures onto the EFS based shared file system. Now you'll also be creating an event bridge schedule rule to invoke the Lambda functions automatically every two minutes to download cat pictures. And these cat pictures which will be stored on the EFS based file system will be accessible. From the EC2 instances and then from there these will be available to the users of our application. Now I'm going to use cat pictures in this demo lesson because it's an excuse to use cat pictures. But this periodically downloaded data could be anything. Any dynamically polled data which the web servers use as part of provisioning an application. You could even have Lambda functions which are invoked in an event driven way when data is available from a remote server. So invoking the Lambda function from the remote side of this data relationship. But that's beyond what we can demo in a short demo lesson. What I want you to gain practical experience of in this lesson is deploying a Lambda function into a VPC and then connecting it to EFS. So let's jump in and get started. And the first thing that we'll need to do is check on the progress of the CloudFormation deployment that you started moments ago. Now in my case this CloudFormation stack. Is still in a create in progress state. And in order to continue with the demo lesson we will need it to be in a create complete status. So go ahead and pause this video. Wait for it to move into a create complete state. And then we can continue. Okay so the CloudFormation stack is now in a create complete state. And we're good to continue. Now the first thing I want to do before we start implementing any of the infrastructure for this demo. Is just to verify a few things. So first we need to verify the stack. And then we need to verify the status of the EFS based file system. So click on services. And then in the find services box just type EFS. And click on there to continue. Once you're there click on file systems. We should only have one file system. It should be called EFS forward slash and then Lambda VPC. Just go ahead and click on it. Once that's open just go ahead and click on the network tab. Scroll down and make sure that you have two mount targets. And both of them are in an available state. And then we can go ahead and click on the network tab. And then we can go ahead and click on the network tab. Scroll down and make sure that you have two mount targets. And then click on that one mount target. Assuming you do then we're good to go from an EFS perspective. Next click on services. And move to the EC2 console. The CloudFormation template should have created two EC2 instances. So once you're at the console click on running instances. Now the CloudFormation stack will have created two EC2 instances. Web A and Web B. Just go ahead and select Web A. And then we're looking for the public IP version 4 DNS name. So go ahead and click on that. on this little symbol to copy that into your clipboard, making sure to use the icon to copy it into your clipboard, not the open address link, and then open that in a new tab. Now what you should see is a generic website, it should have a random coloured background, and it should show no cat images. What this website is doing is loading any images contained within the var www.html.img.com folder, and this folder is where the EFS file system is mounted to, so any images which are on the EFS file system will be dynamically loaded by this web application. Now that looks good, so that's everything working as it should be. Now what you're going to do in this demo lesson is create a lambda function which runs from within a VPC, but which also accesses EFS, and so it needs a number of sets of permissions to allow that. So let's click on services and quickly move across to the IAM console. I just want to quickly demonstrate how the lambda function is provided with those permissions. So click on roles, and then just scroll down, and what you should see is two roles which start with lambda VPC. One of them is lambda role, and one of them is web role. Now the lambda role is the one that I'm interested in. The trust relationship of this role is that it can be assumed by lambda.amazonaws.com, which is obviously the same as the VPC role, but it's not. So I'm going to go ahead and assume this role, and the permissions provided by this role, it has two AWS managed policies. It has the AWS lambda VPC access execution role, which is how it can create network interfaces within a VPC, and that's how it accesses the VPC by injecting interfaces into the VPC, and this is what gives it the permissions to do so. And then it's also got another managed policy, which provides access to the elastic file system. So these are two managed policies that are attached to the lambda execution role. Okay, so this is the end of part one of this lesson. It was getting a little bit on the long side, and I wanted to give you the opportunity to take a small break, maybe stretch your legs, or make a coffee. Now part two will continue immediately from this point, so go ahead, complete this video, and when you're ready, I'll look forward to you joining me in part two.