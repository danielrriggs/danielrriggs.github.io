 Welcome back, and in this lesson I want to cover a pretty important topic, especially so if you use Lambda in production situations, and that's Lambda versions. Now the next few lessons are going to be fairly brief. We're going to be covering a few small but important bits of Lambda functionality, so let's just jump in and get started. So far we haven't used Lambda versions in the course. You've been using functions without publishing, which means that functions can be changed and deployed. What you've actually been doing is directly editing the latest version of a function. So when we talk about a Lambda function, and if we don't refer to a version, then we're referring to the latest version, the one we've been editing, the one that you can make changes to both the code and the configuration settings. Without using Lambda versions, this is all that exists. Now you can take the current state of a Lambda function and you can publish it, and this creates a version of your Lambda function. It has a version number. The first version, logically, is version 1, and the most important point for the next few minutes is that this version is immutable. It's fixed. It can't be changed. So if you reference version 1 of a function, you will always get version 1. The thing that you reference as version 1 is fixed. It's immutable. It doesn't change, and that's really critical to understand. Now a version includes function code, dependencies, runtime, settings. And any environment variables, and I'm going to be talking about environment variables elsewhere in this section. They're all immutably stored inside that version, locked, unchanged forever. And each version has a unique ARN which is assigned and uniquely identifies that particular version of the Lambda function. Now that unique ARN is called a qualified ARN. And as I've just mentioned, it refers directly to a specific version. Now you can also use an unqualified ARN which points at the function without specifying a version. And conceptually, you can think of this as referencing the function itself. It points at $latest, so the version that you can edit and deploy freely within the console. Or it's used to grant access to the entire function, so you can use it within permissions policies. Now just to reiterate, an unqualified ARN is not pointing. It's a fixed, specific version of the Lambda function, and that's key. So you can't rely on an unqualified ARN to point at code which doesn't change. If you need to point at something that's fixed and immutable, you need to use a qualified ARN and use a particular version number. Now visually, it looks like this. So we start with a Lambda function. This is like a magic eight ball. It tells us which is the best animal, and of course, it responds with cats. Now this function has an ARN. It's a function that's called ARN, which contains the service, the region, the account number, the resource, so function, and the function name, which in this case is best animal. So this is an unqualified ARN. It references the function and points at $latest, which is the version that you can freely edit. Now to get a version, we have to publish this function, and this creates version one of this function. And with this, a qualified ARN, which references this particular function, is created. And this is the function that's called ARN. So this is the function that's called ARN. And this is the function that's called ARN. Now let's say at this point that we're trying to be more inclusive, and we want to include doggos. Well we could make a change to the function and deploy this change. So this is now the $latest version, and it uses the same function ARN, which is unqualified. It doesn't refer to a particular version. So this version also prints out doggos are also okay, and of course Whiskers isn't too happy about that. Now this version, though, is a hit. With all of the doggo lovers of the application. And so we might decide to publish it. And this creates a version two. And again, because this is a version, it has its own unique ARN, a qualified ARN, which points at that specific version. Now the important thing to take away about versions is that they are immutable and fixed. Once published, you can't change them. So if we publish something to version one or version two, that version will always have the same code and settings. That's critical to understand for the exam and for real world usage. The code and settings are fixed, as is the qualified ARN for a particular version. Now that's everything that I wanted to cover in this lesson. We are going to be doing a demo covering this and aliases, which I'm going to be talking about next. But at this point, that's all I wanted to cover. So go ahead, complete the video, and when you're ready, I'll look forward to you joining me. in the next.