 Welcome back, and in this lesson I want to briefly cover another important feature of Lambda. And that feature is Lambda environment variables. So let's jump in and get started. Now I'm going to quickly step through the architecture and features of environment variables, and then I'm going to switch across to my console for a quick walkthrough. So an environment variable within Lambda is a key and value pair. So you provide a key and you provide a value. And every Lambda function can have zero or more environment variables. So zero or more key value pairs. Now by default these are associated with $latest, so the editable version of a Lambda function. And while this is the case, you can edit the key value pairs that are assigned to that Lambda function. Now you can take a Lambda function and publish it to a version. And when you do that, in addition to the code and the settings, the environment variable... ... currently assigned to that Lambda function are also stored inside that version. And at this point they become fixed, also known as a mutable. So that's important to understand. When you publish a version, the associated environment variables also become fixed along with that version. Now the environment variables themselves can be accessed by your Lambda function code while it's running within the execution environment. So just like any other variables that are defined within your code, you can also access the environment variables. And of course, like many other AWS features, the environment variables themselves can also be encrypted using KMS to add additional security. Now the way that environment variables are generally used is that it allows code execution to be adjusted based on these variables. So you might pass in via environment variables whether the code is running in a prod or dev environment. And your function code can make different decisions. There are many different things you can do with environment variables, but this is a common architecture. Now I think it's going to be much easier to show you how environment variables are used rather than talk more about the theory. So at this point I'm going to move across to my console to give you a quick walkthrough of how you can utilize environment variables. So let's switch across to the console and get started. So I'm at my console and you can feel free to either watch me do this step or just click on the button. So I'm at my console and you can feel free to either watch me do this step or just click on the button. Or you can follow along in your own environment. This is going to be relatively brief, so it's probably not worth you switching across to your console. But if you do want to do that, then go ahead. Now if you are following along in your own environment, as always, make sure you're logged in to the IAM admin user in the general AWS account, which is the management account of the organization, and make sure that you have the Northern Virginia region selected. We need to be in the Lambda console, so go ahead and type Lambda and click to move to the Lambda console. Depending on when you're watching this lesson, your interface might look like mine or it might look slightly different. You might see in this left-hand menu, and you might need to click on the hamburger to open that up, but if AWS is still making changes to the user interface, then make sure you have this toggle set to use the updated console preview. You should always get into the habit of using the most recent version of any AWS consoles. If you're watching this after AWS have completed the changes, you might not have this toggle. Just make sure you have this toggle set to use the updated console preview. Make sure you're using the UI that looks something like mine. And then we're going to go ahead and create a Lambda function. I'm going to select author from scratch. I'm going to call the function env variables for environment variables. And for runtime, I'm going to select Python 3.8 and accept all the rest of the defaults and click on create function. Now we're going to be operating on the unqualified function, so not a particular version of this function first. And in order to set the environment variables, we need to click on the button. We need to click on the configuration tab and then select environment variables. And currently we have none that are defined. So I'm going to click on edit and then add environment variable. And you'll see that we have the space for a key and a value. So for key, I'm going to enter env, which stands for environment. And then for value, I'm going to put prod. And once I've done that, I'm going to click on save. So this creates a key value pair, a single environment variable. The environment variable is called env and the value of that variable is called prod. Now to demonstrate how this is used, I'm going to click on the code tab. Right click on lambda underscore function. Click on open. And then I'm going to select from this bottom curly bracket all the way to return. So we're going to select from this part. So from return and then including the open curly bracket and the closed curly bracket and everything in between. And then we're going to delete that. Next I'm going to press tab to indent. And I'm going to type print and then a space. And then os dot environ and then a square bracket and then a single quote. And then type the name of the environment variable that you want to print, which is env. Now the editor is going to add an additional single speech mark and a closed bracket. But make sure that you do have this entire line. So print space os dot environ and then square bracket single quote env. And then another single quote and then a closed square bracket. Now to use this, so to use os dot environ, we need to add an additional module. So select straight after import space json and press enter. And then add import space os and press enter. And that's going to add this module, which will allow us to use this. And then one last thing that we need to do is the print statement needs to have brackets around it. So the final line needs to be print space open bracket os dot environ square bracket. Square bracket single quote env another single quote square bracket and then closing bracket. So that's how it should look when it's fully finished. Then I'll click on deploy. Then go to test and then invoke. And we should see execution results succeeded. And if we expand the details and scroll down, you'll see here where it says prod. And this is outputting the contents of the environment variable. So that's just a really simple example of how you can interact with environment variables. You define them on your own. You define them on your functions. And then depending on the language that you use, depending on the run time, there are various different methods that you can interact with them within the code. And I've included a link attached to this lesson which shows you how you can interact with environment variables using node js, python, ruby, java, go, c sharp and powershell. Now at this point that's everything I wanted to cover. It's been a really simple example of a demo using environment variables. But it does give you the basics. It shows the same concepts no matter how many environment variables you need to use across lots of different lambda functions. If you did follow along with this in your own environment, go back to functions, select env variables which is the name of the function you created. Click on actions, select delete and then confirm that. And that will return the account into the same state as it was at the start of this demo component. And with that, that's everything I wanted to cover. So go ahead and finish this component of the video. And when you're ready, I'll look forward to your job. Thanks for joining me in the next.