 Welcome back, this is part 2 of this lesson. We're going to continue immediately from the end of part 1, so let's get started. Okay, so now I'm at my console, I want to demonstrate quickly how we can make our Lambda functions more efficient. Now don't feel that you need to do this in your own environment, the concepts that I'm going to talk about, you can just watch me perform. But if you do want to do it in your own environment, I have included the code snippets attached to this lesson. To start with, I'm going to make sure that I'm logged in to the IAM admin user of the general AWS account, so this is the management account of the organization, and I've got the Northern Virginia region selected. So next, I'm going to go ahead and click in the search box, and I'm going to type Lambda into that search box and move to the Lambda console, and I'm going to create a Lambda function. The function name is going to be Handler Demo, and I'm going to be using Python 3.8, so I'm going to click in this drop-down and select Python. Python 3.8, scroll down, and then click on Create Function. We can accept all of the rest of these defaults. By default, the Lambda function is going to be given the default set of permissions, which will allow it to log into CloudWatch logs, so we don't need any more permissions. Now at this point, just scroll down, right-click on Lambda underscore function dot py, and click on Open, just to keep this interface consistent. Select all of this example code, and then go ahead and delete it. Now, attached to this lesson are two links containing two different versions of the Lambda function. Go ahead and download both of these versions, and you'll need to open version one and paste it into this box. So this is the Handler function, and this is the entry point into our Lambda function. It's where the Lambda product passes through the event data. Anything above this is the initialization area, which will only be run once on a cold start. So this is how it looks in practice. Now this code is intentionally messy, it's not nice, it's not best practice, and of course it's simulating connections. It's not really doing anything. So what happens is I define a variable called dbConnected and set that to boolean false, and then if that variable is false, then I simulate a connection to a database by essentially printing out some lines and then sleeping for five seconds. So that just gives us five seconds of delay to simulate the connection to a database. And then once the database is connected, and we determine that by checking if dbConnected is equal to true, and of course that's set after we connect to the database, simulated of course, but if we are connected to this simulated database, then I output a couple of diagnostic lines saying connected to database, moving to application code, running application, finished running app. So this is just a really simple simulated exercise of connecting to a database and running some code. Now if I go ahead and deploy this code, I'll do that first, and then I'm going to test it. So we're going to click on test. For the test event, we don't really care about this, so enter test under event name, scroll down, click on save, and then click on test again to invoke and test the function. Now it's going to error. We're going to get an error message that the task timed out after three seconds. Now the reason for this is that there is a default execution limit on Lambda of three seconds. To fix that, click on the configuration tab, and then go to general configuration and then click on Edit. We'll see the timeout is currently set to zero minutes and three seconds. I'm going to change this, going to put it as one minute and zero seconds. Save that. And now that this function is capable of running for one entire minute, I'm going to go ahead and go back to test, then close down this previous execution results and click on test again. should execute successfully. We'll see that the status code is 200 and it's finished executing. Scrolling down further we'll see an overview of the execution details. Now there are a couple of important things that I want to point out. First the init duration, so this is the amount of time that's spent setting up the execution environment and this includes any time taken to execute the initialization part of the code. So if you recall, if we just scroll up go to code and then select the lambda function, this is the initialization area, so the bit above the handler function. So let's go back to test. Now after the initialization it took over 5,000 milliseconds to execute our code. And remember this is including the simulated database connection that I'm simulating using time.sleep with a value of five seconds. Now if I want to test this with my code, I need to remember that the code is Now, if we invoke again, so let's close this down and invoke again, we're going to see a similar execution. But it will look slightly different. If we scroll down, now we note there isn't an init time because we're reusing the same execution environment. But it still takes us over 5,000 milliseconds to execute the function. Again, because the simulated database connection is inside the handler function. So we're going to change this now. So I'll close that down and go back to code. Go back to the lambda function and I'm going to delete all of this code and then put in the second version that's also attached to this lesson. I'm going to copy that into my clipboard and paste it in. Now the change with this version is that we're now performing the initial database connection in the initialization area of the code. So this is occurring outside of this handler function. So this will only be executed once during every... cold start of this execution environment. In theory, every time the function is executed after that, it only checks if there is a database connection active. And if there is, it doesn't bother reestablishing it. It just runs the simulated application code. So let's deploy this and then again click on test. Now again, this will take some time to execute. But if we expand this and scroll down, you'll see this time the init duration took 5,000 milliseconds. So this is the time taken to initialize the execution environment and run any code inside the initialization area of our lambda function. The actual duration was 1.3 milliseconds. And we had a build duration of 2 milliseconds. So we get build for the amount of time taken to actually execute our functions. Now this is the initial execution, the cold start. Watch what happens if we invoke again. So I'm going to close down. This result and click test again. Now it's much quicker. If I scroll down, you'll see that there is no initialization execution amount. Instead, it just takes 1.1 millisecond to execute this function. And all further invocations of this function will be equally as quick. Because we're reusing the connection that's defined outside of the handler function. The one that's defined inside the initialization area of the lambda function. Now in terms of how the handler function works, we're going to use the function that's defined outside of the handler function. If I go back to code and scroll down, there are two things that I want to draw your attention to. Inside the environment, we have a folder called handler demo. And inside there is a file called lambda underscore function dot py. So focus on lambda underscore function. Inside lambda underscore function, we actually define an actual Python function. And this is lambda underscore handler. Now if I just scroll down and look under runtime settings. And click edit. It's here where we define the handler. And the default for this is that we have lambda underscore function. Remember this is the file name that's inside the lambda environment. And then we have dot. And then we have the name of the function. So lambda underscore handler. And we've got Python 3.8 selected. And this is the default. So we have the file and then a dot. And then the name of the function. Now you could choose to use different file names and different function names. But the standard is to use the name of the function. Is to use lambda underscore function dot lambda underscore handler. And that's how the lambda service knows that the first thing that gets run on every warm start. Is this lambda handler function. Anything outside this, either we call it. Or it's part of the initialization area of the code. So when we first do a cold start. We run all of this. And then we run this. But when we do future warm starts. We only run. The handler function. And that's how we can achieve those performance benefits. Now this has been a relatively complex topic. But as a developer. It's important that you understand how lambda is architected. And this is how the handler function part of lambda works. It's always best practice wherever possible. To define any shared items. Or long running items. Outside of the handler. And then reuse those. For any future executions. A well designed function should never assume that it's a warm start. It should always assume that it's a cold start. But it should be able to take advantage of warm starts. To achieve performance improvements. And that's exactly what we've done here. With this quick simulated example. We've extracted the shared element. The simulated database connection. Out of the handler. And put it in the function initialization area. And that's improved the performance. Of any executions. After the initial one. With that being said. That is everything I wanted to show you. In the demo component. Of this lesson. Now because I'm running this in my environment. I'm going to go ahead and delete this function. So go to functions. Select the function. Click on actions. And delete. And if you've been doing this inside your own environment as well. You'll need to do the same. At this point though. That is everything that I wanted to cover. In this demo. So go ahead. Complete the video. And when you're ready. I look forward to you joining me in the next.