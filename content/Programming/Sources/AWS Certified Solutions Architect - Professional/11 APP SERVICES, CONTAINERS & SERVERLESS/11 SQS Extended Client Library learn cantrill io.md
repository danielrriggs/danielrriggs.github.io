 Welcome back, and in this lesson I want to briefly talk about the SQS Extended Client Library. Now this is something which features on the exam, and I want to step through the functionality that it provides. Going into it in detail is far beyond the scope of this course, but you do need to understand it at a high level for the exam. So let's jump in and get started. So I just want to step through the scenario of where you might want to use the SQS Extended Client Library. So SQS has a message size limit of 256 KB. So the Extended Client Library is something that you would use when you want to send messages that are larger than this maximum size. So the Extended Client Library allows you to process large payloads and have the bulk of this payload stored within S3. Now the way that this works is that when you send a message using the Extended Client Library, you can provide a payload that's bigger than 256 KB, and when you do that, it sends the message, but it uploads the payload to S3 and stores a link to this payload within the message. When a system decides to receive a message, the Extended Client Library loads the large payload from S3 and then delivers this as part of the message to the thing requesting that message. When you delete a message from a queue and use the SQS Extended Client Library, the extended client library, you also delete the large S3 payload. So this client library essentially provides an interface for SQS plus S3, and it handles the integration workload behind the scenes. So essentially you have one library to use, and if you use this library, you extend the capability of SQS to be able to cope with larger payloads than 256 KB. But you need to remember this is a hard limit for SQS, and it works around this by using S3. And this is useful when you want to send messages or consume messages of up to 2 GB in size. Now the exam often mentions Java and using the extended client library. So if you see any requirements to send messages to an SQS queue larger than 256 KB, then just keep in mind that you can use the extended client library. And equivalent libraries which perform the same task are available for all of the different supported languages when using those languages with AWS. Now that's everything I wanted to cover in this lesson. I just wanted to give you a high-level overview of when you might use the SQS extended client library. And with that being said, that's everything that I wanted to cover. So go ahead and complete this video, and when you're ready, I'll look forward to you joining me in the next.