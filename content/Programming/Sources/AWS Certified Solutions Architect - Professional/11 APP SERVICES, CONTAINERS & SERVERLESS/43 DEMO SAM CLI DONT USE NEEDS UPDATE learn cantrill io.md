 Welcome to this really brief walkthrough lesson where you're going to get the chance to experience how the SAM or serverless application model, CLI, operates. And you're going to deploy a simple serverless application. Now just as a refresher, this is the architecture that we just looked at in the theory lesson. Generally, an application is stored in an online repository such as GitHub. This GitHub repository is synced down to a local repository and this is a local SAM application repository and it has this structure. It has the code for any Lambda functions and it has the SAM template.yaml file which is a serverless application model template. Now we're going to use one of the AWS example SAM applications and deploy that into our AWS account. And we're going to follow these same steps. We're going to download the code. We're going to download the code from a GitHub repository, explore the structure of the SAM application together, and then deploy it into our AWS account. Now because this is an architecture course, this demo is going to be looking at deploying a serverless application from an architectural perspective. So we won't be looking at the code itself. We won't be going deep into any of the deployment technologies. We'll just be looking at the serverless application model CLI from an architecture perspective. Now there are a few things that you need to know. There are a few things that you need to have set up before you get started with this demo lesson. So let's step through those first. First, you'll need the AWS CLI version 2 downloaded and installed for your particular operating system. Now this is something which you should have done at the start of the course. In addition to having this installed, you should also have the command line configured with a configuration profile to connect to your AWS account. So you should be able to run an AWS. AWS s3 ls double hyphen profile and then a4l hyphen master. This is something that you should have configured at the start of the course. And if you have got that configured successfully, that should output a list of any S3 buckets which you have in the account. If it outputs a blank list, that's fine. It simply means you don't have any S3 buckets. But if this command generates an error, it means you haven't done one of the earlier configuration steps. And you need this in order to do this demonstration. Now I'll make sure the link to the CLI installation page is attached to this lesson. And you'll need to download the version specific to your operating system. So whether that's Windows, Mac OS or Linux. Additionally, you'll need the latest version of Python installed. And at the time of creating this lesson, that's Python 3.8.5. Although logically, depending on when you're watching this lesson, this could change. You'll need to download and install this version. And you'll need to install the version specific for your operating system. And the link that I've included attached to this lesson contains downloads for Windows, Linux and Mac OS. Finally, you'll need to install the AWS SAM CLI. And again, this comes with versions for Windows, Linux and Mac OS. Now this page details the installation process. You can safely ignore steps 1 and 2. Because you already have an AWS account. And you already have an IAM user for that AWS account. We won't be using the local testing functionality. And so you can safely ignore step 3. The bit that you need to start at is step 4. Which is to install the AWS SAM CLI. And the instructions for this depend on the operating system. Again, I'll make sure that this link is included attached to this lesson. You'll need to follow the process for your specific operating system. And when you're finished, you should be able to run. A SAM space double hyphen version. And on Mac OS, this is what the output looks like. This could vary slightly depending on your operating system. And when you're performing this demo. Now we use the SAM CLI tool logically from the command line. Now I've got open my Mac OS terminal. If you're using Windows, then you'll need to use your command prompt. For Mac OS or Linux, then it's the normal terminal. Now currently, I'm in my home folder. And to keep things tidy. I'm going to go ahead. And create a directory. I'm going to make a directory called SAM. And then I'm going to move into that directory. Now I've cleared the screen to make it slightly easier to see. And the first step is to initialize a local SAM repository. Now you could sync or create your own custom repository. And then populate it with a SAM application structure. Or you can use the tool to create one on your behalf. Now to do that, it's SAM space init. And then press enter. And this will create a structure for a local SAM application. Now for this demo lesson, we're going to keep it nice and simple. And use one of the AWS Quick Start templates. So go ahead and press 1. And then press enter. Now it prompts you to select a runtime to use. And there are many available. But we're going to use Python 3.8. So press 2. And then enter. You're prompted for a project name. And the default is SAM-app. We're going to change this and use SAM-EFS-app. So type that and press enter. Now this clones the AWS Quick Start templates to your local machine. And you're prompted to select a Quick Start application template. So we're going to use the Elastic File System sample app. So type 5 and then press enter. So this generates the sample application structure. And if I do an ls space hyphen la. And you might need to adjust these commands. Depending on your operating system. Then you'll see that it's created a folder called SAM hyphen EFS hyphen app. And this is our serverless application folder. So if we move into this folder. And then I'll clear the screen and do another file listing. Then this is a pretty typical SAM project. It's got the template dot yaml file. Which is the serverless application model template. And then it has a folder hello underscore EFS for a lambda function. Which I'll step through in a second. What I want to do is to open up this template dot yaml file in my code editor. And just step through its anatomy and exactly what it does. So this is that template file template dot yaml. And this looks much like a normal cloud formation template. It has a template format version. It has a description. And it has a list of resources. But because this is a SAM template. It has this transform at the top. As well as this new global section. So this global section defines variables or global variables. Which can be referenced elsewhere in the template. So this template creates a number of resources. If you're familiar with cloud formation. Which you should be at this point. A lot of this should look familiar. It defines a VPC. It defines a security group. It defines two subnets. So subnet A and subnet B. An EFS file system. Two mount targets for that file system. An access point. Which is how Lambda can access EFS. And I've talked about this elsewhere in the course. And then it defines the hello EFS function. And this is an AWS double colon serverless double colon function. So this is an extension of what you can do with a normal Lambda function definition. Within cloud formation. Now what this is going to create is a VPC with a Lambda function configured for VPC. Networking. But in addition it's going to create a Lambda function. Which we can access via an API gateway. So this SAM template is going to create all of the components of a full serverless application. An API gateway. A Lambda function. And all of the resources that it needs. And the output of this is going to be an API endpoint that we can access. Now the benefit of using SAM and the SAM CLI. Is that all of this is handled online. On our behalf by the product. Now inside this hello EFS folder. Is the definition for the Lambda function. And the Lambda function itself is defined in the app.py file. So let's take a look at that. This is the app.py file. So this is going to be the Lambda function. What this Lambda function does is to define at the top a file. And this file forward slash mnt forward slash Lambda forward slash file. Is a file that this function can create. Or it can load. So if the file doesn't exist. Then it populates it with this contents. So hello comma EFS. So we're going to change that. We're going to change the contents to cats are the best. And then we'll put some exclamation points for effect. So it will create this file if it doesn't already exist. And it will populate it with cats are the best. And then some exclamation points. And it will write out the contents of this variable to that file. So forward slash mnt forward slash Lambda forward slash file. So if that file doesn't exist. It will write it. And then it will return the contents of the file. Together with the fact that it had to create it. If the file does exist. Then it will simply return the contents of the file. And indicate to us that it didn't have to create the file. So in both cases the Lambda function will ensure the file exists. But what we get in return are the contents of the file. And then whether the file had to be created or not. So it's a really simple Lambda function. A really simple example of a serverless application. So go ahead and make that change. And then just save the file. Now returning to the command prompt. I'm just going to type cd double dot. Just to move back up to the serverless application repository. So if I just clear the screen to make this easier to see. This is the level that I'm now at. I'm inside the sam-efs-app folder. And this is what I can see. Now deploying a serverless application is easy. Because it has this consistent standard. The sam-cli knows what to expect. So to deploy the application. Is as simple as typing sam space deploy. And then a space and then double hyphen guided. Now this normally would be enough to deploy this serverless application. Into your AWS account. If you have AWS credentials set. In the default command line. So this uses the credentials that are configured. For your AWS command line. Now because we're using a custom named profile. For this course. We need to add double hyphen. And then profile space. And then a four l hyphen master. And this will make sure this serverless application. Is deployed into our organizational master account. So go ahead and type that. And then press enter. And that will begin the guided deployment. Process. Now if you look at the architecture that's on screen now. This process is done using a cloud formation stack. So the first thing that the command line is asking for. Is the name of the stack. And my usual process is to call this the same as the application. So sam hyphen EFS hyphen app. So type that and press enter. You're prompted for an AWS region to deploy this application into. And in my case it's defaulting to US East one. Because that's the default region which I've selected. But in either case go ahead and type this specific region. So US hyphen East hyphen one. And press enter. Now behind the scenes this deployment is actually doing two different processes. A build and a deploy. So we do have the option of showing us the resources and changes. To be deployed into our account. Now the default is no. Because it will just go ahead and deploy them. Because we're explicitly asking for it to do so. So just go ahead and either just press enter for the default. Or type uppercase N and press enter. We don't need to be informed of the changes to be deployed to this account. Now the SAM CLI will create some AWS resources. And one of these is an IAM role. And you need to explicitly allow the SAM CLI to create this IAM role. So type uppercase Y and press enter to continue. Now the serverless application that we're deploying is open access. It doesn't have any form of authorization. And the CLI prompts us to make sure that we're aware of that fact. Now the default is no. Because it defaults to a secure stance. Normally you wouldn't want to deploy things which don't have authorization. This is a simple demo application though. So it's fine to do so. So type Y and press enter. The command line then prompts if we want to save these items. And we're going to go ahead and say yes. So type uppercase Y and press enter. And this begins the process. So this happens in two phases. The first thing that happens is a stack is deployed into your AWS account. Which creates a SAM CLI bucket. And all of the resources for this SAM application are deployed into this S3 bucket. So first we create a stack which creates this S3 bucket along with all the resources. And then we deploy the application into the S3 bucket. And then we deploy the application into the S3 bucket. Then the CLI deploys the resources into this S3 bucket. And then from this S3 bucket the application is deployed into our AWS account. So step one is to create the SAM CLI infrastructure. And that happens with a specific CloudFormation stack. So we can see now that it's created this managed S3 bucket. And deployed all of the resources into this bucket. And now it's going to begin deploying the serverless application. You'll see that it goes through a number of steps. And if you've paid attention to any of the other demos within the course. These are normal CloudFormation events. So these are the type of events that you'll see within normal CloudFormation deployments. Now at this point this will take some time. So go ahead and pause the video and resume it once this process has completed. Okay so the SAM CLI has finished the deployment. We're told that the CloudFormation stack is now in a create complete status. This is the last event line from the event section which you'll see updating during the deployment. And the final output is the output from the CloudFormation stack which is deployed as part of this application. And this gives us a URL for the serverless application. Now before we test this out I just want to show you within the console UI exactly what's been deployed. So this is the AWS console UI where I'm logged in to the account which I deployed the serverless application. So if I go to CloudFormation what we'll see is two different stacks. We'll see the AWS SAM CLI stack. This is the one which I mentioned at the start. And this deploys two resources. An S3 bucket and an S3 bucket policy. So this is the bucket and if we go inside the folder that relates to the application. So SAM-EFS-app. And then inside that folder we'll see the application template together with any application. And then inside that folder we'll see the application template together with any application. So the SAM CLI uploads any relevant assets into this bucket. And when it begins the deploy of the application creating all the AWS resources it's this bucket that acts as the source. Now in addition to the SAM CLI stack we also have a stack that's specific to our application. So if we open that stack and then go to resources. These are all the resources provisioned by the SAM CLI on our behalf using the application. So the SAM CLI interacts with our AWS account using SAM templates and CloudFormation stacks. And that's how it affects any resource changes in the account required as part of the deployment. Now if we just go back to our command prompt and just copy down the URL for our serverless application into our clipboard. And open that in a new tab. Now that might take a few moments to load the first time because it has to invoke the Lambda function. But what happens is the contents that we updated are outputted and used to create a file which is stored on the EFS file system. Now the second element of this is created underscore file and this tells us that the file was created. This is the first time that we've accessed this application and the file has to be created. Now if we just do another refresh we'll see the same contents but now the file is not created. So these contents are being loaded from this file. Now if we just go back to our application. Now this is a really simple fairly boring example of using the serverless application model and the SAM CLI to deploy a serverless application. But the techniques used are equally as capable of deploying complex large scale serverless applications. Now if you use the SAM CLI to deploy this application and removing it is as simple as going back to CloudFormation. Selecting the SAM-EFS-app stack. And then clicking on delete and confirming it. And that will tear down all of the resources created by this serverless application. To do a full clean up you can also remove the SAM CLI managed bucket. Now you don't have to do that you can leave this in place. But if you do want to remove it first you need to move to the S3 console and delete that bucket in its entirety. So locate the SAM CLI bucket inside S3. So select it and then empty that bucket. You'll need to confirm it by typing or pasting the bucket name and clicking on empty. And that will empty the bucket first. And once emptied then you can select the SAM CLI managed CloudFormation stack. And delete that stack and it will remove all of the resources. And that should result in the account being in the same state as it was at the start of this walkthrough. Now thanks for watching. I know this has been a fairly simple. And relatively uninteresting example of using the SAM CLI. But this is something which I'll be covering in much more detail in the more developer focused certification courses. In this course which is architecture based I just wanted to give you an overview of how all of the components fit together. But at this point go ahead complete this video and when you're ready I'll look forward to you joining me in the next.