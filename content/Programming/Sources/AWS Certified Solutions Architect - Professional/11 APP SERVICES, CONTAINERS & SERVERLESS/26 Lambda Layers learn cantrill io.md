 Welcome back, and in this lesson I want to cover the theory of lambda layers and then give you a quick walkthrough from my console. So let's jump in and get started. Layers are pretty simple to understand. Before I cover their architecture, consider this example without using layers. This function uses the NumPy and SciPy libraries, and it performs some fairly complex maths as part of the function. For this lesson though, what matters more is the libraries which allow this to happen. And these libraries are fairly large. So if you wanted to use this function without layers, it would mean a single huge deployment zip, which contains the function code. But in addition to that, it would also contain the libraries that the function requires. In this case, NumPy and SciPy. Without layers, everything is contained in one deployment zip. If you want to use 10 slightly different functions, which all use these same libraries, then that's 10. So you can use these 10 different large deployment zips. All of which contain these two shared libraries. Now the way that this changes with using layers is let's say we have two functions which both use NumPy and SciPy. But where the function itself is slightly different. With layers, we can split up these two components. First, the function code, which is different for each function. Now these will be different zips. For each different function. But because we're using layers, these can be kept small and only contain the code that's specific to each Lambda function. Then we can have a layer which we can create for the libraries which we use. And crucially, this layer can be used by both functions. So a single layer can be shared between these two functions. Any layers which we use are actually extracted into the forward slash function. And we can also have a new one which is called the opt folder inside the runtime environment. And as such, can be used in the same way as if the libraries were actually included in the function zip. Now layers allow us to use new runtimes which are not currently supported by default with Lambda. And in addition, allow libraries to be externalized. And this means contained within a separate package. So used by, but separate from our code. And the result is deployment zips only contain the specific functions. And that's what we're going to be using in this particular example. So we can create specific pieces of code which are unique to that specific function. They're smaller and everything else is externalized into these shared layers. So we can create layers which are specific for our business. Which contain any libraries that we commonly use across lots of different Lambda functions. And because we have them contained within a layer, we can reuse that layer across lots of different functions. It makes deployment easier. And it makes each individual function zip. And easier to manage. Now at this point, I think it's going to be far easier to give you a walkthrough. Just to demonstrate how this works. And AWS have been nice enough to provide an example which illustrates this really well. And so I want to give you a walkthrough now in my console of exactly how we can use Lambda layers. So I'm going to go ahead and move across to my console. Now once again, you can either watch me do this in my environment or you can follow along in your own. If you are following along in your own environment. Then just make sure that you're logged in to the app. I am admin user of the general AWS account. So the management account of the organization. And as always, you need to have Northern Virginia selected. Once we're at this point, I'm going to move across to the Lambda console. By typing Lambda in the search box. And then clicking. And I'm going to go ahead and create a Lambda function. So I'm going to go ahead and click on create function. But if you are following along in your own environment. As always, if you do have a toggle between the current and updated console version. Go ahead and make sure you're using the updated version. If you're watching this lesson after AWS complete their UI refresh. You might not see this. And that's okay. Just make sure you're using the latest version of the UI that you have available. So I'm going to go ahead and click on create function. And for function name, I'm going to call it amazing maths. For runtime, I'm going to select Python 3.8. And then accept the rest of the defaults. Because by default, it will have all the permissions required. And then I'm going to click on create function. Now attached to this lesson. Is a code snippet. Which is going to perform all of this amazing maths. And output the results. So go ahead at this point while that's creating. And make sure if you are following along in your own environment. That you copy all of that code snippet into your clipboard. And I've gone ahead and done the same. So I'm going to scroll down. To right click on Lambda underscore function. And click on open. I'm going to select this template code. Delete it and paste in the code snippet. Now you'll note that at the top here. It imports numpy and scipy. So these are two Python libraries. That this function needs to operate. So if we just go ahead and deploy it as is. So click on deploy. And then if I go to the test tab. And just invoke it. What we'll see is an error. If I expand the details. The reason for the error. Is there is no module named numpy. And in addition to that. We'll have the same problem with the scipy module. Now the reason. Is that we haven't included these. Inside our Lambda function code. If we wanted to deploy these. Specifically for ourselves. We would need to create a Lambda deployment zip. Ourselves. And upload that to Lambda. We'd need to include these modules. If we wanted to use them. Now the default runtime environment within Lambda. The one for Python 3.8. Does not include either of these modules. So the only choices we have. Are to include them in our deployment zip ourselves. Or we need to use them. So we'll use Lambda layers. And we're going to use Lambda layers. To make this work. So close down this execution result. Then I'm going to click on the code tab. Scroll down. We'll see runtime settings. And then layers. And then I'm going to add a layer. Now you can choose from AWS provided layers. Custom layers. Or you can specify an ARN. Now to keep this demo simple. I'm going to choose AWS layers. And I'm going to add a layer. And then I'm going to add a layer. And I'm going to add a layer. So AWS created a blog post. Which details everything we're doing. Within this demo lesson. So this is a layer that AWS maintain. That contains both NumPy and SciPy. So I'm going to go ahead and select this layer. Then click in the version drop down. And if there are multiple versions of this layer. You'll see these individual versions. Currently there's only one. Which is version 29. So I'm going to select this. This layer is now used as part of this Lambda function. And now if I click on the test tab. And then click on invoke. Now we won't get the error. We'll get execution result succeeded. And if I expand the details. And scroll down. We'll be able to see the expected output. Of this function. We can see that it's doing a number of calculations. It's using NumPy at the top. And SciPy at the bottom. The important thing is not what this is doing. But the fact that we've now used a layer. To add these requirements. And the required modules. Inside of our Lambda function. So we didn't need to include these ourselves. We're using an externalised set. Of libraries that AWS provide. In the form of a layer. And we're now using them within this Lambda function. Or more specifically. We're using a version of that layer. Within this Lambda function. And that's everything I wanted to cover. In this demo component of this lesson. I just wanted to give you some practical experience. Of how you can utilise layers. In your own Lambda functions. Now you can obviously use AWS provided ones. Third party provided ones. Or layers that you create yourself. But creating layers. Is outside of the scope of this course. And this certification. Now at this point that is everything. So just to tidy up. If you were following along in your own environment. You need to go to functions. Select the amazing maths function. Click on actions. And then delete that function. And once you've done so. You can click on the start of this demo component. At this point thanks for watching. Go ahead complete this video. And when you're ready. I'll look forward to you joining me in the next.