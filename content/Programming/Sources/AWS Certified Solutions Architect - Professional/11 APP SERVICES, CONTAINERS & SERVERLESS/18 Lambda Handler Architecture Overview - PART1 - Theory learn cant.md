 Welcome back, and in this lesson I want to cover the Lambda function handler in a little bit more detail. And to explain that, I need to provide a little bit of a refresher on how Lambda functions are executed. So let's just jump in and get started. Lambda function executions have life cycles. You might not think about these because that's the power of Lambda. It's a function as a service product, and it abstracts a lot of this execution information away from the end user. But every time you execute a Lambda function, that function logically is executed somewhere, and it's called a Lambda execution environment. So your code, and let's say that you use Python, and the runtime which runs that code, so let's say Python 3.8, all of that runs inside the execution environment. And logically, at some point, that environment needs to be created, and that takes time. So at a high level, this is why. So what happens? First, the init phase happens, and this is where the execution environment is created or unfrozen. Now this takes time. It depends on what runtime you're using, what layers you have, together with lots of other factors. Now after the execution environment is ready, the function is invoked for the first time. And this part of the process is what's known as a cold start. Running Lambda functions where cold starts occur is inefficient, because it adds stress. It adds significant startup lag to every single function execution. Now the Lambda product tries to make this more efficient. If you run the same function again fairly quickly, this is known as a warm start, because in most cases, this function invocation will run in the same execution environment that was previously set up with a cold start. This is known as a warm start, and when you have a warm start, all that happens is another invocation. The handler part of the function is run, and in theory, this makes the execution much quicker. So as developers, we need to try to make sure that as many of our function executions as possible are warm starts rather than cold starts, because that will lead to a much improved user experience. Now if you don't invoke a given Lambda function for a certain duration, then a shutdown phase happens, and during the shutdown phase, the execution environment is terminated, which would result in another cold start the next time the function is executed. Now let's look at this visually, because I think it will be easier to understand. So as I just touched on, we start with an init phase, and we end up with a shutdown phase. Inside the init phase, we have the extension init, runtime init, and the function init. Now we're just going to be focusing on the function init component in this lesson, but I've included a link attached to the lesson with extra reading if you want to go into additional detail. The shutdown phase has two sub-components, runtime shutdown and extension shutdown, and I'm including these just for completeness. We're not going to be covering these in detail within this lesson. Now in between the init phase of the lifecycle and the shutdown phase of the lifecycle, we have one or more invocations of our Lambda function. And down at the bottom of the screen here, this is the Lambda function. Now before anyone makes any comments, I know that this code is horrible. If you know Python, you'll see a number of horrible ways of doing things. But this isn't about teaching Python. This code that's on screen now has been designed in a very specific way to teach what I'll be showing you in the demo component of this lesson. So every bad piece of design in this code is in there for a reason. Now all Lambda functions have two main parts. First, we have the function that's called the function. And it's called function and utilization code which is the part at the top of a function outside of the handler component. The other part is the handler function This is the part of the Lambda function which you configure when you create the Lambda function. This is the component of the code that the Lambda product passes event data into. When you're using a Lambda function, it's the handler component of that function that will perform most of the logic that that Lambda function does. it's important to see two different components, the initialization component at the top and the handler component at the bottom. These are two very different things. The function initialization part, this is executed once during cold starts. It's run when the execution environment is being initially created by the function init subcomponent of the init part of the lifecycle. It's not run every single invocation. Lambda attempts to reuse the same execution environment every future time that that same Lambda function is executed. And so for all executions apart from the initial cold start, only the handler function onwards is executed. So the handler function and anything that that function calls. Now if you want your Lambda function to run as quickly as possible, so starting as soon as possible from the point of execution, you can run the execution environment from the point when it's invoked and finishing as quickly as possible, then you should aim to put anything which might be reused in the function initialization section. In this example, we're simulating a connection to a database. So if we put that connection at the top in the function initialization area, in theory this only happens once during a cold start. Every future indication can, in theory, reuse that connection and not have to connect each initialization part of the function. So if we put that connection at the top in the function initialization section, we should be able to run the execution environment every time the function is invoked. A well-constructed Lambda function needs to assume a cold start every time and be designed to run in that scenario. But it should also be designed to take advantage of warm starts by reusing connections and any other shared resources. For serverless applications where Lambda functions are being invoked in response to user clicks, the lag time between the function invocation and the completion of the function is a very important factor. So if we want to run the execution environment at the execution is critical. Now I'm going to be showing you just how much of a difference this can make in my console in a moment with a quick example. So it's really important before we do that that you understand the theory. So the function initialization area runs only during cold starts and then the handler component will run every single time the Lambda function is executed. Now after a period of inactivity, environments are shut down and any future invocations will require a cold start. So if we run the execution environment in a cold start, we'll run every single time a Lambda function is executed. So if we run the execution environment in a cold start, we'll run every single time a Lambda function is executed. So if we run the But you can get around this by using provisioned concurrency, which can run the cold start part in advance. So if you know that you're going to be running lots of Lambda functions all at the same time, then you can use provisioned concurrency, which will pre-warm the execution environments in advance. And this can massively improve performance as experienced by your customers. Now at this point, I want to move across to a quick demo. I want to show you how this technique of taking advantage of warm starts can help improve Lambda performance. And to do that, I'm going to switch across to my console. Okay, so this is the end of part one of this lesson. It was getting a little bit on the long side, and so I wanted to add a break. It's an opportunity just to take a rest or grab a coffee. Part two will be continuing immediately from the end of part one. So go ahead, complete the video, and when you're ready, join me in part two.