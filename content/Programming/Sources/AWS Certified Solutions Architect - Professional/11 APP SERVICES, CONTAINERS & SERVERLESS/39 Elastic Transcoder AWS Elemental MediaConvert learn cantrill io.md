 Welcome back, and in this lesson I want to talk about an AWS product called Elemental Media Convert and the AWS product which it replaces called Elastic Transcoder. Now I want to make this fairly brief because this is just a product which you need to know about for the exam and it's a product which you will only use in a certain set of niche use cases but it is part of many media-based serverless architectures, so it's a good one to understand. So let's jump in and get started. Now firstly, to get this out of the way, Media Convert is a relatively new product from AWS which replaces the Elastic Transcoder product. So Media Convert is a superset of the features provided by Elastic Transcoder so for most situations, apart from a few really small exceptions, you will choose Media Convert rather than Elastic Transcoder but the architecture is somewhat the same. Both of them are file-based video transcoding services and as I've just mentioned, Media Convert, which I'll refer to as MC on screen is sort of regarded as Elastic Transcoder version 2. So these are both serverless-based services so these are transcoding services that you pay only for the resources used during the transcoding process and if you aren't familiar with that term, an example of transcoding is taking a video of one format and converting it into another or other examples might be to exchange the audio from one soundtrack to another or convert the bitrate of the audio or just resize a master video into several smaller versions which are optimized for either streaming or download. So these are all media conversion or transcoding-based processes. Both of them are serverless where you don't pay anything whilst transcoding isn't occurring. Now with both of them, you add jobs to pipelines or queues. In the case of the Elastic Transcoder, it's pipelines and in the case of Media Convert, it's queues. But architecturally, they both function in the same way. You add a job to a queue and then the service handles the transcoding and it loads it from S3, it processes it or transcodes it and then stores the output back on a defined S3 bucket. Now one of the advantages that Media Convert offers versus the Elastic Transcoder is that Media Convert supports EventBridge which is basically CloudWatch Events version 2. So any time a job changes status that's being processed by Media Convert, it can generate an event within an EventBridge which can itself initiate event-driven or serverless workflows and that's a really powerful advantage. Now a typical architecture which involves Elastic Transcoder or Media Convert might look like... Let's say that we have a serverless application. A user of our application wants to share a movie of her cat with somebody and so she uploads it to an S3 bucket. Now this is the upload flow part of the application and there are two more parts. We've got the customer flow and the secure customer flow and I'll cover what that means in a second. Now we've configured this S3 bucket to generate an event when any objects are added to the application. So an event is generated which results in a Lambda function being invoked when the video is uploaded. It's the job of this Lambda function to create a Media Convert job. So this job runs and it outputs a streaming optimized version of the video to an output bucket which is more suitable for downloading and streaming. Once finished, Media Convert generates an event which is handled by EventBridge. It invokes... a VideoAddLambda function which adds metadata about the video into a DynamoDB database. And that is the upload flow or the upload component of this serverless application. We now have an optimized version of the media in a bucket and data inside DynamoDB. Now there's also a CloudFront distribution configured with the output bucket as an origin and this is set to be restricted. So all content is private and can be accessed only with a signed URL or a signed cookie. Our application user browsers to our application domain which points at an S3 bucket and this loads HTML and JavaScript into his browser. The HTML handles presentation and the JavaScript runs in browser and can perform client-side logic. Let's assume that he logs in using a web identity and the application is logged in. We can authenticate him using that identity to API Gateway. Now so far this has been an insecure flow. But now that he's authenticated, this is now a secure flow between our user and the rest of this serverless architecture. Now API Gateway uses DynamoDB for data retrieval and Lambda for compute. And let's assume our user selects the movies that he wants to see. It generates a signed URL for that movie. And returns it to the web application running on his laptop. So now he has the signed URL. He can use this to access restricted content on the cloud front distribution. Either permanently or in a time restricted way. The whole application flow is serverless. And it only builds when activity is occurring in the application. So this application has a number of core parts. We've of course got the front end serverless part of this application. But we have the media process. And we have the processing flow which begins when a back end user uploads a video to the input bucket. And this entire flow, the conversion of the master media into a form that's designed for streaming or download occurs in a serverless way using media converter. And all of the transcoding or conversion that occurs can be defined in advance. And then it's initiated in an event driven way when any objects are uploaded to the input bucket. Now for the exam you don't need to have detailed knowledge of either of these products. You simply need to know when to use them. Now you'll use them when you want a solution which can process media files and do so in a serverless or event driven way. Where there are no costs in the time periods outside of when actual processing is occurring. So this is a managed solution. You don't need to worry about auto scaling groups or SQSQs or scaling worker pools. You can simply use media convert in combination with maybe Lambda or EventBridge and have a completely serverless event driven media processing pipeline. And that's really powerful. Now in terms of selecting between the two, Elastic Transcoder is legacy. You should default to media convert. There's almost no situations where you'll pick Elastic Transcoder over media convert. Media convert supports more codecs and it's designed for larger volume and parallel processing. And it also supports reserved pricing. So you can get access to this capability in a much more cost effective way when you use media convert versus Elastic Transcoder. The situations where Elastic Transcoder may be required is if you need to use the WebM, so VP8 or VP9 formats. If you want to use animated GIFs as part of your workflow, so converting things into animated GIFs. Or if you want to use MP3, FLAC, Vorbis or WAV. Any of those require. Elastic Transcoder. For everything else, use media convert. It should be your default. There should be almost no scenarios where you would pick Elastic Transcoder over media convert. What's critical for the exam is that you need to understand the types of architectures or the types of scenarios where you would use media convert. And that's what I detailed on the previous screen. So any media processing workflows where you don't want to manage the process, you want it delivered as a service where you can simply log a job, point it at source media on an S3 bucket and have it bill you only for the processing time used. That's what's provided by media convert. At this point though, that's everything you need to be aware of. So go ahead, complete this video and when you're ready, I look forward to you joining me in the next.