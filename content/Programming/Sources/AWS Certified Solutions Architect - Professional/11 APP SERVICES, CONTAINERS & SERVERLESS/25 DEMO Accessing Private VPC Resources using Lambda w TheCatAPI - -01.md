 Welcome back, this is part 2 of this lesson. We're going to continue immediately from the end of part 1, so let's get started. Now before we create the Lambda function, we need to create an EFS access point, which is how Lambda functions can connect to EFS. To do that, click on Services, type EFS in the search box at the top, and then click to move to the EFS console. Once we're there, click on File Systems, and then click on the EFS forward slash Lambda VPC file system. We need to create an access point that the Lambda function will utilise, so go ahead and click on Access Points, and then Create Access Point. Now for name, go ahead and enter Cat Downloader, which will be the name of the Lambda function that we're going to use. Now optionally, you can specify a root directory path, and what this means is... is anyone connecting to this access point using the root path will be directed at whatever's specified in this box. Now the default is just forward slash, so no translation is applied. We want to make sure that if the Lambda function or the EC2 instance connect to the root point of this EFS-based file system, they both connect to the same location, and so we won't be specifying a root directory path, and it will default to forward slash. Next, we need to specify the POSIX user, which will be used as an identity for any accesses via this access point. So essentially, for anything using this access point to access the EFS-based file system, they will use this user ID and this group ID. Now we're just going to use some simple values, so I want you to enter 1000 for the user ID, 1000 for the group ID, and then scroll down, and we need to specify... the root directory creation permissions, and again, we're going to use the same details. So 1000 for the owner user ID, 1000 for the owner group ID, and then for permissions, 0777. We don't need to worry too much about the permissions, because this is only a demo lesson. If you were integrating this into production, you'd need to have much more of an idea about the POSIX user IDs and group IDs that your system was using, and this would take much... much more in the way of integration, because this is a demo lesson that is beyond the scope of this demo. I simply want you to understand the architecture. Now once you've entered all of that information, scroll all the way down to the bottom and create the access point. And the access point, as a reminder, is the thing that the Lambda function uses to connect to this EFS-based file system. Now we've got the access point created, we can go ahead and create the Lambda function. So click on Services, and then just type... Lambda into the search box, and then click to move to the Lambda console. Once we're there, we're going to create a Lambda function. It's going to be a function that we're going to author from scratch, so leave author from scratch selected. The function name is going to be cat-downloader. For runtime, it's going to be a Python 3.9 function, so select Python 3.9. We're going to provide it with some permissions, so expand Change Default Execution Role, and then choose Use the function. Use an existing role. And then click in the existing role dropdown, and select Lambda VPC-Lambda role, and then there will be some random, that's perfectly fine, just select this role. Once selected, go ahead and click on Create Function to create the Lambda function. Now because this Lambda function is going to be downloading some cat images, we need to make sure that the function can run for as long as it needs to. So click on Configuration, and then General Configuration, and then click Edit. We're going to change the function timeout from 0 minutes 3 seconds to 1 minute and 0 seconds. So this means that this function can invoke and then run for a full 60 seconds. So make those changes, and then just click on Save. Next, we need to change this Lambda function from being a public function to running within a VPC. And that's done down at the bottom. If you just scroll down, you're looking for the VPC. So we're going to go to the VPC section, and then click on Edit. Now we want this Lambda function to be connected to a custom VPC. So click in the VPC dropdown, and you're looking for the VPC that's called A4L VPC. It should have the CIDR range of 10.16.0.0.16. Now you might have others with that same CIDR range, but specifically look for the one that's called A4L VPC. Once you select that, click in the Subnets dropdown, and I want you to select the Subnet Private A and Subnet Private B subnets. So this Lambda function is going to run from either of the private subnets within this VPC. So make sure you've got Private A and Private B selected, and then click in the Security Groups dropdown, and we're going to allocate this Lambda function with the SGWeb Security Group. So we're going to look for the security group that's called SGWeb, and then select it. Now what this means is that when we scroll down and save these options, the Lambda service is going to create two network interfaces inside this VPC. One in Subnet Private A, and one in Subnet Private B. And it's going to do that in advance, and these elastic network interfaces will be used every time the Lambda function invokes. And this is how the Lambda function will have a VPC. So we're going to go ahead and click Save. Now this can take some time. At the top you'll see a dialog saying, Updating the function Cat Downloader, and it will be in blue and show a spinner to show that it's in progress. We need this to finish before we move on. Now this can take anywhere from being almost immediate to taking several minutes. So just go ahead and pause the video and wait for this to change to a success status. And then click Save. So there we go, it's now changed to successfully updated the function Cat Downloader. And in my case, it took about five minutes for that to take effect. So that's the networking changes that we needed to make. The next step is to configure the Lambda function to be able to access EFS. So to do that, scroll down, and below VPC, you'll see File System. So go ahead and click on Add File System. And then click Save. And then click Save. And then click on Add File System. In the EFS File System box, just go ahead and click on it, and select EFS Forward Slash Lambda VPC. Once that's selected, go ahead and click the access point dropdown and select the Cat Downloader access point that you created moments ago. And once that's selected, we need to decide on a local mount path. Now, this path is how the EFS File System will appear inside the Lambda function. So I want you to type forward slash mnt forward slash efs. So this is the path that the lambda function can use to access the efs file system. So enter that and then go ahead and click on save. Now once again you might either see that changes are being made at the top of the screen or you might see that it immediately changes to your changes have been saved. In either case you need to wait for the changes to take effect. So once you see the green dialog the next step is to actually add the lambda function code. The code that will download the cat images from the cat API. Now to update the code click on the code tab, scroll down, locate lambda underscore function dot py, right click and select open. And then delete all of the pre-existing example code. Now in the text instructions which are attached to this lesson is the source code. So I want you to go ahead and select all of the source code for this function and then just paste it into this box. Now just make sure that you do select all of the source code. You should see on the bottom it should say return and then status code 200 and then you should have a closing curly brace and then a blank line right at the end. So make sure that you do copy all of the function code. And this is the lambda function code. Which will download the cat images from the cat API. So the way that this works is every time the lambda function is invoked there's a for loop which executes a number of times determined by this number. It connects to the cat API. It downloads one cat image and then it writes it out to the EFS file system. And it uses a randomly generated file name. So it uses the UUID module to generate a random UUID. And then it appends .png onto the end. So every time this function runs it's going to output a number of cat images onto the EFS file system. So we need to test this function. So to start with click on deploy and wait for it to deploy. To test it click on test. And we're just going to create a dummy test event. So for event name just enter cat download a test. And then scroll down and click on save. And then go ahead and click on test. And now it's testing this function. And it's running this lambda function. Now if you just wait here for a few moments you should be able to see the output logs of the lambda function. You'll be able to see it connecting to the cat API and downloading all of these images. Now to show that that's working if we go to services and then type EC2 into the find services box. And open that in a new tab. You might already have a tab still open. But if you don't let's just go through the process again. Go to running instances. Select one of the services. Select one of these instances. Get the public IP version 4 DNS. And then open that in a brand new tab. Now this time when you open the URL you should see a number of random cat images. So again this website is loading all of the contents of the images folder. Which is now mounted as the EFS file system. So when the lambda function invokes it writes images out to this EFS file system. And now our web server is loading any images which are inside the cat API. So essentially every time this lambda function invokes it will add additional cat images to this folder. Now of course we want more cat images. So we're going to define an automatic schedule. So to do that go back to the AWS console. Click on services. And start typing event. And then as soon as you see Amazon EventBridge open that in a new tab. So we're going to configure an EventBridge rule. And it's going to be a scheduled rule. And it's going to be a scheduled rule. And it's going to be a scheduled rule. And it's going to be a scheduled rule. Which is going to invoke a lambda function periodically. Which is going to invoke a lambda function periodically. So once that's loaded click on create rule. Now we are going to call this rule. Cats every 2 minutes. And that needs to be all one word with no spaces. And that needs to be all one word with no spaces. Now the rule type we're going to choose is schedule. So select that and click next Choose a regular rate. Choose a regular rate. And we're going to have this rule every 2 minutes. So enter 2. And then select minutes in this dropdown. So every 2 minutes this rule is doing the same thing. will be called and we want the target of this rule to be our lambda function. So click next and under select targets make sure that lambda function is in the target drop down and then in the drop down below it the function drop down go ahead and select cat downloader. So this means that our lambda function will be invoked every two minutes. Now that you've configured that click on next, next again and create rule. Now once this rule has been created it will be executed every two minutes and the target of this execution is the cat downloader lambda function. So every time the lambda function is invoked by this rule it will download additional cat images to the EFS file system and as they're downloaded they'll be available from either of the web servers. So at this point just go ahead and pause the video and wait for a multiple of two minutes. Either two minutes, four minutes, eight minutes or ten minutes and once you've waited that time you can resume the video and then we can continue. Okay so in my case I waited about six minutes and now if I return to one of these tabs which I had open earlier and just do a refresh now we should see that we've got loads of cat images. We've not got enough because we can never have enough cats but we can see that every time the function executes it's downloading more and more cat images. the EFS file system and this web application is loading them all in on this page. So in theory you could leave this lambda function running for an infinite amount of time, it would keep invoking, it would keep storing the images on the EFS file system and every image would be loaded by this simple web application. Now we're not going to do that because as everybody knows cat images are addictive and so we're going to take the opportunity to safely end the demo lesson at this point. So to clear up the demo lesson all we need to do is go back to EventBridge, click on the hamburger menu on the left and select rules, select the cats every two minute rule and then select delete and delete again to confirm. Then we need to go to the lambda console, click on the hamburger menu and select functions, select the lambda function, click on actually. And then select delete and delete to confirm. Then we need to click on services, type EFS and move to the EFS console, click on file systems, click on the lambda VPC file system, go to access points, select the access point and then click on delete and then click on confirm to confirm that deletion process. Now there is a slight annoyance with lambda functions when they use VPC networking and that's that when you're using a VPC network, you're not going to be able to use a VPC network when you delete the lambda function, it doesn't always immediately delete the network interfaces that it creates in the VPC. Now just to check, click on services, type EC2 and then click to move to the EC2 console. Once you're there, scroll down and select network interfaces under network and security. Now you should see a number of network interfaces here and that's fine, most of these will be cleared up when we delete the CloudFormation stack. But if you just scroll to the right, in the description column, we're looking for any which say AWS space lambda space VPC space ENI hyphen cat downloader. If you see any with that name, you need to pause and wait for them to vanish from this list before moving on. Assuming you're at the same point as me where you don't have any with that in the description, then you can click on services, type cloud and then click on CloudFormation to go to the last step, which is the cloud. On this point, I'm going to double check the stack, and if you see any of them, then you've got a new one that's been added, which is to delete this lambda VPC stack. So check the box next to that, click on delete and then click on delete stack to confirm. And at this point you've finished all of the tasks within this demo lesson and you've finished tidying up the environment so it will be in the same state as it was at the start of this demo lesson. So I hope this has been fun and I hope you've gained some good practical experience of how to deploy a lambda into a VPC and to the EFS file system. So this is the architecture that you've implemented within this demo lesson. But that is everything that I wanted to cover in this demo lesson. So go ahead, complete this lesson, and when you're ready, I look forward to you joining me in the next.