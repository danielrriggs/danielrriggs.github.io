 Welcome back, and in this lesson I want to talk about a related topic to lambda versions, and that's lambda aliases. Also, I don't know why this icon for this lesson is an alien, so let's just ignore it and jump in and get started. An alias is a pointer to a function version. Remember, you can't change a function version. It's immutable. It has an ARN which points directly at that version. It's called a qualified ARN. That ARN never changes. It's fixed, so it's just like the version. It's immutable. But an alias allows you to create a pointer. So, for example, we might create an alias called prod, which points at the best animal function version 1, and another alias which points at best animal version 2. And each of these aliases, so prod and beta, would have a unique and fixed ARN for the alias. But, and this is where aliases become useful, they can be updated. So the prod alias could be changed whenever required to point at version 2 after the beta period has concluded. Aliases can be changed to reference different versions of our function. And this is useful for things like prod and dev, blue-green deployments, or even A-B testing. Services which use lambda can point at an alias rather than a function or function version, which means that those services never have to be changed. Instead, the alias can be changed as part of deployments to point at different lambda function versions. And this can form part of a normal CI-CD process. So this is really important to know for real-world usage. Now, you can also use alias routing. And this is where you create an alias, but rather than have it be fixed, pointing at a specific version, you can define a distribution of requests. For example, sending a certain percentage of requests towards versions, for example, sending a certain percentage of requests towards versions, for example, sending a certain percentage of requests towards versions, for example, sending a certain percentage of requests towards versions, for example, sending a certain percentage of requests towards versions, and a certain percentage towards version 2. And this can be useful to test out new features, or as part of a beta test. Now there are some restrictions for alias routing, because both versions must have the same execution role, they need to have the same dead letter queue, or no dead letter queue, and both versions must be published, you can't use $latest. So visually, this is how it looks. We start with the lambda function, which we've published to version 1, We start with the lambda function, which we've published to version 1, and an update of this same function, which we've published to version 2. Now we create aliases, let's say prod, which is pointing at version 1, and this alias gets its own unique ARN. Next we create an alias of dev, which currently points at version 2, and this also gets its own unique ARN. Now aliases can be static, so pointing at one version at a time, and in this configuration we might have something like an API gateway sending 100% of the requests to the prod alias. Now as discussed, the benefit is that services like API gateway can be configured to point at an alias, and if we want to change that configuration, rather than having to update API gateway, we can just change the alias. In this example, we might want to change the prod alias to point at version 2 instead of version 1. Now we can also create a dedicated routing alias, such as abl. This could send 90% of requests to version 1 and 10% to version 2, and this might form part of A-B testing. Additionally, we could update an existing alias to do the routing. Again, this could form part of a normal A-B testing procedure. Now this is all of the theory that I wanted to talk about with aliases, but at this point I think it's time for a demo. So in the next lesson, which is going to be a demo, I'm going to step through a simple example of using versions and aliases, and you can either just watch or you can follow along in your own environment. But at this point, that is everything I wanted to cover in this lesson, so thanks for watching. Go ahead and complete this video, and when you're ready, I'll look forward to you joining me in the next.