 Welcome back, and in this walkthrough lesson, I just want to give you some practical visibility of how to work with Lambda aliases and Lambda versions. Now this is a walkthrough demo, so you can either watch me perform these steps, or if you want to, you can perform them within your own AWS environment. And if you are going to use your own AWS environment, then I have included all of the code snippets attached to this lesson. Now to make sure everything goes without a hitch, I'll need to be logged in as the IAM admin user within the general AWS account, so that's the management account of the organization. And as always, I'll need to have the Northern Virginia region selected. Now to get started, I'm going to move across to the Lambda console, so I'm going to type Lambda in this search box and click to move to the console. Now once I'm here, I'm going to create a function, I'm going to author it from scratch, the function name is going to be bestanimal, and the runtime is going to be Python 3.9. Now I'm going to leave everything else as default, because the default execution role that every Lambda function gets does come with basic permissions, enough at least to log into CloudWatch logs, and we don't need this function to interact with any other AWS services. So I'm going to leave everything as default and create the function. Once the function is created, I'm going to scroll down, right click on Lambda underscore function and select open. And this will display the function as default. And this will display the sample code, and I'm going to delete the sample code and paste in the code snippet that's attached to this lesson, which is titled version one. So this is a simple function, just make sure that you are using version one, it should have a comment here called version one. Now this is pretty simple, it doesn't do anything special, it just prints out two status messages. The first one is accessing knowledge, and the second one is just informing the world that cats are the best animals. And the last thing is it returns a done statement. So what I'm going to do first is to deploy this code and then to test it. So to test it, I'm going to click on test. If we do it this way, we'll need to provide a sample event name, so I'm just going to use test and click on save. The new version of the UI allows you to do this directly. We could have just gone straight to the test tab and then click on test. And that would have avoided the need to create this test event as a separate part of the process. So now to test this function. I'm just going to click on test. We'll see the execution result is succeeded. If I expand the details, we'll see this return status message of done. And if I scroll down directly below that, I'll be able to see a log output from this execution. It will list all of the messages that were output within the function, as well as giving us the duration and build duration. So that shows us that this function works without any problems. Now let's say that I want to take this function and create a version. So what I'm going to do is go back to code. I'm going to close down the execution results and select the entire bottom line. So it says return space and then speech mark done exclamation exclamation speech mark. And I'm going to delete that. Instead, I'm going to type this. So return space speech mark code version one and then another speech mark. And once I've changed that, I'm going to deploy it. And that's going to make it really easy for us to see what's going on. So now we can see that this is actually version one of the code. So now if we run a test, if we go to test, click test, expand the details, we can see this is version one. Now to make it really version one, we need to create a version. So to do that, I'm going to go to versions and then publish a new version. For version description, I'm going to say cats are the best and then some exclamation marks and click on publish. And at this point, we now have version one. So if I click at the top just to move back to the function, this is the main function. Let's look at the ARN here. We'll see ARN, Lambda, US East 1, my account number, function and then best animal. So this is the ARN. This is the unqualified ARN that references the function. And specifically, it references this editable piece of code, which is referenced also with dollar latest. Now what I can do is go to version. And then select a particular version of this Lambda function. So let's say, for example, version one. Now we see that this has a qualified ARN. So this is an ARN that references a particular version of this function. So we'll see the same ARN structure. It will have function, the function name, but then it has colon one at the end. So this means that it references a particular version of this Lambda function. Now I can perform the same tests on this function. So I can click on test, click test. And it would output what we expect. Code version one. And if I scroll down, we'll see the status messages that we saw previously. But if we click on code, we're not able to edit the code for a version. So we can open it, but we can't make any changes. Because a version is immutable. You can't change a version of a function. You can only edit the unqualified function. So when you don't specify a version. So let's go back to the unqualified function. If we click on edit code, we're taken away from the version. And we go back to the unqualified function. So this is the thing that we can edit. So to edit this, I want to go ahead and paste in version two of the code. So go ahead and select all of this sample code and delete it. And paste in the second code snippet. So this one is version two. And this is where we say accessing knowledge. Cats are the best. Doggos are also okay. So this is the more inclusive version of this function. And we can click on deploy. And when we do this, this is deployed out as the unqualified function. So now, $latest will be pointing at this rather than a specific version. So when you deploy something out, it deploys it out to the unqualified function. And $latest, or when you don't specify a particular version, they always point at the latest saved deployed version. So in this particular case now, if we go to test and then invoke the function when we don't have a version selected, then it will be outputting this new inclusive version. So Doggos are also okay. Now we want to create a second version of this function. So go back to code. Make sure you've got the lambda function itself open. And then on the last line, so press enter, make sure that you're directly below the last print statement at the same level of indentation. And just add this. So return space code version 2 contained in speech marks. And we're going to deploy that. So again, this becomes the latest deployed version of this lambda function. So $latest. And if we go to test, click test, and then expand this, now we'll see code version 2 and we'll get the new inclusive output. So the one that contains Doggos are also okay. And now I want to create another version. So I'll go to versions. I'll publish a new version. And for version description, I'll call it inclusive and then click on publish. And now we have a version 2 of best animal. If we go back to the function and go to versions, we'll see we have version 1 and version 2. If I click on version 2, go to test and invoke it, and then expand the details, we'll see code version 2. And if I go to the unqualified function and then do an invoke, this will use the latest deployed version, which at this point is still the same code as version 2. But note that these are different things. Version 2 is now immutable. It's fixed. It can't be changed. But the code in the latest deployed version of this function is the same code. But they could be different. The only thing we know for sure is that a version is fixed and immutable. But $latest could contain any code. If we make any changes and deploy them, $latest will contain those changes. And that's why versions are so important. Because they're fixed and immutable. So now I have those two versions, so version 1 and version 2. I'm going to create some aliases. So I'm going to click on the aliases tab and create an alias. I'm going to call this alias prod. And then I'm going to point it at version 1 of the lambda function. So let's treat this as the production version. And click on save. So this alias now points at version 1 of the code. And we could run tests on it just the same as if we were testing a specific version. So if I do an invoke, this is going to invoke the prod alias. Which is pointing at version 1 of the code. So it shows version 1 and it doesn't have our inclusive message. So I'm going to go ahead and create another alias. So go back to the unqualified function. Click on aliases. Create alias. This time I'm going to call this one dev. And the dev alias will be pointed at version 2. So I'm going to click on save. And then again if I run tests. So I'll go to the test tab. And invoke this dev alias. It's going to be the inclusive version 2 of our code. So it's got doggos are also okay. So now prod matches version 1. And dev matches version 2. So let's say now that we're doing a beta test. We've had some beta testers who've been working on the dev alias. And they're reporting no problems. The function version is working as expected. And we now want to move our user base from the prod version. Which is currently version 1. Slowly across to version 2. Which is currently the dev version. We want to perform a slow migration from the prod version of the code. To the dev version of the code. And the end result is going to be that the current dev code becomes the prod version. So how do we do that? Well what we can do is go back to the main unqualified function. And we can go to aliases. And we're going to go into the prod alias. Which remember is currently statically. Statically pointing at version 1 of the lambda function. And I'm going to click on edit. Then I'm going to expand weighted alias. And I'm going to specify an additional version. And this additional version is going to be version 2. Which is currently the dev code. So I'm going to select version 2. And now I'm going to specify a distribution of these weightings. Now if this was actual production usage. What I'd probably do is specify initially a really small weighting for version 2. And make sure that the user base didn't experience any issues. And then I'd gradually increase this from 5 to 10. Then maybe to 20. And once I gained confidence I'd start increasing it quicker. For this demo though I'm going to change this to 50. So now we have a 50-50 distribution of requests. Between version 1. Which is the current production version. And version 2. Which is the current development version. So I'm going to save that. And now this production alias. Will be distributing 50% to each of the Lambda function versions. So version 1 and version 2. If we click on test for this particular alias. And invoke it. And then expand details. We can see initially version 1. Let's invoke it again. Now it's version 2. Let's invoke it again. It's version 2 again. Let's invoke it again. Version 1. Invoke again. Version 1 again. Over time this represents a 50-50. Split. And this can help us gain confidence. That we're happy with the stability of version 2. Of this Lambda function. Now when we are happy with this. What we can do is go to configuration. And then edit this alias. And then instead of specifying a weighted alias. What we could do is change additional version to none. So this removes the additional version. And then change the static version that this alias points to. From version 1 to version 2. And click save. And now any requests for this alias. Will now go to version 2 of the code. So if we click test. And then expand the details. Now 100% of the requests will go to version 2. And we can use this as part of either blue-green deployments. Or A-B testing to help us add stability and confidence. Before we do a migration from one version to another. We can do it either as a big bang approach. But use two different versions. Or we can gradually move across in a weighted fashion. And this has been a very simple example. But you can extend this for real world production systems. Now that's everything I wanted to cover. So just to clean up. I'm going to go back to functions. And if you did this in your own environment as well. Select the best animal function. Click on actions. And then select delete. And once you've done that. The environment is back into the same state as it was at the start of this demo. And we've done everything that we need to do. But at this point that's everything I wanted to cover. So thanks for either watching or following along with this in your own environment. But at this point you can go ahead and complete the video. And when you're ready. I look forward to you joining me in the next.