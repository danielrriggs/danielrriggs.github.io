 Welcome back, and in this brief walkthrough demo lesson, I just want to give you some practical experience of creating a simple API using API Gateway and Lambda. Now to get started, if you are following along with this in your own environment, then as always, you need to be logged in as the IAM admin user inside the general AWS account, which is the management account of the organization, and you'll need to have the Northern Virginia region selected. Once you've done that, I'm going to ask you to open two different tabs. The first one is to API Gateway, and the second one is to Lambda. So I'm going to click in the search box at the top and type API Gateway, open that in a new tab, and then do the same for Lambda. So type Lambda, open that in a new tab, and that's going to give us access to both of the products that we need for this demo. Now we're going to be creating an API which has two different resources, the root resource, and then a resource that's going to be used to create the API. So this is going to be our own implementation of the CAT API, which is going to return some CAT-related information. Now we're going to be using two Lambda functions to provide the compute functionality for this API, and both of these are attached to the lesson and also included in this lessons folder on the course GitHub repository. There are two files, API underscore root underscore resource dot py, and then API underscore list. CATs underscore resource dot py. Now these are really simple Lambda functions. They don't do a great deal. They import two modules. They print the event structure that they receive when they're invoked, and then they create a response to send back to the API Gateway. In the case of the root resource, that's a message saying CATs are the best, and in the case of the list CATs resource, this is going to return an array, so a list of four of my CATs, and we're going to pretend at this point that this is going to be a list of four of my CATs. This is pulling the information from a database. So let's get started and create our API. So to do that, we need to move to the API Gateway console. Now depending on whether you've used this before, you might see various different styles of UI. It might give you this style where you're asked to build or import one of the types of UI, or you could see a getting started user interface. What you need to do is get to the point of where you can create a REST API. So we need to get to the point of where you can create a REST API. So we need to get to the point of where you can create a REST API. So we need to build a REST API. So I'm going to click on Build under REST API. Now you might see this screen if this is the first time you use API Gateway. So either click on Build under REST API, or you'll see this screen potentially if it's the first time you're using the product. So at this point, we'll click OK to dismiss that dialog, and we're going to create a brand new API. Other options you have are to import from Swagger or OpenAPI 3, or use an example API. And we'll be covering importing from Swagger or OpenAPI 3 later in this section. For now, though, we're going to create a new API. Now for API name, we're going to call it MyCatAPI, and then we can put the same for description. Now you're able to pick from three different types of endpoint. Regional, which is the one that we're going to use, Edge Optimized, which is where the API is deployed out to the CloudFront network, and I'll be talking about what benefits and limitations this provides elsewhere in the course. And then we have Private, which is where the API is only available within a VPC. Now for this demo, as I mentioned, we're going to use Regional, so I'm going to select that, and then click on Create API. So this creates the API. This gives us an endpoint to be able to access this API, and it gives us an API configuration. So this is where we can create all of the different options for our API. And I'm going to be going into much more detail, about each of these in dedicated lessons as we move through this section. Now what we have to start with is the root resource. So I mentioned in the previous theory lesson how resources are essentially parts of a hierarchy, or parts of a tree, for our API configuration. And the only thing that we have to start with is forward slash, which is the root resource. Now we are going to utilize that. We're using two resources, the root or forward slash resource, and then forward slash link. So we're going to create the configuration for both of these. Now before we actually create this though, what we'll need to do is to configure the Lambda functions, which will provide the compute functionality for both of these different resources. So I'm going to move across the Lambda console, and just before you do anything, just make sure that if you do see a Use Updated Console toggle on the left hand side, make sure that it's set to Use the Updated or Preview User Interface. Now if you don't have this option, it's possible that you might be watching this video once AWS have finished the UI changes. But if you do see this, make sure that it is selected to On, to use the updated console, which is in Preview. Now we're going to create two APIs, so click on Create Function. We're going to author it from scratch. We're going to call it API-Root-Resource. It's going to be Python 3.8. And then we can accept the rest of the defaults, and click on Create Function. And then once it's created, just scroll down, right click on Lambda-Function.py, and select Open. We need to delete the sample code for this Lambda function, and then we're going to paste in the API-Root-Resource.py code. So this is the code for the Lambda function, which is backing the Root resource. So copy this into your clipboard, and then paste it in to this Lambda function. Once you've pasted it in, go ahead and click on Deploy, and once deployed, you can click on the Test tab at the top, and then click Invoke. And this will just test this function. If we expand Details, what you should see is this response. So this is returning the response that the API Gateway requires. So it should say Status Code 200, Body, Cats are the best, and then for Headers, it should show Content Type, Application, forward slash JSON. And that's what we need. That means this is set up and ready to go. Next, let's go back to the Lambda function, and then we're going to go back to the Lambda console, and create our second function, which will back up the ListCats resource. So click on Create Function, Author from Scratch. This time we're going to call it API-ListCats-Resource. Change the runtime again to Python 3.8, and create the function. While that's creating, let's prepare the code. This time we're going to paste in API-ListCats-Resource.py. So copy all of that into your clipboard. Move back to the console, right-click on lambda-function.py and open. Delete the sample code, paste in what you just copied into your clipboard, deploy that, and then we're going to test it again. So click on Test, and then Invoke. And this time, we're getting a different response. This time we're getting a list of my four cats, Ruffle, Truffles, Penny, and Winky. So this is going to back up the ListCats resource, logically listing my cats. So now let's go back to the API Gateway console, because we have our two backing lambda functions that will provide the compute for our API. So go back to API Gateway. We already have the forward slash, or the root resource. So what we need to do is to create a method within this resource. Remember, it's the method that controls how the client interacts with this resource. So we need to create a get method, because this is how our browser will interact with this API. So we're going to go ahead and click on Actions, and then Create a Method. Once you do, you'll see a drop-down. Click on the drop-down, and we need to select Get, because we're creating a get method, and then click on the tick to create that method. Now it's the method where you configure the integration. So we're configuring the link between the resource and our integration using a method. So when our client accesses the forward slash, or root resource, using get, then they will use this configuration. So they'll interact with the Lambda function that we're going to configure. So the integration type can be one of these options. So a Lambda function, HTTP. We can configure a mock integration type if we're wanting to perform some testing. You can directly integrate with certain AWS services, or use VPC link. Now we're going to use Lambda function. You can set to use Lambda proxy integration, and when you set this, it essentially means that any of the information that API Gateway receives, it's just going to pass on to Lambda as is. It's not going to perform any manipulation. And at this stage, to keep things simple, we're going to select this option. So check Use Lambda Proxy Integration, and then in the Lambda function box, we need to select the Lambda function that we just created for the root resource. So type API, and you should see two Lambda functions. API-root and API-listcats. And we're going to pick the API-root-resource Lambda function. So select that, and then click on Save. At this point, you're going to get a permissions prompt. API Gateway needs to add itself onto the resource policy for the Lambda function. So the resource policy defines what can invoke the Lambda function. And in order to configure this integration, API Gateway needs to have the permissions to invoke the Lambda function. Now API Gateway can do this on your behalf. All we need to do is click OK. So that's what we're going to do. And at that point, we've configured the integration between this root resource and the Lambda function when using this get method. And that means that this API Gateway can now interact with Lambda. Our clients can connect to the API, perform a simple action, and get a response. Now in order for this to take effect, it needs to be deployed. Remember I talked about stages being units of configuration, and I'm going to be covering stages and deployments. In much more detail in a dedicated lesson, which is coming up later in this section. For now though, let's just perform a simple deployment. So click on Actions, and then go down to Deploy API. This is going to deploy this API configuration, so all of this at this point in time, into a stage that we define. So click on Deploy API. There are no current deployment stages, so we're going to create one. So click on New Stage. For stage name, type Dev. For stage description, type Dev. And click on Deploy. And this will deploy this API configuration out to our Dev stage. And now we have an invoke URL for this particular stage. Now if we use the invoke URL without anything on the end of the URL, then it's going to use the root resource, so forward slash. So let's go ahead and do that. Let's open this in a brand new tab. If we go to this, we'll see an output, cats are the best. And this output is being provided by the user. So let's go ahead and do that. Let's open this in a brand new tab. And this output is being provided by the Lambda function that we just created. So if I go back to API hyphen root hyphen resource, and then look at the code, I'll just scroll down, this is the code that's providing the output for that API. So we're invoking this Lambda function, and it is providing the output, the functionality, the compute for this API. Now we can see exactly what's going on. If we click in the search box, and then we type CloudWatch, and open CloudWatch logs, remember, all Lambda functions, if they have permissions, will output to CloudWatch logs. So if we go to log groups, we're looking for forward slash AWS, forward slash Lambda, forward slash API hyphen root hyphen resource, and if we open that, click on that log stream, and we'll see an output of what this Lambda function actually did. So we'll see the first test invocation that we did, and we can expand and see this test event that's been passed into the Lambda function. What we should also see, is that we can see that the Lambda function is being passed into the Lambda function. So if we expand this, we'll see a copy of the full event that was sent to Lambda by the API gateway. Now we can see a lot of useful information inside this. We can see x forwarded 4, which contains the IP address I used to access API gateway, together with the port that's used, so port 443, because this is HTTPS. And we can see all the different details that we passed in as part of the get request to the API. So all of this is forwarded as is to the Lambda function, and the Lambda function can decide what to do with that, and then generate a response. So this is how the functionality has been provided to this API. Now let's expand this and use the other Lambda function. So let's go back to API gateway. So if we go ahead and click on resources directly under my cat API, and then click on actions, we're going to create another resource. We're going to call the resource, list cats, all one word, and that should create a resource with the path of forward slash list cats. Now we don't have to do anything else, just go ahead and click on create resource. That creates the second resource, which is forward slash list cats, and we need to create a method in order to use this. So make sure list cats is selected and click on actions and then create a method. We're going to use get again, so select get, and then click on the tick to create this. We're going to integrate this again with another Lambda function, so we'll need to select use Lambda proxy integration. So check that box, make sure Lambda function is selected, and then in the Lambda function box, just type API, and this time select API hyphen list cats hyphen resource. So this is the other Lambda function that we created. Now we can click on save. It will need to provide itself with permissions to invoke this Lambda function, so click on okay, and that creates this save function. Now we can see that we've created a second method. So now we have the forward slash or root resource with the get method, and we have the forward slash list cats also with the get method. Now one thing I want to demonstrate is the way that we would access this list cats resource is if we open up the page where we have the API open. If at the end of this we just add forward slash list cats, this is how we get access to this resource, the forward slash list cats resource. But if I try and open that right now, we're going to get an error. It's a fairly nondescript error. It says missing authentication token, but the reason for this is that we haven't deployed the updated API configuration. So our dev stage does not actually have this configuration. Remember how I said it was a common mistake that students made to change API configurations and not deploy them. Well, that's what we've just observed. So to correct that, we need to deploy this API configuration. So click on action. And then we're going to deploy it again to the dev stage. So select dev and then click on deploy. And this now deploys out this updated configuration to our dev stage inside our API. If we click on stages and then click on dev and open our invoke URL in a new tab, and then again at the end we add forward slash list cats and press enter, now we'll see an output from this second Lambda function. So this provides a list of my four cats, Ruffle, Truffles, Penny, and Winky. So now we've invoked the second Lambda function. If we go back to CloudWatch logs, go to log groups, click on the API hyphen list cats hyphen resource, click on the latest log stream. In my case, it's the top one. And we'll be able to see that we've had an output from this Lambda function. This Lambda function has been invoked because we've just browsed to this forward slash list cats resource. And we're using the get method. So that's how we can configure a very simple API. Using API gateway. Now at this point, that's everything I wanted to cover in this walkthrough lesson. We're going to be covering all of the different topics in much more detail as we go through them in all of the different lessons in this section of the course. But for now, I just wanted to give you a high level overview of how you can create an API using Lambda integrations. Now at this point, just to clear up, I'm going to delete the API that I've created. And if you've done this in your environment as well, then just click on APIs and select the API that you want to use. And then I'll select the API that you've created from this list. It should be called my cat API. Click on actions, delete, and then confirm the deletion. And that's the account back in the same state as it was at the start of the lesson. At this point, go ahead and complete this video. And when you're ready, I'll look forward to you joining me in the next.