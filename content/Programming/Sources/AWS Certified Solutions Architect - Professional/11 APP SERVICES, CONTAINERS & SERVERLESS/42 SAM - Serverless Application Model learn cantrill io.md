 Welcome back, and in this lesson I want to talk about the AWS serverless application model known as SAM. Now SAM is an open source framework that you can use to build serverless applications on AWS. I want to use this lesson to step through the theory of how it works, and in the next lesson, which is going to be a demo, you're going to get the chance to implement a simple SAM-based serverless application within your own environment. So let's jump in and take a look at the theory of AWS serverless application model. Now before we talk about SAM specifically, I think it's worth just pausing to understand what a serverless application is. It's not just Lambda functions. They're part of it, but it's much, much more. A serverless application is built from many different components. Most of them include front-end code and assets which run in a browser, but are delivered using S3 and CloudFront. This code connects to an API endpoint, and this is generally provided via the API gateway product within AWS. And these APIs are backed by compute, which is generally provided by Lambda. And this might use database services provided by DynamoDB, and then there are event sources, permissions, and much, much more. A serverless application consists of one or all of these, and managing them all in a coherent way is very challenging. And this is what SAM helps us with. The serverless application model, or SAM group of products and features, within AWS actually has two main parts. The first is the AWS SAM template specification. And this is a way to define serverless applications, and it's actually an extension of CloudFormation. It adds components to a CloudFormation template, which are designed to be able to be used in a serverless application. And this is designed specifically for serverless applications. So we've got transforms, globals, and an extended resources section, which can include normal CloudFormation resources, as well as SAM specific ones. Now there is going to be a demo following this lesson, where you'll get to experience this first hand. For now, I just want you to understand the architecture. The next component is the AWS SAM command line interface, so the SAM CLI. And you can use this to build serverless applications. So we've got a demo of this, and we're going to get to the first part of this lesson. So we've got a demo of this, and we're going to get to the first part of this lesson. So we've got a demo of this, and we're going to get to the first part of this lesson. The CMS has serverless applications, which are defined by AWS SAM templates. The CLI provides commands which enable you to verify that the AWS SAM template files are written according to the specification. The CLI allows you to invoke Lambda functions locally for testing and debugging. And then you can use the CLI to package and deploy serverless applications into AWS. The key thing to understand, is that a SAM template applies to you as a whole, So there's a whole set of different possibilities餐 to deploy serverless applications into AWS. The key thing to understand is that a SAM template applies to you as a whole, template defines the application. It's like CloudFormation in that it creates resources, so S3 buckets, DynamoDB tables, Lambda functions, API gateways, but it points at local resources, so local code which can be used to create Lambda functions. Now this code can be packaged up with the template and deployed into AWS. It makes it really simple to work with serverless applications within AWS, and that's what you're going to experience practically in the next demo lesson. Now visually, this is how a typical SAM architecture might look. We start with a CAT-based application, and its code is stored in a GitHub repository. And this is synced down to a local repository as part of the normal development process. So this folder structure contains the code for your application, which could be one or many Lambda functions, and it contains the SAM-template.yaml file, which is the enhanced CloudFormation template, the SAM application template. Now once you have this created, and you could do this manually, or you could use the SAM init command within the CLI to create this structure, which you can then populate with your own assets, well you can use the SAM CLI, specifically the SAM-package command, and this packages up all of those resources, so the template file and any local assets, and it stores them within an S3 bucket in AWS. It also returns a SAM-deploy.yaml file, which contains pointers to the AWS location of all of the assets. And this deploy file is ready to be deployed into AWS, and that's what happens with the SAM-deploy command of the CLI. This command loads in the SAM-deploy.yaml file, and it creates resources within AWS, using CloudFormation, and the assets packaged up earlier and uploaded to the S3 bucket. This end-to-end flow is handled by SAM and the SAM CLI. Now this process can be much more complicated and involve code deploy, and more complicated deployment architectures and versions, but from an architecture point of view, this is what you need to know. So this is how you can use SAM and the SAM CLI to develop and deploy SAM-based applications. Now in the lesson following this one, there's going to be a demo of the SAM CLI, where you're going to get the chance to experience this practically. You're going to package this application up, you're going to deploy it into AWS, and then get a chance to interact with your own brand new serverless application. For now though, go ahead and complete this lesson, and when you're ready, I look forward to you joining me in the next.