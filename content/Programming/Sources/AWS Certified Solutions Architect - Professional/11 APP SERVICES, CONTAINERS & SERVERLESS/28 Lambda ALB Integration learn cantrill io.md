 Welcome back, and in this lesson I want to talk about Lambda and application load balances. There are a few ways that you as a user can interact with Lambda. You can invoke it directly in some way. You can interact with it when you do something in another service which generates an event, so when you upload an object to S3 and it triggers a Lambda function. You can also interact with it indirectly using API Gateway, which you've experienced with the pet Cuddle-a-tron demo elsewhere in the course. Finally, you can interact with it indirectly using an application load balancer. Now I'm going to be covering API Gateway in more depth elsewhere in this course. In this lesson I want to cover using Lambda with application load balancers, so let's jump in and get started. Let's start with an example architecture. We have a user on the left, an application load balancer in the middle, and then a target group on the right, which has a Lambda function within it, the animal search function. This function, lets Bob on the left search for animals by name or ID, and it retrieves information about those animals. Communication between Bob and the application load balancer uses either HTTP or HTTPS. There's no difference from the client side versus using an EC2 instance as the compute backing for this architecture. From the client side, it's still just HTTP or HTTPS. Now, the load balancer, is being configured with the target group containing Lambda functions. And so when it receives a request from the client side, it's synchronously invokes the Lambda function. And this means it invokes the Lambda function passing in some data and waits for a response. The load balancer passes in a JSON structure into the Lambda function inside the event structure, which every Lambda function receives. So the Lambda function needs to interpret that and generate a response. What's actually happening is that the application load balancer is translating the HTTPS request into a Lambda compatible JSON structure on the way to the Lambda function. And then the Lambda function responds with JSON, and this is translated back into HTTPS by the application load balancer on its way back to the client. So from the client perspective, there's no difference versus a normal EC2 based compute backing. The ALB performs a translation, and the Lambda function needs to know how to send what's basically bits of HTML within JSON back in return. And visually, this interaction looks like this. This is an example of the JSON, which the Lambda function is given by the application load balancer. Now, it's cut down a little with some placeholders, but the structure is the same. We have details of the target group the request is routed via. Any query strings included with the request are also included. In this case, a subject equals ROFL query string, and these are delivered as key value pairs. We also have headers also delivered as key value pairs in the JSON structure. The response returned by Lambda might look something like this. It contains what the content is, and then the data that we want to return wrapped inside HTML inside a JSON structure. Now, in both cases, one key value pair in the JSON indicates if the data received or sent is base 64 encoded. As a developer, all you need to do is create the Lambda function code to interpret the JSON that the application load balancer sends and create the JSON, which is returned to the application load balancer. The customer or client side will be unaware that Lambda is involved in any way. To it, it will just be a normal HTTP or HTTPS communication. Now, one more thing before I finish this lesson, and that's multi-value headers. Now, let's step through what this means because it does feature on the exam. This is the same architecture that I've been using as an example so far. This time, Bob is sending a query to the application. It's going to Categoram.io using query strings. In this case, two of them, search equals ROFL and search equals Winky. Without multi-value headers, this is what the Lambda function receives. It receives only the last value. So in this case, search equals Winky, which is translated into a key value of search and Winky inside the query string parameters structure of the event. If we use multi-value headers, we get this, an array of values for the key of search. So we group the query strings with the same key together. So search, and we have an array of all of the values, and this is delivered inside a slightly differently named structure. So we have multi-value query string parameters. The same architecture can be used for both headers and query string parameters. Now, it's not essential to know this in depth, but you should know the high-level architecture for the exam, and it might come in handy in the real world. But at this point, that's everything for this lesson. So thanks for watching. Go ahead and complete this video. And when you're ready, I'll look forward to you joining me in the next.