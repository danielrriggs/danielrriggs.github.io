 Welcome back, and in this first lesson of this section of the course, I want to talk in more depth about two parts of API Gateway, and those are methods and resources. Now I want to quickly visually step through the architecture, and then I'm going to step you through a walkthrough of implementing a simple API in the console UI. So let's jump in and get started. Let's step through a typical architecture of creating an API within API Gateway. Now some of this will be a refresher, and some of the detail might be new. So let's say that we provision an API using API Gateway, and in doing so we pick the type, and two examples are a REST API and a WebSockets API. And we pick if it's private, meaning only available within a VPC, if it's edge-optimized, so deployed using CloudFront, or if it's regional. And for this example, let's assume that it's regional. Now our API is going to have users. And one of them is Julie, who's accessing our API. Now Julie is using a URL, and this is an example. This is called the invoke URL. This URL contains a number of important components. The first is the API Gateway endpoint DNS name. The second is the API Gateway stage. And the third is the API Gateway resource. Now I'm going to step through each of these as a refresher, but also so that you're really clear on what each component is. First, stages within API Gateway. You can think of these like logical configuration objects, almost like mini API Gateways within the API Gateway. Now in this example, we're using the Dev stage, but you can create whatever stages you want. For example, prod, dev, test. You could have version one or version two. You can use them however you want, but understand they're separate, each having their own configurations. And the API is a little bit more complex. The API that you configure and manage is deployed into these stages. APIs can only be accessed and changes only take effect when they're deployed into a stage. And this is a mistake that I've known many students to make in the past. You make a change to an API configuration within API Gateway, and then notice the change doesn't take effect. In order for anything to take effect, it needs to be deployed into a stage. Now inside your API configuration, you create a new resource. For example, forward slash, which is the root resource, or forward slash list cats, which is another resource. Think of these as points in a hierarchy or points in a tree. They're the bits of functionality of your API. So with the invoke URL on the top left, Julie is using the forward slash list cats resource. If she just used slash, then she would be using the forward slash or root resource. These are both different resources and they do different things. It's the resources which our client, in this example, Julie, is ultimately interacting with. Now within each resource, you create methods. For example, get. Now other examples are put, post, delete, or options. Essentially, for each resource, you can have one or more methods. Now methods are what the client, i.e. the web browser in this case, is doing to the resource. What method it's using to interact with that resource. In this case, it's getting something. Methods are also where integrations are configured. In this example, lambda. Integrations are how you configure the link between the resource and method and the thing which provides the functionality. It might be lambda or HTTP, or it might be directly integrating with an AWS service. Now at this point, that's everything I wanted to cover in this lesson. At this point in this section, all I want to do is to introduce or refresh your memory about the core bits of functionality of API Gateway. As we move through this section, I'm going to be covering all of these different components in significantly more depth. And you're going to get the chance to experience them within your own environment as we do demos also within this section. Now in the next lesson, which is going to be a quick walkthrough, I'm going to show you how to create a simple API using API Gateway and lambda. But at this point, go ahead and complete this video. And when you're ready, I look forward to you joining me in the next.