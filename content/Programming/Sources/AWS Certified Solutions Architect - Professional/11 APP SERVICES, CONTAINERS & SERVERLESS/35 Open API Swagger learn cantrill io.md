 Welcome back, and in this lesson I want to very quickly cover Swagger and OpenAPI, and how they both relate to the API Gateway. So let's jump in and get started straight away. Let's start by getting the definition out of the way first. So, the OpenAPI specification defines a standard language-agnostic interface to RESTful APIs, which allow both humans and computers to discover and understand the capabilities of the service without access to source code, documentation, or through network traffic inspection. Now that's the formal definition. That tells you what the OpenAPI specification does. But what does it actually mean? The OpenAPI specification, known as OAS, was formerly known as Swagger. So think of them as the same thing. To be precise, Swagger... was OpenAPI version 2, and we refer to OpenAPI, or OAS, to mean the more recent version 3. Now OpenAPI is an API description format for REST APIs. It's a way of defining what an API is, what it does, how to interact with it, as well as other important information. So you can think of it in a way like CloudFormation, but instead of defining AWS infrastructure, it defines an API. So for example, it defines endpoints and resources. So for example, forward slash listcats, as well as operations on those resources. For example, get forward slash listcats. It also contains valid input and output parameters, as well as authentication methods for the API and its resources. And it contains non-technical information, such as contact details, licensing, and any terms of use for the API. API specifications are also defined as OAS. API specifications can be written in YAML or JSON, and the format is designed to be easy to learn and readable for both humans and machines. Now what matters for this part of the course is that API Gateway is capable of importing the format and generating it. So this can be useful as a backup method and for migrating configurations between different APIs. Now this is going to be far easier to show you in a quick console walkthrough, so that's what I want to do next. I'm going to go ahead and switch across to my AWS console. So this is my AWS console. I'm currently logged in as the IAM admin user of the general AWS account. So this is the management account of the organization. And as always, I've got the Northern Virginia region selected. So next I'm going to go to API Gateway. And all I've got configured within API Gateway is the MyCat API, which is the API that I demonstrated how to set up in a previous demo lesson. So if I go inside, I'm going to go to the MyCat API, click on Stages, select the Dev Stage, and then just open this Invoke URL in a new tab. You should recognize this. You'll get the message Cats the best. And then on the end here, if I just add forward slash list cats, then you'll see a list of my four cats, Ruffle, Truffles, Penny, and Winky. And this is the same API that I demonstrated creating in a previous demo lesson. So if I go back to the API Gateway tab, what I'm able to do is on the Dev Stage, which is the only stage I have configured, I can click on the Export tab, and then I have the option to export the configuration of this stage using either Swagger or OpenAPI version 3. So if I choose to export as OpenAPI 3, then I have three options. I can export as OpenAPI 3, I can export as OpenAPI 3 plus API Gateway extensions, or I can export as OpenAPI 3 with Postman extensions. Now because we're using API Gateway, I probably want to include the API Gateway extensions. So this makes sure that the file which is exported has all of the relevant configuration information that relates to API Gateway. So if I select the option with API Gateway extensions, then I have the ability to choose to export either using JSON or YAML. And if I click the button, I'll be given the option to save this configuration, which for the moment I'm just going to go ahead and cancel. Scrolling down though, I will see the output of this export. So this is the JSON file which contains all of the required configuration information for this API. You'll be able to see the information that's relevant for the API itself. So within the server section, there's a URL configuration part as well as a base path containing forward slash dev. And then under the paths configuration, we store all of the information relevant to the resources and methods. So you can see here we have forward slash listcats. It's a get method. Scrolling down further, it contains the configuration information for the integration itself. In this case, the integration with our lambda function, which provides the functionality for the listcats resource. So this is the type of configuration that you'll get when you choose to export an API configuration using OpenAPI 3. Now the YAML version of this contains the same basic information just expressed in YAML rather than JSON. We've got the same configuration elements, storing the same, server-based information, the path-based information, as well as any integrations. So if I go ahead and save one of these, let's use the JSON version, and I'm going to choose to save that to my local file system. If I want to restore this, then I can go to APIs and create a new API. But instead of building it, I'm going to import this OpenAPI version 3 file. So I'll select create API, scroll down, and under REST API, rather than building it, I'm going to choose to import it. So I'll click on import. The other options still exist, so new API, clone from existing API, or example API. But for this lesson, what I'm focusing on is the import from Swagger or OpenAPI version 3. So I'm going to go ahead and click on select Swagger file. I'll choose the OpenAPI version 3 file, which I just created. So this is this one, and click on open. You'll see the same file that I just downloaded expressed in JSON. Then I'll scroll down, and I'll click on import. And this will create a new API using this file, which I've just imported. And there we go. We can see the same resource structure. We have the root resource, which is forward slash, and then the get method with its integration, which is using the API hyphen root hyphen resource lambda function. The list cats resource together with its get method, which is using a lambda integration, this time API hyphen list cats hyphen resource. Now to use this, I could go ahead and click on actions and then deploy API. I don't want to deploy this out to the same stage. So I could use dev and then deploy. And at this point, the API configuration, which I've restored from the Swagger file is deployed out to the dev stage. And this whole API is now in the same state as it was when I took the export. I could go ahead and open this, but it wouldn't work because there is an additional step, which I need to do. Even though I've restored this configuration into the API gateway. Remember, you need to add permissions. On the Lambda resource policy to allow API gateway to invoke the Lambda functions. And this isn't something that happens automatically when you restore a configuration. So that's something which I'll be covering in a different lesson for now, though. That's everything I wanted to cover. I just wanted to give you an overview of how you can export an API configuration using Swagger or open API 3, as well as how you can use those export files to import that into a new API. At this point, that's everything I wanted to cover though. So go ahead and complete this video. And when you're ready, I'll look forward to you joining me in the next.