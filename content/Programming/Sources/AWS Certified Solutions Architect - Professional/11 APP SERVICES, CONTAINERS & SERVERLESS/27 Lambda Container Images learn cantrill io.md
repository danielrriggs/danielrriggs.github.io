 Welcome back, and in this lesson I want to talk about Lambda container images. Now this might appear a little odd at first, Lambda and containers, but it's actually a pretty cool feature which you need to understand as a developer or somebody involved with the development process. So let's just jump in and get started. Now until recently, what we knew about Lambda was that it was a function as a service or FAST product. And this means that you create a function, you upload code to that function, and it executes. And you'll build based on the execution time. Now this is awesome, but it has two main problems. And both are related to specific processes in place within many organizations. First, many organizations use containers. And I've covered these earlier in the course. Specifically, they have CI-CD processors which are built to deploy code to containers. This means they can do local testing, ensure container images work, and then in theory deploy these to production. Knowing that they will work without any issues. Now Lambda historically lacked the ability to be included in these processes. And it lacked a good way of locally testing functions prior to deployment. Now Lambda is now capable of being used with container images. This means you can maintain your existing build processes and build a container which can be deployed to Lambda. It's an alternative way of packaging your function code and using it with the Lambda product. Now the way that this works is that, if you have a container, you can deploy it to Lambda. The way that this is done is via your usual container build processes. But you include the Lambda runtime API. And this is a package which allows interaction between a container and Lambda. Now this is included inside your container images. And it's a requirement to use the container image with Lambda. What's also awesome is that you can include the AWS Runtime Interface Emulator, or RIE for short. And this lets you do local testing. It emulates many of the interfaces for Lambda so that you can test it without using Lambda for real. And again, this is awesome for organizations who include automated testing as part of CI-CD systems. And visually, it looks like this. We have a source repository, and for this example, let's say that this is GitHub. When code is committed, a business's container-based CI-CD system begins. And it might be that initially a local container image is produced. Something which is designed to be run locally or forms part of automated testing. The key element here is that it's not designed to be run on AWS. And so it includes emulation using RIE. So it includes the base layer, dependencies which include the Lambda runtime client, an emulation layer, and any code which the Lambda function uses. And all of this is packaged into an image which can be used to create a container. Because this container image has the runtime interface emulator included, it can be used for local testing. So interactive on a workstation or as part of some automated procedure. When the testing is successful, the same CI-CD process can build a container designed for Lambda itself. And this includes everything in the previous build except the emulation. We have the same base layer, the same runtime client, and other dependencies and the function code. And this is packaged into a container image. Now this container image can be added to the Elastic Container Registry known as ECR. And can be used as required as the basis for Lambda functions. You end up with the same Lambda function. It's operationally equivalent. But it's been created via integration with an existing container-based workflow. Now that's everything that I wanted to cover in this lesson. Just covering the architecture of how you can use containers. And how you can use container images with Lambda. It's a great feature and it changes some of the build patterns for Lambda-based applications. It means that you can be a lot more flexible in how you package and build functions. At this point though, that's the end of the lesson. So go ahead and complete the video. And when you're ready, I'll look forward to you joining me in the next.