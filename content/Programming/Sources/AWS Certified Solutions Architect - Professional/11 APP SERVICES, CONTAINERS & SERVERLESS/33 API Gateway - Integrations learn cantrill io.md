 Welcome back, and in this lesson I want to focus on API Gateway integrations in a little bit more detail than I've covered elsewhere in the course. Now we have a lot to cover, so let's just jump in and get started. Earlier in the course I stepped you through the high-level architecture of API Gateway. Now before we start looking at integrations in more detail in this lesson, I wanted to give you a quick refresher. So we have API Gateway in the middle here, and this acts as the endpoint for our API consumers, which could be mobile applications, other APIs, or even web applications loaded from static hosting on an S3 bucket. In any case, these connect to the API running on the API Gateway using the endpoint DNS name. It's actually the job of the API Gateway to act as an intermediary between clients and what's called APIs. So what's an API Gateway? It's an API Gateway that integrates with the API Gateway to the API Gateway. Now, API Gateway is capable of connecting to HTTP endpoints running in AWS or on-premises. It can use Lambda for compute, and this is typically used in serverless architectures, and can even directly integrate with some AWS services such as DynamoDB, SNS, and Step Functions. And I'm going to be covering these in much more detail elsewhere in this section. Now there are three main features that are required to integrate with AWS. There are three phases in most API Gateway integrations. The request, which is where the client requests something. The service provided by the integration, so the backend endpoint which provides the functionality. And then the response phase. The request phase at a high level does three things. It authorizes, validates, and transforms incoming requests from the client into a form that the integration can handle. And then the response takes the output from the integration, transforms it, prepares it, and returns it to the client. Now because you're taking a course which requires more detail, the request and response phases are actually split into two parts each. First is the method request phase. This defines everything about the client's request to the method. So the path to use, the headers and parameters, anything which controls the interaction with the client. Then we have the integration request phase, which is where the parameters and other data from the method request are provided to the integration. Now the transfer of data from the method through to the integration, this can involve translation or it can be passed through as is. And this is known as proxying. And you're going to hear me use that term later on in this lesson. At this point the request is then passed on to the endpoint, which could be one of the various types which I'll talk about very soon. The endpoint then has to provide a response, and this is known as the integration response. This phase handles the conversion of data provided by the backend endpoint into a form that the method can respond back to the client with. Or if we're using proxying, then it can pass it directly through to the method. And we're going to be talking about proxying more very soon. So this includes things like headers, status codes, bodies, and anything else that the backend endpoint provides. And then lastly is the method response, which is how the communication is delivered to the client. For the exam, just remember that for the integration request and integration response components, you're either just passing data straight through, or you're converting the data format in some way. If you hear proxy, then that generally points to just passing the data through. Other terms which you might want to remember, if you hear the term method mentioned, then this is relating to the interaction with the client. And if you hear integration mentioned, then that's the interaction with the backend endpoint. So that's the summary. Now let's talk about integrations and what options we have to work with. So API methods which are on the client side decide what the client request to the method is like. And they're integrated with a backend endpoint via integrations. And there are different types of integrations. Now I want to make sure in this lesson that you have a good understanding of when and where to use the various different types of integrations. The first type is called mock. And this type of integration allows API Gateway to return a response without sending the request further towards any backend. And this is useful for API testing because it can be used to test the integration setup without incurring charges for using a backend. And in addition, it doesn't even need a real backend to be configured. So remember, mock equals testing. Next we have HTTP. And this type of integration lets an API expose HTTP endpoints in the backend. This is known as a HTTP custom integration. With this type of integration, you have to configure both integration request and integration response. Because the way you accept data via the method request might not be the same format as the data that the custom HTTP endpoint expects it. So there needs to be some form of translation. And the same is true in reverse. The way that the data is provided back by the HTTP endpoint might not be in the same format as the API Gateway returns to the client. And so an integration response configuration is also required. Next is HTTP proxy. And this is a subtype of HTTP integration, but where proxying is utilized. The HTTP proxy integration allows a client to access backend HTTP endpoints with a streamlined integration setup. You don't need to configure the integration request or the integration response. API Gateway passes the incoming request from the client through to the HTTP endpoint and passes the outgoing response from the HTTP endpoint to the client. So remember, proxying is where information is passed to the endpoint as is and information is accepted from the endpoint as is. It means the endpoint has to understand the format from API Gateway and pass information back in a valid format to API Gateway. Now this is where things start to get a little confusing. When you're configuring the next set of integration types within the UI, you might see that it looks slightly different with different terms. There's an option for Lambda and an option for AWS services. But what I want to do is explain how it actually works behind the scenes. So the next type of integration I want to talk about is AWS. And this type of integration allows an API to expose AWS services. In AWS integrations, you have to configure both the integration request and the integration response and set up necessary mappings from the method request to the integration request. And then from the integration response to the method response. That also needs mapping. So let's go ahead and do that. So let's go ahead and do that. Now you can use this AWS integration types for Lambda functions as well. Now you can use this AWS integration types for Lambda functions as well. But you have to configure how the data incoming from the client is mapped through to the integration and then back again. So it's a relatively complex way of using Lambda integrations. Now there's also one which internally is known as AWS proxy. And this is where the integration request and response don't have to be defined. Instead, API Gateway passes the data through the API gateway passes the data through unmodified. Now this currently supports using Lambda. And this is what we've been using in the user interface so far. It means that Lambda has to be able to interpret what it gets sent within the event structure and respond in a format that API Gateway expects in return. Now configuring Lambda integrations within the console UI, you only see one option. It's Lambda integrations. Now you can pick to use Lambda and then set to either proxy or not. And if you don't, then it's using the AWS integration type. If you do, it's using the AWS proxy integration type. You've so far in the course only used AWS proxy, which means that the Lambda function is responsible for all of the data conversion. So reviewing the event data that it gets and then formatting the response data back so API Gateway can understand it. Now this is low admin overhead, but it does mean that all the heavy lifting needs to be done by the Lambda function. Now let's look at this visually because I think it will make more sense. I want to explore how the flow of information works for each of the integration types. Let's set up the example architecture first. We have four clients communicating with four API Gateway APIs. And this is the method, request, and response part of any communication. The first integration type is easy. It's the mock integration. There's no real backend, so there's no real integration request and response configuration as such. Essentially, API Gateway just responds. And this type of integration is for testing. Next we have HTTP and AWS. And for this example, both of these are not using proxying. With both of these types, the integration request and response have to be configured using mapping templates, which define how data is moved and what transformations are applied. Now there's more admin overhead, but this supports the widest range of backend endpoints. And it doesn't force any configuration changes on those backends. So if you have an existing HTTP API backend and you want to integrate it with API Gateway, and if you can't make any changes to that HTTP endpoint, then this is the option to pick because API Gateway performs all of the heavy lifting. Next we have AWS Proxies. And this is what happens if you select Lambda and proxy options in the integration configuration. Using this option, no mapping templates are required. Integration request and integration responses are just passed straight through to the Lambda function and then back to API Gateway. The Lambda function must be able to handle the data formats provided and needs to keep in mind the expectation of formats when responding. Now this is the least admin overhead, but with this type of integration, the function is responsible for a lot of the heavy lifting. So remember, if you're using this integration type, the function will require additional code to format or translate the formats between API Gateway and the function and the reverse. Now finally we have HTTP Proxy. And this is the same as AWS Proxy, only using HTTP rather than Lambda functions. Now again, no mapping templates are used, and the HTTP endpoint needs to handle the data coming in and must provide data in the correct format on the way out. So again, with both of the proxy options, it's the Lambda function or the HTTP endpoint which have to perform the heavy lifting and any translation of data required to that endpoint and from that endpoint. Now at a high level, mock is for testing. HTTP and AWS are the most configurable, but that means more admin overhead. HTTP is great if you have an existing endpoint and you want to integrate it into API Gateway, because then you use a mapping template and API Gateway performs the heavy lifting. AWS Proxy and HTTP Proxy are low admin overhead, no mapping templates are needed, but the endpoint resource does all of the heavy lifting. Now one final thing before we finish up with this lesson, and this is just a step through, exactly what a mapping template is. So a mapping template is used for AWS and HTTP non-proxy integrations. The mapping template is able to modify or rename parameters between the method and the integration. It can also modify the body or headers of the request and perform filtering, so removing anything which isn't required. Now a mapping template uses the velocity template language known as VTL, and I've included a link attached to this lesson which provides additional information if you want to learn about exactly how this template language works. The key thing to understand is that a mapping template sits between the client and the integration endpoint, translating data on the way to the integration endpoint and then translating data on the way back from the integration endpoint. And it's only needed when you aren't using proxying. Now a common exam scenario which you should be aware of is where you have an exam and you want to integrate an existing SOAP API with API Gateway. So SOAP is an alternative style of API architecture and it's something that has a lot of usage in legacy systems. So to use API Gateway and to integrate a SOAP endpoint, you need to use mapping templates to translate data on the way through to the SOAP endpoint and on the way back from the SOAP endpoint. And this is really important to know for the exam because this tends to feature on at least one of the main points of the exam. So you want to have at least one question in the exam. Now that's everything that I wanted to cover. I know that this has been a lot of theory and you're going to get exposure to how this works through the various demo lessons in this course. But I wanted to cover all of the theory in one go because it all relates to each other. Now at this point that is everything I wanted to cover in this lesson so go ahead and complete the video and when you're ready I look forward to you joining me in the next.