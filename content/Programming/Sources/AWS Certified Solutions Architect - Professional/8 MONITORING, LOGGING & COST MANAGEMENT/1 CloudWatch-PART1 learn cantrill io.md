 Welcome back, and in this lesson I want to talk about CloudWatch. CloudWatch as a product provides services to ingest, store, and manage metrics. Now I'll be covering what this means in detail soon, but when you hear CloudWatch, think metrics. Now there are sub-products which I cover elsewhere in the course, such as CloudWatch logs, but if you see the term CloudWatch, then assume that it refers to the main CloudWatch product, which is metrics-based. Now it's a public service, which means that it has a public space AWS endpoint, which means that it can be accessed from AWS and on-premises environments. Now within AWS, you need to make sure that any services which use CloudWatch have access to the public space endpoint, and usually this is via an internet gateway or an interface endpoint within a VPC. Many AWS services include native management plane integrators, and they can also be installed from one endpoint to another, so we're talking about servers or developers whose servers are not necessarily compatible with, for example, cloud interfaces. It's only Web2.1, Azure's, or other main saudian systems. So if someone uses CloudWatch server causers, all those servers could serve as an other solution. So I'm trying to able to talk about an Figure 1 backups, or the thing that we have in C2, that allows us to actually, of running processors or their CPU or memory usage, or it could provide details of disk space usage within the operating system. For the exam, you need to understand that certain things require an agent. So anything which conceptually is only visible inside the instance operating system or inside the service generally requires an agent. Now, you can also use CloudWatch as a service from on-premises environments using the agent or directly using the API, and you can also build the product directly into your applications, again, using the agent or APIs or a combination of both. Both of these deliver what's known as custom metrics into the system. Now, once the data is inside CloudWatch, it's stored in a persistent way, and you can view and interact with it via the console UI, the command line, or the APIs, and it also provides dashboards and applications. And you can also use it to monitor and anomaly detection, which are features that we'll discuss more as we move through the course. Lastly, it provides CloudWatch alarms, and these react to metrics. Now, an alarm can either be in the OK or the alarm state, and these can be configured to generate notifications if the data which these alarms monitor breach normal levels or normal ranges, and this is based on criteria which either you provide or that the system detects as anomalous. So those are the architecture concepts, and visually, it looks like this. So architecturally, we have the AWS zones, public in orange in the middle, and the private VPC zone at the bottom in green. And then at the top in red, we have on-premises and internet-connected environments. Now, CloudWatch sits in the AWS public zone, and it stores metrics. I've included a few examples here, so CPU, memory, disks, requests, latency, and errors, and various services then publish data into CloudWatch. Examples include on-premises virtual machines and applications, as well as internet-connected applications. And then at the bottom, services in VPCs can publish data into CloudWatch, either using a combination of public IP addressing and an internet gateway, which allows services to connect with the public CloudWatch agent. So this needs a combination of public IP version 4 or IP version 6 addresses and an internet gateway. Or alternatively, private instances can connect also from a VPC, but this time using an interface endpoint. And this allows connections directly without using public addressing. So with this architecture, an internet gateway isn't required, and it's replaced with an interface endpoint. And both of these allow VPC services to publish data into CloudWatch. Now once the data is within CloudWatch, we can use it to create alarms or view the data using the console, the CLI, or the APIs. Alarms can contain actions, which can be SNS notifications, or used to influence scaling policies within auto-scaling groups, or can even be the starting point for event-driven workflows using EventBridge. Now that you have a refresher on the architecture of CloudWatch, now I want to spend some time really focusing on the concepts. If you haven't had much involvement with products which focus on data before, then some of the terms used might not be all that intuitive. Really try and focus on this section of the lesson and only move on when you really understand these terms. It's really important if you want to use CloudWatch day-to-day or answer any CloudWatch questions in the exam. Now the first term to introduce is a namespace, and this is a very important term. And think of a namespace as a container within CloudWatch. Generally, AWS use these for all the different AWS services. So we'll have the EC2 namespace, which is AWS forward slash EC2, and the Lambda namespace, which is AWS forward slash Lambda. Now AWS services will always start with AWS forward slash, and namespaces which you create won't start with AWS forward slash. Now conceptually, these separate the metrics from different services. It's possible you might have metrics with the same name, but for different services or different applications. And so namespaces act as containers. Next, we've got data points, and these are the smallest components of CloudWatch. They have a value, let's use an example of 42, which represents 42% CPU. A timestamp, which represents the number of timespaces, which represents the date and time when the value was taken, and then optionally a unit of measure, for example, percent. Data points are the individual points of data, the things that CloudWatch is recording and managing. Next, we've got metric. Now it's really important that you understand how CloudWatch thinks about things. Strictly speaking, a metric is a time-ordered set of data points. Now examples of metrics include CPU utilization, network in, and disk write bytes. And these are all examples of EC2 metrics. Now every metric has a metric name and a namespace. In this example, CPU utilization is the metric name, and then the namespace is AWS forward slash EC2. Now do you see the immediate problem at this point? If we only had metric name and namespace to identify metrics, then it would get pretty messy. We couldn't easily separate data points for different EC2 instances. And this is where the next concept comes in really handy. And this is traditionally one of the most difficult concepts to understand within CloudWatch, and that's dimensions. A dimension is a name-value pair which is provided when you add data points into CloudWatch. So as well as providing the value, the timestamp, and a unit of measure, you also provide the namespace, you provide the metric name, and you provide zero or more dimensions. Now at their simplest, dimensions are the way that the CPU utilization metric for EC2 differentiates between instances. In this example, we specify a dimension where the name is instance ID, and the value is the actual instance ID. So i, hyphen, and then all y. And another instance would have the same dimension. So the name being instance ID, and the value being i, hyphen, and all twos. Now dimensions can also be used to aggregate data. So for example, for data about CPU utilization, then AWS adds additional dimensions, such as auto-scaling group name, image ID, instance ID, and instance type. And this means for a given metric, in this case CPU utilization, you can aggregate data. So you can see CPU utilization for one specific type of instance across an entire region of AWS. Or you can get access to CPU utilization for all instances within one single auto-scaling group. Now dimensions are very complex, but they can also be really powerful. Using dimensions fully is beyond the scope of this course, but you do need to understand the basic idea. So let's take a look at some of the features that are available in the architecture. Okay, so this is the end of part one of this lesson. It was getting a little bit on the long side, and I wanted to give you the opportunity to take a small break, maybe stretch your legs or make a coffee. Now part two will continue immediately from this point. So go ahead, complete this video, and when you're ready, I'll look forward to you joining me in part two.