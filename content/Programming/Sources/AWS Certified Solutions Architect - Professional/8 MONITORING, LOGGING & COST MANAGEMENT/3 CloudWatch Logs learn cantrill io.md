 Welcome back, and in this lesson I want to firstly provide a quick refresher on CloudWatch Logs, so let's jump in and get started. For the exam it's important to understand that CloudWatch Logs has two sides to the product, the ingestion side and the subscription side. The ingestion side is concerned with getting logs into the system, and the subscription side is concerned with what other products can use those logs for other activities. So let's step through some of the integration at the ingestion side first. It's important for the exam that you get a feel for how CloudWatch Logs accepts data. So first, CloudWatch Logs is a public service which is designed to store, monitor and provide access to logging data. So if you see any question in the exam which talks about logging data across a fleet of resources, then you should default to CloudWatch Logs. CloudWatch Logs can provide logging ingestion whether services are within AWS, within on-premise environments, any IoT devices or even applications, whether they're running in AWS or on-premises. The product supports native ingestion of logs from various AWS products and services, but if you want to bring in application logs or system logs from any on-premises systems, then you need to install the CloudWatch agent. The CloudWatch agent can be configured to allow ingestion from almost any type of logging output, whether that's application, whether that's system logs or even bespoke logging items that you configure in a bespoke or custom application. CloudWatch Logs can also ingest logging from VPC flow logs. It can also ingest logging information from CloudTrail, which covers AWS account level events and API calls. And various... Other products such as Elastic Beanstalk, ECS, API Gateway or Lambda can all be configured to use CloudWatch Logs to store their data. It can even be used to store Route 53 DNS request logging, so it can bring in data from all of the different global Route 53 endpoints and provide access to that logging in one single location. So CloudWatch Logs, as I said moments ago, should be the default for any questions which involve logging. Within AWS. Now architecturally, we start with CloudWatch Logs, which is a regional service. So you send logging data into a region, in most cases the same region as the service generating the logs is in. Now there are exceptions to this. Certain global services such as Route 53 send their data into US East 1. But in general, a service that runs in AP Southeast 2 will send its logging data in to AP Southeast 1. Within this region, logging sources generate what are known as log events. Logging sources can be anything which is able to send logging data into CloudWatch Logs. So all of the services which I just mentioned and more. Log events consist of two parts. A timestamp and a raw message. The timestamp represents when the event occurred. And the raw message is the actual message sent by the application. Or service which is sending in the logging data. So let's say that you're sending in logging data from various different EC2 instances. For a Linux log file called var log messages. Now for that log file, every line in that file will be one log event. So it will have one date and timestamp. And it will have one raw message. Now for a given source, those log events will be collected into a log stream. And you can have more. Than one log stream. So a log stream is a sequence of log events that share the same source. More specifically, if we're logging one particular log file. For example, var log messages. Then each log stream will represent all of the log events for that log file. On one particular EC2 instance. Or one particular on-premise server. So in this example, if we have three EC2 instances. All logged. Logging var log messages. Then we'll have three different log streams. Each of those log streams will contain all the logging events for that one log file. For that one EC2 instance. Now around those log streams are log groups. Generally for a given resource, say var log messages. You will have one log group. Inside this log group will be log streams. And each log stream represents one instance's data. For that one resource. So var log messages. Inside those streams will be log events for that resource on that instance. So you need to try to do your best to understand this structure. So a log event is a timestamp and a raw message. A log stream is a collection of those log events for one given resource. Taken from one given source. So with this architecture, one log stream represents instance A. One log stream instance B. One log stream instance C. Inside those log streams. Are all of the log events for var log messages. And then the log group generally represents the thing being monitored. So var log messages. Now this structure is pretty consistent. You would also, for example, have a log group for a given lambda function. Inside that log group for that given lambda function. You would have streams for particular time periods that invocations happen. And the streams would contain log events for the output of that lambda function execution. But it's also. On the log group that you set things like retention. Permissions and encryption. By default. Log groups store logging data indefinitely. But you can set a time limit to control costs. Permissions are also controlled on a per log group basis. So you can limit who has access. And you can choose to encrypt logs at rest using KMS. Another feature provided by CloudWatch logs is a metric filter. And. A metric filter is something which gets defined on a log group. And it looks for patterns in any log streams within that group. An example might be looking for a certain application crash. Or a failed SSH login attempt. When it matches that pattern against anything which occurs in any of the log events. Within any of the log streams. Then it can use that data to influence a metric. So a metric is created from the metric filter. So if you're looking for failed. SSH login attempts. Then you would define that pattern inside the metric filter. And the metric would represent the number of failed SSH attempts. Or the number of application crashes. Once you've got that metric. Then that metric can be treated like any other metric inside CloudWatch. And it can be used to generate alarms. And these alarms can themselves be used as the initiators of events. So to do other things like invoke a lambda function. Or send a notification to the team. Now there are a few ways to get logging data out of CloudWatch logs. First you can create an S3 export. And this is a manual process which can take up to 12 hours. So it's not real time. And the result is that logging data is stored in an S3 bucket. Now be aware you can't encrypt this data with SSE-KMS. You can only encrypt it with SSE-S3. And also it's not real time. I need to... To stress that point. So if you're exporting logging data from CloudWatch logs. Using the S3 export feature. So create export task. Then it's not real time. It can take up to 12 hours. Now you can configure CloudWatch logs. To allow for the real time delivery of logging data to other destinations. So if you use CloudWatch logs to be the AWS concentration point. For all of your logging information. Then that could be delivered out in real time to other systems. Now the way that this is done. Is on a per log group basis. So let's look at this visually. We start with the Categram Production log group. And to configure real time delivery. Then you need to create a subscription filter on that log group. The subscription filter configures a few things. A pattern. Which means that you can limit what gets handled by that filter. You can set the destination ARN of the destination. The distribution. Which controls how logging data gets grouped up. As it's sent to the destination. And then you can define permissions for CloudWatch logs. To get access to that destination. Now what type of destination you use. Depends on your use case. For example if you just want the data being sent to S3. For long term storage. Then you can configure a Kinesis data firehose as the destination. And have that send the data through to S3. Now remember Kinesis firehose. Is only near real time. There is a buffer. Either 60 seconds. Or based on data amount. And so this architecture only works. If you don't care about truly real time delivery. If you only need near real time delivery. Of logging data through to destinations. Then Kinesis data firehose. Is an excellent product to use. It's really cost effective. As long as you don't care about real time. Now for real time delivery. You have a few options. You can natively use subscription filters. To deliver into Elasticsearch in real time. And this is via an AWS managed Lambda function. Alternatively you can develop a custom Lambda function. To deliver into any resource that you desire. As long as you can create the appropriate code. Now both of these are real time. Essentially all of the logging data gets delivered to a Lambda function. Which is invoked. And that Lambda function. Can do what you want. And can do whatever it wants. With that logging data. You can also use Kinesis data streams. As a destination. Meaning that anything which can integrate with data streams. Can get access to the logging data. In real time. Now subscription filters can also be used. To create a logging aggregation architecture. Let's assume that we have three accounts. The Categram account at the top. Our production account in the middle. And then at the bottom. A new application Dogogram. And then at the bottom. In each of these accounts. We have separate and isolated copies. Of CloudWatch logs. We can use subscription filters. To send all of the logging information. To a central Kinesis data stream. Now we could have consumers. Analyzing data. Directly from this stream. Using KCL. And for long term persistence. We could use the Kinesis data firehose. To persist all of that data. Into S3. Or send it on to other logging. And visualization solutions. So this is another really common architecture. Where you use subscription filters. For aggregation of logging data. From many different accounts. Or many different regions. Now let's quickly step through. A number of summary points. Before we finish up this lesson. And all of these will be essential for the exam. For any log management solutions. In the exam. You should default to using CloudWatch logs. And again this goes for both. On premises. And AWS. You can export logging data to S3. Using create export task. But this is not real time. And it can take up to 12 hours. To create this data within S3. If you only care about near real time. Or you just want to persist your logs. As quickly and as cost effectively. As possible. Then you can use the Kinesis firehose. As a destination for a subscription filter. You can use Kinesis firehose. Either to store your logs. Or in S3. Or you can use it to support delivery. To any supported firehose destination. And I talk about those. In the dedicated Kinesis firehose lesson. Elsewhere in the course. If you need real time handling. Of your logging data. Then you need to configure a subscription filter. To send that data through to Lambda. Or a Kinesis data stream. So Lambda can be configured. To deliver to almost anything. Whereas a Kinesis data stream. Allows any KCL consumer. So any applications. Or any dashboards. Can be configured. To read logging information. That's delivered to a Kinesis data stream. You can also deliver the logging data. To Elasticsearch. Using an AWS managed Lambda. And don't forget. The metric filter functionality. So if you want to take. Incoming logging data. And do text searches. On that logging data. Generate a metric. And use that either. As part of a simple notification. Solution for your team. Or as the initiation point. For an event driven architecture. Then you can do that. Using metric filters. With that being said. That is everything I wanted to cover. Around CloudWatch logs. For this exam. Go ahead. Complete this lesson. And when you're ready. I look forward to you joining me. In the next.