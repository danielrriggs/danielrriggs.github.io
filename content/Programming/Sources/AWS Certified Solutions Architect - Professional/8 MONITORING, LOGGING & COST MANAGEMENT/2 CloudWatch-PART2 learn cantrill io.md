 Welcome back, this is part 2 of this lesson. We're going to continue immediately from the end of part 1, so let's get started. Now when you publish data points into CloudWatch, you do so with a certain resolution. Metrics produced by AWS services by default use standard resolution, which means they have a 60 second granularity. This means for CPU utilization, if you published 42% with a 60 second resolution, then it would be viewed as having 42% for that entire 60 second duration. Now high resolution metrics cost more, but they have a 1 second resolution. For high resolution metrics, CloudWatch stores the data with a resolution of 1 second. So for CPU utilization, if you published a data point into a 60 second granularity, then it would be viewed as having a 42% for that entire 60 second duration. If you published a data point every second and used high resolution, then you could see the CPU utilization value at 1 second period, so 1 second granularity. The resolution determines the minimum period, which can be specified and get a valid value. Now if you publish data into a high resolution metric with a 1 second resolution, then you can read and retrieve it with a period of 1 second, 5 seconds, 10 seconds, 30 seconds. Or you can read and retrieve it with a period of 1 second, 5 seconds, 10 seconds, 30 seconds. Or any multiple of 60 seconds. The metric resolution defines the minimum time period that you can get one particular data point for. So if you have a 60 second resolution or a 60 second granularity, then the data points will be valid for that 60 seconds. Now for a given resolution, CloudWatch retains data for a certain period of time. For anything less than 60 seconds, then data is retained for 3 hours. For 60 seconds... For 60 seconds, data is retained for 15 days. For 300 seconds, data is retained for 63 days. And for 3600 seconds, data is retained for 455 days. Now as data ages, it's aggregated and stored for longer periods with less resolution. So CPU utilization being stored every second could be viewed at that 1 second resolution for 3 hours. And then it will be available in 60 second resolution for 15 days, 5 minute resolution for 63 days, and 3600 second resolution for 455 days. Now it's pretty common that as data ages, the detail matters less and less. And storing everything with 1 second resolution indefinitely would be hugely expensive. Now next we've got statistics. And these are ways that you can take data... ...which are... ...which occurs over a period and aggregate it in a certain way. For example, say that you have 1 second resolution for CPU utilization. Well if you wanted to view the minimum, maximum, or average values over 5 minute periods, then you can do that using statistics. It takes data for a period and it aggregates it in a certain way. Next we've got percentile. And a percentile indicates the relative standing of a value. For example, we could say that we want the 95th percentile. And this means that 95% of the data is lower than this value. And 5% of the data is higher than this value. And percentiles help you get a better understanding of the distribution of your metric data. And help you eliminate outliers. So if you look at CPU utilization over a data set. And look at the P95 value. Then it removes 5% outliers. And it helps you identify a normal value or a normal range for a certain percentage of a data set. Okay, now at this point I just wanted to introduce all of those terms. Let's move on and talk about alarms. Now alarms can be created and they monitor or watch a given metric over a specified time period. An alarm, based on reviewing that data, will either be in an alarm or an okay state. And this will be based on the value of the metric versus a threshold which you define. So you might want an alarm which monitors CPU utilization. And if the value is above 50% for two periods of 60 seconds. Then you might want the alarm to alarm. And alarms can be configured with one or more actions. Which can initiate actual actions on your behalf. The action could be a notification sent to an SNS topic. Or a notification sent to a Facebook post. Or an auto scaling policy modification. And even you can use event bridge which can be configured to start off an event driven execution. When an alarm changes state. Now if you set an alarm on a high resolution metric. Then you can specify a high resolution alarm. And this can have a period of 10 seconds or 30 seconds. Or you can set a regular alarm with a period of any multiples of 60 seconds. The important thing to understand. Is that the resolution of a metric directly affects the resolution options that you can have on an alarm. Which uses that metric. Now let's have a look at all of these concepts visually. The difficult thing to understand with CloudWatch is always the data structure. So the terms, the concepts, the way things are used within the product. So we start off with namespaces. And these are the containers within CloudWatch. In this example AWS forward slash ECC. And AWS forward slash EBS. It's inside these namespaces where metrics are. Every metric has a namespace. And this keeps the metrics separate and isolated. Now metrics are time ordered sets of data points. And each of these data points which represent one reading. Have a timestamp and a value as a minimum. And then optionally a unit of measure. In this case we're showing IOPS. So input output operations per second. On an EBS volume. And so the unit is count. Next we have dimensions. And these are useful to present alternative perspectives on data. Or to allow granular access to say one instance. Sending in CPU utilization data. In this case we have three instances. All publishing data to the CPU utilization metric. And then we have the AWS forward slash EC2 namespace. And with dimensions we can attach name value pairs. In this case instance ID. And the actual instance ID of the instance. And this is how we can filter all of the data points. For CPU utilization to expose specific instances. We can have other dimensions though. For example EC2 lets you use instance type as a dimension. And then AWS metrics allow aggregation across dimensions. So for example you could look at CPU utilization across these different dimensions. So you could look at all of the CPU utilization data points. For all instances which have a T3.X large instance type. Or you could look at all of the CPU utilization for all instances within a specific auto scaling group. Now lastly we can take all of the data contained in these dimensions. And generate an alarm. And the alarm can be either in an OK state or an alarm state. And this is based on certain criteria. So we have the ability to specify the period. The evaluation period. The number of data points required to generate an alarm. And then the condition to look at so the criteria. In this case we are generating an alarm for any high CPU utilization. And maybe we are generating this for maybe any EC2 instance. Or maybe we are looking at EC2 instances of a certain type. Or those which belong to a certain auto scaling group. We've got this level of flexibility with CloudWatch. So CloudWatch is the product that you would use if you are dealing with metric data. If you want to look at telemetry of various different AWS services. Any of the particular data points for those services. If you want to generate alarms based on that data coming from those products. Then you can use CloudWatch. Remember you can also get access to rich data using the CloudWatch agent. And you can also have on premises systems or internet connected systems. Publishing data into CloudWatch. All they will need is the agent and permissions to publish that data into AWS. Now at this point that's everything that I wanted to talk about relating to CloudWatch. I know it's been a lot of theory. But these are things that you'll need to understand for the exam. Now at this point go ahead and complete this lesson. And then when you're ready I'll look forward to you joining me in the next.