 Welcome back. In this lesson, I want to quickly cover a topic which is important to understand when dealing with load balancing and scaling in general. And that's user session state. I'll be covering what it is and why it matters to you as a solutions architect. Now the lesson will be a bit abstract, so let's jump in and get through it as quickly as we can. Session state, put simply, is a piece of server-side information. Information that's specific to one single user of one application. Generally, session state is one specific interaction with that application. Now examples of things which are stored by session state are shopping carts on online web stores, workflow positions in a web application, and login state of an application. So imagine that you log in to Amazon.com. The second you log in, a session is established. Between you and that application. And that session, the state of that session, has some data. The fact that you're logged in and that you stay logged in means that there's something linking you to the application. Imagine that you search around the website, you order some yummy cat treats or dog food, and you put that into your shopping cart. The fact that those items are maintained in your shopping cart means that data is held somewhere, representing the state of your shopping cart. When you click on checkout, you move through a step-by-step ordering process. So stock levels are checked, address details are verified, credit card details are checked and charged, and then you're told that your order is complete. All of those steps from the initial login, the shopping cart, and your flow through the order process, it's all stored in this abstract piece of data called a session state. If that session state data is ever lost, then you will see user experience or UX issues. Your users might be logged out, their place in the order process lost, and shopping carts might be emptied. Depending on your application and choices you make, session data might last minutes, hours, days or weeks. And so any disruption to that session state can be very visible to your customers. The data is stored in a session state. The data is stored in a session state. The data which represents session state is either stored on a web server or an application server, in which case it's known as internally stored session data, or it's stored externally. If session data is stored externally, meaning not on your web or your application servers, then those servers are known as stateless, and I'll cover what that means very shortly. For now though, let's explore why this matters, and we'll do so visually. To illustrate this, we'll use a little bit of a web browser. To illustrate why session state is so important beyond the theoretical concept, let's look at an example. Let's assume that Bob is browsing to Amazon.com. And for now, we're going to assume that this application is running through an elastic load balancer. The job of a load balancer is twofold. Firstly, its job is to abstract Bob away from the underlying infrastructure. So if instances fail, if scaling events occur, all of that should be hidden from Bob. Bob should simply be connecting to a single endpoint which provides access to the application. And this endpoint is the elastic load balancer. Now the second function of a load balancer, and this is given away in the name, is that it should distribute or load balance any incoming user requests across all of the registered compute. And in this particular case, this means that Bob is going to be, or should be, redirected first at instance two. Now that Bob's connected to the application, let's assume that he browses the site and places some items in his cart ready to order. So the items are in his cart, he's connected to instance two, he realizes that he needs to go to get his credit card ready to pay for his products. Now using this architecture, Bob's session data is internally stored on instance two. So everything that Bob has done so far, will be stored in instance two. All of the interactions that Bob's had with Amazon.com, they're stored internally on instance two. Now while Bob's finding his credit card, let's assume that we have a major failure. And at this point, instance two fails. Now the load balancer is smart enough to immediately reroute the connection from instance two, so that Bob is now connected to instance one. Because the load balancer abstracts away from the actual registered compute, Bob's laptop doesn't see or notice any differences in terms of what he's connected to. So Bob's laptop still thinks that he's connected to the elastic load balancer, only behind the scenes, the load balancer has changed the actual physical instance that it's using to deliver the service to Bob from instance two to instance one. Now it's at this point that understanding exactly how session data is stored, becomes really important for a solutions architect. Because behind the scenes, Bob is now connected to a completely different EC2 instance. If this application is configured to store session data locally, then that instance, so instance one, has a completely different set of data from the failed instance two. And because of this, Bob loses his shopping cart and is immediately logged out of the application. So behind the scenes, Bob has essentially been connected to a completely different instance, a different copy of our application. It has a different set of user session data, and so Bob completely loses his shopping cart, potentially he gets logged out, and whatever stage in the order process he was at before the server failure, that is also lost. And this obviously represents a pretty suboptimal situation. So Bob is now connected to the application, and he has a completely suboptimal user experience. Now in reality, I've painted a very over-simplified picture of the application architecture. I've maybe suggested that the instance that Bob is connected to only changes if in this example, instance two fails. But in reality, the load balancer could have been alternating Bob between instances one, two, and three, every time Bob clicks his mouse or moves to a different page of the application. And I'll be talking about a few ways around this problem later in the section. But what I want to focus on now is externally hosted sessions. So let's have a look at that next. With externally hosted sessions, the data for session state, as the name suggests, is stored on an external system. So anything that's outside of the instances themselves. This results in instances which are stateless, and this means that the failure of any one particular instance no longer represents something that's important from a user experience perspective. So let's have a look at how this changes exactly how this architecture works. So Bob connects to instance two, and then the load balancer will move Bob's connections between any of the registered instances at random. Because the session state isn't stored on any one particular server, if instance two in this example fails, even if a connection is in progress, it's simply redirected to another working instance with no disruption to Bob. With externally hosted sessions, it means that any connection to any available application instance will have access to that same shared session data. With this application, it means that the load balancer is free, to direct user connections to any available instance. And whichever instance is used, it has access to the same set of shared session state data, meaning that the user experience is maintained across all of the different instances. And because the load balancer is free to load balance all of the connections across all of the available instances, this represents an optimal scaling solution. Now I'll be talking more about session state later in this section of the course where I'll be focusing specifically on how Elastic Load Balancers handle sessions. But for now, I wanted you to understand how sessions work and the importance of them. For now, just go ahead and finish this lesson, and then when you're ready, I'll look forward to you joining me in the next.