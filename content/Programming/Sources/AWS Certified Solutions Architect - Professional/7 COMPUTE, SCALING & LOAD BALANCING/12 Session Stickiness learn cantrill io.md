 Welcome back, and in this lesson I want to talk about session stickiness, and this is a continuation of the user session state topic which I covered earlier in this section of the course. But this lesson will be focused more on how elastic load balancers handle sessions. So let's jump in and get started. Stickiness is an easy concept to explain if we step through it visually. So let's do that. So we start this architecture with our customer Bob, a load balancer, and a set of backend EC2 instances. Stickiness allows us to control which backend instances are used for a given user connection. Without any form of stickiness, any sessions which Bob or anyone else makes are distributed across all of the available backend instances based on fair balancing, together with the results. If Bob clicks three times, if he accesses three different pages, then chances are he'll be connected to multiple backend EC2 instances. Generally this means a fairly equal distribution of load across all of the registered instances. It's the best way to implement a scalable application. But it means if we implement the storage of sessions internally on each of the EC2 instances, then EC2 instances will be able to be used for a given user connection. So let's get started. So let's get started. So let's get started. Each one of those instances will have different session state data for a given user. So Bob might connect to EC2-2, add items to his cart, and get ready to pay. But then if he moves to EC2-1, his cart might be empty and his point in the order process could be lost. If the application stores its session externally, as you learned earlier in this section, then this isn't a problem. But not every application stores its session data externally. And for those that don't, we need a solution. And that solution is session stickiness, which is a feature of elastic load balancers. Now the way that you enable session stickiness differs depending on the load balancer. With classic load balancers, it was done on a load balancer basis. But for application load balancers, you enable stickiness on a per-target group basis. So once you enable stickiness as a feature, when a user first makes a request to an application load balancer, the load balancer generates a cookie, which is called AWS ALB. The cookie represents the user and their session, and it's delivered to the user's laptop, phone, or computer. Now the AWS ALB cookie has a duration, which you define when you enable this feature. And valid durations are... ...anywhere between one second and seven days. As long as the cookie is valid, then it represents that user and that session. Now the way that the cookie is used is that it's provided by the user to the load balancer every time that the application is accessed. So from that initial point, from the first point the user accesses the load balancer and receives the cookie, from that point forward, that cookie is provided to the load balancer, along with every session. ...every time that the application is accessed. ...every request. Now under normal conditions, the cookie is used to ensure that for any future connections, they all use the same backend instance. In this case, EC2-2. So this means that the user can maintain a consistent connection to one of the backend instances, and because of that, the session data will be consistent. They'll have access to the same user session state, because they'll be connected to that... ...same backend instance. Now the backend instance that this particular cookie refers to, so the backend instance that the user's session is locked to, can change under a few circumstances. The first of these is that if the instance that the cookie maps to fails, so if it has a failure, then a new instance will be selected, and any future connections for that user will be directed at that new instance. And that allows the architecture to self-heal, and that allows the architecture to self-heal, in the normal way. Another condition which results in a change of backend instance, is if the cookie expires. And at this point, the cookie will expire, it will be removed, and any future accesses to that load balancer will result in a new cookie being created, and a new backend instance will be picked and locked for that particular user. Session stickiness is designed to allow an application to function using a load balancer, if the state of the user session is stored on individual servers. It allows an application which doesn't handle sessions well, to function as part of a load balance solution. Now ideally an application would store its state externally, so using an external service, but where that's not possible, session stickiness is a great solution. Now session stickiness does have its problems, it can cause an uneven load on backend instances, because individual users are locked to particular instances, if you have one or more heavy users, then they can easily saturate a server. Without stickiness, and if sessions are stored externally, then the load balancer will level load across all of the backend instances, and this is the optimal solution. Session stickiness impacts this in a negative way, but sometimes it's required if you're attempting to load balance a server, or attempting to load balance applications, which don't handle state in an optimal way. Now there are a few key points that you'll need to understand for the exam, so these are the key takeaways of this lesson. So session stickiness locks a user session to one backend instance, and it does this by creating a cookie called AWS ALB, which is held by the client. Now the cookie is called AWS ALB for application load balancers, but for classic load balancers, while the functionality is the same, the cookie is called AWS ELB for elastic load balancer. Now you shouldn't have to remember the difference for the exam, because classic load balancers are now a legacy product. So remember the name of the cookie, AWS ALB, and exactly how this works from an architectural perspective. Now sessions can move between backend instances, under one of two situations. The first is that the cookie expires, and obviously this has a lifetime between one second and seven days. The other reason for changing backend instance is if we have an instance failure. So if we have an instance failure, a health check will determine that that instance is no longer in a healthy state, and no further connections will be sent to that instance by the load balancer. And in addition, any sessions affected by session stickiness will be allocated a new backend instance. So the cookie will be updated. Now you should enable session stickiness if an application doesn't use external sessions. The optimal solution is to use an application which does store its session data externally to the instances, and not use session stickiness, because that will ensure that the application scales optimally, and any load goes across the application. And that's the way to do it. So you can use session stickiness across all of the available backend instances. But if you do need to use an application that doesn't use externally stored session data, then stickiness will help maintain the user experience. There'll be no disruption caused by moving user sessions across backend instances, as long as session stickiness is enabled. Now on the exam, be on the lookout for any keywords in questions, such as user logouts, lost shopping carts, or lost progress, because all of these suggest a loss of session state. And if that is the key part of a question, then you should try and evaluate whether the application also in that question stores session data externally or on the instances. And if it stores it on the instances, then using session stickiness is likely to be the correct solution to fix the problem in a question. But just to reiterate, session stickiness is not an optimal solution. Where possible, applications should be designed and implemented to store their session data externally off the EC2 instances. And then you should disable any form of session stickiness to allow the load balancer to level load across all available backend instances. Now I do expect this to feature on the exam, so it's something that you should be comfortable with before moving on in the course. But at this point, that's all of the theory that I wanted to cover. So go ahead and complete this lesson. And then when you're ready, I'll look forward to you joining me in the next.