 Welcome back. In this lesson, I want to go into a little bit more depth on EC2 networking. If you're designing solutions at this level, you need to have a wider and deeper understanding of what kinds of functionality EC2 provides from a networking perspective. So let's jump in and get started. Now it's easier if we look at this visually. So we start with an EC2 instance, and this one is running in US East 1. And I'm keeping it simple, so we have three availability zones, US East 1A, 1B, and 1C. Now instances, when you launch them, they're created with a primary ENI or Elastic Network Interface. This can't be removed from the instance, and it can't be detached and attached to any other instance. It's essentially linked to the instance for its entire lifecycle. It's only removed when the instance is terminated, at which point it's deleted. Now you can add additional Elastic Network Interfaces, either when you create an instance or afterwards. And these can be in different subnets, but only those in the same availability zone. You can't create a network interface in US East 1B or 1C in this example, and attach it to the EC2 instance in 1A. Remember that key point, because it tends to feature on the exam. Now additionally, these secondary interfaces, can be detached and reattached to other EC2 instances. It's only the primary ENI which is linked to the EC2 instance for its lifecycle. Any of the secondary interfaces can be detached and moved to other EC2 instances, as long as they're in the same availability zone. Now I wanted to draw out all these key points individually, because there are some architectural situations where you might want two or more ENIs on a single instance. Often in multiple subnets. The key takeaway is that you can't do this in different availability zones. So that's really critical to remember. Different subnets is fine, different availability zones is not supported by EC2. Now let's move in and look at the interface architecture in a little bit more detail. Now another important thing to understand about Elastic Network Interfaces, is that security groups are associated with the EC2 interface. Not with an EC2 instance. So in this example, both of the ENIs which are attached to the EC2 instance, could have separate security groups. This security group architecture, when combined with having multiple ENIs, each in different subnets, allows us to be much more granular with our security. We might have two ways to connect to the instance. And because we have different security groups, we might have one public interface and one management interface. Each with a different level of restriction. So our sysadmin on the bottom left will have freer access to the instance, maybe using a private direct connect or site to site VPN link, versus the normal user on the top left, who could in theory be a bad actor trying to exploit vulnerabilities in our instance or operating system. And so we can apply, more stringent security controls, by changing the security group on the interface at the top. So by having multiple network interfaces in multiple subnets, it allows us to be much more granular and controlled, about the way that we design the security of our EC2 instances within AWS. Now all of this doesn't prevent us as solutions architects, from further protecting the resources by using network ACLs. And these are around the subnets, that the resources are within. So in this particular case, we have two network ACLs, one around the public subnet, and one around the management subnet. And again, each of these can be configured with a different set of rules, so apply a different set of restrictions, based on the type of traffic entering the EC2 instance. Now for real world usage, and for the exam, what I need you to understand, is that from a networking perspective, it's the ENIs which are important. Most networking configuration, which can influence how EC2 instances can be interacted with, is performed at the interface level, not at the instance level. It's the network interfaces which have security groups on them, it's the network interfaces which are in subnets, and thus are influenced by network ACLs. Now let's zoom in a little bit more, and look at the interfaces specifically. Every elastic network interface is allocated, a single primary private IP version 4 address, which is obtained using DHCP, and this IP address is within the range, that's allocated to the subnet, that the interface is contained in. Now this IP address remains the same, for the lifetime of the elastic network interface, and so because an EC2 instance, is always launched with a primary network interface, we know that every instance, at the very least, has one. Primary private IP version 4 address, which is static for the lifetime of the interface, and thus it's static for the lifetime of the instance. So by default as a foundational fact, we know that every EC2 instance, has a single primary private IP version 4 address, that never changes. This is important to understand, I've seen many clients migrating to AWS, who are very focused on IP planning, and addressing. They're used to dealing with static IPs, and explicit allocation and planning of IP addressing. With AWS, generally we just launch an instance, it gets a primary private IP version 4 address, which is the same for the lifetime of that instance, and that's because this IP address is allocated, to the primary network interface, and it doesn't change for the lifetime of that interface. Now this private IP version 4 address, is exposed to the operating system running on the instance, so it will be configured on the network interface, of the operating system. This isn't the same with all of the different IP addresses, that you'll need to understand, but private IP version 4 addresses, are visible within the operating system. Now in addition to the primary private IP version 4 address, elastic network interfaces can also be allocated, with one or more secondary private IP version 4 addresses, and the exact number depends on the type of instance that's being used. Larger instances will generally allow more IP addresses. Now these secondary IP addresses are also exposed to the operating system, that's running on the instance. So the operating system will have full visibility, of all of these secondary IP addresses. It's important to know that IP addresses are linked, to the elastic network interface, not to the instance. So if IPs are allocated, to secondary interfaces, they can be moved between instances, by moving the elastic network interface itself. And remember that one for the last part of this lesson. Okay, so now let's talk about public IP version 4 addressing, and be careful with this part of the lesson, because the detail really matters. If you launch an instance into a subnet, which is set to allocate public IP version 4 addresses, or if you explicitly decide to launch an instance, with a public IP version 4 address, then it's allocated a public IP version 4 address, or more specifically, the primary interface of the instances. Now this IP is not static. If you stop and start an instance, which forces it to move between EC2 hosts, then this IP address changes. This IP address is also not visible to the network interface, on the operating system, that's running on the IP. So if you want to see how this works, you can go to the internet gateway, and you can see that the IP address is running on that instance. IP traffic is translated by the internet gateway, when it leaves a VPC. And the reverse happens when it arrives back to the VPC. It's translated again, before being passed to the EC2 instance. It's never a valid solution to any problem, to configure a network interface, within an operating system, with a public IP version 4 address, or a network interface. So if you want to see how this works, you can go to the internet gateway, at the edge of a VPC. If you want a static IP version 4 public address, then you can have one of these. These are called elastic IP addresses. You allocate these to the AWS account, and then you associate them, with either an instance, or a network interface. Associating one of these with the instance, associates it with the primary network interface. If you associate an elastic IP, with the instance, then the IP version 4 address, that it originally had, so the non-elastic one, is released, and you can never get back that same IP address. If you remove the elastic IP, from the primary interface, then it gets a new, dynamic, non-elastic, public IP version 4 address. You can allocate up to one elastic IP, per private IP version 4 address, and these can be moved between interfaces, and could be moved between instances, because they belong to the AWS account, not to a specific instance, or interface. Elastic IPs are generally used, for entry points to applications, things which don't or can't use DNS, and also needs static addresses. Another important point to know, is that elastic IPs are charged, if they're not associated with an instance, or when the instance they're associated with, is not running. So you don't get charged for elastic IPs, if they're in use, but if they're allocated to your account, and not currently being used, there is a charge. Now elastic network interfaces, also have one or more public IP version 6 addresses, a MAC address, and one or more security groups. IP version 6 addressing, if enabled, are all publicly routable. So keep that in mind. Because they're publicly routable, they're also configured on the network interface, within the operating system, running on that instance. So unlike IP version 4 addresses, where you only ever have the private address configured, within the operating system, for IP version 6, because it's publicly routable, you always have the IP version 6 address, configured on the operating system network interface. The MAC address is a hardware ID, for the interface itself. This is used to identify the interface, to the operating system. It's used for lower levels, for networking, and often used for software licensing. But more on that in a second. Finally, each ENI has one or more security groups. When evaluating if data can enter an interface, all the security groups are evaluated together. And if an allow exists, then the traffic is allowed to enter, or leave the interface. Remember though, this is per interface, which means different interfaces, can have different security rules configured. So it can apply to different types of traffic. One last thing that I want to talk about, which has an architecture implication, is that each interface has a flag, which is associated with that interface, which performs what's known as source and destination checks. By default, if an interface processes traffic, where it's not the source or the destination for that traffic, then it's dropped. And disabling this check, allows an ENI to process, traffic that it hasn't created, or that it isn't the final destination for. And this is used for various different network, or security appliances, such as NAT instances. Now at the associate level, generally this was presented as an instance option. So you would disable or enable source and destination check, for the instance. But at a professional level, you need to be aware, that it's actually configured on a per interface basis. And when you enable, it for the instance via the UI, what you're actually doing is setting this, on the primary network interface. So anytime you interact with an instance, from a networking perspective, generally you're interacting with the primary network interface, for that instance. Now up to this point in the lesson, I focused on the technical aspects of EC2 networking. So where ENIs can be created, and what features they have. But what matters just as much, is to understand what architectures, this allows us to design and implement. So being able to have multiple network interfaces, multiple security groups, multiple IP addresses, allows us to create some pretty powerful architectures. We can use different networks with a single EC2 instance. So the example that I illustrated towards the start of this lesson, was having a management network and a public network. You can use different networks to manage your instances, so allow more open access, for admin staff, or have specific isolated networks, which apply specific restrictions. You can also use network interfaces for software licensing. So some legacy software is actually linked to the MAC address, of a specific interface on a specific machine. And so you can create a secondary network interface, attach it to an instance, use this MAC address for the licensing, and if you ever need to migrate the software, you can detach it, attach the network interface from the instance, move it to another one, attach it, and the software will continue to operate, because it's licensed based on MAC address, which is linked to a specific network interface. You can also use different network interfaces, for security or network appliances. So have traffic coming into one interface, and out of another, or vice versa. So having different network interfaces, or different subnets, gives you a lot of flexibility, for security or networking appliances. You can also use multiple network interfaces, to create multi-homed instances, with different workloads on specific subnets. So if you had a traditional multi-tier application, with a web server, an application, and a database server, you could have the web server communicating with the application server on one interface, and the database server communicating with that same application server, on a different interface. And on each of those interfaces, you could, in theory, apply different services. So you could have multiple network interfaces, and you could have multiple security controls. You can also use multiple network interfaces, to implement a low budget, and simple high availability solution. Imagine if you have two EC2 instances, which provide access to an application. You could have a secondary network interface, which is associated with one of those instances, at any one time. That interface has an IP address. It's through that IP address, that the service is provided. And if the instance fails, you can then have to change the network interface, to a different instance, and fail over as part of a high availability solution. So you have a lot of flexibility, by creating architectures using multiple network interfaces. Now with that being said, that's everything that I wanted to cover in this lesson. I just wanted to give you an architectural overview, of the types of features and capabilities that are provided, by using multiple network interfaces with EC2. At this point, I'll leave you with a quick overview of the features, and the features that are provided in this lesson. And when you're ready, I'll look forward to you joining me in the next.