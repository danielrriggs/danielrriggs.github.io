 Welcome back, and in this lesson I want to cover two protocols which are used by elastic load balancers. These are X forwarded for and the proxy protocol. Now both of these achieve a similar aim, but you have to understand when and where you would use both of them. So let's jump in and take a look. Now before I talk about both of these, it's worth stepping through why they are both required. Consider this architecture. A client using the IP address of 1.3.3.7 connects to an application server, so no load balancer gets used with this architecture. With this type of connection, one thing which the server is able to do is to record the IP address of the client, something which is very common and often essential for logging, site localization, security, and many other use cases. Now they do this. They do this by just looking at the source address of packets which they see as coming from the client IP address. With load balancers, this becomes more complicated. Consider this situation. Two customers who are looking to connect to our application, but now do so via a load balancer. And the load balancer, logically enough, load balances these connections over multiple backend instances. The problem that we've introduced here is that our client is not able to connect to the application server. Our clients connect to the load balancer, and then the load balancer connects to our backend servers. We know, looking at this architecture, that one backend connection is for Bob, and one is for Julie. The problem is that we don't know which is which. Both connections, from the point of view of the application servers, originate from 1.3.3.3.3.3.7, which is the IP address of the load balancer. There's no IP address of the load balancer. There's no IP address of the load balancer. There's no IP-based method of linking the original client IP address to the connections made from the load balancer through to the application servers. That's where X-Forwarded and the proxy protocol come in handy. Now let's step through each of them and see how they work, as well as how they differ. So first, let's look at X-Forwarded. This is a HTTP header. It's part of HTTP, which means that it only works with HTTP, and HTTP is a header. It's a layer 7 thing. It's a header which uses this format. So X-Forwarded, hyphen 4, and then a client. And this header is added or appended by proxies and load balancers as requests flow through those devices. So when the original client makes a request via a load balancer, this load balancer adds the client's IP address as this header as the leftmost item in the list. And then as it passes through, it's added to the client's IP address. And then as it passes through, it's added to the client's IP address. And then as it passes through, it's added to the client's IP address. through more proxies or load balancers any others are added into this list. So in this case you can tell the original client IP address is 1.3.3.7 and this request has also gone through proxy 1 and then proxy 2. It's the load balancer or the proxy which is adding or appending to this header and using it you can see the path that a request moves through from the client through to the final back-end server. Now the back-end server needs to be aware of this header and needs to support it. This might be in the form of an Apache module for example because from a networking perspective the request to the back-end server will still be from the closest proxy or load balancer but the X forwarded for will contain the original client and any intermediate proxies and load balancers. Now the header is supported when using HTTP or HTTPS configurations on classic or application load balancers. Network load balancers don't support it because they don't support layer 7 of the OSI stack and this is a layer 7 header. Remember when using HTTP or HTTPS listener configurations with application load balancers connections are terminated at the load balancer and so it has access to be able to view and or modify this header. But what about non-HTTP and non-HTTP? We might need the same functionality but when we're using a network load balancer to manage non-HTTP and HTTPS protocols. So let's look at an alternative. The proxy protocol works at layer 4. It's actually a layer 4 so TCP header which is added as data passes through a proxy or load balancer. It means that it can work with a wide range of layer 7 protocols including HTTP, and HTTPS but also other protocols such as FTP or SMTP. Now there are two versions a human readable version 1 and a binary encoded version 2. Version 1 is supported with classic load balancers and version 2 is supported with network load balancers. Importantly because application load balancers only support HTTP and HTTPS listener configuration they do not support proxy protocol. And so you generally use this with network load balancers which are then able to load balance a wider range of application protocols running at higher layers of the OSI stack. Now one very common situation where you might use proxy protocol, let's say that you had a requirement for unbroken HTTPS encryption between clients and your back-end instances. Well you couldn't use application load balancers as these can only listen using HTTP and HTTPS configurations and this will terminate the HTTP or HTTPS connections and then create new ones. So in this type of situation where you need this unbroken encryption you choose to use network load balancers with a TCP configuration. This means that it could support an unbroken HTTPS connection as what's being load balanced is the TCP connection. This means though that since the HTTPS connection is unbroken X.forwarded for cannot be used because there's no access to the encrypted HTTP headers. So in this case you choose to use the proxy protocol where the client IP header is added at the TCP level. That way the unbroken layer 7 payload could be carried by TCP and the IP addressing information is added at that level without requiring a MAC address or a HTTP address. So this is a very common situation where you might have to use network load balancers with a HTTP configuration. And so you choose to use change to the upper layer application protocol. Now this has been a lot of fairly in-depth theory and you just need to be aware of the differences between these two for the exam. So when you'd use x forwarded for and when you'd use proxy protocol. If you're comfortable with both of those then that's everything you need to take away from this lesson. So go ahead and complete the video and when you're ready I'll look forward to you joining me in the next.