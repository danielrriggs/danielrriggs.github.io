 Welcome back, and in this lesson I want to briefly cover two related topics, connection draining and deregistration delay. Now these are pretty simple to understand, so I want to cover them as quickly as possible, so let's jump in and get started. Connection draining is a setting which controls what happens when instances are unhealthy or being deregistered. The default behavior is that the load balancer closes connections and the instance receives no new connections. Connection draining allows existing connections, known as in-flight requests, to complete for a certain amount of time, while no new connections will be sent to that instance. It's a way of gracefully removing connections from a specific instance and reduces disruption to applications. Connection draining is a setting which controls what happens when instances are unhealthy or being deregistered. Connection draining is supported on classic load balancers only. It's not called the same thing on network or application or gateway load balancers. So if you see the name connection draining, be careful to remember that it's classic load balancers only. And this setting with classic load balancers is defined on the load balancer itself. Remember, classic load balancers don't use the target group architecture, which is used by all of the newer generation of load balancers. Connection draining is a timeout value between 1 second and 3600 seconds, with the default being 300 seconds. If an instance becomes unhealthy because of a failed health check, then this doesn't apply. If an instance is taken out of service either manually or by an auto-scaling group, then it's listed as in-service instance deregistration currently in progress. And if you're using an auto-scaling group, it waits. It waits for all connections to complete before terminating or for the timeout value, whichever occurs first. The important point to take away from this lesson is that connection draining is for classic load balancers, and this feature allows for connections to be gracefully removed from a load balancer as an instance is going out of service. The other thing I want to talk about is the deregistration delay. And this is essentially the same feature with slight differences, but which is supported on application network and gateway load balancers. Now because of the new architecture, this is defined on target groups, not the load balancer itself. And essentially this controls the same process as connection draining. It essentially stops the load balancer sending requests through to deregistering targets. So existing connections can continue until they complete naturally, or until the deregistering is complete. So this is the default configuration for the load balancer. Now this is enabled by default on all of the new generation of load balancers. The default is 300 seconds, and valid configurations are zero or disabled through to 3600 seconds. So these are both different names which control roughly the same process. It gives a graceful way for connections to age out and complete up to a timeout value, which minimizes delay. It's a very useful feature for all of the new generation of load balancers. And it's a very useful feature for all of the new generation of load balancers. So this is the default configuration for the load balancer. So this is the default configuration for all of the new generation of load balancers. As part of auto scaling groups, or to take instances out of service for maintenance activities, and all of this without disrupting the application that you're provisioning to your users. Now that's everything you need to know. You just need a basic understanding for the exam. So go ahead and complete this video, and when you're ready, I look forward to you joining me in the next.