 Welcome back. In this lesson, I want to talk about two different ways that you can configure an EC2 instance, bootstrapping and AMI baking. Now, systems might use one or the other, or even both, in order to achieve efficient and timely deployment. And in this lesson, I want you to finish by being completely comfortable in using one versus the other, or even both, in your own architectures. So let's jump in and get started. Now, before I talk about both of these techniques in detail, I want to focus on the base concept of what being ready for service or being complete means from an application perspective. Let's say that you have an application, and I'll use the example of Categram Enterprise. And this is an elastic application, meaning it runs inside an autoscaling group and has a load balancer to abstract customers away from the underlying infrastructure. Now, if you're using a Categram Enterprise, you're going to want to use a load balancer to abstract customers away from the underlying infrastructure. Now, when the application receives additional load, a scaling event occurs, which causes one to be added to the desired value of the autoscaling group. And this causes an additional EC2 instance to be provisioned. Now, at a professional level, you need to think about this in terms of so what. What happens at this point? Well, the autoscaling group instructs EC2 to launch a brand new EC2 instance, using the configuration of the EC2 instance, and the application receives additional load. And this is the configuration that it has inside its launch template. Now, you've launched an EC2 instance before. You know that a basic EC2 instance can take anywhere up to five minutes to launch, and this is just for its pure native state. After five minutes, when the EC2 instance and its operating system are ready, then you'll probably have to install and configure the application on that EC2 instance. You'll need to test that it's functioning okay, and combined, how long will this take? Logically, that depends on the software. It could be 20 seconds or 20 minutes. Remember the advanced VPN demo lesson earlier in the course, where you installed FR routing? Remember how that took 10 or more minutes to download and build the application? And that wasn't really a complicated application. So you have to keep in mind that when it comes to provisioning complex applications on EC2, you have the time to launch the EC2 instance, as well as the time required to install, configure, and test the application software. Now, finally, at the end of the process, the instance will be ready for service, or it will fail. If it's ready, it can be brought into service from the point of view of the autoscaling group and begin servicing requests. And this is potentially 5, 10, or 20 minutes after the autoscaling group requested this additional EC2 instance. If the instance fails to build correctly, then the process might need to be restarted again, which can cause further delay. So why does this matter? Well, let's take a look. Let's continue with the same example, the CataGram Enterprise application. And as I mentioned on the previous screen, this is running inside an autoscaling group, which has a load balancer to abstract the customers away from the underlying infrastructure. Now, if you're using a single-use application, you can use the same application to run the application. Now, if you're using a single-use application, you can use the same application to run the application. Now, conceptually, right now, we have a single EC2 instance, which is supporting the load of four users. Logically, because the CataGram Enterprise application is about cats, it becomes massively popular, and so additional company users want in on the feline action. The single instance we have now is over capacity. Without additional action, the user experience will be impacted, and so based on this additional load, the design will be more complex, and the user experience will be more complex, and so based on this additional load, the desired capacity of the autoscaling group updates. It adds an additional two to the current value of one, meaning that the desired capacity is now at three. Now, the problem that we have is that we already have load on the system right now. There are now 12 users rather than the original four, and no additional instances to provide the capacity to service those additional users. So what happens next is that, based on the desired capacity increase, the autoscaling group begins to scale, and EC2 begins provisioning new instances, but this takes time, anywhere up to five minutes, at which point the base operating system on those instances are ready to go. But we only have the bare EC2 instances. We still need to install the software, and let's say that this takes an additional 20 minutes. And let's say that this takes an additional 20 minutes. But this takes an additional 20 minutes. Now, after this 20-minute period, the instances are ready for service, they get added to the load balancer, and the service can now support those additional eight users, bringing the total to 12. The time taken for EC2 to provision instances and then the time required to install the software, these are both examples of provisioning or ready-for-service lag. This is the total time between when the autoscaling group . . . compute instances from EC2 and the point at which they're ready. It's the time when capacity is potentially running below the level required to service the load placed on that system. And as a solutions architect, you need to minimize or avoid this period as much as possible because it's what causes customer disruption or a bad user experience. So let's look at how we can avoid this ready for service lag. When you think about an application which you need to install on one or more EC2 instances, think about it as three individual components. First, we have the base application and any dependencies that the application has. This might include operating system updates, frameworks, or runtime environments such as Python or Node.js. Anything that the application needs to run and then the base install of the application itself. Next, we have any application updates from that base install to the current point in time. Now, depending on how your application is designed, you might have major base versions and then hotfixes, or you might just bundle them all together. So every hotfix is merged into the application and you have lots of big self-contained major versions. But conceptually, an application consists of the base install and then any dependencies, as well as one of the application updates. So this is a very simple way to do this. So let's look at how we can avoid this ready for service lag. Now, lastly, we have the application configuration, the part of the application which is specific to a given customer or a given customer install. Now, the base install of the application is usually very slow to perform. It has lots of moving parts and for larger applications can take significant amounts of time. But it's also pretty generic and doesn't change all that much between different installs and different clients. The application updates are still relatively slow, but they're quicker than the base install. So it can still take some time. And these are usually mostly generic, not really changing between the different customer or different situational installations. In contrast, the application config is usually very fast to perform, but it's also usually very specific for a given client. So these are three different components which have different architectures in terms of the install speed and how specific they are to a given client. So because each part of an application has different characteristics, as a solutions architect, we need to think about each of these in a slightly different way. With the base install, because it doesn't change often or between clients, we need to optimize this for speed. So minimize the amount of time it takes for the base installation of the application in some way. Now we've got various different AWS tools that we can utilize to improve the speed, which I'll talk about a little bit later on in this lesson. For the application configuration, because it's fast anyway, we focus as solutions architects on the flexibility, making sure however we implement it, that we maintain this flexibility. Because it's the flexibility, we can't just go and do it all at once. So we need to make sure that we're doing it all at once. So we need to make sure that we're doing it all at once. which will allow us to deploy this application for different clients in different regions, for different particular installs. So we need to focus on maintaining the ability to customize this component of the applications. Now within AWS, we have two main choices for ways that we can decrease the provisioning lag for a particular application. We've got bootstrapping and we've got AMI baking. So now that I've set the scene, I want to focus on the topics of this lag. And talk about the differences between bootstrapping and AMI baking. And we're going to start with bootstrapping. Bootstrapping is a way of building EC2 instances, which isn't particularly fast, but it is super flexible. It's designed when your priority aim is the flexible, automated building of EC2 instances together with applications. Simply put, you provision an EC2 instance, but while doing so, either manually or using automation, you add a script into the user data of that EC2 instance. Now the script is delivered to the operating system and using a system called CloudInit, the script is executed by the operating system on that instance. The operating system could be Windows or a variant of Linux, but in either case, the instructions are run when the instance is launched. This process is flexible, but it also takes time. If you have an application which takes 20 minutes to install and 5 minutes to configure, if you only use bootstrapping, then the whole process would take 25 minutes. But when finished, you have an instance which is ready for service. So an EC2 instance with the application installed, ready to service requests from your user base. Now bootstrapping, because of its flexibility, means that you can do anything. Anything you can script. Anything you can automate can be done using bootstrapping. But conceptually, realize that all bootstrapping is, is just launching an EC2 instance and running a script which performs an application install or application configuration. So it takes time. Contrast this though with AMI baking, which we're going to talk about now. With AMI baking, the idea is to front-load the time and effort required to install and configure an application. With this method, you launch and master EC2 instance. And once you've launched that instance, you perform all of the time-consuming tasks upfront. So you patch the operating system, install any application components, and then install and configure the application. Your aim is to get the instance to a ready or almost ready state at this point of the process. It's also worth mentioning that there's nothing preventing you from using bootstrapping. To get the instance to this point, so that you have an automated process. Bootstrapping is just a method of doing a software install. So as part of AMI baking, you can either manually install and configure things, or use bootstrapping via user data to automate the install and configuration process. In either case, once you have the instance in a ready state, you can configure an AMI from the master EC2 instance. So you're taking the actual instance, and you're going to configure it. So you're taking the actual configuration at a point in time of the master EC2 instance, and saving it into an AMI. Now from that point, you can use the AMI to create any number of instances. You might use it to create one or 100. AWS doesn't care. Because you front-loaded all of the time-consuming work, the time taken to provision EC2 instances is vastly reduced versus bootstrapping. Essentially, using this custom AMI, you can create any number of instances. Essentially, using this custom AMI, you can create any number of instances. Essentially, using this custom AMI, you can create any number of instances. The provisioning time is the same as if you were using a normal AMI which just contained a base operating system. So AMI baking is used when you want to bake into an AMI all of the time-consuming parts of an installation or a configuration. The trade-off is that it's harder to adjust things once they're AMI baked. Once you've downloaded certain versions of applications and performed certain configuration, and then created an AMI from the state of that instance, it's relatively difficult to change that configuration. So generally, you would look to bake into an AMI the time-consuming parts or the parts which changed relatively infrequently. Now one common misunderstanding that many people have is that bootstrapping and AMI baking are mutually exclusive, that you have to pick one or the other. That's not true. In fact, you can achieve the best results by combining the two. By combining both of these different processes. Now a combination of these architectures looks something like this. You start off in the same way by launching a base EC2 instance. Then you do the difficult part, the time-consuming part. You perform the base application install and configuration. What you leave off though is any customer-specific or situation-specific configuration. We leave that until later. Once you've done all of the time-consuming parts, you make an AMI. And so far, this might sound like AMI baking, and it is. It's the same process. The difference though is that over the top of this baked-in configuration into the AMI, when you're launching EC2 instances, you can customize this base AMI using user data. So any custom configuration, any sensitive data that you don't want to live on an AMI, you can use user data and bootstrapping to add this at launch time as you launch multiple instances. Now this has the benefit that you still get fast provisioning time by using AMI baking. So you bake in any time-consuming parts into the AMI. And this means you don't need to do this on a per-instance basis. But you also get the benefit of being able to customize this AMI baked configuration by using bootstrapping at launch time. Now this is an architecture that's often used in production and often features in the exam. If you have a requirement to get an application into service within 10 minutes, but the install takes 20 minutes, then your only option is to AMI bake all or part of the application. But you can split this up. You can bake in only the time-consuming and static component of the application install, and then use bootstrapping to add that layer, but the flexibility over the top. So if you see any exam questions which require you to provision something as part of an auto-scaling group, and you see there are time restrictions on how quickly that application needs to be brought into service, then think about doing any of the time-consuming parts as part of an AMI bake, and then performing the configuration using bootstrapping via user data as part of launching the EC2 instances. Now in terms of this lag, imagine that you're using a computer, and you have a very variable load placed on an application, where an application could be using one instance at one moment, and then it requires to scale up to 10 really quickly during peak periods. As a solutions architect, you can achieve much better performance and a much improved user experience by lowering this lag time as much as possible. So as soon as an auto-scaling group requests additional compute instances, they're brought into service as soon as possible. And you can do that by using a combination of AMI baking and bootstrapping. And by understanding both of them, you can be an effective solutions architect. But with that being said, that's everything that I wanted to cover in this architectural theory lesson. So go ahead, complete this lesson, and when you're ready, I'll look forward to you joining me in the next.