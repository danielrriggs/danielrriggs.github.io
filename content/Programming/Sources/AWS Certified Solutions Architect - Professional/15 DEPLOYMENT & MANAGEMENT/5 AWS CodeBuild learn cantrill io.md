 Welcome back, and in this lesson I want to talk in more detail about AWS CodeBuild. Now CodeBuild is another component of the CICD platform available within AWS and forms part of most code pipelines. So let's just jump in and get started. CodeBuild is a build-as-a-service product. It lets you control a managed build environment where you only pay for the resources consumed during the build process. Now normally, if you weren't using CodeBuild, you'd need to run up something like a Jenkins server which would consume resources all of the time, whether you were using it for builds or not. With CodeBuild, it's a pay-as-you-use service, so you consume resources only while the builds are happening. Now CodeBuild is an alternative for part of the functionality which a tool such as Jenkins, provides. So Jenkins does more than just building, but it's a tool that you have to configure extensively before you can use it. CodeBuild is actually used for both builds and tests, so both of those occur within a build environment. Now CodeBuild uses Docker to create consistent build environments, and these can be customized, and I'm going to explain how this works very soon. CodeBuild also integrates with AWS services, such as KMS for encryption, IAM for permissions, VPC for networking, CloudTrail for API auditing, as well as S3 for storage and much more. Now architecturally, CodeBuild gets source from things like GitHub or CodeCommit, CodePipeline and even S3. It builds and tests code, and the environment that it uses to build can be customized using a file called buildspec.yaml. Now take note of the spelling, and take note that this file has to be in the root of the source. Both of these are really important. They both come up in the exam, so do your best to remember both points. So first, the spelling of the file, and then second, the fact that it has to be located in the root of the source for the application that you're building. Now CodeBuild can log output relating to builds to S3 and CloudWatch logs. It can store build metrics, within CloudWatch, and it pushes events relating to builds into EventBridge. So you can have event-driven responses to your build process using things like Lambda functions. In terms of build environments which CodeBuild supports, currently this includes Java, Ruby, Python, Node.js, PHP, .NET Core, Go, and many more. At an associate level, you just need to have this foundational understanding. In-depth knowledge is something much more important at the professional level. Now visually, it looks like this. So CodeBuild works around build projects and build environments. These are used to build and test code. The environment can be customized by either making your own Docker images, or by using the buildspec.yaml file within your code repositories. Now the build projects themselves and controlling builds, this comes from the S3, from the CLI, the SDKs, or code pipeline. During builds, CloudWatch is used for metrics and logging data, and as I just mentioned, any build events are sent through to EventBridge. Any artifacts, so this means the outputs of the build process, these are generally sent to S3, and again, this is controlled by the buildspec.yaml file. Now I want to focus on that file for a few minutes before we finish this lesson, because it's hugely important. For the exam especially, and if you use CodeBuild for real world usage, the buildspec.yaml file is essential to understand. First, really focus on the name, the spelling, and the fact that it has to be at the root level, so the top level of your source. It customizes what happens during the build process. And when you hear the term build, think of build and test, because technically they work in the same way. A test is essentially a build, and a test is a test. A test is essentially a type of build. Now a buildspec file has a few major components. First, are four different phases, and again, remember these because they do feature on the exam. We have the install phase, which is generally used when you need to install base level things in the build environment, so things like frameworks or other tooling. Don't use this for things like code dependencies, this is only for major environment installations. The next is the pre-build phase, and this is where you can sign into things or install dependencies required by the application that you're building and testing. Next is the build phase, which runs the commands used to build the application. So this is the main part of the process, the part where your source code is built into your application. And then finally we have post-build, and this might be used to package things up, or to push Docker images, or to do explicit notifications. Focus on just trying to remember when you would use each of these phases, because they do come up in the exam. The buildspec file also allows you to define environment variables, which might be used during the build process. These might be related to the shell or general environment variables, and can integrate with the parameter store or secrets manager, which code build fully integrates with. And then finally we also have the artifacts part of this file, which are typically used to build the application. This is a high level controls what stuff to put where, so it allows you to customize artifacts and where they're stored. And that's it, that's the buildspec.yaml file. Remember the spelling, remember the components, and remember that it has to be at the root of your source. With that being said, that's everything I wanted to cover in this lesson for the theory of code build. At this point go ahead and complete the video, and when you're ready, I look forward to you joining me in the next lesson.