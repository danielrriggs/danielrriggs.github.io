 Welcome back, and in this really brief lesson, I want to talk about how RDS and Elastic Beanstalk can interact when using database services within Elastic Beanstalk environments. Now, this is going to be a relatively brief lesson, but it's really important to understand for the exam. So let's jump in and get started. Now, if you want to use database services, so RDS, within your Elastic Beanstalk environments, you have two different choices. First, you can create an RDS instance directly within an Elastic Beanstalk environment. And if you do that, it's linked to that specific EB environment. Now, this means if you delete the environment, then you delete RDS, and that can result in data loss. It also means that different environments have different RDS databases and different data. So you need to be really careful when using RDS that's linked to an Elastic Beanstalk environment. And generally, you'd only ever do this for small-scale development and test environments. So that's something that's really important to keep in mind. And if you do create an RDS instance that's linked to an Elastic Beanstalk environment, then you'll automatically gain access to some environment properties. And these are RDS underscore hostname, RDS port, RDS DB name, RDS username, and RDS password. And any application code that's running within... ...an Elastic Beanstalk environment can use these environment properties to get access to the information needed to connect to the RDS instance. You also have another alternative, and that's to create the RDS instance outside of Elastic Beanstalk. Now, if you do this, you have to manually create the environment properties. So the same ones that I just discussed, the hostname, the port, the database name, the username, and the password. But if you choose to use this method, then the environments will be... ...separate from the RDS database. So you can make any changes that you want to environments safe in the knowledge that your data will be safe. Because changes to environments will not make any changes to RDS. Now, something that's really critical for the exam, if you want to decouple an existing RDS instance... ...so one that's running within an Elastic Beanstalk environment... ...if you want to take this and separate it from the Elastic Beanstalk environment, then there is a process to use. Third... ...first, you need to take a snapshot of that existing RDS instance, which is within the Elastic Beanstalk environment. Then you need to enable delete protection on that RDS instance. Then you need to create a brand new Elastic Beanstalk environment with the same application version. Next, you'll need to ensure that the new environment can connect to the existing database. So make sure the security group configuration is correct, and make sure those environment properties are correct and pointed. Then you need to enable delete protection on that RDS instance. Then, once you've got all that in position, and once it's verified, then you can swap the environments... ...either using a cname swap or using external DNS. So now this new environment, which doesn't have a built-in RDS instance, it's now pointing at the existing RDS instance. So the lifecycle is separated. Now you need to terminate the old environment. Now this is going to try to terminate the RDS instance. Because the lifecycle... ...is linked between those two things. But because you've enabled delete protection, this won't be possible. The RDS instance will refuse the request to terminate that instance. So the old environment is going to be removed, but then you need to go to the CloudFormation console... ...and look for the CloudFormation stack that matches that old environment. And you'll note that it's in a delete failed state. Now this delete failed state is as a result of not being able to terminate the RDS instance. So now you need to manually delete the stack and choose to retain any stuck resources. And the one specific one that we'll be looking for at this point will be the RDS instance. So choose to retain that, delete the stack, and you've now completed the migration from an RDS instance that's inside an EBI environment... ...through to having a separated EBI environment and an RDS instance. Now this is a process that's fairly easy to follow, but it's something that you should remember for the exam. Now at this point, that's everything I wanted to talk about. So now I'm going to go back to the CloudFormation and I'm going to go to the CloudFormation. And I'm going to go to the CloudFormation and I'm going to go to the CloudFormation. So to cover, I did promise it would be brief. Go ahead and complete the video. And when you're ready, I'll look forward to you joining me in the next lesson.