 Welcome back, and in this demo lesson, I just wanted to briefly step you through the important features of AWS CodeCommit. Now, don't feel as though you need to do this in your own environment. You can if you wish, or you can just watch me step through the key features. Now, I'm logged into the IAM admin user of the general AWS account, so this is the management account of the organization, and I've got the Northern Virginia region selected. So I'm going to go ahead and move across to the CodeCommit console. So I'm going to click in the search box, type CodeCommit, and then open that in a new tab. So the basic entity of CodeCommit is a repository, and a CodeCommit repository, as you've learned in the theory lesson, is exactly the same as the repositories which you've used on things like GitHub or GitLab, apart from the fact that it's delivered by AWS and it integrates with AWS services. So I'm going to go ahead and create a repository. I'll have to give the repository a name, so I'm going to call it CategramRepo, I can optionally give the repo a description, but I'm not going to do that. Instead, I'll just scroll down to the bottom and click on create, and that's it. Creating a repo is as simple as that. Now, you'll have seen from earlier in the course how you can clone a repository from something like GitHub down to your local machine, and you do that by using the git clone command as well as the repo URL. And the process with CodeCommit is exactly the same. Now, the difference is that we need to authenticate, in order to access this repository. Now, security for a CodeCommit repository is configured in a number of ways. Just like with GitHub, two popular methods to access a repository in order to clone it or interact with it in other ways is using HTTPS or SSH. Now, in order to use HTTPS, you need to authenticate using a username and password, and if you decide to use SSH, then you need to use an SSH key pair. Now, both of those are actually configured, from the IAM console. So, if I click in the search box, type IAM, and open that in a new tab, I'll be able to configure the authentication information within my IAM user record within IAM. So, I'm going to click on users. Now, I'm currently logged in as the IAM admin user, so I'll select that user. Then I'll click on the security credentials tab, and if I scroll down below the sections that you've used up to this point in the course, so we have sign-in credentials and then access keys, if I continue scrolling down, we have SSH keys for AWS CodeCommit, or I can upload an SSH public key in order to access a CodeCommit repository, or alternatively, I can generate credentials to access this repository using HTTPS. Now, if you've already got an SSH key pair, generally, this is the better method to use to access a repository. So, if you're used to using GitHub, especially within corporate environments, generally, you'll access this, using an SSH key pair. For this demo, I'm going to go ahead and generate some HTTPS credentials. So, I'm going to click on generate credentials, and these credentials are going to be generated for my specific IAM user. So, I'm going to go ahead and clone this repository. So, I'm going to leave this screen up and return back to the CodeCommit console, and I'm going to copy the git clone command into my clipboard. Then I'm going to move across to my terminal, and I'm already inside a directory which I've created specifically to test this, and I'm going to paste in that git clone command. Now, when I press enter, I'm going to be prompted for authentication information. And just to get ready for that, I'm going to go back to my AWS console, go to the IAM console, and just copy down this username. Then I'll go back to my terminal, and I'm going to press enter. So, now I'll be prompted for the username, so I'm going to paste that in and press enter. Then it will ask for my password. So, I'm going to show this password and copy it into my clipboard. Now, this will obviously be deactivated by the time that this video is live, so don't attempt to use either of these credentials. I'll go back to my terminal, paste this in, and press enter. And I'm told that I've cloned an empty repository, and this is logical because there are no source files within this repository. If I do an ls space hyphen la, I'll be able to see the cloned repository, and I'll be able to see the cloned repository, on my local machine. So, I'm going to move into that folder. I'll clear the screen to make it easy to see, and because I've got this terminal configured to do so, I can see that I'm currently using the master branch of this repository. Now, there are no files inside this folder, so let's fix that. Now, in order to create a file, I'm going to run nano, which is my command line editor, and then a space, and then catagram.cobol. So, I'm going to go ahead and press enter, and then I'll just type some code, and I'll save it by pressing control and o, and then enter and control and x to exit out of that application. So, now if I do an ls space hyphen la, now I have this catagram.cobol file within my local folder for this repo. So, now I'm going to add this file, so I'm going to do a git space add, then a space hyphen big A space, and then a period. And this will add all of the files in this local folder, ready to commit to this repository. Once I've added that file, I'm going to run a git space, commit space hyphen m for message, space, open speech mark, and then important space, cobol space code, and then hyphen how retro, and then close it with a speech mark. Then I'll press enter, and that's going to have committed the changes that I've made so that new file to my local repository. And now I want to push that up to our code commit repository. To do that, git space push and then press enter. That's going to push my local copy of this repository up to the code commit repo. So, if I go back to the console, go back to the code commit console, click on code, now I'll be able to see the contents of the repository that have now been pushed up to code commit. And we can see that we've got catagram.cobol, and if I click on it, I'll be able to see the contents of this file. So, some code here. So that's been a simple example of how you can interact with code commit. Now this lesson isn't about the functionality of a source code repository. If you need to learn about how a source code repository works, then I'm intending to add a git and github fundamentals lesson to the start of this course, once I've finished all of the exam lessons. Now you can either watch this if it's in place when you see this video, or you can do your own research to learn how a repository works. They all function in much the same way, but I want to focus in this lesson on code commit specifically. Now there's a couple more things that I want to show you before I finish up with this brief demo lesson. I can click on commits, and I can see all of the commits which have been committed to this repo. So I've only done one so far, which is when I committed the important COBOL code, how retro, and I'm able to browse exactly the same code as I did when I submitted the code. I can also see the branches of this repository. Currently there's only master, which is the default branch, but if this was a real project, then you would probably have many different branches. Now if I go to settings, I can view all of the important information about this repository, such as the name, the repository ID, and then the unique ARN for this repository. I can click on notifications, and create a notification rule, which can send a notification based on events on this repo. So I could create a notification, decide whether for detail type I wanted full or basic, and this just changes the amount of information that's sent along with this notification, and then I could choose which events trigger that notification. So I could generate a notification when a commit happens, when a pull request happens, when statuses change, when rule overrides happen, when a pull request happens, so when sources updated, created, status changed, or merged, as well as various branch and tag actions. And I could send this notification to either an SNS topic, or an AWS chatbot using Slack. Now as well as notifications, if I just scroll down and cancel this, just discard this notification rule, and then go to triggers, I'm also able to create a trigger. And this is distinctly different, because this allows us to generate event driven processes, based on events that happen on this repository. So just like notifications, the architecture is the same. A trigger is going to occur when something happens on this repository, but it's going to generate an event, which can be sent to Amazon SNS, or, and this is the important part, it can invoke a Lambda function. So this functionality allows you to create rules, where a Lambda function is invoked, and can itself perform any action, which you can code in that Lambda function, when something happens on this repo. So for example, if we clicked in the events drop down, we can create a trigger that happens on all repository events, when pushes occur to an existing branch, or when branches are created, or tagged, or deleted, or tagged. So we can create any functionality that we want, based on these types of events. And this is really important, because this does feature on the exam, and this is a powerful feature, which as I've mentioned, allows you to create event driven workflows. Now in terms of the security of CodeCommit repos, I've demonstrated how this is controlled from within the IAM console. Now the username and password, which I've showed you how to define within IAM users, this is used for authentication. So this is how, when we use git from the command line, we're able to log in with a username and password. So that's authentication. Now authorization, so controlling what we have access to, inside CodeCommit, that's based on IAM identity policies. So because I'm logged in using the IAM admin user, and because this is an admin user, I'm able to perform any actions on this CodeCommit repo, and access any of the data contained within it. But you are able to be much more granular, and define certain IAM users, are only able to perform certain operations, or that they're only able to access certain repositories. So it's really granular, and it's fully integrated, with both AWS security, and AWS event and notification products. Now at this point, that's everything I wanted to cover in this demo lesson. That's everything that you need to be aware of for the exam, and it's the type of product, which you will get familiar with very quickly, as you have need to use it in the real world. It shares much of the functionality with GitHub repositories, and you can interact with CodeCommit repositories, using the same tooling that you're already familiar with. Now at this point, what I want to do before I finish the demo, is just click on close here, and delete these credentials. So I'm going to select them, click on actions, and then delete the credentials. Then I'll need to type delete here, and then click to confirm that deletion. And at this point, that's everything that I wanted to demonstrate. So go ahead and complete this video, and when you're ready, I'll look forward to you joining me in the next.