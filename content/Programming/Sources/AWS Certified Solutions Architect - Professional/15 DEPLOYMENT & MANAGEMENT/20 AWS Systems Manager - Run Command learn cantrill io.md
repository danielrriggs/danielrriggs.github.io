 Welcome back. In this lesson, I want to talk about a specific feature of AWS Systems Manager, which will be an essential tool in your toolbox, but also it features on the exam all of the time, and that's Systems Manager Run Command. Now, I want to keep this brief because while it's important, it's also pretty simple to understand. So let's jump in and get started. Run Command is a powerful feature of Systems Manager. Simply put, it allows you to run commands on managed instances. More specifically, it allows you to take a command document from the Systems Manager document store and have the agents on managed instances execute it. Now, it allows this all without any access to the instances via SSH or the remote desktop protocol. Command documents can be executed on individual instances based on tags assigned to instances or resources. Now, Command documents can be created on a single source group, which can be created based on definable queries, which themselves can be run across AWS and on-premises infrastructure. Now, command documents define what steps a command has, and they can be reused and accept parameters. Command documents can be simple things like running a shell command, joining a machine to a domain, or even creating a fully featured domain controller. Anything that you can define, you can create within a domain. Command documents can be run on a single source group, which can be run across AWS and on-premises infrastructure. Now, the structure of these documents is outside of the scope of this lesson, but I will include a link attached to this lesson with some extra reading if you're interested. Now, if you're running a command on lots of managed instances, then you can control a few things via rate control. The first is concurrency, which defines how many instances you should run the command on at a time. And then the second option is the error threshold, which defines how many instances you should run the command on at a time. And then the third option is the error threshold, which defines how many individual commands running on individual instances can fail before the whole command fails. Now, the output of commands can be sent to S3, and the system can generate notifications which can be sent to the simple notification system. One really cool aspect of the product, though, is that you can actually have a run command as a target of an event bridge rule. Event bridge you can think of as CloudWatch Events version 2, but it lets you create event-driven execution of run commands. Now, this could be simple things like scheduled execution or run commands which run in direct response to an event or occurrence within an AWS account. Now, it's a really cool and super flexible feature of Systems Manager, and visually it looks like this. So we have a pretty typical AWS environment with the Systems Manager endpoint and service running in the AWS public domain. Now, there are loads of ways of initiating run commands, and a selection of these includes Systems Manager itself, event bridge, the console UI, and the command line interface. Now, these interactions are formed from two things, the chosen command document and target, together with any parameters for that document. Now, these two in combination cause a command document to be loaded from the document store and into Systems Manager for processing. Now, I talked about on the previous screen that there are three common ways to choose which instances have these commands executed on them. You can either create a resource group via a query, target specific tags, or explicitly pick an instance. These managed instances will all have the Systems Manager agent installed on them, which is how the communication will happen to actually execute the command document. Now, the Systems Manager service will communicate with the agents, sending the command document, and so the command, for each of them to execute. You can influence the number of instances which run these command documents at a time via the concurrency setting, as well as defining how many can fail before the whole job fails, and this is using the error threshold option. Now, run command is really powerful in isolation because it provides some really useful functionality where you can execute any command or any set of commands that you can design across AWS and on-premise instances. But why it's even more powerful is because it supports other Systems Manager features such as Patch Manager, which I'll be covering next. For now, though, this is all the theory that you'll need. So go ahead, complete the lesson, and when you're ready, I look forward to you joining me in the next.