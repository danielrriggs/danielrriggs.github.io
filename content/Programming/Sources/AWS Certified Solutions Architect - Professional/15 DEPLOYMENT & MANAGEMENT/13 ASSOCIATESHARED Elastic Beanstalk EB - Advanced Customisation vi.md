 Welcome back, and in this lesson I want to very briefly talk about a feature of Elastic Beanstalk called EB Extensions. But before we do that, I want to quickly show you something, and to do that I'm going to move across to my console. So I've got my AWS console open now, I'm logged in as the IAM admin user to the general AWS account, which is the management account of the organisation, and I've got the Northern Virginia region selected. And this is the same region and same account that I created the Elastic Beanstalk application and environments within in earlier walkthrough lessons in this section of the course. Now what I want to demonstrate is if I move across to CloudFormation, Elastic Beanstalk uses CloudFormation to provision the infrastructure that it uses for its environments. Now I've got two environments configured within this region in this account. I have the Categram POC-MV, which is the region that I created the Elastic Beanstalk application in. which is the development environment, and the Categram-MV-PROD, which is the production environment. And when these environments were created inside Elastic Beanstalk, it utilised CloudFormation to provision the actual infrastructure. If we go into one of these stacks and then click on template, we can see a CloudFormation template which has created all of the different resources. Now this is pretty difficult to read in this view, but it does create all of the different resources. But it does create all of the infrastructure that's needed for the environment as you create it. And if you make modifications to either of those Elastic Beanstalk environments, then it's going to perform an update stack operation to make those infrastructure changes. So it's important for the exam that you're aware that Elastic Beanstalk is actually based on CloudFormation. It uses CloudFormation to perform the infrastructure actions rather than doing them directly. And that's important because now I'm going to start talking about the infrastructure actions. So I'm going to start talking about EB extensions, and you'll see that they're also based on CloudFormation. Now because Elastic Beanstalk utilises CloudFormation, so CloudFormation is its foundation for creating and updating infrastructure, it's possible to use CloudFormation to customise it. And we do this using a feature of Elastic Beanstalk known as EB extensions. So EB extensions are a way that you can include directives to customise Elastic Beanstalk environments. And the way that this works is inside the application source bundle, so the zip file or the .wa file that contains the EB application version that we want to deploy, we actually adjust what's inside that application source bundle. Specifically, we create a .eb extensions folder. And then inside this folder, anything added which is JSON or YAML and has the .config extension is treated. And then inside this folder, anything added which is JSON or YAML and has the .config extension is treated. As an EB extensions configuration file. So this is CloudFormation formatted. It uses CloudFormation definitions to influence the configuration of the Elastic Beanstalk environment itself. So we can change configuration settings or we can create brand new resources. Now these configuration files, so anything ending in .config inside EB extensions, they have a number of different major configuration elements. The first is OTP. Option underscore settings. And this allows you to set options of resources. We've got the resources section which allows you to create entirely new resources. An example, if you wanted to provision Elastic Ash and use it with Elastic Beanstalk, you could do that using EB extensions. Now in addition to this, and you will have learned about these different headings when I covered CloudFormation in it, there's also packages, sources, files, users, groups. Commands, container commands, and services directives which can be inside these .config files. And so you can use EB extensions to create files, download material, run actual commands on the instances, create users and groups. There's lots of different things that you can do with these .eb extension files. And I've included a link attached to this lesson with some examples provided by AWS and by the community. But at this point, that is. Everything I wanted to cover, I just wanted to give you a high-level overview of how to use EB extensions. And just make sure that you're aware that Elastic Beanstalk is using CloudFormation as a foundational resource. It's the way that it creates and updates and eventually deletes AWS resources. With that being said though, that is everything I wanted to cover in this lesson. So go ahead and complete this video. And when you're ready, I'll look forward to you joining me in the next.