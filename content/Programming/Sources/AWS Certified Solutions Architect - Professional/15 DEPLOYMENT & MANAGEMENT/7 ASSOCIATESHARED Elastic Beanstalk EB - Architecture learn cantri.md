 Welcome back, and in this lesson I want to talk about Elastic Beanstalk, known as EB. Now let's start with the basics. Elastic Beanstalk, which again is known as EB or Beanstalk, is a platform-as-a-service or PAS product within AWS. Platform-as-a-service is a type of service model where the vendor handles all of the infrastructure, with you as an end-user generally just providing the code of some sort, which is executed by the infrastructure provider. So you provide some code, and the platform-as-a-service product manages any infrastructure running around this code. Now Beanstalk is a developer-focused product, so it's not something which is going to be used by non-technical end-users. At a high level, it provides managed application environments. The idea is that the user of the service provides application code, and Elastic Beanstalk... handles the infrastructure environment to be built around that application. Put simply, from a use case perspective, it lets a user of the service focus on code, and removes a lot of the infrastructure overhead, versus something like CloudFormation or implementing everything manually. Now this is an important distinction for the exam. CloudFormation provides full control of infrastructure, but you have to design it, write it, and manage it. It's... infrastructure as code. Elastic Beanstalk tries to take the application code from a developer, and allow them to run it within AWS, without really giving much thought to the infrastructure, which is built around an application. Understanding this key distinction will help you do well in the exam. So if you're faced with a question where an end user needs to host an application, then it might be that Beanstalk is not the right solution. If you have small development teams, or an individual developer who just wants to get an application up and running as easy as possible, then Beanstalk could be perfect for that scenario. Now Beanstalk is actually fully customisable. As a developer, if you want to influence the infrastructure which gets created, then you can do that. You can have as much or as little control as you want. Another thing to understand as a solutions architect, is that Beanstalk, behind the scenes, uses normal AWS products, and services. It uses things like EC2, load balancers, SQS queues, and it creates and manages all of those using CloudFormation. So you can influence what gets created, by providing custom infrastructure definitions. But behind the scenes, Elastic Beanstalk is essentially a product, which manages the implementation of different AWS products and services, based on your application requirements. So the whole thing about the product, is to remove the requirement to manage infrastructure. It allows a developer or a development team, to simply focus on coding an application, and then Beanstalk can manage the provisioning of the infrastructure, which that application requires. And any custom definitions about additional infrastructure that's required, can be committed along with the application source code, to a repository. Essentially, you've got complete flexibility, over how much control you want, around an application environment. And that's what makes Beanstalk a really powerful, application as a service environment. Now one thing that you really do need to be aware of for the exam, is that it doesn't come for free. Using Elastic Beanstalk requires application support. There are some tweaks that you're going to need to do as a developer, to make this product work for your application. So it doesn't come for free, and it's not something that can be used by a non-technical end user. So that's really important to understand. Now we're going to start by talking about Elastic Beanstalk platforms. Beanstalk is capable of accepting code written in many different languages, known as platforms. Now these are important to know for the exam. Don't focus so much on the individual languages which are supported. Instead, get a feel for what's supported and what isn't, from an architectural perspective. Now when we're talking about platforms, you can categorize them into three categories. You've got the platforms which are built-in and natively supported. So runtimes which Beanstalk supports natively. You've got Docker, which allows you to force pretty much anything that you want, any runtime that you want, or any Docker application into Beanstalk. And then finally you have custom platforms, which I'm going to talk about in a second. Now the built-in supported languages are things like Go, Java SE, or Tomcat with Java. There's also things like the Java application, which is a little bit more complex, like .NET Core, which runs on Linux, or .NET on Windows. Or we've got runtimes such as Node.js, PHP, Python, or Ruby. And these are examples of currently natively supported runtimes or platforms. And I'll include a link attached to this lesson, which provides a list of all of the currently supported platforms. Now there are also Docker options. First, we've got single container Docker or multi-container Docker. Single container Docker allows you to store a Docker image on a repository and download this straight into Elastic Beanstalk if you want to run just a single containerized application. Multi-container can be used if you have an application which uses multiple containers. And this uses ECS behind the scenes to coordinate these multiple containers. There's also pre-configured Docker, which is what Beanstalk uses to provide support to runtimes before they're natively supported. Now at the time of creating this lesson, an example of a pre-configured Docker container is one which supports Glassfish Java 8. Before that, the Go runtime was initially supported in this mode before it was added to the natively supported list. So when you hear pre-configured Docker, this is the way that Beanstalk supports things before they're natively supported. From a user perspective, if you're wanting to use any form of Docker, then you're going to be using single container Docker. So that's the first thing. Now, the second thing is that if you're using Docker or multi-container Docker to either introduce containerized applications into Beanstalk or to support any runtimes which the product doesn't natively support, anything that you want to bring in, any complex applications that aren't natively supported, you can use Docker. Now, you can create your own custom platforms using Packer. So Packer is a product which you can use to create your own custom platform, and you can use it to create your own custom Docker. So if you want to utilize any of these platforms, if you're a developer, if you don't want to worry about the infrastructure that's required to host your code, then using Elastic Beanstalk is a potential option. Now, often one of the hardest things about Elastic Beanstalk is understanding the concepts and terms that it uses because they aren't immediately intuitive. Let's start by repeating that the job of Elastic Beanstalk is to provide environments for applications that you as a developer might use. When you initially think of an application, you probably think of code. And when you think of an environment, you think of an infrastructure platform that code operates within. And while both of those are correct thoughts, when it comes to Beanstalk, the names mean something slightly different. And I want to make sure that by the end of this lesson, you understand the terms that Beanstalk uses, even though they're slightly different from what you would use in a Docker environment. So let's start by repeating that. So we start with an Elastic Beanstalk application. Now, the term application within Elastic Beanstalk means something slightly different than might initially seem logical. An application using Beanstalk terminology isn't code. Think of it like a container for everything related to an application. Picture the Categram application. Inside Beanstalk, an application is a container for everything related to an application. The container is the application that is used to store the application. Everything that is used to store an application is everything about that application. Not the code specifically, but all of the infrastructure that it uses, all of the actual code, so the versions of the physical application. All of that together is what's known as an Elastic Beanstalk application. So conceptually, it's a container or a folder for everything that an application is and everything that an application uses. Now, an application version is something which belongs to an application container. And this is a specific, labelled version of code which your application uses. In this example, Categram version 1.1, 1.2, 1.3, all the way to the current version 1.3.3.7. Now, these are all stored within S3, and we refer to these as source bundles. So if you see any mention of source bundle in the exam, it's probably referring to Beanstalk. Now, these source bundles are either in the source or in the application. They're either zip files or WAR files, so W-A-R. And these are stored in S3. Now, within a Beanstalk application container, there are one or more environments. And these are subcontainers of the application containers. It's environments which actually have infrastructure within them. And it's within environments that you deploy and run a specific application version. So you create an application. That application has a specific application. And then you deploy and run a specific application version. And then you deploy and run a specific application version. Which are named versions of the application code. And you can deploy these into specific environments within an Elastic Beanstalk application. In this example, we have prod, test, and processing. Now, when you create environments, you have to specify an environment tier. And options for this are a web server tier or a worker tier. The web server tier is designed to communicate with end users. The worker tier is designed to process work in some way from the web tiers. Now, the way that this is architected is that both environment tiers, so web and worker, they have within them auto-scaling groups. Web tiers accept connections from customers via an Elastic Load Balancer. Any workloads generated by the web server tier can then be passed into an SQS queue as messages. That queue is then sent to the server. The queue holds the messages and then they're received by the worker tier. And the worker tier scales based on the number of messages in the queue. The web tier scales based on the incoming requests to the load balancers. Now, this is all handled behind the scenes by Elastic Beanstalk, including creating instances which perform all of this front-end and back-end processing. Now, there's a lot of configurability available within Beanstalk, which is important for production usage, but this lesson has a purely exam architecture focus, and so you don't need to be aware of any of that for the exam. Now, application versions, which remember are stored in S3, can be deployed out into the environments within our Beanstalk applications. So each environment is running a specific application version at any one time, and different application versions can be freely deployed into environments running within our Beanstalk application container. Each environment has its own DNSC name, so in this case, prod, test, and processing, and end users of your application can either directly connect to a specific environment using the specific DNSC name, or they can go via a generic DNS name, such as the amazingcategoram.com. But in either case, end users are entering your Beanstalk application by connecting through to a specific environment. In this case, let's use the example of our end user connecting through to the prod environment, so the one at the top in blue. Now, one of the advantages that Beanstalk provides is that it manages all of the infrastructure for our application, so the user interacting with the Categram application would be via the prod environment. But let's say that we create a brand new version of our Categram application, an experimental version. Let's say that it provided access to super high resolution images of all of our cats. Well, what we could do is we could deploy that new experimental version of the application to one of our environments. Let's say the green test environment. Now, at this point, we've got two running environments, one running our stable production version in blue at the top, and one running our experimental version in green at the bottom. We could allow a select group of users of our application to perform some initial beta testing by directly accessing the experimental environment, so the test environment in green at the bottom. This would be using experimental code, but let's assume that they've given us really good feedback. Everything's working as it should, and everybody can access the new 4K versions of the cat pictures, and all of the existing application functionality operates as expected. Well, at this point, we're stuck in the middle, and we need to get the right setting up. So we can go to our一 after. And we can go to our one after. And we can go to our one after. point we could then perform what's known as a cname swap and this swaps the cnames for the environments between each other. This now makes the new primary and live environment the green environment at the bottom. Now if we have any problems with users connecting to this new experimental version we can swap back, we can regress, we can just do the same process again and that means the cname will be swapped back again and our user base would again connect to the blue environment at the top. But if everything's going as expected and if our user base is happy with the green environment at the bottom then we could maintain our users pointing at that environment, we could delete the old blue environment entirely, take a clone of the green environment and then if we needed to do this process again we could deploy a new application version to that new environment and when we're ready perform another cname swap. This is known as blue green deployment. It essentially means that you can provision a brand new environment for your application, perform testing and then when you're ready you're able to swap over to this new environment. You only delete the old environment when everything's operating as it should and this gives you a really effective regression plan if you have any application problems because you can always return to the old environment. So that's the architecture of Elastic Beanstalk visually. Before we finish this lesson there are just a few final points to make. The final key points that I want to talk about. First, remember for the exam this does not come for free. You do need to have applications which fit using Elastic Beanstalk. You need to be a developer and you need to be able to tweak your application to fit into the platform as a service feature set provided by Elastic Beanstalk. Now Elastic Beanstalk from a scenario perspective is great for small development teams. So if you have a small development team and you're able to tweak your application to fit into the platform as a service feature set, maybe you don't have any specific infrastructure team and you don't want your developers distracted by managing infrastructure, then you can use Beanstalk as a great way to create and manage infrastructure for applications and all your development team need to do is create the code, create all of the base constructs inside Beanstalk and then deploy a new application version to a new environment every time they want to test new versions. It essentially offers automated infrastructure that wraps around application code and it allows development teams with no infrastructure skills to effectively create and manage infrastructure for those applications. You can use Docker for anything which is unsupported so keep that in mind for the exam. If you face any questions which involve Beanstalk and mention databases, well always default to databases being provisioned and maintained outside of Elastic Beanstalk. You can create an RDSM application for any of your RDSM applications and you can create a RDSM instance or a self-managed database outside of Beanstalk and then point Beanstalk applications at that database but you can also create databases within a specific environment inside Beanstalk. The problems start if you're using the full feature set of Elastic Beanstalk, so using blue-green deployment or operating with really dynamic environments. If you create databases within an environment then they're lost if the environment is deleted. So when you're doing blue-green deployment, you're not creating a database within an environment. If you're doing blue-green deployments and swapping over the c-names to move your customers between environments, you don't want the database to be stored within that environment because as your customer base would be moved over, if it had a separate database then potentially you would have data loss or inconsistency issues. So make sure that your databases are hosted outside of Elastic Beanstalk and I've seen that come up in exam questions so really keep that in mind. By default you should always assume that databases should be stored within that environment. If you're using Elastic Beanstalk, you should have a very specific reason for creating databases within the product. With that being said though, that's everything that you need to be aware of from a theory perspective for the exam. So go ahead, complete this lesson and then when you're ready, I'll look forward to you joining me in the next.