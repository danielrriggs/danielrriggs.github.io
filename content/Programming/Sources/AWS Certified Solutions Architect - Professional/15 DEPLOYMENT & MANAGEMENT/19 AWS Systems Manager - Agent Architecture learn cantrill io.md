 Welcome back. In this lesson, I want to talk about AWS Systems Manager, often called by its old name SSM, or the Simple Systems Manager. Now, at the professional level, we have to understand Systems Manager in a lot more detail, and so I've split all of the content I want to cover into a few lessons as part of a mini-series. In this lesson, I want to give you a quick refresher and talk about the agent architecture of Systems Manager. So let's jump in and get started. Systems Manager is a product which lets you manage and control AWS and on-premises infrastructure. The larger your environment, and especially if it's hybrid, the more important it becomes to have some form of systems management solution, and Systems Manager meets this need. Now, the product is agent-based. To use it, an agent needs to be installed on servers and instances, and this agent communicates with the Systems Manager service. The agent is installed by default on most modern AWS AMIs, and this includes both Linux and Windows AMIs. On older AMIs or on-premises environments, you need to handle this manually or using another configuration management system. And for any non-AWS instances, there's actually a separate process that you need to follow to convert them from being normal virtual or physical machines into managed instances. And you won't be able to do this without an agent, companies like Microsoft and Cisco who already had an agent deployed on their desktop that they can just run. So for theata TMS ways and ways. With the file is of course theág mn in performance because it responds to altitude examinations to improve accessibility and Due to this power burning, this agent can actually signal incoming or incoming requests with a laws that will allow its agent to immediately view it. The first might be the command statement page. I store recognizes, calls,查 extraction data etc. The method A translation of the data is linked in the download data section and AgentsHow upload successfully. Trans tokwheel news A operating system patches and hotfixes, the instance hardware details, services which are running and you can even specify custom things to perform as part of this inventory. It can also patch assets, you can define maintenance windows and baselines and have it automatically patch instances in a safe and controlled way. Now Systems Manager can also run commands on managed instances or whole groups of instances and I'll explain how this works in more detail in another lesson in this series. If you ever just want to run something on an instance then often the easiest way is to use the run command feature of Systems Manager. Now you can also manage the desired state of instances. This is more complex than the run command but you can define a certain desired state. Say that you always want certain ports to be blocked on the instance firewalls and the product will maintain that. So if those ports are ever opened it will close them again. It will maintain that desired state. So desired state will do whatever is required to move instances from the current state into a desired state. Now there's also the parameter store which is able to store configuration information and secrets but I'll also be covering another product called Secrets Manager which is better at handling secrets. It's specifically designed to handle secrets and rotations. So that's the one thing you can do to use it in your own way. So let's have a look at the next step. So this is a feature called Session Manager. Session Manager is the most common feature that you will find in your own system. It's a feature that allows you to access all the processes and the system itself. And then finally and you will see me use this feature throughout the course, we've got Session Manager. And Session Manager allows you to securely connect to EC2 instances even those in private VPCs. As long as an instance has connectivity to the Systems Manager endpoint then you can use Session Manager to connect into it. Now for this lesson I want you to be comfortable with how instances or on premises physical or virtual servers connect to the system. So let's take a look at the Session Manager. So Session Manager is a feature that allows you to connect to the systems manager product and become managed instances. Managed instances is the name for anything which is managed by Systems Manager. So let's take a look at that visually. Systems Manager is capable of managing both AWS based instances and your fleet of servers running on premises. Now the service itself runs as a public service. So within the AWS public zone. There are two ways the integration between instances and services can be done. One is through the AWS public service. The other is through the AWS server address which depends on whether a failure to ..." The third method is through構jer. There are two ways Anthom is able to manage resources. One is through host whatever it's called. There Дан HiX I mean all accounts. Or quickly IS aMS. You refer it as business vendor . AMIs, but there are also manual downloads and manual instructions for older AMIs, custom AMIs, or even on-premise servers. Once the agent is installed on AWS instances, it needs two things in order to become a managed instance. First, it needs an instance role attached to the instance with the permissions required to interact with Systems Manager. Assuming it has an attached instance role, then it also needs network connectivity to the Systems Manager endpoint, and this can be in the form of an internet gateway or a VPC endpoint. With all that in place, instances can become managed instances, so controllable and manageable via Systems Manager. At the on-premises side, the process still needs the same basic components, but the actual logistics of joining servers to Systems Manager is slightly different. Systems Manager works with both physical and virtual servers. For this example, let's assume that we have four. Connectivity between these servers and Systems Manager will occur over the public internet, and so this needs to be in place in order for this communication to work. To securely join servers to Systems Manager, we need to create managed instance activations. As part of creating these, you need to create a managed instance activation. To do this, you need to select an IAM role, which the on-premises servers will indirectly use in order to receive their permissions. For each activation, you'll receive an activation code and an activation ID. When configuring the agent on on-premises servers, you'll need this information to complete the setup. So with all this information, this establishes a secure communications channel and management agreement between the servers and the Systems Manager. This means that the servers now appear as managed instances. So that's how to install and configure Systems Manager agents. Over the coming lessons, we're going to explore specific features of Systems Manager, which will be useful for a professional level solutions architect and for the exam. And these are going to include run command, which allows you to execute commands on managed instances, as well as patch manager, which allows you to control the system. And finally, we're going to explore how to deploy and control software and hot fixes across an entire fleet, both within AWS as well as on-premises. Now, thanks for watching. Go ahead and complete the lesson. And when you're ready, I look forward to you joining me in the next.