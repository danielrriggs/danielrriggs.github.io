 Welcome back, and in this lesson I want to talk about another component of the CI-CD workflow within AWS, and that's AWS CodePipeline. If you recall from the summary lesson, this is an overview of the CI-CD system within AWS. It includes CodeCommit, which I just covered in more detail, as well as CodeBuild, CodeDeploy, and CodePipeline. Now, CodePipeline is the glue, the orchestration which makes all of the other components work together, and it's this product which I want to focus on in this lesson. So again, CodePipeline is a continuous delivery tool. It's designed to control the flow of a development lifecycle from source code, through build steps, and then finally through to deployment. It's the glue which holds everything together. Now, pipelines, and you can have as many of these as you want, they're built from stages. Stages can be almost anything you want. You're free to architect pipelines from as many or as few stages as make sense. Stages contain actions, and these are either sequential or parallel actions, and the movement between stages can be automatic or based on manual approval steps. Now, actions within stages can consume artifacts or generate artifacts, and you can think of these as free. Now, if you're using a pipeline, you can use a pipeline to create a pipeline. You can create a pipeline using a pipeline, and you can create a pipeline using a pipeline. Now, this might be used for loading source or for storing some intermediate binaries or any generated assets, anything that you can configure based on your requirements. Now, I'm going to show you how all of this looks visually on the next screen. For now, I'm just introducing the terms. Any changes to the state of pipelines, stages, or actions generates events which are published, and if you've been using a pipeline for a long time, you can also use a pipeline to generate notifications. Now, you can also interact using CloudTrail for monitoring API calls, or using the console UI for human interaction. And visually, it looks like this. So architecturally, we start with a pipeline, S3, and EventBridge. Now, the pipeline contains stages. In this case, I'm going to use a pipeline for monitoring the event. In the case of a pipeline, I'm going to use a pipeline to generate notifications. Now, if you're using a pipeline, In this example, we have three, but you can structure pipelines however you want. And inside these stages, you can have single actions, you can have parallel actions, or you can have sequential actions. Artifacts, which I've just mentioned, are just files or other assets which can be consumed by actions, in this case, which is known as an input artifact. Or they can be generated as the output of actions, in which case it's called an output artifact. Moving the flow of code through its lifecycle is based on binding the output of one action to the input of another. The code moves through stages, and this can be automatic, so when one stage finishes, another starts, or it can require manual approval. Now eventually, the pipeline might make it through to a deployer. And this might use CodeDeploy, which I'll be covering elsewhere in this section, to deploy your application out to some form of infrastructure. Now as I mentioned on the previous screen, any state changes to the pipeline, stages, and actions will be published into EventBridge, so they can be consumed elsewhere in AWS. So this is the architectural theory of how AWS CodePipeline works. And then over the next few lessons, I'm going to be going into C++. And I'm going to be giving you significantly more detail on how you can use CodeBuild and CodeDeploy to complete this CodePipeline lifecycle architecture that's available within AWS. Now at this point, that is everything I wanted to cover, so go ahead and complete this video. And when you're ready, I'll look forward to you joining me in the next.