 Welcome back and in this lesson I want to briefly cover how you can use Elastic Beanstalk and Docker together to add some extra functionality. Now let's just jump in and get started because we have a lot of theory to cover and I want to keep this as brief and efficient as possible. There are a few ways to run Docker containers or containerized applications within Elastic Beanstalk. The first is single container Docker mode, referred to as Docker in the console UI. Now with this method you can only run one container on one Docker host so it's not very resource efficient. This mode uses a single provisioned EC2 instance running Docker that's managed by Elastic Beanstalk. It doesn't use an ECS cluster. Now to use this mode you have to provide one or more of a few different pieces of extra configuration. First you can provide a Docker file such as the one that we're going to be using in the 1.0. This is the walkthrough component of this lesson and I'll step through this when we move across to the console. This is what you use if you want to create a new container image and container directly from this Docker file. If you want to cleanly build a container directly within Elastic Beanstalk then use this option. And I'll be stepping you through how this works in the later walkthrough part of this lesson. Alternatively you can use a file called dockerrun.aws.json. Specifically version 1 of this. This file configures things like the image to use, which ports to use and which volumes to provide. As well as many other Docker or container style attributes. Finally if you use docker-compose which is a set of tooling in the Docker ecosystem. Then you can also use a docker-compose.yaml file. So this is for single container deployment. You also have the option for multi-container deployments. You can create Docker environments. That support multiple containers per EC2 instance. So you can use the multi-container Docker platform for Elastic Beanstalk. When you choose this method Elastic Beanstalk uses ECS to coordinate container deployments within environments. So Elastic Beanstalk uses ECS to create an ECS cluster. And then within this EC2 instances are provisioned to host containers. And the load balancer is used for high availability. Now Elastic Beanstalk takes care of Amazon ECS tasks including cluster creation, task definition and task execution. The instances in the environment each run the same set of containers. Which are defined in a docker-run.aws.json file. Specifically it has to be version 2 of this file. Now when you're using multi-container mode. Any images which are used need to be stored in a container registry such as ECR. Now visually this is how it looks. We have an Elastic Beanstalk environment with a provisioned ECS cluster. Inside this we have an auto scaling group which provisions EC2 instances. Which are themselves used to host the tasks and containers defined by the cluster. So the important thing to take away from this lesson is that standard single container docker mode. Only supports a single docker container per Elastic Beanstalk environment. In order to get the most out of docker. Both from a flexibility point of view and from a resource usage point of view. Elastic Beanstalk lets you create an environment where it creates an ECS cluster. Multiple EC2 instances running docker. And that allows you to run multiple docker containers side by side on the same docker host. So if you see any mention in the exam of needing multiple containers per instance. This points at multi-container docker mode. Now I want to step you through a simple docker deployment on Elastic Beanstalk. So at this point we're going to do a walkthrough. You can either follow along in your own environment. Or you can watch me perform this implementation. To do this I'm going to move across to my AWS console. So I'm currently logged in to the general AWS account. So this is the management account of the organization. And I'm using the IAM admin user. And I've got the Northern Virginia region selected. So to get started with this walkthrough. I'm going to move across to the Elastic Beanstalk console. So I'm going to type Elastic Beanstalk. Right click. Open that in a new tab. Again you can choose either to follow along in your own environment. Or just watch me perform this implementation. So we're going to implement a sample docker application. Now just moving across to my code editor. This is the docker file that we're going to be using to create the single container docker environment within Elastic Beanstalk. So this is actually a docker file. Which creates a simple web based game. And it's called 2048. So this doesn't do anything special. This line is the basis for the image. Then it performs some system updates. And installs some prerequisites. So installs the Nginx web server. The zip utility and curl. Then it downloads and installs all of the files for this web based application. And then it runs this command to start the web server. So using this docker file we'll end up with a container which is hosting a web based game. Now that docker file is linked attached to this lesson. So go ahead and download it. Because you'll be using it in a second. So to use it I'm going to go to applications. And I'm going to create a new application. For the application name I'm going to call it 2048. Which is the name of the web based game. I'm going to scroll down and click on create. Now the application is ready. I'm going to create an environment within this application. So I'll click on create new environment. I'll choose a web server environment. And click on select. I'll leave all the defaults for the environment name. This is called 2048-env. For platform I'll be selecting managed platform. In the drop down under platform I'm going to pick docker. Because we're going to be using a single container docker environment. I'm going to leave all of the other details as default. So this is going to be docker running on 64 bit Amazon Linux 2. Now under application code instead of using a sample application. We're going to be uploading this docker file directly. To elastic beanstalk. So I'm going to select upload your own code. Then I'm going to choose local file and click on choose file. Now at this point you need to select the docker file that's attached to this lesson. If you haven't downloaded it already go ahead and do that. Then you'll need to locate that docker file. Select it and click on open. It will take a few moments to process that file. Once you see file successfully uploaded. You can go ahead and click on create environment. And at this point it's going to take that. docker file and begin provisioning a single container docker environment inside this elastic beanstalk application. Now that's going to take a few minutes to provision so I'm going to go ahead and skip ahead to when this is completed. If you're doing this in your own environment you of course need to pause the video and wait for yours to complete and then we'll be good to resume. Okay in my case it took a little under five minutes to reach the point of when the environment is healthy and if we just go ahead and click on go to environment it's going to open this web-based game. Now this is a pretty simple concept you just need to use the direction arrows to force all the blocks in one direction and when two blocks of the same number touch they merge into one. So this is a really fun game as a side tangent absolutely check it out it is pretty fun but this illustrates the power of being able to use elastic beanstalk to deploy docker containers. Now if we return to the elastic beanstalk we're going to see that the docker container is now in the same place as the beanstalk console look under recent events and then click on show all we can see exactly what's happened during this deployment. So it's created the environment it's created a target group a security group an auto scaling launch configuration an auto scaling group it waits for ec2 instances to launch which of course are going to be running the docker engine allowing them to host containers then it creates the auto scaling group creates cloud watch alarms a load balancer adds an instance and then continues to launch. So this is a really fun game and I'm going to go ahead and complete the deployment and in a few short minutes we move from having a docker file through to a completely working provisioned environment using docker and that's the power of elastic beanstalk it's a platform as a service product which is capable of creating infrastructure around any code which you upload. Now that's everything I wanted to cover for the walkthrough component of this lesson if you did follow along in your own environment just make sure that you do clean up so click on applications select the 2048 application and then click on the docker container application click on actions and then delete application and you'll need to either type or copy or paste the name of the application and click delete to confirm that deletion. At this point though that's everything I wanted to cover in this lesson so go ahead and complete the lesson and when you're ready I'll look forward to you joining me in the next.