 Welcome back, and in this lesson I want to talk about another feature of Systems Manager, which is the Patch Manager. And Patch Manager is an essential part of any complex or hybrid environments running in either AWS or AWS and on-premises. Patch Manager allows you to patch Linux and Windows managed instances. Now we've got a lot to cover, so let's jump in and get started. Patch Manager is an essential feature of Systems Manager for any larger AWS deployments, especially hybrid ones. It's also really important for the professional exam, and as annoying as it is, there are some really obscure facts that you're just going to need to remember. Now I'm going to highlight these. I hate doing it, but it is that important to remember a number of really important points for the exam. Now there are a few main components to Patch Manager as a product. I want to quickly go through these first, and then we can look in detail at how the product functions, as well as its architecture visually. So the first component of Patch Manager is a patch baseline, and you can have many of these defined within the product. There are predefined ones, custom ones, and there are different ones depending on the operating system. Essentially, a patch baseline defines what should be installed, so what patches and what hotfixes. Now don't worry, I'll talk about these in more detail in a second. Next, we've got patch groups, and these define groups of resources within Systems Manager. So which specific resources do you want Systems Manager to patch on your behalf? Next, we've got maintenance windows, and these define the time slots where patching can take place, as well as other things. Maintenance windows are essentially the glue that connects everything together and allows Patch Manager to do its job. And I'll talk in more detail of exactly how this works architecturally when we look at the architecture visually. Next, we've got run command, and run command is used as the base level functionality to manage the patching process. So whenever any patch operations occur, it's generally the run command that's operating behind the scenes to actually perform that process. Next, we've got the term concurrency and error threshold. Error threshold? Concurrency defines the number of managed instances. Which are patched at any one time. And error threshold defines the number of errors which can occur before the whole patching process stops. Lastly, we've got one more term to talk about, and that's compliance. After patches are applied, Systems Manager can check the successful compliance versus what's expected. So a part of any mature patching process is to determine after the fact whether a patch has been applied successfully. And that involves the inventory feature. And that includes the amount of patch manager. And I'll be talking about that in more detail later in the lesson. Now this is the part that I'm really going to hate. This is the part that I normally don't like doing. I'm going to step you through some key terms and keywords that you really need to remember for the exam. So I don't like creating training courses which teach based on keywords and specific terms that you need to remember. I prefer teaching you so that you know how the architecture functions from a wider perspective. Now you're going to learn that anyway. Including a practical demo lesson later in the section. But you do for the exam need to be aware of a number of really important names. Because you'll see them mentioned in both questions and the answers. So on the previous screen I talked about patch baselines. Patch baselines define what patches get installed. And AWS provide a few predefined ones which cover various different operating systems. Now as I just mentioned, the exam uses the name. Of these baselines within questions. So while I hate doing this, you do need to remember these. So pay specific attention to the names of these baselines. For Linux the format of the patch baselines is AWS hyphen and then the operating system. And then default patch baseline. So these define explicitly the patches to be applied and their operating system specific. And in the case of Linux, distribution specific. So for example there's AWS hyphen Amazon Linux 2 default patch baseline. And this logically as the name suggests is the default patch baseline for the Amazon Linux 2 distribution of Linux. There's also AWS hyphen Ubuntu default patch baseline. And logically this is for the Ubuntu distribution. Now they're all different and unique. Because different distributions use different repositories to deliver the software updates. Generally anything which is classified as security related. Or anything which has a severity level of critical or important. Is included in these default patch baselines. Any patches are automatically approved seven days after release. When using these predefined baselines. Now we've also got Windows patch baselines. Which are handled by AWS. And this is called AWS hyphen default patch baseline. And this contains critical and security updates. There's also one called AWS hyphen Windows predefined patch baseline hyphen OS. And this is the same as the one directly above it. So AWS hyphen default patch baseline. Only it uses a different name. Now we've also finally got AWS hyphen Windows predefined patch baseline. And this includes the same default OS critical and security updates. But also contains any patches for Microsoft applications. Now I really apologise for this. There's no way of getting past the point. That you need to remember these names. You don't specifically need to remember them word for word. And be able to write them down. But you do need to remember them. And you need to remember them. And you need to remember them. And you need to remember them. And you need to remember them. And you need to remember them. And you need to remember them. And be able to write them down. But you do need to be able to recognise them. So if you see AWS hyphen Amazon Linux 2 default patch baseline. You need to know that this contains security updates for Amazon Linux 2. You need to be aware that AWS hyphen default patch baseline is for Windows. And then you need to be aware of the differences between AWS hyphen Windows predefined patch baseline hyphen OS. And then the same but hyphen OS hyphen Windows. And then the same but hyphen OS hyphen Windows. And then the same but hyphen OS hyphen Windows predefined patch baseline hyphen applications. So focus on this particular screen. You do need to remember all of the information that is on this screen. Again I apologise in advance. But this is just one of those occasions where you do need to memorise all of this information. So that's a baseline. But now I want to look visually at how baselines are used in the wider architecture of Patch Manager. So let's look at that. I think it's easier to understand the flow of Patch Manager. I think it's easier to understand the flow of Patch Manager. I think it's easier to understand the flow of Patch Manager. So we'll start with the product itself. Systems Manager. Patch Manager. And once we have this, step one is to either define or pick a baseline or a group of baselines to use for your infrastructure. You might have a pretty sizeable group of different baselines if you have different versions of Linux and Windows within your fleet of managed instances. Now step two is to create patch groups which will act as a base for your infrastructure. Now step two is to create patch groups which will act as a base for your infrastructure. Now step two is to create patch groups which will act as a base for your infrastructure. So these are defined either via tags or queries and once configured they act as groupings of compute resources. In this case EC2 instances. So this gives us the baselines which define which patches to use and the groups which define what gets patched. So step three is to create a maintenance window. Now the name of this one is a little misleading. It's not just a window. In the maintenance window In the maintenance window define a schedule, you also define a duration, you define the targets to use, so the patch groups in this case, and you also define the task or tasks to run on those patch groups. So maintenance windows are essentially the glue which tie all of the different components together. You can think of the maintenance window as the configuration item that you use to actually configure the patching of one or more machines as part of patch groups. Now this is another really important part, and I need to stress this because this is essential for the exam. As part of patch manager, there is a run command called aws-runpatchbaseline, and this is actually step four. Please try to remember this one for the exam because it's essential that you can at least recognize it. It's this run command that is actually used to patch the machines inside patch. So this run command is provided with a patch baseline and target information, and then it's this run command that's used to actually patch the machines. So the run command is executed because it's defined within the maintenance window, and it's passed in some information, so items from the maintenance window. Things like the specific patch baseline to use, or any other runtime information. Details from the maintenance window, such as the concurrency, and the patching of the machines. So this run command is provided with a patch baseline, and error threshold. And again, remember it's this run command, so aws-runpatchbaseline, that actually performs the tasks on the individual managed instances. So the run command is executed against the patch groups that you've defined, passing in all of the data into the systems manager agent running on these managed instances, and this causes them to download the patch data and apply those patches. So remember the process is, you have a patch baseline, you have a maintenance window which defines all of the configuration, and then you've got the run command aws-runpatchbaseline, which orchestrates the download and installation of the software patches on all of the managed instances. You've actually got an additional step in addition to this, a fifth step that's run after the patching process finishes. Once patches are applied, there needs to be a compliance component. So checking that patches are applied, and that the general state of the operating systems match what a business requires. And this uses the inventory feature of systems manager to keep track of everything about the managed instances. So the configuration, the hardware, and the state of the operating system, and any application patches. So the inventory is a crucial part that runs constantly to manage the state that the instance is in. So performing inventory of all of the metrics, of all of the items, all the configuration items, and all of the configuration items, and all of the configuration items, and all of the items that you might want to be aware of as an administrator. This is performed by the systems manager inventory feature, and this forms a crucial component of any mature patch management system. And that is patch manager. For real world usage, a theory lesson isn't going to be enough, which is why I'm going to have a demo coming up very soon in this section of the course, where you'll be able to get to use the product from a practical perspective. For the exam specifically though, this is everything that you'll need to know about patch manager. But do perform the demo lesson that's later in this section to gain that additional practical exposure. Because even if you don't need to use it in the real world, I think it will benefit you even from an exam perspective. So go ahead, complete this lesson, and when you're ready, I'll look forward to you joining me in the next.