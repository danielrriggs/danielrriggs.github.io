 Welcome back, and in this walkthrough lesson, I want to step you through how you can update the application version that's used within an Elastic Beanstalk environment. So to do this, I'm logged in as the IAM admin user in the general AWS account, so this is the management account of the organization, and I have the Northern Virginia region selected. Now you can either watch me perform these steps, or you can perform them in your own environment. And if you do decide to perform these steps within your own environment, then all the files that you'll need are linked to this lesson. Make sure that you go ahead and download and extract them now, and then if you choose to, you can use them later on in this walkthrough. So what I'm going to start by doing is moving across to the Elastic Beanstalk console. So I'm going to type Elastic Beanstalk, and then click to move to that console. Once I'm there, I'm going to make sure I click on Applications, and then select the Categoram POC application. Now I currently have two environments, Categoram POC-MV, which is the dev environment, and Categoram POC-MV-PROD, which is the production environment. Now I'm going to update the production environment, so I'm going to go ahead and click on that environment. And just as a reminder, if I click on Go to Environment, this is what the application running in the production environment currently looks like. Now I don't know about you, but I think having green on one side and grey on the other. is a little bit boring, and so why don't we go ahead and update this application to use green and red rather than green and grey. Everybody knows that green and red go together really, really well. So this is obviously going to improve the look of the application considerably. So go back to the Elastic Beanstalk console. Now, what we can do, we can do this in two ways. Either we can upload and deploy directly from this option within the environment, or alternatively, inside the environment. So within the Elastic Beanstalk application, so Categoram POC, we can click on Application Versions. So every version of the application, every application version that you upload and then deploy onto environments, this is located within the application versions. So Elastic Beanstalk maintains a record of all of the different versions of applications deployed into any environments for this Elastic Beanstalk application. And you can even configure application versions. So if you want to configure an application version, you can configure an application version lifecycle settings, where you can enable this and delete from this history, optionally deleting the source bundle from S3 or not. And this is something which might well come up in the exam. If you use CI CD systems and regularly add new application versions, then you will need to perform some lifecycle management of this area of an Elastic Beanstalk application. So what we're going to do is I'm just going to switch across to my Finder window because I'm running Mac OS. So what we're going to do is I'm just going to switch across to my Finder window because I'm running Mac OS. So what I've actually done is download the application source bundle for this sample application. So this is called Python.zip, and I'm going to include this attached to this lesson. Then I've expanded the Python.zip application, and it creates this folder. So inside this folder, we have application.py, we have cron.yaml, and we have eb sampleapp-python.iml. So inside this folder, we have application.py, we have cron.yaml, and we have eb sampleapp-python.iml. application.py file. So I'm going to go ahead and open that in a text editor. So this is that file opened in my favorite text editor. And if we scroll down all the way down to the bottom, we'll see this is the HTML that defines our Python application. It has two sides. One is the little message and the other contains the links. Now, if we go back to our web browser, this is the message and this is the link. So what we want to do is edit the color of this background. So what we can do is identify the class that this links area uses, and it's called links column. So if we scroll up and we look for dot links column, we can see that we have a background hyphen color definition, and it's currently set to hash echo zero, echo zero, echo zero. And that's the hex color code for gray. What we need to do is change it to a suitable replacement. Now, I've already gone ahead and done this. If I move back to my finder window, I've already configured a version two of this application with an updated application.py file. So if I open this and then locate that same section, so I'll just scroll down, looking for dot links column. So that's this. And I'll just make sure that we're looking at these side by side. So on the left is the original version and on the right is the version that I've updated. So I've changed the version to the original version. So I've changed the version to the original version. So I've changed the version to the original version. So I've changed the background hyphen color to be hash foxtrot foxtrot zero zero zero zero. And this is the hex color code for red. So I've already gone ahead and edited that file. And then I've put it in a separate directory and I've run a command to generate a new version of this directory. So this is called python v2 dot zip. Now I've included this updated zip file attached to this lesson. So you don't need to run the command to create it. But if you did want to create an application source bundle, then what you could do is make sure that you've created a new version of this folder. So you can actually create a new version of this file. So I've created a new version of this folder. And I've created a new one. And if I go inside the folder, I need to make sure I've created a new version of this folder. So in my case, I need to go inside python v2. And once I'm inside that folder, I'd run this command, which would generate a python v2 dot zip from all of the files within this folder. Now I don't recommend performing this in your own environment because we need to make sure that this file is correct. And so if you do make any errors, then you could generate a badly formed python v2 dot zip file. And that would affect the next stage of this demo. So I do recommend that you just use the Python v2 dot zip file. And then I'm going to go into my my already created version of this file which is attached to this lesson. So what I'm going to do at this stage is I'm going to deploy this updated application source bundle to one of our environments. So I'm going to select Categram POC. I'm going to select the development environment so that's Categram POC-env because we don't want to deploy anything to production until we've tested it in development. So I'm going to click on the development environment and once it's loaded I'm going to click on upload and deploy. Now I'm going to select an application source bundle so I'm going to click on choose file and then I'm going to select the python v2.zip file. So I'm going to select that and click on open. Under deployment preferences we're able to select the type of deployment that we want to do and by now you should have watched the theory lesson on deployment policies and so you should understand all of these different options. Now because this is a development environment I'm going to use the all at once deployment policy. So I'm going to select that option and then I'm going to click on the application source bundle and then I'm going to option and then I'm going to click on deploy. So now the environment starts to update and if I click on view events we'll be able to see all of the updates that affect this environment. So we're given a message that an environment update is starting and that it's deploying a new version to instances so let's just keep refreshing. Because it's an all-at-once deployment it doesn't perform any intelligent split of the instances within this environment. Instead it deploys the new version of the application directly onto the instance and then performs a health check and then completes the environment update. So now we have a new version of this application. If I click on go to environment now we can see the much better looking version of this application with green on the left and red on the right and this is a really good looking version of this application. It's a massive improvement. If we go back to the Elastic Beanstalk console and click on application versions under Categram POC then now we can see different versions of this application. So we have the sample application hyphen one which is the v2 zip which I just uploaded and the original sample application which we've previously had deployed and if I wanted to deploy this new version onto the production environment I could do that by clicking on the production environment and following the same process. Now that's everything I wanted to cover in this brief walkthrough lesson. I just wanted to give you some practical exposure to how to deploy application applications. So if you're interested in learning more about application versions within an Elastic Beanstalk environment as well as some exposure of how to create application source bundles. But with that being said that is everything that I wanted to show you in this walkthrough lesson. So go ahead and complete the lesson and when you're ready I'll look forward to you joining me in the next.