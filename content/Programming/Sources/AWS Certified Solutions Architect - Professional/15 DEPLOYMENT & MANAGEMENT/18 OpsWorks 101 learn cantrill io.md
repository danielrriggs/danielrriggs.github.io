 Welcome back, and in this lesson I want to cover AWS OpsWorks. Now, OpsWorks is a configuration management service that provides AWS-managed implementations of Chef and Puppet. Now, Chef and Puppet are automation platforms which allow you to use code to automate the configuration of your servers. If you already use Chef or Puppet within your on-premises environments, then OpsWorks can be a great way to re-implement infrastructure within AWS as part of a migration. If you use neither Chef or Puppet in your current infrastructure platforms, then OpsWorks isn't going to be an ideal solution. OpsWorks, to get the most benefit, requires knowledge of Chef or Puppet, and so it tends to feature in the exam as an ideal solution to take advantage... of current skill sets when you're moving infrastructure into AWS. Now, OpsWorks is something which features much more on the DevOps Engineer Professional exam. As a solutions architect, you only need to know the components, when to and when not to use OpsWorks, and a few key facts which I want to talk about throughout this lesson. Now, we do have a lot to cover, so let's jump in and take a look. Now, before I get started talking about OpsWorks, I want to place the product... conceptually in relation to the other products that you can use within AWS to manage infrastructure. Imagine a scale from left to right. Things at the left side offer the most control, but also have high admin overhead. Things at the right are the reverse. They offer less control, but also have low admin overhead. On the left, we'd include things such as manually implementing infrastructure or using scripting. This offers a lot of information about the infrastructure, but also a lot of information about the OpsWorks. This offers a lot of information about the infrastructure, but also a lot of information about the OpsWorks. This offers a lot of information about the infrastructure, but also a lot of information about the OpsWorks. This offers a lot of information about the OpsWorks. This offers a lot of information about the OpsWorks. This offers a lot of information about the OpsWorks. This offers a lot of information about the OpsWorks. This offers a lot of information about the OpsWorks. This offers a lot of information about the OpsWorks. This offers a lot of information about the OpsWorks. This offers a lot of information about the OpsWorks. This offers a lot of information about the OpsWorks. This offers a lot of information about the OpsWorks. This offers a lot of information about the OpsWorks. This offers a lot of information about the OpsWorks. This offers a lot of information about the OpsWorks. This offers a lot of information about the OpsWorks. This offers a lot of information about the OpsWorks. This offers a lot of information about the OpsWorks. This offers a lot of information about the OpsWorks. This offers a lot of information about the OpsWorks. This offers a lot of information about the OpsWorks. This offers a lot of information about the OpsWorks. This offers a lot of information about the OpsWorks. This offers a lot of information about the OpsWorks. This offers a lot of information about the OpsWorks. This offers a lot of information about the OpsWorks. This offers a lot of information about the OpsWorks. This offers a lot of information about the OpsWorks. This offers a lot of information about the OpsWorks. This offers a lot of information about the OpsWorks. Why layers are functional is because next we have recipes and cookbooks, and both recipes and cookbooks are generally applied to layers. And so it makes sense that everything in a layer would do the same thing, because recipes can do things like install packages, deploy applications, run scripts, or perform reconfiguration. And because they're applied to layers, then they'll do this against all instances within that layer. Now, cookbooks are a collection of recipes which can be stored on GitHub. So remember GitHub when it comes to cookbooks, because this might easily score you an extra mark in the exam. Now, next we've got lifecycle events, and these are special events which run on a layer. So examples of this are setup, configure, deploy, undeploy, and shutdown. Now, these lifecycle events were always historically something which I struggled to understand. Until I started to use OpsWorks. So a really common example which I like to use with my students to illustrate the power of lifecycle events is this. Say that you have a new instance which is being added to a layer running your application servers on EC2. When you add a new server to a layer, the configure event would run, and it would run on all instances within that stack. Now, the configure event could run a series of recipes. So the configure event would run a series of recipes on all instances in that stack. So not just the one instance which has been added. And an example of why this might be important is to reconfigure all of the instances in that stack to have an awareness of all of the others, including the one instance which has recently been added. So the configure lifecycle event is generally executed whenever instances are added or removed from any layers in the stack. And it's an effective way to perform. Reconfiguration on any instances which are part of a stack. So to build awareness of all the instances or update configuration on all instances in the stack when a new instance is added or removed. Now, next, we have instances themselves, and these are compute instances managed by OpsWorks. Now, you can actually use EC2 instances and on-premise servers. So keep that in mind for the exam when you're using instances, though. There are a number of instances that are on-premises. There are a number of instances that are on-premises. There are a number of instances that are on-premises. There are three types of instances. We have 24-7 instances, and these are started manually and run until you stop them. We've got time-based instances, which can be configured to start and stop on a schedule to help accommodate change in load on a system based on a known schedule. So if you have a known period of time when you know that you'll get heavier load on a system, then you can use time-based instances to accommodate, that regular time-based or schedule-based load. Lastly, we've got load-based instances, and these can be set to turn on or off based on system metrics. So these work in much the same way as an auto-scaling group, where they can be turned on or off based on incoming load to a stack. Now, OpsWorks supports instance auto-healing, and this means that if the instance fails or if an agent stops communicating with the service, then OpsWorks auto-heals the instance. So it automatically stops and restarts the instance. I mentioned before that you can also incorporate on-premise compute resources, so Linux-based compute resources, into a stack, which were created outside of OpsWorks. So Amazon EC2 instances that you've created directly using the console UI, the CLI, or the API can be included within OpsWorks stacks as instances. And also, on-premise instances running on your own hardware, whether they be free or not, can be included in OpsWorks. And so, if you're using an OpsWorks app, you can also use an OpsWorks app, which is a physical or virtual, can also be used within OpsWorks. The last key component that you need to understand are OpsWorks applications. You can store applications and related files in a repository, such as an Amazon S3 bucket. Each application is represented by an OpsWorks app, which specifies the application type, as well as containing any information needed to deploy the application from the repository to your instances. And this could include things such as a repository URL and password. Now, when you deploy an OpsWorks app, OpsWorks triggers the deploy event. And the deploy event runs the deploy recipes on any applicable instances. And that's how an OpsWorks app gets deployed to instances within the OpsWorks stack. Okay, so now I've covered all of the key points. Let's have a look at the architecture visually. At this point, everything should start to make sense. So visually, this is how the architecture looks. At the core of the product, we've got the OpsWorks stack. Think of this like a container for resources. And the stack integrates with other AWS services, including CloudWatch for metrics and monitoring, as well as IAM for full control over stack permissions. Inside the stack, we've got layers. And remember, layers are functional things. They contain infrastructure with the same function. In this example, we've got a stack of layers. And remember, layers are functional things. In this example, we might have three layers, a database layer, an application layer, and even a load balancer layer. Now, to these layers, you'd actually apply recipes. And these might be built-in recipes or come from a cookbook repository, which is hosted on GitHub. So in this example, the DB or database layer acts as a container for an RDS instance, which has been created outside of the OpsWorks project. The load balancer layer logically has a load balancer, and it's used to distribute traffic equally across another layer. It acts as the entry point to the stack from the public internet in this example. Now, OpsWorks, as I mentioned previously, has the concept of apps. And apps are deployed from S3 or HTTP. And then using the deploy recipe, these apps are deployed into instance. And then, as I mentioned, you can deploy them from any other device. And you can even deploy them into instances, in this case, in the application layer. Instances, as I just talked about, can be ones which are manually created and stopped and started. And these are called 24-7 instances. You can also create instances which are time-based, allowing the stack to adjust the number based on predictable schedules. And then finally, load-based instances, which are based on metrics which apply to those instances. So this visually is the architecture of the OpsWorks. of OpsWorks. So if you remember this architecture and combined it with all of the theory that I've talked about throughout this lesson, then you'll have all of the information that you'll need to answer any exam questions which you might encounter. Now once again you'll need much more OpsWorks knowledge if you are tackling the DevOps Engineer Professional exam, but for the Solutions Architect Professional you only need this level of knowledge. So an architecture level of knowledge, an understanding of how all of the different components fit together, and exactly what features each provides. So at this point that's everything that I wanted to cover, so go ahead, complete this lesson, and when you're ready I'll look forward to you joining me in the next.