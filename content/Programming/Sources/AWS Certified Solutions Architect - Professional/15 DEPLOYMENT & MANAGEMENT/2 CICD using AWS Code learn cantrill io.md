 Welcome back. In this lesson, I want to talk about continuous integration and continuous deployment, known as CICD, within AWS. Now this is something which features in detail in the Developer Associate and DevOps Engineer Professional exams. From an architect perspective, you need an awareness of the high-level components and how they fit together. So that's what I want to cover in this lesson. Now we do have a lot to cover, so let's jump in and get started. Before I talk about what CICD is, let's quickly step through a pretty typical workflow for a small development team. Let's say that we have a developer working on the code for the Categram application, and I talk about this throughout the course. He or she might have all of the source code stored on their local machine. They make changes, do tests and builds, and for a solo developer, this probably makes complete sense. But what about when other developers arrive? They might have some issues that they've added, maybe ones who aren't in the same building or even the same country. Then, storing code locally won't work. So we need a centralized source code control system and repository system. And one very common system is Git, and its online repository system, GitHub. Using GitHub and Git means that all the code, and crucially all of the versions of that code, are stored centrally inside the repository. When a developer is working on a project, they need to know what they're doing. When a developer adds new code, they commit that code locally and then push it into the repository. Any other developers can pull from the repository to get the latest code versions downloaded onto their local machines. If another developer makes any code changes, they're pushed into the repository, and then any other team members can pull these changes down from the repository to their local machines. Periodically, based on the code that's in the repository, you might want to run some tests and build that code into a release version. And you can do that using a build server. A build server pulls the code in, it does some tests, then runs some builds, then maybe some more tests, and then it puts any build artifacts back into the repository. Now, it's slightly more complicated than this, though, because there isn't only one set of code in most repositories. There's actually a concept known as branches. And let's take a look at that next. In most large-scale source code repositories, you often have multiple branches. There's always going to be the main branch. You often have a development or dev branch, and usually many more. Now, the main branch you can think of as the main branch of the repository, the production branch. It's often the one which is automatically deployed out to production servers, and so its usage has to be carefully controlled. You'll often find that only senior developers are allowed to write into this branch. And then only for serious code changes. And even for these serious code changes, then only in a tightly controlled way, controlled by senior management. And senior management often do, and should, get nervous about anyone committing code into the main branch. Essentially, it's bad practice to do this. The main branch is often the thing that contains the main version of your application code. It's the risky branch. It's the thing which gets deployed into production situations. What should happen with a mature development team is you create an additional branch. And a branch is a parallel set of code which starts off the same as the one that's in the main branch. But then you might have junior developers who start to commit changes to this separate branch, and over time, it begins to separate. Because you might have these junior developers committing code to a separate branch, it has no impact on production, and so the devs are freer to experiment and work on this branch without causing any of the senior managers stress, because it's not taking place on the main branch. It's not affecting the primary source code of the application. Now in this example, when the junior development team finishes working on the main branch, the developers working on the feature, or periodically, the development branch is merged back into the main branch. And this results in the main branch having exactly the same code as the development branch. Now once these changes are in the main branch, then other developers can pull these changes down to their local machines. Now keep in mind on this screen, I'm showing the main branch and the dev branch, so only two branches. It's possible that there are many different branches in the development branch. There are many more branches in busy source code repositories. You might have test, stage, or branches for specific major features. Now the issue is that if too much code is changed or altered, then you can potentially have conflicts when merging things back into the main branch. If two branches change one specific part of code in a similar but completely different way, then how can that be resolved? How can that conflict be resolved if the same part of the code is changed in two different ways? The fix for that is to merge often into production, so that the other branches can also get those changes, and it means that the chances for any conflicts are minimised. Now that you have an idea about repos and branches, let's look at what a code pipeline is and what it does. Code projects can generally be conceptually broken down into a number of stages. Now the exact number of stages differs depending on the project. It can be a number of stages depending on who you ask, but usually when I'm thinking about it, there are four. Code, build, test, and then finally deploy. The code stage focuses on the actual coding process and the storage and version control of code. The build stage is about building code, so taking source code together with any libraries and frameworks and putting everything together to generate an output. The test stage is about testing the code against various expectations. And then finally, the deploy stage is about getting your code out to the various compute environments where it will finally run on. Now these are all independent, but often related stages. For example, when building code, you will often have testing during that process, so build and test are often part of the same overall process. Now all of these stages together, the whole process is called a pipeline or a development pipeline. It starts at one end and code flows through step by step through each of the independent stages. Now before AWS, a developer, developer manager, or DevOps engineer had to create this process end to end. This type of role used software such as GitHub or Bitbucket for the code phase, specifically the code repository. And then CI CD systems such as Jenkins for the build stage, often Jenkins again for the test phase, and then other custom tooling or Jenkins for the deploy phase. Now there are two key things to understand about this method before AWS. First, it's self-managed. You had to install and configure it and then integrate it. And second, because it's third party tooling, integrating with AWS is something that often takes a lot of time. It takes some work and can result in sub-optimal outcomes. Now AWS provide a range of tools which support all of these different processes. The code phase is supported by CodeCommit, which is a code repository product similar to GitHub, but with integration with AWS. CodeBuild is a product which can handle the build process and the testing process. It's fully configurable and can also integrate with various different AWS systems. And then finally, we've got CodeDeploy, which supports the deployment of applications within AWS. Now these services integrate with AWS from a logging perspective. From an events perspective, they can generate notifications, they can be restricted using IAM permissions, but they are all still isolated products. The glue which binds them together is a product known as AWS CodePipeline. This is an orchestration tool which can link all of these products together. If code is committed into CodeCommit, then it can start a build, run some tests, and then deploy that code out to AWS infrastructure. Now when it comes to architecting CodePipeline, each pipeline has stages. Every pipeline needs, at minimum, a source stage which defines where the source code is located. But, and this is really important for the exam, it's tied to a branch as well as a repository. So a pipeline is linked to one and only one branch within a repository. With the previous example, we might want to create two pipelines. One pipeline linked to the development branch of the Categram application, and another linked to the main branch of the Categram application. Each with a source stage which uses the AWS CodeCommit product. Now these would be independent, so if a commit was made to the development branch of CodeCommit, then it would initiate a build stage using CodeBuild, and a deploy stage using CodeDeploy. And that would deploy the code onto maybe Elastic Beanstalk or other AWS services which form part of the dev or test environment. Now the same would be true if code was committed or merged into the main branch. First, this would start CodeBuild, or maybe something else such as Jenkins. This might include some testing as part of this stage, again, within CodeBuild or Jenkins. And if successful, you might have a manual approve step or proceed straight to CodeDeploy, which would deploy the code out to the production environment. And again, this could be something like Elastic Beanstalk or other AWS services which can be integrated with CodePipeline and CodeDeploy. Now before I finish with this lesson, there are a number of keywords and concepts which you need to understand to do well in the exam. The first are a pair of files which are known as BuildSpec and AppSpec. And these are either YAML or JSON files which influence CodeBuild or CodeDeploy respectively. So I'll make sure I include links attached to this lesson which detail exactly what each of these files does and how they're constructed. But the BuildSpec file is essentially a code that is built into the application. And the AppSpec file is essentially a collection of build commands and related configuration that CodeBuild uses to run a build. So you can include BuildSpec as part of your source code or you can define the specification when you create a build project. So essentially the BuildSpec file is used when you want to influence the way that the build process occurs within the CodeBuild product. Then the AppSpec file is a similar file, but this one is for CodeDeploy. So the AppSpec allows you to influence exactly how a deployment process proceeds. So you need to include an AppSpec file with certain types of deployments if you want to influence how that deployment occurs. So in the exam if you see BuildSpec you know that it refers to CodeBuild. If you see AppSpec you know that it refers to CodeDeploy. But both of them could in theory refer to CodePipeline. Especially the question, is talking about a full end-to-end deployment pipeline. Now again I'll make sure I include links detailing both of these attached to this lesson. Now next I want to talk about the various different things that CodeDeploy can actually deploy out to. There are different destinations that CodeDeploy can deploy your application into and it's important that you understand all of these for the exam. So the first is CodeDeploy itself. CodeDeploy can be used to deploy an application onto one or more EC2 instances using a deployment group. So you can use CodeDeploy to directly deploy onto EC2. You can also use CodeDeploy to deploy into Elastic Beanstalk or OpsWorks. With Elastic Beanstalk you have to specify an environment name. With OpsWorks you need to specify a stack and a layer. But the product can deploy into both of these AWS products. It can also be used to deploy using Cloud. So it can be used to create a new stack or update an existing stack or create a new or updated change set. So it can be integrated with CloudFormation to deploy out into AWS infrastructure. CodeDeploy can also deploy into Amazon ECS, so the Elastic Container Service or it can deploy into ECS using a blue-green deployment model. Now this can be either direct, so you can integrate it directly into ECS or you can configure a deployment group and configuration so that you can deploy it out using a blue-green deployment model. Now I'll be covering blue-green deployment models elsewhere in the course but essentially the principle is that you create a brand new environment to deploy a brand new code version into. You verify that new environment is working as intended. You move customers over to that new environment and then you remove the old one. So that's a blue-green deployment model. One environment is the blue environment, one is the green and you only delete the existing environment once you're sure that the new one is functioning as intended. You can also use CodeDeploy to deploy into products such as Service Catalog or you can use it to deploy an Alexa skill. You can also use CodeDeploy to simply store the built version of your application onto S3. Now you need to remember all of these for the exam. Try to commit them to memory. Specifically remember the function of the build spec and app spec files and definitely read the links that I've attached to this lesson as you've got time because they will be useful for the exam. With that being said, that's everything I wanted to cover in this lesson so go ahead and complete the lesson and when you're ready I'll look forward to you joining me in the next.