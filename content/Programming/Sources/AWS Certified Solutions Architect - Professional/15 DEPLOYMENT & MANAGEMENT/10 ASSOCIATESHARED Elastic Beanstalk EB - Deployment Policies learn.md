 Welcome back, and in this lesson I want to talk about Elastic Beanstalk Deployment Policies, also known as Deployment Types. Now we do have a lot to cover, so let's jump in and get started. We're going to look at these Deployment Policies visually, because it will make it much easier to understand. Before we do though, I just wanted to quickly step through the options that we have available. And to be clear, when I talk about Deployment Policies or Deployment Types or Deployment Methods, I mean the same thing. I'm talking about how application versions are deployed into environments within Elastic Beanstalk. The first type of Deployment Policy is all at once, and this one is pretty simple. A new application version is deployed onto all instances within the Elastic Beanstalk environment at the same time. There's no intelligence, it's quick, and it's simple. But it causes outages during the deployment, and doesn't really have a great method of handling failures. So this is a good one to use. It's good to use for development or testing environments, but nothing which has any importance. Next is Rolling Deployments, and with this method you deploy the new application version in batches. Each batch is taken out of service, the new application version is deployed onto that batch, and then when they pass health checks, they're put back into service. Now this means you have additional control, and it's safer, because the process continues only when the current batch passes its health checks. But it does mean a loss in capacity, as instances are removed from service while the deployment is happening. But this means there's no increase in cost, because the capacity of the environment is maintained. Now next we have the same, but using an additional batch. And this means that rather than instances being removed from service, upgraded, and then returned, which causes drops in capacity, with this method an extra batch of instances is being added. Meaning your hundred batches are being added, and the number of instances being removed is being added. But the hundred percent capacity is maintained throughout the process. Now this is better for real environments with real load, as customers don't experience any drop in performance. Both of these methods, so rolling with additional batch and rolling, will mean though you'll have two application versions running at the same time. Next we have Immutable. Now here a new temporary auto-scaling group is created with new instances running the new application version. Once you're happy, you can set up a new instance, Once you're happy, once the instance has passed the health check, they're moved to the original autoscaling group, the original instances are terminated, and the temporary autoscaling group is removed. Now the benefit of this method is it's the easiest to roll back. Only when you've verified that everything is okay are any changes made to the original infrastructure. Finally, we have traffic splitting. And with this type, a new temporary autoscaling group is created, instances are created inside it with the new application version. So, so far this is just like a mutable, but with this method you can control the distribution of traffic between the old version and the new version. And this allows you to perform a type of A-B testing with the new application version. If the new version is good, you can move over to it. If not, you can simply move all of the traffic back and then remove. the new infrastructure. Now with this method, there are no capacity drops. It is largely risk-free. It does provide the additional testing flexibility, but it does come at an additional cost because of the extra instances required during the deployment. Okay, so these are the high-level types of deployment. So let's step through how it looks visually. First, let's look at all-in-one deployment. With this method, we start with an environment with three instances. When we start with an all-in-one deployment, all three of the instances receive the deployment at the same time. All instances are offline and the service is interrupted for the duration of the deployment. When it finishes, we have two possible outcomes. Either everything is good or everything is bad. Either the application is available or not. Now architecturally, this is nice and simple to understand. It's nothing complex. So it's a simple method, but it does come with some significant limitations. And so generally, you wouldn't use this for any production usage. At this point, let's move on to the next deployment method. With a rolling deployment, the architecture of the environment is the same. When the deployment starts though, one batch, and let's assume for now that this is one instance, that batch is disconnected from the load balancer. It's out of service. At this point, the deployment of this batch starts and finishes. Only at that point do we move on to the next batch, which is disconnected, and the new version of the batch is not available. The new version of the application is deployed, and when that batch completes, the final batch is done. It's rolling through the environment, upgrading batches one at a time. Now in this case, I selected a batch size of one instance. I could have picked two or ten. With this method, you need to pick a batch size based on how many instances you can tolerate being out of service at any one time. Because each time a batch is upgraded, you're going to have a corresponding capacity drop, based on the size of the batch that you select. If you have three instances and you pick a batch size of one, then you'll be missing one instance for the duration of the deployment. If you have six instances and pick a batch size of two, you'll be running with four instances during that deployment. Pick a batch size of three, and the overall deployment will be quicker, because there are less deployment steps, but you'll be missing three instances for the entire duration. So this type of deployment is great. When you want to step through all of the instances within your environment, taking things batch by batch, so you can identify any problems before moving on to the next. So next we have rolling with additional batch. This deployment type is similar to rolling deployment, but we don't drop any capacity. So let's say that we start with an environment with four instances, and all four are running the currently deployed application version, which is version one. Now to make this easier to understand, here's a guide on what the colouring and shapes mean. A green tick means the current version, a yellow circle means not yet upgraded, and a red circle with a down arrow means that a deployment is occurring. This method of deployment starts by us having a version two of the application, and we decide to deploy it into this environment. Immediately, whatever batch size that we pick, let's say that it's two instances, this means that a new batch is deployed, running the new version of the application. This now means that we have a 150% capacity for our application, because we chose a batch size which is half of the number of instances which our environment is running. So now we have two application versions running within our environment. Four instances on version one, and two on version two. Now next, the deployment starts on two of the original instances, and these are removed from service, taking the capacity back down to the original 100% levels. Once this batch is finished, they're added back into service, and then deployment happens to the next batch. Again, they're taken out of service, the new version is deployed, and when finished, they're brought back into service. And this means that we now have a total of six instances running version two. This is two more, so one batch more than we originally had. So once the whole thing is finished, the extra batch is removed, and the environment again has four instances. Now this process takes longer. There will be additional costs for the extra instances running during the deployment, but it's safer and great for production usage, because we don't drop any capacity. Now let's move on again to the next type of deployment, and this is a mutable deployment. With this method, we start with a similar environment, three instances running the same batch, and the same version of the application, so version one. When we start the deployment, the original instances aren't touched at all. They're treated as a mutable. Instead, a temporary auto-scaling group is created, and within it, another full set of instances are immediately created, and the new application version is deployed onto these instances. So once finished, the result is two sets of instances. The original set running version one in the original auto-scaling group, and the new set running in the temporary auto-scaling group, deployed with version two. Once completed and the health checks pass, the new instances are moved into the original auto-scaling group, the original instances are terminated, and the temporary auto-scaling group is deleted. Now this method has the highest cost, because it uses double the instances, but it offers the quickest and lowest risk rollback, because if anything goes wrong, the original instances are available until right at the end of the process. So until the deployment finishes in its entirety, none of the original infrastructure is touched in any way. Now the next method is similar to this. The traffic splitting deployment method is just like a mutable, but when a new set of instances are ready, traffic can be split between the new and old. So in this case we're sending 50% of the traffic towards the new version on the right, and leaving the remaining 50% on the original version. Now this is a form of A-B testing, and it allows that extra set of verification before you finish the deployment. Again this means your regression path is relatively quick, because you can just return the traffic flow and remove the new instances. And once you're entirely happy with the new version, you can move all of the traffic flow over to the new instance. Traffic splitting is a fairly new method of deployment introduced in the middle of 2020, so I think it will take some time to feature on any of the exams, and even so it will be much more prevalent at the professional level rather than the associate. Now at this point, final concept before we finish this lesson, and that's blue-green deployment. Now this is not really something that's built into Elastic Beanstalk, but the idea is simple enough. You have two different environments, a blue environment on the left and a green on the right. You have a DNS record pointing at the blue, running version 1 of the application, you deploy version 2 to a brand new environment, you run both of them at the same time, get people to beta test the green environment on the right, and then when ready you can change the DNS to point at the green environment. Now this is really good because it means you have complete control over these two different environments, and you're not relying on the green environment to be able to deploy the new environment. So you can control when the switchover occurs, and leave the original environment in place for as long as you want to. Now you might see blue-green deployments mentioned in the exam, it's not really something that you'll be tested on in depth, but I wanted to make sure that you're aware of the concepts at a high level. Now at this point that's everything I wanted to cover about Elastic Beanstalk deployment policy, so go ahead and complete this video, and when you're ready I'll look forward to you joining me in the next one. Thanks for watching. I'll see you next time. Bye. Next.