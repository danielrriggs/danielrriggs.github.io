 Welcome back, and in this lesson I'm going to be talking about AWS CodeDeploy, which is the final piece of the puzzle for AWS CI-CD pipelines. Now I won't be covering how to use CodeDeploy in this lesson, as I'm going to be including a bonus demo later in this section. Now this will be good for the real world, but for the exam you only need to know the theory that's contained in this lesson. Now let's just jump in and get started straight away. CodeDeploy, as the name suggests, is a code deployment as a service product. It's an alternative to using things like Jenkins, Ansible, Chef, Puppet, CloudFormation, and others for deployment. But crucially, it deploys code, not resources. So this isn't a tool which you'll use to create a fleet of EC2 instances for Categram. This is just going to deploy the Categram application to an existing fleet, of EC2 instances. Now CodeDeploy is capable of deploying to EC2 instances, on-premise servers, Lambda, and even ECS. And it can actually do more than just code. While it can deploy code, it can also deploy pre-built applications, web applications, configuration, executable files, packages, scripts, media, and much more. The product integrates with other AWS services, as well as having... close integration with the CodeStar suite of products. So it's the logical tool to choose for deployments, if you already define your pipelines within CodePipeline, and build with CodeBuild. Lastly, and this one is really important to understand for the exam, to use CodeDeploy with on-premises servers or EC2 instances, you need to have an agent installed, which communicates with the product and performs deployments when instructed. So this is an agent-based deployment product, when it's being used with EC2 instances or on-premises servers. So keep that one in mind. Now at this point, I want to focus on the one really important thing that you need to understand for the exam, and that's the app spec file. So the app spec file is either YAML or JSON formatted, and it controls how deployments occur on the targets of that deployment. So it manages deployments, and it contains both configuration, and lifecycle event hooks. Now I'm going to talk about the configuration first, and then the hooks later on. So in the configuration section of this file, we have three different components. We have files, resources, and permissions. So files only applies to EC2 and on-premises servers, as does permissions, and resources only apply to ECS and Lambda. So this is really critical to understand files and permissions, for EC2 instances or on-premises servers. Resources are for ECS and Lambda. Now files provides information to CodeDeploy about which files from your application should be installed on the instance during the deployment's install. So this is how you configure which things are installed. Resources is specific for Lambda and ECS. For Lambda, it contains the name, alias, current version, and target version of a Lambda function. So this can be used to control all of the surrounding details about the Lambda function that's being used for the deployment. For ECS, the resources section contains things like the task definition, or container and port details used for routing traffic to your container. So that's important. Conceptually, think about this as the configuration for the thing running your application. And remember that the resources section only applies to Lambda and ECS. It's not used for EC2 instances or on-premises servers. Now the permissions section details any special permissions and how they should be applied to the files, directories and folders, which are defined in the files section. So if you use the files section to copy any files from your application onto these deployment targets, then it's the permissions section that's going to be used to set any special permissions on those files and folders. So this is really important, and as with the files section, it only applies to EC2 or on-premises servers. Now strictly speaking, these aren't part of the deployment process. You can think of them conceptually as configuration for what files and folders get copied in to your instances or on-premise servers, and then the configuration for what runs your application. So these are all things that surround deployment, but aren't part of the core deployment process itself. Now next we have lifecycle event hooks. And the function of these depends on the compute platform that you're using for your deployment. So it changes depending on whether you're using EC2 instances, on-premise servers, or whether you're using Lambda or ECS. Now if you're using EC2 or on-premises servers, then each of these lifecycle event hooks runs one or more scripts, for each of the hooks. If you're deploying to Lambda or ECS, then each of these lifecycle event hooks specifies Lambda functions to run during each of the deployment lifecycle stages. So the architecture changes on whether you're deploying out to EC2 or on-premises servers versus Lambda or ECS. And if an event hook is not present, then no operations are executed for that event. So no scripts are executed, and no Lambda functions are invoked as part of the deployment. Now specifically what each of these event hooks does depends somewhat on what is being deployed and where it's being deployed. But at a high level, the application stop lifecycle event hook, this occurs before the application is downloaded. So you can specify scripts or control events to gracefully stop the application or remove any currently installed packages. And this is all in preparation for the deployment. So application stop is generally used when you want to prepare for the actual deployment itself. Download bundle is when the code deploy agent copies the application down to a temporary location. Now the next is before install, and this is an event that you can use for any pre-installation tasks. Maybe you want to decrypt some files or create a backup of the current application or configuration, anything that you want to do before the install itself, you can do within before install. Now the next hook is install, and during this part of the deployment lifecycle, the code deploy agent copies the application files from the temporary location to the final destination folder. So this is performed by the code deploy agent and you can't run any scripts during this step. So this is something that's handled on your behalf by the code deploy product and code deploy agent itself. Now after install, as the name suggests, this is the part of the deployment lifecycle which allows you to perform post-install steps. So performing any application specific configuration, maybe changing file permissions or applying licensing, anything that you want to do after the install, you can perform during the after install part of the lifecycle. And then we have application start, and this typically is used when you want to restart or start any services that were stopped during the application. So this is the part where you've performed all the configuration and now you're wanting to start up the application service or services. And then the final step, and this is really important for the exam, is validate service. And this is the part where you're going to verify that the deployment was completed successfully. This is really important for the exam, so do your best to remember this last step. This is the part where you're going to verify that the deployment was completed successfully. This is the part that's going to allow CodeDeploy to determine whether the deployment was successful or not. And it's your opportunity to look at the application specifically, so check any application logs or perform any tests to verify that the application has been deployed as expected. Now for the exam, you need to do your best to remember both the order and the names of these individual components of the deployment lifecycle. Now for the exam, you don't need experience, of using CodeDeploy and all of the other products. You just need to know the architecture at a high level. For real world usage, you do need to be comfortable with how to use them, and that's why there's a bonus demo lesson coming up soon in this section of the course. Now at this point, this is the last piece of the puzzle. So CodeDeploy generally forms the last step of a CI CD process running within AWS. So it starts generally using CodeCommit or maybe using another repository, such as GitHub. Then we move through to CodeBuild. Maybe we're using CodeBuild to both build and test. And then finally, we move to CodeDeploy, which actually moves the application that we've built and tested through to its final destination. So that's all of the theory that you need to understand for the exam. We've covered all of the major components, and of course, it's all orchestrated by CodePipeline, which is a really essential product to understand. Now with that being said, that's all of the architectural theory that I want to cover in this last lesson of this series. And once again, there is going to be a demo which is coming up later on in this section, which is going to give you some practical experience of using all of these different products together. Now this is a bonus lesson. It's not required for the exam. And my intention is to create this as a version 1.1 of the course once I've released all of the exam important topics as a version 1. Now at this point, that's everything. So go ahead and complete the video. And when you're ready, I'll look forward to you joining me in the next.