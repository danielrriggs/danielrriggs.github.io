 Welcome back and in the second walkthrough lesson where I'm going to be stepping you through how to interact with Elastic Beanstalk using the console UI, I want to go ahead and create a second environment within our Categram POC application. So I'm going to select Applications on the menu on the left and then go inside the Categram POC application. Once I'm inside this, I'm going to create a new environment and this is going to be the Production Environment. So I'm going to click on Create New Environment. Remember from the theory introduction lesson, environments can either be worker environments or web server environments. And for this walkthrough, I'm going to stick to Web Server Environments. So I'll select that and then click on Select. Now for Environment Name, I'm going to use Categram POC-env-prod. So we can distinguish between the original environment, which is the dev environment, and this new one, which is for production. So once I've set that... I'll scroll down and here we can set the platform options just like we did for the initial environment. So you can click in the drop-down and pick the platform type that you want to use for this production environment. And to keep things consistent, I'm going to choose Python. I'm going to choose the same platform branch that I picked for the previous environment. So the latest version of Python running on Amazon Linux 2 and the same platform version. Now just like before, I'm going to choose the same application type that I picked for the previous environment. And to keep things consistent, I'm going to choose Python. Now if I'd defined any specific application versions for the Categram POC application, then I could choose to deploy a specific version by selecting this option and choosing it from the drop-down. I could also choose to upload some code. And I want to introduce this term, which is Source Bundle. The Source Bundle is the name for the archive. That you upload to Elastic Beanstalk that contains all of the source code and any other files required by Elastic Beanstalk. So I could choose to upload a Source Bundle directly from my computer or from Amazon S3. Now I'm going to choose the sample application because I want to make sure that both this environment, so the production one, and the dev environment, which I created with the application, are using the same sample code. Now this time, instead of clicking on Create Environment, which I did create, previously, now I'm going to click on Configure More Options because I want to step through some of the additional configuration options that you have when creating an environment. So I'm going to click on this button. Now at the top here, you're presented with some configuration presets, and all of these influence the options that you can set manually down towards the bottom. You have single instance free tier eligible, single instance using spot instance. So this is a way that you can obtain access to discounted compute resources, and you can also use the option to create a new environment. So you can create a new environment, and you can also use the option to create a new environment. So you can create a new environment, which AWS has spare within a region. We won't be using this, and generally this is not something you do for a production web server environment. You also have the ability to select high availability, and that's the option that I'm going to pick for this walkthrough. And this changes the architecture of the environment. Now the environment will consist of anywhere between one and four instances, and it will be configured so that it can automatically recover if any of the individual instances experience any failures. Like I said, we have high availability using spot and on demand instances. This is similar to the single instance using spot, only this adds high availability and allows a mixture of spot and on demand instances. And then finally, you can click on custom configuration, which will allow you to set all of these options manually. Now software, if I just click on edit, this allows me to specify which proxy server is going to be used. In this case, it's engine X. It allows me to enable Amazon X, which is the distributed tracing product I talk about elsewhere in the course. I can elect to have my log stored on S3, so the instances can be configured to upload logs to Amazon S3. I can also configure it so that instance logs are streamed into CloudWatch logs. Remember, I could request the last 100 lines from the individual instances using the logging option. But if I enable this, then the logs are automatically delivered into CloudWatch logs, which allows us to, to explore them and potentially let us create things like metric filters. So we can create some event driven response to any of the items in these logs. We can also use this to specify environment properties. And these are obviously key value pairs, which are accessible within the environment. Next, we have the instances section and it's here where we can specify detailed configuration about the instances which make up this environment. So we can specify details on the root volume type, the size, and the available IOPS. And we can also decide which security groups are going to be attached to these instances. Now we can click on edit next to capacity and choose whether to use on demand instances or to combine purchase options and instances. So this gives us quite some flexibility on how we want the composition of the fleet within this elastic beanstalk environment. Now we can keep it simple. We could change the instance type. We could change the AMI that's used to create these instances. We could click in the availability dropdown and pick whether we wanted any AZs in the region, any one AZ, any two AZs, or any three AZs. And then if we wanted to be even more granular, we could explicitly specify the placement. So define which availability zones are used within a region. We can also configure the scaling triggers which are used for the auto scaling group that the environment uses. We can decide on a metric to use, the statistic to use, the unit to use, the period, the breach, duration, as well as the upper and lower thresholds. And for advanced users of elastic beanstalk, this allows us to be really granular with the configuration of the scaling inside the environment. We can change options for the load balancer. So you can pick between an application load balancer, a classic load balancer, and a network load balancer. You can also pick to use a dedicated load balancer exclusively for this environment, or use a load balancer that's scoped to your account. So you can create a load balancer and then use it across multiple elastic beanstalk environments. For listeners, you can specify the port and protocol that load balancer is listening for. In this case, it's TCP port 80, which is HTTP. But as I'll talk about later in this section, if you do want to allow HTTPS, then you do need to add an additional listener configuration so that the load balancer will listen for TCP port 443. Under processes, you can specify the protocol, and port, and protocol that you want to use. You can specify the protocol and port that the load balancer uses to route requests. And this allows you to be even more granular with the way that the load balancer integrates with the instances inside this environment. You can define rules, and you can even enable access logging from a load balancer perspective. So you can enable it and then point it at an S3 bucket and an optional prefix. Inside rolling updates and deployments, this is where you can select the type of deployment that's used for both application deployments, and configuration updates. Now configuration updates is out of scope for this particular exam. The only thing you need to understand for this exam is the application deployment type, and I'll be covering the various different options in a separate lesson. So we have all at once, rolling, rolling with additional batch, immutable, and traffic splitting. And they will feature in a dedicated lesson elsewhere in this section of the course. Moving on, we can configure various security settings such as the service role to use, the EC2 keypad, and the server. So we can configure various security settings such as the service role to use, the EC2 keypad, and the server. So we can configure various security settings such as the service role to use, the EC2 keypad, and the server. So we can configure various security settings such as the service role to use, the EC2 keypad, and the server. As well as any IAM instance profile. So this governs what permissions the instances receive. You can also configure monitoring and health reporting, either using enhanced or basic. You can specify various custom metrics for the instance and the environment. You can enable health monitoring customization. So you can ignore application 400 series or ignore load balancer 400 series. And you can stream health events into CloudWatch logs. Again, you need to enable it, specify a response, and then you can configure the settings. You can also enable the settings. You can enable the settings. You can also enable the settings. You can also enable the settings. You can also enable the settings. Now you've got the option to configure managed updates as well as notifications. I'll not be covering these in detail because they're outside the scope of this particular certification. If you want the Elastic Beanstalk environment to be part of a VPC, you can configure that here. But again, this is outside the scope of this particular certification, so I'm not going to cover it. Finally, the last thing before we finish with this walkthrough lesson, you are able to configure a database within an Elastic Beanstalk. Now you need to be careful with this. Whilst you can configure an RDS database to be created within an environment, it means if the environment is deleted or otherwise adjusted, this can affect the database. For production usage, it's a much better practice to create the RDS instance outside of Elastic Beanstalk and then pass in the details to your application running within your environments. And that just separates the lifecycle of the application from the application itself. So if you want to configure a database that's outside of Elastic Beanstalk, you can configure the settings to be created within an Elastic Beanstalk environment. And that separates the lifecycle of RDS from your Elastic Beanstalk environment. That's much better and much less risk when you're making changes to environments. Now at this point, that's everything I wanted to cover about these detailed configuration settings. Now what I'm going to do for this walkthrough is just go ahead and select the high availability preset to make sure that all of these options below have appropriate values. Then I'm going to scroll down and click on create environment. Now this is going to create a second environment with a new environment. So I'm going to go ahead and click on create environment. And there we are, within this Elastic Beanstalk application. So now I'll have the production environment which is called catagram poc-env-prod as well as the original environment that I created with this application. So this gives us the ability to step through some of the things that we can do with multiple environments. Now at this point this will take some time, so I'm going to go ahead and finish this walkthrough lesson. If you're doing this in your own environment, then logically you'll need to wait for this to finish. then logically you'll need to wait for this to finish. But I'm going to finish at this point because next I want to talk about the various different deployment types available within Elastic Beanstalk. So now you've seen how to deploy an additional environment. The next thing that you need to learn about is how you deploy application versions into those environments. And there are a number of different options that you need to be comfortable with. But at this point, that is everything I wanted you to cover in this walkthrough. So go ahead and complete this video. And when you're ready, I'll look forward to you joining me in the next.