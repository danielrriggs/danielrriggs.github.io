 Welcome to this demo lesson where you're going to get the chance to implement a non-portable CloudFormation template. Now, in order to understand how to implement CloudFormation templates in a best practice way, you need to understand why a non-portable template is bad practice, and what kind of things it can cause when you try to implement it. So first, in this demo lesson, we're going to implement together a non-portable template. So to do that, let's start off by making sure that we're logged in to the management account of the organization, and you're using the IAM admin user. Additionally, make sure you're logged in to the Northern Virginia region, so US-East-1. At this point, click on the Services drop-down, and type CloudFormation to move across to the CloudFormation console. Now, at this point, we're going to create a CloudFormation template from scratch. So you'll need to move across to your phone. You'll need to use your favorite code editor. Now, it's rare that we actually create CloudFormation templates from scratch.  cThere's a wealth of reference material which you can use to create templates. You'll find many repositories of pre-made templates available on the internet, and in fact, AWS make available the AWS CloudFormation user guide, which includes lots of different template definitions for all of the different logical resources that we can create. Now, I've currently got the reference page for EC2 open. So the resource type that this refers to is AWS, double colon, EC2, double colon, instance. And this shows you the structure of an EC2 instance logical resource defined using JSON and then YAML. And if we scroll down, it gives details on all of the different properties, whether those properties are mandatory or optional, and whether changing those properties is mandatory or optional. And whether changing those properties will cause the resources to be replaced. And I'll be talking about resource replacement in a future CloudFormation video. If you scroll down all the way down to the bottom, generally for different logical resources, AWS will provide you with a few examples. In this case, examples on how to create a simple EC2 instance using JSON and an equivalent one with YAML. Now, we're going to create this one from scratch together. So from my code editor, we're going to create a brand new CloudFormation template. Now, I'm going to write this one in YAML, but attached to this lesson, I'm going to include two pre-made templates, one in YAML and one in JSON. So you can refer to those as you create your own manually within this demo. So we're going to get to work creating this YAML CloudFormation template. So in my code editor, towards the bottom right, I can select the type of file that we're creating. And this just does some syntactical highlighting. This is not essential, but it makes it easier for me to demonstrate how the formatting of a YAML template is for a CloudFormation template. So don't worry about it if you don't have this functionality. I'm going to go ahead and locate YAML and then select it. And I'm going to begin creating this CloudFormation template. Now, in previous CloudFormation theory videos, I've stepped through the basic structure of JSON, and YAML, but this will be the first CloudFormation template we've created from scratch. So all logical resources inside a CloudFormation template are contained within the resources section of that template. So that's what we need to start with. So I'm going to go ahead and type resources and then a colon. And then I'm going to press enter and note how it already indents the cursor to the next level of indentation because this understands the YAML file format. If your code editor doesn't do this automatically, each level of indentation is two spaces. So we're going to create two logical resources in this template, an S3 bucket and an EC2 instance. So the next step is to name the first logical resource that we're going to create. And we're going to keep it simple, so we're going to call this one bucket. So type bucket and then press colon and then press enter again. Now all CloudFormation logical resources need to have a type. You're telling CloudFormation what the type of resource is that you're wanting to create. And the first resource we're creating is an S3 bucket. So to define the type of this logical resource, we use type and then colon and then a space. And then we need to provide the type of this resource. Now if I go back to this resources reference and I'm going to type S3 in the search box at the top and press enter. And the top result is AWS double colon S3 double colon bucket. So I'm going to open that link. And this gives you the format for the logical resource of type S3 bucket. Now if we look at the YAML definition, we can see at the top here we've got type colon and then AWS double colon S3 double colon bucket. And this is the correct type to create an S3 logical resource. So I'm going to copy that into my clipboard, go back to my code editor, and it's far safer to enclose everything in quotes. So I'm going to put a single quote and then paste in AWS double colon S3 double colon bucket and make sure it's finished by another single quote. So we can see that we've defined the type of this S3 bucket. Press enter. And next, most logical resources within CloudFormation are configured using a properties definition. So we're going to do that. We're going to type properties and then a colon and press enter. And in the case of the S3 bucket, we're going to define a name for that bucket. To define a name on the bucket, we're going to use bucket name with an uppercase N and then a colon, then a space, and then again single quotes. And then for the name of the bucket, I want you to go ahead and pick the same name as the one that I'm using for now. So we're going to pick AC and then cat, and then picks, and then 1333333. So that's a total of six 3s and then a 7. And that's enough to create our S3 bucket. So next press Enter, and then I want you to press Delete and move the cursor back to the same level of indentation as bucket. So we're creating this next logical resource at the same level as the bucket logical resource. And this is going to be an EC2 instance. So we're going to call this logical resource instance, just to keep it simple, and then a colon, press Enter. We need to define the instance type. So move back to this reference, and then in this search box type EC2 and then a space and then instance and press Enter. And don't worry, I'll be including a link to this reference attached to this lesson. At the top here you should see AWS double colon EC2 double colon instance. Go ahead and click on that. And if we scroll down to the YAML, we'll see the resource type for an EC2 instance, which is a resource type. So we're going to copy that into your clipboard and then move back to your code editor and then enter type and then a colon and then a space, then two single quotes, paste in the EC2 instance type. So that's what you should see. We're now defining a logical resource called instance using this resource type. So AWS double colon EC2 double colon instance. Press Enter. And then we're going to define some properties. So type properties and then a colon and press Enter. So when we're creating this EC2 instance, we're going to provide it with a few pieces of information. The first piece of information is going to be the type of the instance. And we're going to define that by typing instance and then type with an uppercase T and then a colon, again quotes, and inside this T2.micro. At the end of this line. Press Enter again. And the final property which we're going to define is the AMI ID to use for this instance. We're going to define that by typing image ID with an uppercase I and then a colon, then a space, and then just put some single quotes. And then we need to return to the AWS console. Now we need to move to the EC2 console first. So click on the services dropdown and select EC2 in recently visited or search for it in the search box at the top. Then go ahead and click on launch instance. And then click on launch instance. And we need the AMI ID for Amazon Linux 2. So make sure you're in the Northern Virginia region and then copy down this entire AMI ID. It should start with AMI and then a hyphen and then should have random numbers and letters. So make sure you select it all and make sure it refers to 64-bit x86. You don't want that. If you want the 64-bit ARM AMI ID, you want the one that refers to x86. So copy that into your clipboard. Then move back to the code editor and just paste that in. So this is a CloudFormation template which will create two resources. We've got two logical resources defined, bucket and instance. And when we use this template to create a CloudFormation stack, it will create two corresponding physical resources. So let's go ahead and do that. Now, the first thing that we need to do is to save this template file so that we can load it into CloudFormation. So go ahead and save this somewhere logical and call it nonportable.yaml. And then go ahead and save that file. And once it's saved, we can move back to the AWS console and load it in. Okay, so this is the end of part one of this lesson. It was getting a little bit on the long side and so I wanted to add a break. It's an opportunity just to take a rest or grab a coffee. So I'm going to go ahead and save this file. Part two will be continuing immediately from the end of part one. So go ahead, complete the video, and when you're ready, join me in part two.