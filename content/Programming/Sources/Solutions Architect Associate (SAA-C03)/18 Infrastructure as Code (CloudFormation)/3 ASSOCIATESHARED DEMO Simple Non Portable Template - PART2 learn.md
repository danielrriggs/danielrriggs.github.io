 Welcome back. This is part two of this lesson. We're going to continue immediately from the end of part one, so let's get started. So back at the AWS console, click on Services and move back to CloudFormation. Click on Create Stack. We're going to select Template is Ready and then Upload a Template File. Click on Choose File and select the non-portable .yaml file that you just saved. Select that file. Click on Next. For Stack Name, we're going to call this one Non-Portable 1, so enter that and then click on Next. Scroll to the bottom. Next again. Scroll all the way down to the bottom and click on Create Stack. So this is going to go ahead and create the CloudFormation stack using the template that we've just created together. And for every logical resource in that template, it's going to go ahead and create a corresponding physical resource. Now this might take a few moments to update. So just keep clicking on the refresh icon. And what you should see is an error. This should not complete and there's a reason that it won't complete. If I just return back to the template, you'll note how we explicitly defined a bucket name within this template. Now S3 bucket names need to be globally unique across all regions in all AWS accounts. And I've already got a bucket in one of my accounts called AC CatPix 1.333.333.7. So I can't create a new bucket with this name and you also can't create one in your account because it's globally unique. So all regions and all accounts. So when we're looking at why a CloudFormation stack fails to create, we can always go to the Events tab and look for the first piece of red. In this particular case, it tells us that AC CatPix. 1.333.333.7 already exists. And that's the reason why this stack can't create. So let's go ahead and delete this stack by clicking on the delete button and then confirming with delete stack. Then we're going to go back to our code editor and I want you to go ahead and use a name that's unique to you. So you can either change the first part of the bucket name or you can add some additional unique digits to the end. In my case, I'm going to add two extra digits. So I'm going to add two extra threes just to make sure it's unique. And again, you need to make sure that your name is entirely unique. Once you've updated the bucket name with a name that's unique to you, go ahead and save that template and then move back to the console. Refresh and just make sure that this stack is entirely deleted. It has. Click on stacks and then click on create stack and follow that same process through again. So make sure that template is ready is selected. Click on upload a template. Click on choose file. Choose that template file that you've just updated. Next. We're going to call it non portable one for the stack name. And because we've already deleted the previous stack, we will be able to use this same name again inside the same region. So enter that and click on next. Scroll down. Next. Scroll down to the bottom and create stack. Now again, this is going to take a few minutes to create. So just keep clicking on the refresh icon. And note all of the different events which are added to the event log for this stack. So you'll note that it says creating progress for the bucket and creating progress for the instance. You'll see another line saying resource creation initiated. And this means that it's actually provisioning the physical resource that corresponds to the logical resource within the template. Keep hitting refresh. You'll probably find that the bucket is created first. So the bucket will move into a create complete state. And it might take you another minute or so before you're in this state when the instance itself also moves into create complete. Now once all of the logical resources move into create complete, the stack itself will be in a create complete state. And if you click on the resources tab, you'll see that you have two physical resources that correspond to the logical resource in the template. So we created one logical resource called bucket. And that's created a corresponding physical resource. We created a second resource called instance, which has created a physical EC2 instance. So this seems to have worked. This template has created two resources within this region within our AWS account. What we're going to do now is create another stack. So I want you to click on stacks. I want you to click on create stack with new resources. We're going to use the same template. So make sure template is ready is selected. Select upload a template. And choose the same non-portable template that you used a second ago. Select that. Click on next. For stack name this time, we're going to use a different name. So non-portable 2. Click on next. Scroll down. Next. Scroll down again. And then click on create stack. So because the previous stack created successfully, you would assume that this new stack is also going to create successfully as well. Let's keep hitting refresh. And note how we're getting an error. So the standard practice with CloudFormation if you do get an error is to look for the first error message in this event log. So the first red message is this one. And it tells us what the problem is. It says in my case, AC CatPix 1.333.333.337 already exists in another stack. The problem is because the S3 bucket name is explicitly defined within our stack. It means that we can't use the template twice because that would try to create a duplicate bucket name. And you can't have two buckets with the same name in any region in any AWS account. So this is one reason why this template is not portable. You can't use it multiple times because the bucket name has been explicitly specified. Now let's go ahead and delete both of these stacks. First, select non-portable 2. And click on delete. And then delete stack. And do the same process with non-portable 1. So select that. Click on delete. And then delete stack. Now if we just click on non-portable 1 and then go to events. You'll be able to see that because we're deleting the stack, that's deleting the logical resources. And along with that, the physical resources that are tied to those logical resources are also deleted. So one of the benefits of CloudFormation is that it matches the lifecycle of the logical and physical resources. So by deleting the stack, we're deleting those physical resources. And it has the effect of tidying up the account after we've finished with this infrastructure. So keep clicking on refresh. After a couple more moments and another refresh, all of the logical resources move into delete complete. And the stack itself is removed from the list. So now we have no stacks in this region. Now in theory, that means that we can go ahead and use this CloudFormation template. And create another instance and another S3 bucket. Because we no longer have a bucket with that name, we can create a new CloudFormation stack. Which will create a new bucket and instance. And it will be fine. It will create successfully. And if we did that in this region, that would be the case. It would work fine. But I want to demonstrate a second element of non-portability of this template. To do that, click on the region drop down and just select a different region. So something other than Northern Virginia, US-E-1. I'm going to use Oregon, which is US-W-2. So select that. And once you've moved to Oregon, you should still be at the CloudFormation console. So click on create stack. And we're going to do the same process. So make sure template is ready. Select upload a template file. Choose file. Select the non-portable.yaml template that you created earlier. Click on next. We're going to call this one non-portable 1. So enter that. Click on next. Scroll down. Next. Scroll down. And click on create stack. So based on everything that you know to this point, this should create successfully. Because we don't have an S3 bucket with the name of the logical resource inside this template. So the logical resource should be able to create a corresponding physical S3 bucket with that name. Because it's no longer in use. So the S3 bucket should work fine. And we shouldn't receive an error. So click on refresh. And we do receive an error. So the normal practice again is to look for the first red error message inside the event log for the stack. So this is the line, create underscore failed. And it tells us what the problem is. It says that the image ID and then it lists the AMI ID that we entered into our template does not exist. Now the problem here is that Amazon machine images are unique to a region. So when we copied down the AMI ID for Amazon Linux 2 and entered it into our template, we used the Amazon machine image for the US-E-1 region. And that means that this template can't be deployed into any other region because another region would use a different AMI ID. So this is another reason why this template isn't portable. Because we've explicitly defined both the name of the bucket. Which means this template can only ever be applied once within our account. And we've also explicitly hardcoded the AMI ID that only exists in US-E-1. And that means that this template can only ever be applied in one AWS region. Now these are two examples of how you can make a template non-portable. If a template is non-portable, it means there are often restrictions on how you can make it. In how many times you can apply the template. In what regions you can apply the template. Or what accounts you can apply the template. In this case we have a restriction on the number of stacks that can be created from this template. In this case it's only ever one. And we also limit ourselves to US-E-1. So this is a non-portable template. And over the next few videos in this series, I'm going to explain the theory of how you can change a non-portable template to be a best practice template. So let's start with this portable template that can be used to create many stacks in many regions in many accounts without having any of these problems. We're going to use a number of CloudFormation features to make this happen. And you'll see how a template can be evolved from being non-portable to being portable. Now that's everything I wanted you to do in this demo. So just to tidy up, move back to the AWS console. Just delete this non-portable one stack that's within the Oregon region. Wait for that to delete. And once it's deleted, just go ahead and click on the region drop-down and change back to Northern Virginia. And at this point you've done everything required in this demo video. You've seen how to create and use a non-portable template. So go ahead and complete this video. And when you're ready, I'll look forward to you joining me in the next.