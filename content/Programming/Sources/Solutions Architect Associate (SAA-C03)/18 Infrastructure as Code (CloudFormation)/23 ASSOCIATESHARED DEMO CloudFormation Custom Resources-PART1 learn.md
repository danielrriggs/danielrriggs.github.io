 Welcome back, and as a reminder, this is the architecture that we're going to be implementing in this demo lesson. So we're going to start off with a CloudFormation template, and this template is going to be used to create a stack. Now this stack is going to contain a number of different types of logical and physical resources. First, it's going to create a standard S3 bucket, and this is going to be an empty S3 bucket. Then it's going to create a Lambda function, and this Lambda function will be the backing for our custom resource. So once this Lambda function's created, the CloudFormation stack is going to create a custom resource, and it's going to pass it an event. And that event is going to contain the location of some media. So once it has this event, once it knows the location of the media, the Lambda function is going to invoke. It's going to load the media from a media bucket. And transfer that into the empty AnimalPix bucket, which is now going to be full of images. Then once these images have been transferred, the Lambda function will signal the CloudFormation stack that it's completed the tasks successfully. And this will be a demonstration of how you can use CloudFormation as well as custom resources, not only to provision things, but also to customize them in any way that you can code. In this case, using a Lambda function to download images. And populate an empty bucket. Now the reverse of this will also be using the Lambda backed custom resource. So when it comes time to delete the CloudFormation stack, the first thing it's going to do is send the delete event to this custom resource, which remember is Lambda backed. This Lambda function is going to receive the event. It's going to be invoked. And it will then connect to the bucket and remove all of those objects out of the bucket, leaving an empty AnimalPix bucket. Then the Lambda function. And will signal back to the stack that this task has been performed successfully. And the stack will then delete the Lambda backed custom resource. Once this is done, the stack will then move on to delete the rest of the resources, including the now empty S3 bucket. And this is the architecture that we're going to step through during the remainder of this demo lesson. So let's go ahead and switch across to the AWS console and get started. Now to get started, as always, make sure that you're logged in to the general AWS. Account. So that's a management account of the organization and you'll need to have the Northern Virginia region selected. Now attached to this lesson are two one click deployment links. One of them is to create a normal S3 bucket and the other is to create an S3 bucket using a custom resource. So I want to demonstrate the difference between these two methods. And to do this, I want to first review the CloudFormation template that each of these links deploys. And then. Explore exactly what happens during the deployment. So to start off, we'll be deploying the normal S3 bucket. So this is the CloudFormation template that you're going to deploy when you're using the normal S3 bucket. One click deployment. So it's nothing complicated. It's a simple CloudFormation template written in YAML. It has a resources section. It's deploying one logical resource called animal picks, and that resource is of type AWS double colon S3. Double colon bucket. So that's going to deploy an S3 bucket. So let's move back to the console. And then attached to the lesson, you want to go ahead and click on the normal S3 bucket. One click deployment. This will take you to a quick create stack page. Everything's pre-populated. The stack name is S3 bucket normal. You just need to go ahead and click on create stack. Now this will be a fairly quick deployment. First, the stack moves into a create in progress state. If we refresh it. Then it shows the animal picks logical resource being created, and that's in a create in progress state. Let's keep refreshing. The animal picks logical resource moves into create complete. And then the stack itself also moves into create complete. And if we move to the resources tab, we can see that the stack has created the logical resource animal picks and the corresponding physical resource, which is the actual S3 bucket. Now, if we go into this S3 bucket. By clicking on that link will be placed at the S3 console and see that this is an empty bucket. If we go back to the cloud formation screen, click on delete to delete the stack and then confirm it. You'll see that this process works without any problems. The stack will move into the delete in progress state. If we move to events, you'll see that the resource itself will move into delete in progress. Then the resource will be delete complete. And then the stack itself will be in delete complete. And that will mean that. The S3 bucket has been deleted with no problems. So let's close down this additional tab and I want you to again use that same one click deployment link again to deploy the standard bucket. You'll see the same quick create stack page. Click on create stack again to create this stack for a second time. Let's wait for it to complete. You'll see the same process happening. The logical resource is created. The logical resource itself now moves into create complete. And after a few more moments, the stack as a whole. Is now in a create complete state. If we click on resources again, we have the logical resource ID, a corresponding physical resource and clicking on that will move us to the S3 console. Now next we're going to upload some objects to this bucket. So go ahead and click on upload and then click on add files. Now attached to this lesson is another link which will allow you to download the assets used in the demo. So go ahead and click that link to download it to your local machine and then extract it. Once you've done that, go inside the folder that gets created. And inside there you'll find two images, Anna and Yuri. So select both of these JPEG objects and click on open. This is going to upload those objects to this bucket. So scroll down and select upload. Once that's finished, click on close. And let's just open up each of these objects to make sure that they work. So click on the object, select open. If you get a pop-up blocker dialogue, then just click on preferences and allow that to work. And then you can see that the object is now in the folder. So click on preferences and allow that pop-up. There we go, this is Anna. Let's close that tab. Move back to the bucket. This time open Yuri. Select open. And we can see that both of these objects open with no problems. So close that down. And then we're going to move back to the CloudFormation console. And this time with some objects in that bucket, we're going to try to delete this CloudFormation stack. So click on delete and then confirm by clicking on delete stack again. And let's just watch what happens. So we'll click on the events tab. This time we'll receive an error. We won't be able to delete this stack because we can't delete one of the resources. And we can't delete one of the resources because we're not able to delete S3 buckets which contain objects. So this illustrates one of the downsides of using CloudFormation to create things such as S3 buckets without using custom resources. Whenever we interact with resources, if we upload objects to our cloud, we can't clean up the infrastructure by using only CloudFormation. And that's where custom resources come in really handy. So to fix this problem, let's go back to the S3 console. Just move to the main S3 console. The bucket that's been created by this CloudFormation template should be called AnimalPix. So select it. Select empty. Copy and paste or type permanently delete into the box. And then click on empty. That'll take a few seconds once it's completed. Click on exit. And then once that bucket's emptied, we can move back to CloudFormation. Then select the S3 bucket normal stack and click on delete. Because this has failed to delete previously, you're given the option to retain the resource rather than generating another error. So if you check this box and this resource were to error again, it would be retained, but then the rest of the stack could be deleted with no problems. Because we've cleared the problem that caused the previous error, because we've emptied the bucket, we don't need to select this. We can just go ahead and click on delete stack. And this will delete the logical resource, the S3 bucket, and the stack will delete without any other problems. We'll need to wait for a few moments. After a couple of refreshes, we'll see the logical resource is deleted, the bucket is deleted, and now our account is back in the same state as it was at the start of the demo lesson. So that's the process without using custom resources. And next, I want to show you how this differs when we update the S3. We are using a custom resource. Okay, so this is the end of part one of this lesson. It was getting a little bit on the long side, and so I wanted to add a break. It's an opportunity just to take a rest or grab a coffee. Part two will be continuing immediately from the end of part one. So go ahead, complete the video, and when you're ready, join me in part two.