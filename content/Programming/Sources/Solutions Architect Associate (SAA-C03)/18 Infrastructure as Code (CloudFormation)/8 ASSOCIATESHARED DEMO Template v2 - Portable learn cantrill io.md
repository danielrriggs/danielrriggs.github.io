 Welcome back and in this demo lesson I want to step you through how you can enhance a CloudFormation template in order to change it into one which is fully portable. Now as a reminder, if I just move across to my code editor. Now attached to this video is a link that I want you to download and extract. And inside the folder which is created will be all of the CloudFormation templates that you'll need for this demo. Now I've gone ahead and opened all of the four templates that are contained within that folder and they're on my screen right now. Now there are four JSON files. So nonportable.json, portable-stage1, portable-stage2 and portable-stage3. And these are the four different stages that we're going to step through in this demo lesson. Now there's also an equivalent four files using the YAML format. So you can go ahead and load up whichever. You feel the most comfortable with. But these are the templates that we're going to be using. Now if you're going to follow along in your own environment. As always you need to be logged into the IAM admin user of the general AWS account. So this is the management account of the organization. And you'll need to initially have the Northern Virginia region selected. But in this demo lesson we are going to be using other AWS regions. Now moving back to my code editor. This first file nonportable.json. This is the template that we ended up with at the end of the previous demo lesson. Now this has one major problem. It's not portable. And let me demonstrate why. So in my AWS console I'm going to type CloudFormation in the search box at the top. And then click to open the CloudFormation console. And once I'm there I'm going to create a stack. Now I'm going to upload a template file and choose a file. Now at this point I'll need you to locate the folder which you downloaded and extracted moments ago. Which is a template. And I'm going to use the template that I've attached to this demo lesson. So go inside that folder and we're going to start by loading the nonportable.json template. So I'm going to open this. I'm going to click on next. For stack name I'm going to call it nonportable. I'm going to click next. Next again. And click on create stack. Now this stack creates two resources. An S3 bucket and as well as that an EC2 instance. So let's just refresh this a couple of times and wait for this to create. And then we'll go back to our demo lesson. It's already started creating the S3 bucket and the EC2 instance. The S3 bucket has finished creating. And it should only take a few more moments to do the EC2 instance. Go ahead and pause the video and wait for your EC2 instance to finish creating. And then we're good to continue. Now in my case it only took a few more moments. And now we have this nonportable stack in the create complete status. If I click on resources you'll be able to verify there are two logical resources. Bucket and instance. As well as two corresponding physical IDs. So now we've got that in place. I'm going to go ahead and create stack again with new resources. Upload a template file. Choose the file. Going to use this same template again. This time I'm going to call the stack nonportable2. Click on next. Next again. And create stack. Now after a few moments click on refresh. And you should see that the stack creation has failed. And it's moved into a rollback in progress state. And after a few moments it will change away from rollback in progress. You'll see that the stack will start deleting the resources. And after a few moments that process will complete. Now why we have an error here is that inside the CloudFormation template. If we look at the nonportable.json template. It's actually explicitly defining the bucket name for this bucket. And because of that we can't apply more than one of this template in any AWS account in any region. Because once this bucket name is used it can't be used again. Now we can improve the portability of this. So what I'm going to do is first I'm going to delete the stack that errored. And then I'm going to delete the nonportable stack. The one that's in a create complete state. Now if I move back across to my code editor. Let's go ahead and look at portable stage 1. What I've done in portable stage 1 is I've removed the explicit bucket name on this bucket. So instead of in the nonportable.json template. Where I have a properties key and then a bucket name key. And then this is the value. So AC cat pics 1 333 333 337. In the portable stage 1 template I've removed that explicit naming. So let's have a look at what difference this makes. So I'm going to go ahead and create stack with new resources. Going to upload a template file. Going to choose the file. And I'm going to use portable stage 1.json. Going to click next. Going to call the stack name portable.json. Click next. Next again. And then click on create stack. So this is going to create the same two resources. The S3 bucket and the EC2 instance. Now you're going to see this stack is going to move into a create complete state. Everything should work absolutely fine. So just pause the video. Wait for your stack to move into create complete. And then we're good to continue. Okay so now your stack should be in create complete. And as you can see that template works absolutely fine. But let's go ahead and try something else. What we're going to do is click on this drop down and change to US-E-2. So we're going to click on that region. That's going to take us to the CloudFormation console. We should see no stacks but we're going to create one. So create stack. Upload a template file. Choose file. And then portable-stage1.json. So let's go ahead and load this into this region. So next. Portable stage 1.json. Click on next. Next again. And create stack. Now this is going to fail for a really important reason. And I want to spend a few moments stepping through exactly why. So we're going to see the image ID does not exist. The reason for that is that AMI IDs are regional things. So you can't use an AMI ID from US-E-1 when you're applying this in a different AWS region. So the AMI that I'm referencing in this template doesn't exist. So I'm going to go ahead and create a new one. And I'm going to click on this. And I'm going to click on this. And it's going to say that the template doesn't exist in the US-E-2 region. It only exists in US-E-1. So I'm going to go ahead and delete that. And then I'm going to move back to US-E-1. Before I delete this stack, I want to point out a really awesome feature of CloudFormation. If we go into this stack and click on resources. Remember how in this template we did not specify a bucket name for this bucket. And in the absence of an explicit bucket name. is that CloudFormation creates one on our behalf. So it takes the name of the stack, so portable stage one, then it appends the logical resource ID, so bucket, and then it appends some random. And so this means that this name will be unique no matter how many times you apply this template. Firstly, because the name of a stack in a region itself has to be unique, the logical resource is unique within that template, and then this randomness ensures that this is entirely unique. So especially with S3, by not providing a bucket name, you ensure that the template is portable, at least from the point of view of S3. So now let's go ahead and delete this stack, and then go back to the code editor. Now stage two enhances this further. Rather than explicitly providing the image ID, instead I've created the AMI ID parameter. And this parameter is really... referenced within the instance logical resource, rather than explicitly stating the AMI ID to use. So when I apply this template, let's move back to the console, create stack with new resources, upload a template file, choose the file, and then pick portable stage two dot JSON. Then we'll click next. We'll call this portable stage two. And now we have this empty parameter value for the AMI ID. Now if we go back to my code, I'm going to go ahead and copy and paste the AMI ID from the portable stage one template. So this is the AMI ID for the US East 1 region. If I wanted to apply this template in a different region, I'd need to provide a relevant AMI ID for that region. Now at this point, this template is actually portable. Because the bucket name is dynamically created, the AMI ID is given as a parameter value, the template will work in any way, in any AWS region, and can be applied as many times as required. So I'll go ahead and click on next, next again, and then create stack. But we can extend this one more step to make this a truly portable template. If I go back to my code editor, and go to portable stage three, I want to explain exactly what I've done for this final stage of this portable template. Now it's always best practice where possible to minimize the number of parameters on a template, and where possible, provide default values for these parameters, so that the user isn't required to provide any input. I've also changed the AMI ID parameter from being a simple string parameter that the user needs to input, to being a type that you might not have experienced before. So this is actually referencing a number of parameters which AWS make available via SSM. So you can access these public parameters, and in this particular case, it's allowing us to access an AWS EC2 image ID. And when we use this path, we're actually going to retrieve the latest AMI ID for Amazon Linux 2, in the particular region that you're referencing this from. So just to reiterate, what's going on here is we're referencing in the image ID key of the instance, we're referencing this parameter. But for the default value for this parameter, we're providing something that says, this is going to be replaced by SSM, and we're going to get in return the AMI ID for the most recent version of Amazon Linux 2, in the particular region that we're creating the stack in. So this is a really powerful feature that means that it always works, and it automatically adjusts based on when AMIs are updated, and which region it's being applied in. So if I go back to my console, go ahead and delete portable storage, and I'm going to go to portable stage 2, create a new stack with new resources, upload a template file, choose the file portable stage 3 dot JSON, click on next, call this portable stage 3, and notice here how it's referencing forward slash AWS forward slash service, forward slash AMI hyphen Amazon hyphen Linux hyphen latest, and then Amazon Linux 2 x86 64. And this is going to be a dynamic reference. It's going to give us back an AMI ID which we can use. So if I click on next, next again, and then create stack, it's going to create the stack fine in this region, and that's going to also work in any other AWS region, because the SSM parameter store is always going to provide us with the correct AMI ID to use. And attached to this lesson, I've included a link which gives some examples of the other parameters, the other public parameters that are available from SSM parameter store. So these are really useful, and you can use these within your CloudFormation templates to dynamically reference things which are regional specific or changed or updated by AWS. And so this is a really powerful feature. Now that's everything I wanted to cover. I just wanted you to get some experience of how you can modify templates to be more portable. There's a number of really important default rules. Attempt to make a template so that you don't explicitly define any naming. So don't name things in a template. If these names need to be unique, you prevent the template from being used to create multiple stacks. Where possible, like I've done with this example of an S3 bucket, allow CloudFormation to create the physical resource name rather than explicitly defining it. Additionally, where possible, try not to use anything regionally specific such as key names or AMI IDs. As a good first step, get these as parameter values with a suitable default, so at least it will work in one AWS region without any parameter input. But where possible, and this is the difference between stage 2 and stage 3, attempt to utilize features of CloudFormation which allow you to dynamically reference things. And one really good example is using the SSM parameters to get the latest AMI IDs to use for your EC2 instances. Now these have just been relatively simple examples, but this is the level of detail that you're going to need to get started with CloudFormation and for the exam. At this point though, let's go ahead and make sure all of the resources are cleared up. So go ahead and delete any stacks that you have in the US East 1 region, switch to US East 2 and make sure that 2 is cleared of resources, and then you're good to complete this video. Now that is everything I wanted to cover, so go ahead, complete the video, and when you're ready, I'll look forward to you joining me in the next.