 Welcome back. This is part two of this lesson. We're going to continue immediately from the end of part one, so let's get started. Next, we're going to look at the next file, which is CFN init with signal. So instead of doing the configuration within this user data section of the EC2 instance logical resource, now we're going to use the CFN init feature. Now this looks much more complex than it actually is. If we scroll down, you'll notice that under the EC2 instance logical resource, there's this extra component, so the metadata component. And we're passing in this block of data, AWS double colon cloud formation double colon init, and then we're passing in configuration directives that are used by the CFN init helper tool. Now I'll include a link attached to this lesson which details how this works. I've covered it in a separate theory lesson in this section of the course, but essentially this is an alternative way of configuring an instance. This is a desired state configuration tool. So essentially when we use the CFN init helper tool, which is down at the bottom, the way we use this is essentially we run the command and we pass in the stack, the resource, and the region. And it uses this to get access to that metadata. And what it will do is apply the desired state. So it's going. To apply all of this configuration information. So it will install any packages within the packages section. In this case, a web server. It will create any files contained within the file section. In this case, the HTML file. It will run any commands within the command section. In this case, sleeping for 300 seconds. And then it will adjust the configuration of any services specified in the services section. In this case, it will make sure that the service is running and that the service starts. Every. Time. The instance itself restarts. So this is just another way of performing configuration. And again, I've detailed this in a separate theory lesson elsewhere in the course. So now I'm going to go back to CloudFormation. I'm going to create a stack with new resources, upload a template file, choose the file and use CFN init with signal. So click on open. Next. Call it CFN init. Next. Next again. And create stack. Now this will take a few minutes to create, but essentially what it's going to do is define this logical resource. Then it's going to use the user data. It's going to run the CFN init command, which will apply this desired state that specified in this section. And based on the status code of CFN init, it's going to send that back to the CloudFormation stack. So this is going to operate in exactly the same way as the previous example where the stack itself won't move into a create complete state. Until the entire bootstrapping process has been completed. Only now the bootstrapping process is going to use the CFN init helper tool plus the CloudFormation init data rather than the user data. Now at this point go ahead and pause the video and wait for your stack to move into a create complete state. And then you're good to resume. And there we go. This stack's now moved into a create complete state. And if we refresh the events, the same process was followed. The resource itself didn't move into create complete. Until this success signal was received from the instance itself using the CFN signal command. Now what I want to do at this point is to help you understand how you can diagnose issues with CloudFormation. So go ahead and click on the resources tab. And then click on this next to the instance to open that in a new tab. We need to connect to the instance. So right click on the instance. Go to connect. We'll be choosing EC2 instance connect. Make sure it says EC2 hyphen user. And click on connect. Now within this instance. In the instance operating system there's a folder called forward slash VAR forward slash log. And inside that folder if we do an LS space hyphen LA. You'll see lots of different log files. Now there are a couple of ones that are interesting. First cloud init hyphen output dot log. So if we just output that file to the screen. So cat space and then cloud init hyphen output dot log. And as you can see here I got a permission denied message. Because these files are not readable by the user that I'm logged in as. So if you get this error when you're attempting to run cat or tail on any of these log files. Then just make sure you run shudu before the command. You'll see in a second I'm going to run shudu cat and then cloud hyphen init hyphen output dot log. So if you get any permissions errors then just add shudu at the beginning of the command. This will show us the output of the user data component of the bootstrapping process. So you'll see that all of the commands that have been executed as part of the user data are shown inside this log file. Now because we're using cfn init the only things we see in this case are the cfn init command and then the cfn signal command. If we were using user data itself rather than cfn init. This file would show us a full output of any issues. Experienced as part of the bootstrapping process. And this is a useful way to diagnose any provisioning issues with cloud formation. Now I'll clear the screen. Do an ls space hyphen la again. If you are using cfn init. Then you have the equivalent here which is cfn hyphen init hyphen command. So to output that to the screen. shudu space cat space cfn hyphen init hyphen cmd dot log. And output that. And here we have the output. And we can see all of the different things that have been done by the cfn init command. So we can see that it's performing an install of the Apache web server. It's installing all of those packages. And then if we do an ls space hyphen la again. And cat the other file. So cat space cfn hyphen init dot log. This allows us to see a granular overview of exactly which desired state actions have taken place. So we can see all of the different sections that have been executed. That it's installed. The httpd package via yum. No groups, no users or no sources have been specified. It has created this html file. And so on. We can see all of the actions that have been completed by the cfn init process. So again remember those files. They're really important for real world usage. Cloud hyphen init hyphen output is good for diagnosing normal bootstrapping using user data. And then this pair of files. So cfn hyphen init hyphen command dot log. And cfn hyphen init dot log. They're useful for diagnosing cfn init processes. Now let's just close this down. We still do have the one problem with this stack. In that it won't update the configuration of the instance. If we do an update stack and change our parameter values. So to fix that. Go ahead and delete this stack. And we're going to look at the fourth and final iteration of these stacks. So inside my code. So inside my code editor. This time this is exactly the same as the previous stack. Only now it's using the cfn hoop utility. To detect changes to the metadata for the instance resource. So if I scroll down. Now in addition to all of the existing metadata. What we also have is the configuration for cfn hoop. So here we're specifying in this configuration file. The stack that we're using. The region that we're using. An interval. Which is how often it checks for configuration changes. And then in the second configuration file. We're defining what should happen when an update occurs. So in this case it's triggered when the stack is updated. This is the resource that it's looking at. So this is the metadata that it's monitoring for changes. And if this metadata changes. It will run this action. Now this action is another execution of the cfn init helper tool. The effect of this will be. That when we apply the stack for the first time. It will be exactly the same as the previous one. But when we do an update stack operation. And change the parameter value. This will mean that this part of the metadata will be updated. Because this references the parameter. And then the cfn hoop utility will detect that change. And rerun cfn init. So let's test that out. Let's go ahead and create a new stack. Upload a template file. Choose the file. Use this fourth iteration. Cfn init with signal and cfn hoop. Click on next. Call this stack cfn hoop. And leave the message as default for now. Click on next. Next again. And then create stack. Now again this will take about six or seven minutes to finish creating. So go ahead and pause the video. And resume it once your stack moves into a create complete state. Okay so now the cfn hoop stack is now in a create complete state. So let's click on resources. Let's expand this physical ID column. And click on this icon next to instance to go to the EC2 console. Let's right click on this instance. And select connect. And then click on connect. So this instance is now in the same state as the previous example. With the webpage saying cats are the best. However this instance also has cfn hoop installed and configured. So if we go cd space forward slash var. And then cd log. And then we do a listing. In addition to the log files we explored previously. There's now cfn hyphen hoop dot log. And we're going to cat this. So shudu space cat space cfn hyphen hoop dot log. And you'll see that it's initialized with this end point. So it's pointing at the correct regional end point. And then it's skipping resource because it's currently in create in progress. And it's telling you that there is no change in path resources dot instance dot metadata dot awl. And it's telling you that there is no change in path resources dot instance dot metadata dot awl. And it's telling you that there is no change in path resources dot instance dot metadata dot awl. And it's telling you that there is no change in path resources dot instance dot metadata dot awl. So it's monitoring this resource path. And this matches if we scroll to the top. This matches this. So this is monitoring this metadata for any changes. So how do we make those changes? Well if we go back to the console. And what I'm going to do is run the tail command. So shudu space tail space hyphen f space. And then I'm going to tail this file. So cfn hyphen hoop dot log. And then I'm going to tail this file. So cfn space hup dot log. So tail is going to show us the last set of lines in this log file. And hyphen f just means that it's going to monitor that constantly until we exit from this utility. We'll go back to the CloudFormation console. Make sure that the cfn hoop stack is selected and click on update. We'll use the same template. Click on next. And this time we'll change this message. We'll add lots of exclamations. And we'll add lots of exclamation marks. Because cats really are the best. So once we've added all of those. Click on next. Next again. Scroll down and update stack. And while that's updating. Go back to the EC2 instance connect window. And let's just wait for the result. So I've got this configured to monitor for changes every one minute. So we've just made a change. In order for this to take effect. The stack needs to be in an update complete state. Which it is. So let's go back to the EC2 instance connect window. And the next time that cfnhoop monitors this metadata. It should detect a change. And it will rerun cfn init. There we go. We can see a message. Data has changed from the previous state. So it's detected the change in the metadata. And the reason why the change has occurred. Is because we changed the parameter message. And that parameter is referenced. If we scroll down. Under this section. So when we're creating a file. We're creating var www.html.index.html. This references message. Therefore changing the parameter means this changes. This means the metadata has changed. And so cfnhoop detected that change. And it rerun cfn init. So if you press control and c. To exit out of this. And just do a listing. ls space hyphen la. Now if we do a sudo space cat space. cfn hyphen init dot log. This will be an output of the cfn init process. And we should see where cfn init. Has evaluated the metadata. And reapplied this desired state. And reapplying the desired state. Includes updating this file. So we can see this line here. It says var www.html.index.html. Already exists. Moving that to a backup. And then writing content to this html file. So we've updated this html file. Using this. Html file. Using a rerun of cfn init. So cfn init is desired state. It applies the state specified. Within this metadata section. Of this template. And so it only applies the changes. So if we go back to the instance itself. So go to the ec2 console. Click on this instance id. Let's copy the public ip version 4. Addressing to our clipboard. And open that in a new tab. We'll be able to see our updated html page. And this update's been put in place. And this update's been performed. By using a combination of cfnhup and cfninit. At this point that's everything I wanted to cover. So let's just get started on deleting this stack. Go back to the stack and click on delete. And once that's deleted. All of the resources you've used in this demo lesson. Will be removed from your account. So I know that this has been a long demo lesson. And we've been covering. Many different aspects of cloud formation. We've covered normal user data. We've covered user data using cfn signal. We've covered the default user data. We've covered moving from user data to cfninit. Also using cfnsignal. And then finally we've used a combination of cfninit. Cfnsignal and cfnhup. To implement a template that is portable. But also a template. Which can automatically update. The resources created by that template. Including operating system updates. Within ec2. Now at this point that is everything I wanted to cover. Hopefully it's been useful. And given you a lot of practical experience. And I hope that you've enjoyed this video. And I hope that you've enjoyed this video. And that you've gained experience with how to use these tools together. But at this point that is the end of this demo lesson. But at this point that is the end of this demo lesson. So go ahead and complete this video. And when you're ready I'll look forward to you joining me in the next.