 Welcome back. This is part two of this lesson. We're going to continue immediately from the end of part one, so let's get started. Now, as a reminder, this was the CloudFormation template that you've just deployed using the first one-click deployment. This is the CloudFormation template that you're about to deploy using the second one-click deployment. Now, this is much more complex, but I want to step through it just so that you have a high-level understanding of exactly how this works. Now, the important part, if I scroll down, is the lambda function. And this lambda function is going to be the thing that provides the functionality to our custom resource, which is going to copy objects into our S3 bucket when stacks are created and delete objects from the S3 bucket when the stack is deleted. Now, you don't need to understand exactly how to code in Python. This lambda function is used as backing for our custom resource. This lambda function receives an event, and this event contains all of the information that this lambda function needs to use in order to copy objects into the S3 bucket when a stack is created and to delete objects from that bucket when the stack is deleted. So it receives an event which contains details of the bucket that's also been created by this CloudFormation template. And then it has some parameters which point at a bucket which I own, which contains... some animal pictures. So what it does is it performs different actions depending on whether it's a create operation or a delete operation. If it's a create operation, then it uses the copy objects function. If it's a delete operation, then it uses the delete objects function. So the copy objects function just essentially connects to the bucket that I own and copies all of the objects into the bucket that's created in your account using this template. The delete function is a function that's used to copy objects into the S3 bucket when a stack is created. The delete objects function goes through the bucket in your account and deletes any objects contained within that bucket. So it's nice and simple, but the part that makes this work, if we scroll all the way to the top, we have a simple S3 bucket resource. So this is the same S3 bucket logical resource as we had in the previous template. But now we have this custom logical resource. So we can see that it's custom because it starts with type custom double colon. And then in our case, we just call it S3 objects. Anything after the custom means that it's a custom resource. Now we're providing it with some additional information inside this property section. We're providing it with a service token, a source bucket, a source prefix, and a bucket. Now this service token is the only mandatory part. This is how we tell CloudFormation what backs this custom resource. it's the thing that CloudFormation will send the event data to in order to use the custom resource. In our case it's the Amazon resource name for the lambda function that provides the backing for this custom resource. So it's the lambda function that will receive the event data and so we need to specify it in this field as the service token. The source bucket is a bucket that I own in my account called cl-randomstuffforlessons and then the source prefix, so the folder inside that S3 bucket which contains all of the objects that this will copy, this is called custom resource. So essentially it's passing in the name of the bucket to copy these things into, the name and prefix of the bucket to copy the things from, and then the service token which is the lambda function that will perform all of these tasks. And of course we also need this role to give the lambda function permission. So this is what this role does, it essentially just gives this lambda function the permissions required in order to interact with both of these buckets. So that's the CloudFormation template that gives us all of this additional functionality. And this template and the simple one are both contained in the zip file that you downloaded attached to this lesson. So let's move back to the AWS console and this time I want you to open up the second one-click deployment. This is also going to take you to a quick create stack page. This time the stack name will be S3 bucket custom. You'll need to scroll down to the bottom because we're creating an IAM role. You'll need to check the box under capabilities and then click on create stack. Now this stack creation process is a little bit more involved than the previous one. So let's step through exactly what's happening. So first it's creating the stack. Second it's creating the normal stack. So first it's creating the normal stack. So first it's creating the normal S3 bucket and the logical resource name for that S3 bucket is AnimalPix. Now the reason that it's creating this S3 bucket first is because there is a dependency order inside the template. If we move back to the CloudFormation template, you can see that the AnimalPix bucket has no dependencies, neither explicit or implicit. Now the custom resource here, this references the lambda function and the S3 bucket. And so this is a little bit more of a challenge. And so this has an implicit dependency on both of those. We're not defining it using a depends on statement, but it is implicit because it references both of these other resources. Now the role also can't be created because this also references the AnimalPix bucket. So it can't be created until the AnimalPix logical resource moves into a create complete state. And then the lambda function, this can't be created until the IAM role is created. So this is a little bit more of a challenge. So this is a little bit more of a challenge. And so this is why it appears to be sequential. Why nothing else can be created until the S3 bucket reaches create complete. So let's refresh. Now that the S3 bucket reaches create complete, then the IAM role creation begins. The IAM role has finished creating. So then the copy S3 objects function starts. So this is the lambda function. And then once the lambda function reaches a create complete state, well because this and the S3 bucket, are then both in create complete, the custom resource itself can then start creating. And this is the custom AnimalPix logical resource. This moves through the process. This gets to create complete. And then the stack itself is now in a create complete status. Now if we click on the resources tab, and we look for the AnimalPix S3 bucket, and then we go ahead and click on it to open it in a new tab, you'll note that this already has three images inside it. NEMO1, NEMO2, and NEMO3. So let's click on one of these, and then open, and this opens a picture of Nemo the cat. So if we just refer back to this architecture diagram, what's happened is we've used this template to create a stack. The stack's created an S3 bucket and a lambda function. The lambda function has been invoked by declaring a custom resource. It's been passed an event. The event data contains the location of the media bucket, and the name of the event. The name of the animal picks bucket, and it's downloaded the media and put it into the animal picks bucket, and then it's signaled a success back to CloudFormation using the endpoint that's defined within the custom resource. Now we can have a look at the logs that have been generated. If we click on the services dropdown, let's just type CloudWatch, hover over it in this dropdown, and then click on logs to move to CloudWatch logs. Once we're there, click on log groups, and you should see a log group for this lambda function. Go ahead and click on it. Inside the log group should be a single log stream. Click on that, and you'll see the logs for this one specific invocation of this lambda function, which remember is backing this custom resource. So if we expand this first info line, this is the event that's been received by the lambda function. So it's a JSON object. It contains the stack ID of the stack that's used this lambda function via the custom resource. Here's the response URL, and this is the URL that the lambda function can use to indicate a success or failure. This is the resource properties part, and if we move back to the CloudFormation template, and just scroll to the top here, this matches this component. So it contains the service token, the source bucket, the source prefix, and then the animal picks bucket. And if we move back to the console here, we can see the animal picks bucket, the source bucket, the service token, and then the source prefix. So this is all information that's passed into the lambda function when you use a custom resource. So the lambda function takes all of that information, and it begins copying all of these objects into the S3 bucket. So Nemo1, Nemo2, and then Nemo3. Then it just outputs the URL that's being used to send the response. This is just diagnostic information. Then it outputs the response that will be sent back to CloudFormation, and this is just a JSON data structure that will be sent back to CloudFormation to indicate that this was a successful invocation. So this gets sent back to CloudFormation, and that's how CloudFormation knows that this custom resource has executed successfully. So it can then adjust the status of the resource in the CloudFormation stack into create complete. So I do recommend just pause the video and just step through this to make sure that you're entirely comfortable at a high level with how this architecture works. It's not all that complicated, but you need to understand how all of the moving parts fit together. At this point, move back to the CloudFormation console. Click on the bucket name again to open a tab to the S3 console. Select Upload, and then Add Files. And then I'll want you to go to the folder that you downloaded and extracted in Part 1 of this demo, and then manually upload the Anna and Yuri objects. So select both of them. Click on Overlay. Open. Scroll down to the bottom, and then click on Upload. And we're doing this just to show that this CloudFormation custom resource can also delete any objects which have been uploaded manually by a human. In this case, these two cat images. So these have been uploaded. So click on Close. Now you should have a total of five objects in this S3 bucket. So next, we're going to go back to CloudFormation. We're going to make sure that we have the S3 bucket custom stack selected. Click on Delete. And then confirm that by clicking Delete Stack. Then we'll click on Events. Updates will start to show in this Events list. And it's going to start by deleting the custom resource. So Copy Animal Pics. And it's doing this because it's doing it in reverse dependency order. So because this logical resource was the last thing to create, it will be the first thing to delete. So let's refresh. This has happened really quickly. We can see that it's deleted. The custom resource. So Copy Animal Pics. Then it's deleted the Lambda function. Copy S3 Objects function. Then it's deleted the S3 copy role. And then finally, because the bucket has been emptied by this custom resource, it's deleted the Animal Pics bucket itself. And if we go back to CloudWatch Logs. Let's go to Log Groups again. Select the same log group. Scroll down. Select the other log stream. So the one with the most recent time stream. And then click on Delete. And then click on the most recent time stamp. And this time you'll be able to see the output for a delete event. So you still have this event data. It's telling us this time it's a delete operation. Again, we have a response URL. It's giving us the same properties that's contained within the CloudFormation template. So the Animal Pictures bucket, the Source bucket, the Service Token, and the Source prefix. And then all of the other information that this Lambda function needs to interact with CloudFormation. So it responds to CloudFormation with a response saying that everything's OK. CloudFormation can then move this logical resource into Delete Complete. And then the stack itself can be deleted. So this has just been a really simple example of how you can implement this architecture. Your own CloudFormation custom resource. Now you should do this demo as many times as you need to become comfortable with exactly how this works. It's a really great feature of CloudFormation that you can use to extend it. It's functionality well beyond what it can do natively. And it's something that features in exam questions in the Developer and SysOps Associates as well as the SA Pro and DevOps Pro. So it's really critical that you understand this so you can answer any exam questions in the exam that you're taking. Now at this point you've completed everything that I wanted you to do in this demo lesson. Including clearing up the account by deleting all of these CloudFormation stacks. So I hope you enjoyed this lesson. I hope you gained a lot from it. And I hope you've got a basic understanding of how custom resources work within CloudFormation. Go ahead, complete this video. And when you're ready, I'll look forward to you joining me in the next.