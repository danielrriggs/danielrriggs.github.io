 Welcome back and in this demo lesson I want to step you through the evolution of a CloudFormation template using progressively more complex and feature-rich techniques. So we're going to start with a CloudFormation template which just utilizes user data. We're going to evaluate the weaknesses. Then we're going to enhance this by using CFN signal within the user data. Then we're going to change from using user data to CFN init. And then finally we're going to include CFN hub which allows our CloudFormation template to dynamically adjust the configuration of the EC2 instance whenever the stack is updated. Now to get started if you are following along in your own environment you need to be logged in as the IAM admin user to the general AWS account which is the management account of the organization. And you'll need to have the Northern Virginia region selected. Now there's a link attached to this lesson which will allow you to download. All of the assets that we're going to be using within this demo. If you download and extract that file it contains a number of CloudFormation templates. We have one user data. YAML to user data with signal. YAML three CFN init with signal and for CFN init with signal and CFN hub. So make sure you've got all of those downloaded because it's going to make it easier to step through them over the course. Of. This demo. Now in this demo lesson we're going to be using the YAML format because I want you to have experience with both Jason and YAML. Now we're going to start by applying this first CloudFormation template so one underscore user data dot YAML. So let's go ahead and do that. So back in the console click in the search box and move to the CloudFormation console. By now you should be used to how to do this. Then we're going to create a stack. Upload a template. File click on choose file and then locate the folder that was created when you downloaded the link attached to this lesson and when you extracted it. So go into that folder. It should be called CFN init CFN signal CFN hub. And then we're going to upload the one underscore user data dot YAML file. So select that click next. We're going to call it user data. Accept all of the defaults. Click on next scroll to the bottom. Next again scroll to the bottom. And click on create stack. So this stack should create relatively quickly. It creates an S3 bucket an instance and an instance security group. So these resources should complete relatively quickly. So go ahead and pause the video and resume it immediately once your stack moves into a create complete. There's something specific which I want to show you. So you should wait at your computer until the stack completes. OK so in my case that took about 60 seconds. And my stack is now in a create complete state. So what I'm going to do is to click on the resources locate the instance logical ID. Click on this icon next to it to open that in a new tab. That should take us directly to the EC2 console with this one instance displayed. Click on it. Copy down the public IP version 4 address by clicking on this icon next to the IP. Don't use this open address link and then open that in a new tab. Now what you'll find. Is this doesn't load. Now the reason for that is important. Let's just go across back to my code editor and review exactly what this template is doing. So this is the template and everything else in this template is not what I want to focus on. What I want to focus on is this user data section. So this part. So what this user data does is it performs an update and upgrade using the yum command and then it simulates some other processes and it simulates those processes by running a sleep 300 and sleep 300 will wait for 300 seconds which is five minutes. So I want you to imagine that this is a legitimate bootstrap process. So before we do anything else maybe we need to install some software packages or other dependencies. So this is going to wait for five minutes. Now the problem is is that CloudFormation has no awareness of this. So from CloudFormation's perspective it initiates the creation of these resources. So it's not just a simple bootstrap. It's a process that allows us with easy to to launch an instance. And as soon as this easy to instance reports as being in a running state. Well from CloudFormation perspective it's ready to go. So CloudFormation has no awareness of any bootstrapping process which occurs on the easy to instance. And this can be a problem if you have lengthy bootstrapping processes. So in this case we've simulated it artificially by adding a five minute bootstrap process you'd encounter the same problem. Now at this point just go ahead and pause your video for five minutes. And then once that five minutes is over we'll continue and evaluate the state of this easy to instance. After about five minutes time the instance should load and you should see a simple web page saying cats are the best. So this is what we expect to see after the 300 seconds of simulated bootstrapping have concluded and the rest of the setup process can begin. Now the second thing that I want to highlight just returning back to my code editor for a second is that the HTML page that you're viewing is actually created using a CloudFormation template parameter this so dollar open curly brackets message close curly brackets. If we go to the top of the template this is actually a CloudFormation template parameter. So let's move back to the console and just look at exactly what happens when we change this parameter. So I'm going to go back to CloudFormation click on update. Going to update this stack. But use the current template and then I'm going to change this message from cats are the best to cats are the best with lots of exclamation marks and then I'm going to click on next scroll down next again scroll down and update stack. So now we're going to see the stack move into an update in progress. So we see the stack itself moving into an update in progress status. If I refresh we can see the instance is now itself moving into update in progress. If I go back. To the EC2 console and just hit refresh. You'll see that this instance is stopping. Now the reason for this is that if we make any changes to the user data of a logical resource within CloudFormation so by changing this parameter what we're actually doing is changing this user data. Well when that happens it's known as an update with disruption. What this means is that the instance is going to be stopped and then started. So two things will happen the instance will be stopped. It will lose its public. IP version for address and then after an amount of time it will move into a pending state. It will regain a new public IP version for address and then it will move back into the running state. Once it's back in the running state we should be able to connect to it. So if I select it it will have a new public IP version for address. So I'll copy that into my clipboard and then open that in the same tab. So now I've opened the new IP address and notice how the cats are the best message hasn't changed. Now the reason for this is that this user data by default is only applied once when the instance is launched. So even though we've changed it it doesn't have any effect. So with this simple user data template we have two major problems. First the disconnect between the bootstrapping process and when the stack completes. So if the bootstrapping process lasts for 10 minutes then the stack will move into create complete much before the bootstrapping has finished. And that could lead to a bad user experience. And secondly the instance configuration is not updated when the stack itself is updated. And those are two things that we need to fix. So to fix those I'm going to go back to my AWS console and just delete this stack. So I'm going to delete the user data stack entirely that will delete the EC2 instance. So I'll close down these tabs that we don't need anymore. Now back in my code editor. Now let's look at the user data with signal. So there's only two changes we've got to make to this template. If I scroll down now we have a creation policy within this logical resource. And I've talked about the theory of creation policies elsewhere in the course. So this creates an endpoint that can be signalled to indicate when this instance is really in a created state. And the way that this is used if I scroll down to the user data we have one line at the end which is this line. And what this does is to signal the success or failure of the previous command to the stack the resource and the region. So once this bootstrapping process has been completed it will signal to the stack whether it's been successful or not. So let's go ahead and apply this. So if I go back to the console create stack with new resources upload a template choose file this time to use the data with signal click on open click on next. Leave everything as default. Call this one signal. Click on next. Next again and create stack. Now this is going to take some time to create it will be longer than the previous stack that we created. This one's probably going to take about six or seven minutes to finish creating. So what I want you to do is to pause the video wait for your stack to move into a create complete state and then immediately I want you to resume this video and we can continue because I want to explain the difference that this CFN signal makes to the architecture. Of this stack. So pause the video but keep an eye on the process and resume the video immediately when the stack moves into a create complete state. Notice this line in the events. This says received success signal from unique ID and then the instance ID. So I want you to focus on this for a second because I'm going to explain what's just happened. So once I refresh this one more time my stack is now in a create complete state. Now before I explain this I'm going to click on resources. I'm going to click on this icon to open the EC2 console direct to this instance click on the instance copy the public IP version 4 address into my clipboard and immediately open that in a new tab. Now straight away this web page loads and this is an important distinction. So moving to my code editor what happened here is that CloudFormation initiated the creation of this logical resource. Then it told EC2 to launch the physical resource. EC2 launched that using this user data but then CloudFormation was expecting a signal and it's this command at the end of the user data that forms that signal. So this instance explicitly notifies CloudFormation when the bootstrapping process has been completed. So the reason this time that this stack took longer to create was that the instance resource the EC2 instance this logical resource was waiting until that signal was received before it moved into a create complete state. And until this resource and every other resource are in a create complete state the stack itself cannot move to create complete. The effect of this was that by the time the stack itself moved into a create complete state the instance had finished its bootstrapping process. And that's a really powerful feature especially if the platforms that you're provisioning with CloudFormation have a lengthy bootstrapping processes you can make sure that by the time the stack is in a create complete state everything within that stack is created configured and ready for service. Now we do still have the problem with this stack that if we do do an update stack operation that web page won't update. And the reason yet again is because the user data is the thing that's creating that web page and the user data is only executed once by default when the instance launches. So even if we update the stack with a new parameter even though the user data itself will change. The instance won't rerun this configuration. And so the old existing HTML file will still be there and it won't be updated. So I'm just going to go back to CloudFormation and delete this stack. OK so this is the end of part one of this lesson. It was getting a little bit on the long side. And so I wanted to add a break. It's an opportunity just to take a rest or grab a coffee. Part two will be continuing immediately from the end of part one. So go ahead complete the video and when you're ready. Join me in part two.