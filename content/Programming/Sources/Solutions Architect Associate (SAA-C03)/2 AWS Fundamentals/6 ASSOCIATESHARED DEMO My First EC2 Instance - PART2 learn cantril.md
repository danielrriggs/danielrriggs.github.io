 Welcome back. This is part two of this lesson. We're going to continue immediately from the end of part one, so let's get started. Now in my case this took about three minutes, but now the status check has changed into two out of two checks passed, and this means that we're good to continue. AWS will run a few checks on an EC2 instance as the final stage of the provisioning process, so you want to make sure that an instance has passed two out of two of these checks before you continue. Now you can see just from this item in the list that this instance has been provisioned into US-East-1C. You can also see that this instance has its own public IP version 4 address and public IP version 4 DNS name, so both of these can be used to access this instance over the public internet. And if we scroll all the way to the right, you'll be able to see the security group name, the key name, as well as the launch time of this instance. If you click on the instance, you'll be able to see the security group name, then on the pane on the bottom, you'll be able to access much more detailed information. You've got the details tab, which gives you high-level important information about the instance. You can click on the security tab to see security-focused information, the networking tab to see details of all the networking configuration. Likewise, the storage tab will give you an overview of all of the storage. Click on status checks to get information about the pass or fail states of the status checks, and then click on the security group name, and then click on monitoring, and you'll be able to get access to all of the metrics gathered by CloudWatch for this EC2 instance. And again, these are all things that you'll be learning about elsewhere in the course. For now, what I want to do is demonstrate how you can connect to the terminal of this EC2 instance. So if you go ahead, we'll move back to the details tab to keep this easy, just right-click on the instance, and then click on connect, and this will open the connect dialog. Now, there are many different ways, that you can connect to an EC2 instance. You could use EC2 Instance Connect, which is a web console to access the EC2 instance. I'll quickly demonstrate that. So leave all of this as default, and then click on connect, and via your web browser, you'll be taken to the terminal of the EC2 instance. So this is a really easy way to get access to an EC2 instance console without having to use an SSH application. So as you can see, we're at the instance, operating system now, and we can interact with this EC2 instance, just like we will be able to do shortly when we connect using SSH. The other option we have is Session Manager. Now, this won't work in this case, because there's some additional configuration that's required. What we're going to demonstrate though, as the last part of this lesson, is connecting using a local SSH client. So go ahead and click on SSH client, and you'll be presented with some information on how to connect. Now, what you'll need to be able to connect is a local SSH client, and this comes with Mac OS, Linux, and modern versions of Windows. You'll also need the key file in order to connect, and this is the a4l.pem file, which you downloaded earlier. So what I'm going to do is to go ahead and move across to my terminal, and you should do the same. If you're running Windows, this will be your command prompt. Now, I've downloaded my SSH key to a specific folder on my local machine, and that's my download key. So I'm going to go ahead and move to my downloads folder. On Mac OS, this is cd space, and then downloads. This might differ on Linux or Windows, but essentially you need to move to the folder where your key pair is stored. So back to the console. Now, the next step is to change the permissions on this key pair file. If you don't change the permissions, then you can get an error, and I want to demonstrate exactly what that error might look like. So instead of following the key file, what I'm going to do is copy down this bottom example command. This is the command which will use the local SSH client, and it will use the key pair that you downloaded to connect to the EC2-user of this EC2 instance. And the EC2 instance that we're going to connect to is going to use the public IP version 4 DNS of the instance. So just copy all of this command into your clipboard, move back to your terminal, and just paste this. And then I just copy this into my download folder, and then I'm going to go ahead and move around. Paste that in and press enter. You'll be prompted to verify the authenticity of this host. Now if this were a security sensitive situation, then you'd want to check that all of this matches. That the key fingerprint matches what you expect. In this case, because we're just connecting as part of a demo, we can just go ahead and answer yes to this and press enter. Now you'll be potentially informed that you're trying to connect using an unprotected private key file. And what this means is that this key file is accessible to other users of your local machine. So you've downloaded this key to your local machine, but it's possible that it's accessible to other users. So not just your local user. And this is a security risk if you share this machine with anyone else. For example, family members or work colleagues. So the local SSH client wants you to correct the permissions on this private key file. Now AWS will give you the command to use. To correct these permissions. And this command works on macOS or Linux. You're essentially using the chmod command. With 400 which sets a specific type or set of permissions. And then on this private key file. So copy this into your clipboard and then move back to your terminal. Now this will only work if you're using macOS or Linux. Because chmod is a command that will adjust a file's permissions under those two operating systems. So I'm going to paste that in and press enter. If you're using Windows, this process is slightly different. And I've included a link attached to this lesson which details the steps that you need to do to correct the permissions on your local key file. So at this point I'm going to assume that you've either run chmod 400 and then this key file. Or you've corrected the permissions based on the process detailed in the link attached to this lesson for Windows. Once you've done that, move back to the AWS console. Copy down this command. And then this connection command again. Move back to your terminal. Paste that in and press enter. And this time it will connect you to the EC2 instance. Because the permissions on your local key file are correct. So what's actually happened by using this command is it's connected to this EC2 instance. And it's proven that you have the right to connect to the EC2 instance. By offering proof that you control the instance. And that proof is this local key file that you have on your local machine. When you created this key pair within the AWS console. So if we just go back to AWS. Go to EC2. Scroll down and go to key pairs. When you created this key pair. In our case that's A4L. There are actually two parts to this key pair. The public part and the private part. Now we downloaded the private part. But the public part is actually stored by AWS on the EC2 instance when it's being provisioned. And so by having the private part on a local machine. It compares this to the public part that's on the EC2 instance. And it allows us to connect. And that's why it's relatively safe for this demo that we have this EC2 instance. Which can be connected to by anyone on the public internet. Because the only party which can authenticate to it is you. Because you have this local key file. Now throughout the course we're going to be connecting to EC2 instances in various different ways. All of the ways which I've just demonstrated. Using your local SSH client. We'll also be using the local SSH client. We'll be using Instance Connect. And we'll be using Session Manager. Now if you create any Windows EC2 instances. That's a slightly different process still. And we'll need to use an application called Remote Desktop. But that's beyond the scope of this lesson. Now that's everything I wanted to cover in this lesson. I just wanted to give you a really basic introduction. On how you can provision an EC2 instance. What I want you to do before you finish up. Is to click on Instances. Select the instance that you've just created. Make sure it's the right one. Should be called My First EC2 Instance. Right click. Move down. And then click on Terminate Instance. Please do make sure it's the correct one. It should be called My First EC2 Instance. It's the one that you've just created. And then go ahead and click on Terminate. If you want to tidy this up completely. Scroll down. Click on Security Groups. Find the security group that's called My First Instance Security Group. And then select it. Then click on the Add. Actions drop down. Scroll down. And click on Delete Security Groups. If you get a message telling you that this security group can't be deleted. That's because it's still associated with this EC2 instance. Which is still in the process of terminating. So go back to Instances. Refresh and make sure it says Terminated. Then return to Security Groups. Make sure My First Instance SG is still selected. If it's not, select it. Click on Actions. Scroll down. Delete Security Groups. And then click on Terminate. And then click on Terminate. Confirm that it is the correct security group. And then click on Delete. At this point that's everything I want you to do in this video. So go ahead and complete the video. And when you're ready, I'll look forward to you joining me in the next.