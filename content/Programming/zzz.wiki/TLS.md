Transport Layer Security. Newer version of [[SSL]].

Authenticates the server, client, and encrypts the data.

SSL / TLS "certificate" is synonymous. These terms are often used interchangeably.

TLS 1.0 & 1.1 are considered vulnerable. TLS 1.2 is most recent & is considered secure.