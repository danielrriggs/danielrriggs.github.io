AWS Session Manager may be used to SSH into EC2 instances via a Web Browser.

Usage:
Login to AWS. Search for the EC2 product > Instances (running) > Select an instance id. (top right) > Connect > Session Manager > Connect.