## Bootstrapping EC2 using User Data
Bootstrapping is a process where scripts or other bits of configuration can be run when an instance is first launched, meaning that an instance can be brought into service in a certain pre-configured state.

**EC2 Bootstrapping**
![[EC2 Bootstrapping.png]]
- Bootstrapping allows **EC2 Build Automation**
- User Data - Accessed via the meta-data IP
- http://169.254.169.254/latest/user-data
- Anything in User Data is **executed** by the **instance OS**
	- **ONLY on Launch**
		- (Not restart)
- EC2 doesn't interpret, the OS needs to understand the User Data

- Its **opaque** to EC2 .. its just a **block of data**
- It's **NOT** secure - don't use it for passwords or long term credentials (ideally)
- User data is limited to 16 KB in size
- Can be modified when instance stopped
- But **only executed once at launch**

Boot time to service time - How quickly after you launch an instance is it ready for service, ready to accept connections from your customers.
This includes provisioning the instance, software updates, installations, or configurations.

**Boot Time To Service Time**
![[Boot Time To Service Time.png]]

## Enhanced Bootstrapping with CFN-INIT
This topic is beyond the scope of the Solutions Architect Associate exam, but knowledge of CFN is good to have, and it will help to answer any automation-style questions on the exam.

CloudFormation Init is a way that you can pass complex bootstrapping instructions into an EC2 instance.

- **cfn-init** helper script - installed on EC2 OS
- Simple configuration management system
- Procedural (User Data) vs Desired State (cfn-init)
- Packages, Groups, Users, Sources, Files, Commands and Services
- Provided with directives via **Metadata** and **AWS::CloudFormation::Init** on a CFN resource

![[cfn-init.png]]

CFN-init can be configured to watch for updates to the metadata on an object in a template, and if that metadata changes, then CFN-init can be executed again, and it will update the configuration of that instance to the desired state specified inside the template. This is not something that user data can do. User data only works the once when you launch the instance.

**CFN Creation Policies and CFN Signals**

![[CreationPolicy and Signals.png]]

Without CFN signal the resource creation process inside CFN is pretty dumb. CFN creates the resources (the ec2 instance) & passes in the user data, but it doesn't know if the user data was successful. The instance will just be marked as complete.

The CFN signal command understands how to communicate with the specific CloudFormation stack that it's running inside. The -e $ part represents the state of the previous command. If the output of the previous command is an OK state or an error state, it is sent as a signal by CFN signal to the CFN stack. If success, the resource is moved into complete state. If error, the resource shows an error. If nothing happens for the timeout period (15 min), CFN assumes an error.

## EC2 Instance Roles & Profile
**EC2 Instance Roles**
![[EC2 Instance Roles.png]]
An IAM role has a permissions policy attached to it. Whoever assumes the role gets temporary credentials generated (which are specified in the permissions policy).

An EC2 **instance role** allows the EC2 service to assume that role. An instance role is a specific type of IAM role designed so that it can be assumed by an EC2 instance.

We need some way of delivering those credentials into the EC2 instance so that applications running inside that instance can use the permissions that the role provides. There's an intermediate piece of architecture - the **instance profile**. This is wrapper around an IAM role. The instance profile is the thing that allows the permissions to get inside the instance.
	When you create an instance role in the console, an instance profile is created with the same name. But if you use the command line or Cloud Formation, you need to create these two things separately.
		When using the UI and you think you're attaching an instance role direct to an instance, you're not. You're attaching an instance profile of the same name. It's the instance profile that's attached to an EC2 instance.

When IAM roles are assumed, you're provided with temporary security credentials, which expire; these credentials grant permissions based on the role's permissions policy. Inside an EC2 instance, these credentials are delivered via the instance metadata. An application running inside the instance can access these credentials and use them to access AWS resources such as S3. EC2 and the secure token service liaise with each other to automatically renew the credentials, so they're always valid.

*Applications running in the instance need to be careful about caching these credentials*.

- Credentials are inside meta-data
- iam/security-credentials/**role-name**
- Automatically rotated - Always valid
- **Roles should always be used** rather than adding access keys into instance
- CLI tools will use ROLE credentials automatically
	- As long as the instance role is attached to the EC2 instance, any command line tools running inside that instance can automatically make use of those credentials.

###### EC2 Instance Role Demo
**Instance Role Creation - Reminder**
![[Instance Role Creation - Reminder.png]]

Remember, when we create a role, it creates a role and an instance profile with the same name (A4LInstanceRole). It's the instance profile we're going to attach to the ec2 instance.

This is the meta-data pathway through which the EC2 instance gets the credentials:
curl http://169.254.169.254/latest/meta-data/iam/security-credentials/
curl http://169.254.169.254/latest/meta-data/iam/security-credentials/A4LInstanceRole

This link shows the configuration settings and precedents that the command line utilities use in order to interact with AWS. whenever you use the command line interface, each of these is checked in order.
[Configuring settings for the AWS CLI - AWS Command Line Interface](https://docs.aws.amazon.com/cli/latest/userguide/cli-chap-configure.html#cli-configure-quickstart-precedence)

When cleaning up infrastructure - first, we deleted the Role (IAM > Roles, deleted the A4LInstanceRole). Then, when we go to EC2 > Instances > RClick, Security, Modify IAM Role; A4LInstanceRole is still there. This is because it's displaying the **instance profile** which gets created with the role, not the role itself.

## SSM Parameter Store
Parameter Store lets you create parameters with a name & a value. Many AWS services integrate with the Parameter Store natively. You can also use the CLI tooling on an EC2 instance to get access to the service.

![[SSM Parameter Store.png]]

- Storage for **configuration** & **secrets**
- 3 types of parameters: String, StringList & SecureString
- Used to store secrets like: License codes, Database Strings, Full Configs & Passwords
- Hierarchies & Versioning
	- Just like object versioning in s3, we can have different versions of parameters in Parameter Store
		- Hierarchies
			- Ex: /wordpress/DBUser; /wordpress/DBPassword
				- This can be accessed either by using its full name, or requesting the wordpress part of the tree.
- Plaintext and Ciphertext
	- ? integrates with KMS to allow you to encrypt. It's an extra layer of security.
- Public Parameters - **Latest AMIs per region**
- Changes can create events
	- These events can start off processes in other AWS services

Parameter Store is a [[public service]].
Different types of things can use the Parameter Store: applications, EC2 instances, all the things running on those instances, Lambda functions.

###### Demo Commands
CREATE PARAMETERS
/my-cat-app/dbstring        db.allthecats.com:3306
/my-cat-app/dbuser          bosscat
/my-cat-app/dbpassword      amazingsecretpassword1337 (encrypted)
/my-dog-app/dbstring        db.ifwereallymusthavedogs.com:3306
/rate-my-lizard/dbstring    db.thisisprettyrandom.com:3306

GET PARAMETERS (if using cloudshell)
aws ssm get-parameters --names /rate-my-lizard/dbstring 
aws ssm get-parameters --names /my-dog-app/dbstring 
aws ssm get-parameters --names /my-cat-app/dbstring 
aws ssm get-parameters-by-path --path /my-cat-app/ 
aws ssm get-parameters-by-path --path /my-cat-app/ --with-decryption
*The permissions to interact with parameter store are separate than the permissions required to decrypt (for the KMS key we chose when we created the parameter)*

## System and Application Logging on EC2
- CloudWatch is for metrics
- CloudWatch Logs is for logging
- Neither natively capture **data inside an Instance**
- CloudWatch Agent is required (to provide visibility inside the EC2 instance). Captures data & sends to CW or CWLogs.
- CloudWatch Agent needs **configuration** and **permissions**

![[Logging on EC2.png]]

CW Agent needs a config. Adding long term credentials to an instance is bad practice (bad & unmanageable at scale). Best practice is to create an IAM role to interact with CWLogs. Then, we can attach this IAM role (w/ access to CW & CWLogs service) to the EC2 instance.

1 log group for each individual log we want to capture.
1 log stream inside that log group for every EC2 instance that's injecting that login data.

How to get this up & running?
Manual:
Log into the instance, install the agent, configure it, attach a role, and start injecting the data.

Automation:
Use CloudFormation to include that agent configuration for every single instance that you provision.

CloudWatch agent comes with a number of ways to obtain and store the configuration that it will use to send this info to CWLogs. One of those ways is to store the agent configuration in Parameter Store as a parameter.

###### CW Agent Install Demo
After manually installing & configuring the CW Agent in an EC2 instance, if we go to AWS Systems Manager > Parameter Store, there's now a 'AmazonCloudWatch-linux' parameter, which is a json document with the full configuration of the cloudwatch agent. We can use this parameter to configure the agent on this EC2 instance as well as any other EC2 instances we want to deploy.

## EC2 Placement Groups
There are currently three types of placement groups for EC2:

**Cluster Placement Groups**
![[Cluster Placement Groups.png]]
- **Cluster** - Pack instances close together
	- Used when you want highest **performance**. Highest throughput & lowest latency.
	- Best practice is to use the same type of instance & launch them all at the same time. This ensures that AWS allocate capacity for everything that you require.
	- Because of their performance focus, have to be launched into a **single availability zone**.
		- When you create the placement group, you don't specify an availability zone. When you launch the first instance or instances into that placement group, it will lock that placement group.
			- The idea with cluster placement groups is that all of the instances within the same cluster placement group generally use the same rack, but often the same EC2 host.
				- Transferring data between instances within that cluster placement group, they can achieve **single stream** transfer rates of 10 GB per second versus the usual 5 GB per second, which is achievable normally.
				- To achieve these levels of performance, you need to be using instances with high performance networking. You should also use enhanced networking on all instances.
	- Offer little to no resilience.
	- Can span VPC peers - but impacts performance
	- Requires a supported instance type
	- Use the same type of instance (**not mandatory**)
	- Launch at the same time (**not mandatory - very recommended**)
	- **10Gbps single stream performance**
	- Use cases: **Performance, fast speeds, low latency**

**Spread Placement Groups**
![[Spread Placement Groups.png]]
- **Spread** - Keep instances separated. Use for maximizing **availability** of an app.
	- Ensure max **AZ resilience**.
	- 7 instances per AZ (HARD **LIMIT**)
	- Provides infrastructure isolation.
	- Each **INSTANCE** runs from a different rack.
	- Not supported for Dedicated Instances or Hosts.
	- **Use Case** : Small number of critical instances that need to be kept separated from each other.

**Partition Placement Groups**
![[Partition Placement Groups.png]]
- **Partition** - groups of instances spread apart. If a partition fails, you lose all the instances in it.
	- Designed for when you want more than 7 instances per AZ but still want separate fault domains.
	- Divided into 'partitions' MAX 7 per AZ.
	- Each partition has its own racks - no sharing between partitions.
	- Instances can be placed in **a specific partition**
		- ...or auto placed
	- Great for topology aware applications
		- Ex: HDFS, HBase, and Cassandra
	- Contain the impact of failure to part of an application
	Designed for huge scale parallel processing systems, where you need to create groupings of instances and have them separated. The difference between spread and partition placement groups is that, with partition placement, is that with spread placement, it's all handled for you, but you have that seven instance per AZ limit. But with partition placement groups, you can have more instances, but you or your application, which is topology-aware, needs to administer the partition placement.

## Dedicated Hosts
Pay for the host, then you don't have any extra EC2 instance charges for the capacity of that host.

- EC2 Host **dedicated to you**
- Specific family e.g. a 1, c5, m5
- **No instance charges** ... you pay for the host
- On-Demand & Reserved Options available
	- On-Demand - good for short term or unknown requirements.
	- Reservations: 1 or 3 year terms.
- Host hardware has **physical sockets and cores**
	- This dictates how many instances can be run on that host
	- Some enterprise software is **licensed** based on the number of physical sockets or cores in the server

![[EC2 Dedicated Hosts.png]]

A few types of dedicated hosts are designed to operate with one specific size at a time. For example, an A1 host can host one of these:
16 A1 medium instances or 8 large, or 4 1xL, or 2 2xL, or 1 4xL. All but a few dedicated hosts require you to set that in advance & you can't mix & match.

Newer hosts, running the nitro virtualization platform, there's more flexibility:

![[EC2 Dedicated Hosts - Nitro Virtualization Platform.png]]

Dedicated Hosts limitations:
- **AMI Limits** - RHEL, SUSE Linux, and Windows AMIs aren't supported.
- **Amazon RDS** instances are not supported.
- **Placement groups** are not supported for Dedicated Hosts.
- Hosts can be shared with other ORG Accounts... **RAM** (Resource Access Manager).
	- Visibility & security separation
		- Those other accounts can only **see** instances that they create on that dedicated host
		- You, as the owner, can **see** all the instances running on your host, but you can't **control** any of the instances created by the other accounts.
Motivation to use dedicated hosts in the real world will generally be around software licensing.

## Enhanced Networking & EBS Optimized
Enhanced Networking improves the overall performance of EC2 networking. It's required for any high-end performance features, such as cluster placement groups.

![[EC2 Enhanced Networking.png]]

- Uses SR-IOV - NIC is **virtualization aware**
	- Without this, the physical NIC isn't aware of virtualization, so the host has to sit in the middle, controlling which instance has access to the physical card at a time. This process takes place in software. It's slower & consumes host CPU. Under heavy load (CPU or IO), it can cause drops in performance, spikes in latency, and changes in bandwidth. It isn't an efficient system.
- No charge - available on most EC2 Types
- **Higher I/O** & **Lower Host CPU Usage**
- More **Bandwidth**
- Higher packets-per-second (**PPS**)
- Consistent lower **latency**

**EBS Optimized** (it means adding dedicated capacity for storage networking to an EC2 instance)
- **EBS** = Block storage over the **network**
- Historically network was **shared** - **data** and **EBS**
- EBS Optimized means **dedicated capacity** for EBS
	- Some stack optimizations have taken place and dedicated capacity has been provided for that instance for EBS's usage. It means that faster speeds are possible.
- Most instances **support** and have **enabled by default**
- Some support, but enabling costs extra

###### Source: TutorialsDojo
**Enhanced networking** uses single root I/O virtualization (**SR-IOV**) to provide high-performance networking capabilities on supported instance types. SR-IOV is a method of device virtualization that provides higher I/O performance and lower CPU utilization when compared to traditional virtualized network interfaces. Enhanced networking provides higher **bandwidth**, higher packet per second (**PPS**) performance, and consistently lower **inter-instance latencies**. There is no additional charge for using enhanced networking.

Amazon EC2 provides enhanced networking capabilities through the **Elastic Network Adapter** (**ENA**). It supports network speeds of up to 100 Gbps for supported instance types. Elastic Network Adapters (ENAs) provide traditional IP networking features that are required to support VPC networking.

An **Elastic Fabric Adapter** (**EFA**) is simply an Elastic Network Adapter (ENA) with added capabilities. It provides all of the functionality of an ENA, with additional **OS-bypass** functionality. OS-bypass is an access model that allows HPC and machine learning applications to **communicate directly** with the **network interface hardware** to provide low-latency, reliable transport functionality.

- (**exam**) **The OS-bypass capabilities of EFAs are not supported on Windows instances**.
	- // (**exam**) EFA vs ENA
	- If you attach an EFA to a Windows instance, the instance functions as an Elastic Network Adapter without the added EFA capabilities.

...
Although you can attach an Intel 82599 **Virtual Function (VF) interface** to your Windows EC2 Instances to improve its networking capabilities, it doesn’t support the t3a.medium instance type that is being used in the HPC cluster.
...
