**Amazon Managed Service for Prometheus** is a serverless, Prometheus-compatible monitoring service for container metrics that makes it easier to securely monitor container environments at scale. It collects container metrics.

- Uses an open source Prometheus data model.
- Uses PromQL query language.
- Grafana (**Amazon Managed Grafana**) can use Prometheus for its data queries to generate visualizations of the collected metrics.