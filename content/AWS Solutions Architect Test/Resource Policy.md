Just like an [[AWS/Identity Policy]], but as the name suggests, they're attached to resources instead of identities.

Resource policies have one major difference to identity policies, and that's the presence of an explicit [[AWS/principal]] component.
