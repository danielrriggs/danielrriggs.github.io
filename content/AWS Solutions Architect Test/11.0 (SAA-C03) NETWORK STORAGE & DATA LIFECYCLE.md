## EFS Architecture
Provides network-based file systems which can be mounted within Linux EC2 instances and used by multiple instances at once.

- **EFS** is an implementation of **NFSv4** (a common shared storage standard - Network File System, version 4)
- With EFS, you create **Filesystems** which are **mounted** in Linux (EC2).
	- Linux uses a tree structure for its file system
	- Devices can be mounted into folders in that hierarchy
	- For example, an EFS file system could be mounted into a file system called /efs/media
- EFS Filesystems can be **shared** (mounted) on many EC2 Instances
	- EFS storage exists separately from an EC2 instance
		- EBS is block storage, whereas EFS is file storage, but Linux instances can mount EFS file systems as though they are connected directly to the instance.
- Private service, access to EFS file systems is via **mount targets**, which are things inside a VPC
- EFS file systems can be accessed from **on-premises** via **hybrid networking** methods (even though EFS is a private service)
	- VPC peering, VPN connections or AWS Direct Connect (DX)

**Elastic File System (EFS)**
![[Elastic File System (EFS).png]]
- EFS runs inside a VPC.
- Inside EFS, you create file systems and these use **POSIX** permissions.
	- // more info on POSIX: [File-system permissions - Wikipedia](https://en.wikipedia.org/wiki/File-system_permissions). Summary: it's a standard for interoperability used in Linux. All linux distros will understand POSIX.
- The EFS file system is made available inside a VPC via **mount targets**. These run from subnets inside the VPC. The mount targets have IP addresses taken from the IP address range of the subnet that they're inside.
	- To ensure HA, put mount targets in multiple AZs.
	- Just like NAT GWs, for a fully HA system, you need to have a mount target in every AZ that a VPC uses.

(**exam**):
- EFS is for **Linux-only** instances
- EFS offers two performance modes - General Purpose & Max I/O
	- **General Purpose** = default for 99.9% of uses
	- **Max I/O** - suitable for anything that's highly parallel - big data, media processing, scientific analysis, etc.
- **Bursting** and **Provisioned** Throughput Modes
	- Burst mode works like GP2 volumes inside EBS - has a burst pool, but the throughput scales with the size of the file systems
- **Standard** and **Infrequent Access** (IA) Classes
	- IA is lower cost. Designed for things which are infrequently accessed.
- Lifecycle Policies can be used with classes (just like S3 // [[4.0 (SAA-C03) SIMPLE STORAGE SERVICE (S3)#S3 Lifecycle Configuration]]

// More info. Not needed for exam:
[Amazon EFS performance - Amazon Elastic File System](https://docs.aws.amazon.com/efs/latest/ug/performance.html)
[https://docs.aws.amazon.com/efs/latest/ug/storage-classes.html](https://docs.aws.amazon.com/efs/latest/ug/storage-classes.html)
[https://docs.aws.amazon.com/efs/latest/ug/lifecycle-management-efs.html](https://docs.aws.amazon.com/efs/latest/ug/lifecycle-management-efs.html)

## Implementing EFS - PARTs 1 - 2
[1-Click Deployment](https://console.aws.amazon.com/cloudformation/home?region=us-east-1#/stacks/create/review?templateURL=https://learn-cantrill-labs.s3.amazonaws.com/awscoursedemos/0019-aws-associate-storage-implementing-efs/A4L_TWO_EFS_EC2_AL2023.yaml&stackName=IMPLEMENTINGEFS)
[Lesson Commands](https://learn-cantrill-labs.s3.amazonaws.com/awscoursedemos/0019-aws-associate-storage-implementing-efs/lesson_commands_AL2023.txt)

EFS > create file system. Pick a VPC.
Choose mount target ... best practice is that any AZs within a VPC where you're consuming the services provided by EFS you should be creating a mount target.

**Part 2**
Wait for mount targets to finish creating.

Connect to EC2 instance.
`df —k`
See that the EFS volume isn't attached.
In Linux, all devices or all file systems are mounted into a folder.
First, create a folder for the EFS volume to be mounted into.
`sudo mkdir /efs/wp—content`

`sudo dnf —y install amazon—efs—utils`
Install a set of tools to allow the OS to interact with the EFS product.

We want it to be mounted every time the instance is restarted, so we need to add it to the FSTab file.
`sudo nano /etc/fstab`
Add a line:
file-system-id:/ /efs/wp-content efs \_netdev, tIs,iam 0 0
(ex:) fs-0628600c5b14a66a8:/ /efs/wp-content efs \_netdev, tIs,iam 0 0

`df -k`
See that the EFS volume still isn't mounted.

`sudo mount /efs/wp—content`
This uses what we've just defined in the FSTab file.

`df -k`
See that it's now mounted.

`cd /efs/wp—content`
`sudo touch amazingtestfile. txt`
Create a text file. It gets put on the network file system. Then, connect to EC2 instance B. Do the same as above to setup EFS. See that the text file we created is there.